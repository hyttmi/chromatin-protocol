---
phase: 26-documentation-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [db/README.md, db/version.h]
autonomous: true
requirements: [DOC-05]

must_haves:
  truths:
    - "README documents data-at-rest encryption: master key auto-generation, file permissions, HKDF derivation, transparent decryption"
    - "README documents trusted_peers config option and localhost lightweight handshake behavior"
    - "README documents writer-controlled TTL in signed blob data, TTL=0 permanent, TTL>0 expiry, tombstone expiry + GC"
    - "SIGHUP section lists trusted_peers as reloadable"
    - "Architecture paragraph no longer references '7-day protocol constant'"
    - "Crypto table HKDF row mentions both session keys and at-rest key derivation"
    - "version.h VERSION_MINOR is '5' and VERSION_PATCH is '0'"
  artifacts:
    - db/README.md
    - db/version.h
  key_links:
    - "Deletion paragraph reflects tombstone TTL flexibility (not permanent-only)"
    - "Config JSON example includes trusted_peers"
    - "Config options list documents trusted_peers"
---

<objective>
Update db/README.md to document all v0.5.0 features (DARE, trusted peers, TTL flexibility) and bump db/version.h to 0.5.0.

Purpose: Operators can understand and configure every v0.5.0 feature from the README alone.
Output: Updated README.md, updated version.h
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-documentation-release/26-RESEARCH.md
@db/README.md
@db/version.h
@db/config/config.h
@db/crypto/master_key.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with v0.5.0 feature documentation</name>
  <files>db/README.md</files>
  <action>
Make the following targeted edits to db/README.md, preserving all existing content and patterns:

1. **Crypto table HKDF row** (currently "Key derivation (session keys from KEM output)"):
   Change purpose to: "Key derivation (session keys, at-rest encryption key)"

2. **Architecture paragraph** — replace "TTL (7-day protocol constant)" with "TTL (writer-controlled, per-blob)" so the sentence reads naturally.

3. **Architecture Deletion sentence** — update "Tombstones are permanent (TTL=0) and block future arrival of the deleted blob" to "Tombstones block future arrival of the deleted blob; they can be permanent (TTL=0) or time-limited (TTL>0), in which case they are garbage-collected by the expiry scanner."

4. **Architecture paragraph** — after the Transport sentence ("All subsequent messages are AEAD-encrypted."), add: "**Encryption at rest** protects stored blob payloads with ChaCha20-Poly1305 using a key derived from a node-local master key via HKDF-SHA256."

5. **Config JSON example** — add `"trusted_peers": []` after the `"allowed_keys": []` line.

6. **Config options list** — after the `allowed_keys` bullet, add:
   `- **trusted_peers** -- IP addresses (no ports) whose connections use a lightweight handshake without ML-KEM-1024 key exchange; localhost (127.0.0.1, ::1) is always trusted implicitly (default: `[]`)`

7. **SIGHUP signal description** — change "updates `allowed_keys`, `rate_limit_bytes_per_sec`, `rate_limit_burst`, and `sync_namespaces`" to "updates `allowed_keys`, `trusted_peers`, `rate_limit_bytes_per_sec`, `rate_limit_burst`, and `sync_namespaces`"

8. **Features section** — add three new entries at the end (before ## Performance):

   **Encryption at Rest** -- All blob payloads are encrypted with ChaCha20-Poly1305 before writing to disk. The encryption key is derived from a node-local master key (`data_dir/master.key`) via HKDF-SHA256. The master key is auto-generated on first run with 0600 file permissions. Back up this file -- without it, stored data is unrecoverable.

   **Lightweight Handshake** -- Connections from localhost (127.0.0.1, ::1) and addresses listed in `trusted_peers` skip the ML-KEM-1024 key exchange, completing the handshake without post-quantum crypto overhead. Both sides verify identity via ML-DSA-87 signatures over a shared challenge. Non-trusted peers always perform the full PQ handshake.

   **Writer-Controlled TTL** -- Each blob carries its own TTL set by the writer in the signed data. TTL=0 means permanent (no expiry). Blobs with TTL>0 are expired by the node's periodic scan. Tombstones also support writer-controlled TTL: permanent tombstones (TTL=0) persist indefinitely, while time-limited tombstones (TTL>0) are garbage-collected along with their index entries.

9. **Scenarios section** — add a new scenario after "Rate-Limited Public Node":

   ### Trusted Local Peers

   For multi-node setups on a trusted LAN, skip the PQ handshake overhead between nodes:

   ```json
   {
     "data_dir": "./data",
     "bootstrap_peers": ["192.168.1.10:4200", "192.168.1.11:4200"],
     "trusted_peers": ["192.168.1.10", "192.168.1.11"]
   }
   ```

   Connections between listed peers use a lightweight handshake with ML-DSA-87 identity verification but no ML-KEM-1024 key exchange. Localhost connections are always trusted implicitly. Reload the trusted peer list at runtime with `SIGHUP`.

Do NOT create a CHANGELOG.md (YAGNI). Do NOT add a performance benchmark row for trusted handshake.
  </action>
  <verify>
    <automated>grep -c "Encryption at Rest" db/README.md && grep -c "trusted_peers" db/README.md && grep -c "Writer-Controlled TTL" db/README.md && grep "7-day protocol constant" db/README.md; echo "Should show 0 matches for 7-day"</automated>
  </verify>
  <done>
  - README has "Encryption at Rest" feature entry with master key details
  - README has "Lightweight Handshake" feature entry
  - README has "Writer-Controlled TTL" feature entry
  - README has "Trusted Local Peers" scenario
  - Config JSON includes trusted_peers
  - Config options list documents trusted_peers
  - SIGHUP lists trusted_peers as reloadable
  - Architecture paragraph says "writer-controlled, per-blob" not "7-day protocol constant"
  - HKDF row mentions at-rest encryption key
  - Deletion sentence reflects tombstone TTL flexibility
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 0.5.0 and verify all tests pass</name>
  <files>db/version.h</files>
  <action>
Edit db/version.h:
- Change `#define VERSION_MINOR "4"` to `#define VERSION_MINOR "5"`

The file should read:
```c
#define VERSION_MAJOR "0"
#define VERSION_MINOR "5"
#define VERSION_PATCH "0"

static constexpr const char* VERSION = VERSION_MAJOR "." VERSION_MINOR "." VERSION_PATCH;
```

Then build and run all tests:
```bash
cd /home/mika/dev/chromatin-protocol/build
cmake .. && cmake --build .
ctest --output-on-failure
```

All tests must pass. Do NOT use `--parallel` flag on cmake --build (eats all memory per user preference).
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol/build && cmake .. && cmake --build . && ctest --output-on-failure</automated>
  </verify>
  <done>
  - version.h reports VERSION "0.5.0"
  - All tests pass (zero failures)
  </done>
</task>

</tasks>

<verification>
1. `grep "Encryption at Rest" db/README.md` — matches in Features section
2. `grep "trusted_peers" db/README.md` — matches in Config, SIGHUP, Features, Scenarios
3. `grep "Writer-Controlled TTL" db/README.md` — matches in Features section
4. `grep "7-day protocol constant" db/README.md` — returns NO matches (stale language removed)
5. `grep 'VERSION_MINOR "5"' db/version.h` — confirms version bump
6. `ctest --output-on-failure` in build/ — all tests pass
</verification>

<success_criteria>
- README documents DARE: master key management, auto-generation, file permissions, HKDF derivation
- README documents trusted_peers config and localhost handshake behavior
- README documents configurable TTL: per-blob TTL in signed data, tombstone TTL flexibility
- SIGHUP section includes trusted_peers
- Architecture paragraph updated (no stale TTL language)
- version.h reports 0.5.0
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-documentation-release/26-01-SUMMARY.md`
</output>
