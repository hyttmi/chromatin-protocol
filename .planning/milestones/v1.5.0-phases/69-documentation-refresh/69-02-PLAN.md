---
phase: 69-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/PROTOCOL.md
autonomous: true
requirements: [DOCS-04]

must_haves:
  truths:
    - "Every message type in PROTOCOL.md has byte offsets matching the encoder source in peer_manager.cpp"
    - "The message type reference table lists all 58 types with correct values and descriptions"
    - "Payload sizes documented for types 30-58 match actual response sizes in encoder code"
  artifacts:
    - path: "db/PROTOCOL.md"
      provides: "Verified wire protocol documentation for all 58 message types"
      contains: "TimeRangeResponse"
  key_links:
    - from: "db/PROTOCOL.md"
      to: "db/peer/peer_manager.cpp"
      via: "byte-level format documentation"
      pattern: "big-endian"
    - from: "db/PROTOCOL.md"
      to: "db/wire/transport_generated.h"
      via: "enum values"
      pattern: "TransportMsgType"
---

<objective>
Verify db/PROTOCOL.md byte-level wire format documentation against the actual encoder source code in peer_manager.cpp for all 58 message types.

Purpose: SDK developers rely on PROTOCOL.md as the single source of truth for building compatible clients. Any byte offset or payload size error causes protocol incompatibility.
Output: Verified (and corrected if needed) db/PROTOCOL.md with all 58 message types matching encoder source.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-documentation-refresh/69-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify client protocol types 30-40 byte formats against encoder source</name>
  <files>db/PROTOCOL.md</files>
  <read_first>db/PROTOCOL.md, db/peer/peer_manager.cpp (lines 700-1100 approximately -- search for WriteAck, ReadRequest, ReadResponse, ListRequest, ListResponse, StatsRequest, StatsResponse, ExistsRequest, ExistsResponse, NodeInfoRequest, NodeInfoResponse encoder sections), db/wire/transport_generated.h (lines 23-84 for enum values)</read_first>
  <action>
Perform a systematic verification of types 30-40 in PROTOCOL.md against the encoder source code in peer_manager.cpp.

**Verification procedure for EACH type (30: WriteAck, 31: ReadRequest, 32: ReadResponse, 33: ListRequest, 34: ListResponse, 35: StatsRequest, 36: StatsResponse, 37: ExistsRequest, 38: ExistsResponse, 39: NodeInfoRequest, 40: NodeInfoResponse):**

1. Find the encoder section in peer_manager.cpp (search for the response building code -- look for `TransportMsgType::WriteAck`, `TransportMsgType::ReadResponse`, etc.)
2. Read how the response payload is constructed: what fields, what sizes, what byte order (big-endian uint32/uint64)
3. Compare against the corresponding table in PROTOCOL.md (lines 431-552 for types 30-40)
4. Check: field names match, offsets are correct (cumulative from 0), sizes are correct, byte order notation matches
5. Check: total payload size in the description matches the sum of all field sizes

**If any discrepancy is found:** Fix the PROTOCOL.md table to match the encoder source. The encoder source code is the ground truth.

**If no discrepancies are found:** Leave the section unchanged. Do NOT rewrite sections that are already correct.

Document each verification result as a comment to yourself (not in the file) to track what was checked.
  </action>
  <verify>
    <automated>grep -c "WriteAck\|ReadRequest\|ReadResponse\|ListRequest\|ListResponse\|StatsRequest\|StatsResponse\|ExistsRequest\|ExistsResponse\|NodeInfoRequest\|NodeInfoResponse" db/PROTOCOL.md | xargs test 11 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - db/PROTOCOL.md contains documentation for all 11 types (30-40)
    - Each type's byte offset table has offsets that match the cumulative sum of field sizes
    - WriteAck section documents blob_hash (32 bytes) + seq_num + status fields
    - ExistsResponse section documents exists flag (1 byte) + hash (32 bytes) = 33 bytes total
    - NodeInfoResponse section documents variable-length format with length-prefixed strings + big-endian ints + supported_types list
    - No payload size in a description contradicts the actual field size sum in the table
  </acceptance_criteria>
  <done>Types 30-40 in PROTOCOL.md verified against peer_manager.cpp encoder source, all byte offsets and payload sizes match</done>
</task>

<task type="auto">
  <name>Task 2: Verify v1.4.0 query extension types 41-58 byte formats against encoder source</name>
  <files>db/PROTOCOL.md</files>
  <read_first>db/PROTOCOL.md (lines 620-828 for v1.4.0 query extensions), db/peer/peer_manager.cpp (lines 1100-1600 approximately -- search for NamespaceListResponse, StorageStatusResponse, NamespaceStatsResponse, MetadataResponse, BatchExistsResponse, DelegationListResponse, BatchReadResponse, PeerInfoResponse, TimeRangeResponse encoder sections)</read_first>
  <action>
Perform a systematic verification of types 41-58 in PROTOCOL.md against the encoder source code in peer_manager.cpp.

**Verification procedure for EACH request/response pair:**

1. **NamespaceListRequest (41) / NamespaceListResponse (42):** Find encoder for NamespaceListResponse. Verify: response has has_more (1 byte) + count (4 bytes BE) + entries (N*40 bytes: namespace_id 32 + blob_count 8 BE). Request: after_namespace (32 bytes) + limit (4 bytes BE) = 36 bytes min.

2. **StorageStatusRequest (43) / StorageStatusResponse (44):** Find encoder. Verify: 44 bytes total with total_bytes (8 BE) + max_bytes (8 BE) + tombstone_count (8 BE) + namespace_count (4 BE) + blob_count (8 BE) + reserved (8 BE).

3. **NamespaceStatsRequest (45) / NamespaceStatsResponse (46):** Find encoder. Verify: 41 bytes with found (1) + blob_count (8 BE) + total_bytes (8 BE) + latest_seq_num (8 BE) + quota_bytes (8 BE) + quota_count (8 BE).

4. **MetadataRequest (47) / MetadataResponse (48):** Find encoder. Verify variable-length response: status (1) + hash (32) + timestamp (8 BE) + ttl (8 BE) + size (8 BE) + seq_num (8 BE) + pubkey_hash (32).

5. **BatchExistsRequest (49) / BatchExistsResponse (50):** Find encoder. Verify: response is count (4 BE) + N boolean bytes.

6. **DelegationListRequest (51) / DelegationListResponse (52):** Find encoder. Verify: count (4 BE) + N*64 bytes (pubkey_hash 32 + blob_hash 32).

7. **BatchReadRequest (53) / BatchReadResponse (54):** Find encoder. Verify: truncated (1) + count (4 BE) + entries (variable: status 1 + hash 32 + data_len 4 BE + data).

8. **PeerInfoRequest (55) / PeerInfoResponse (56):** Find encoder. Verify trust-gated: untrusted = peer_count (8 BE) = 8 bytes; trusted = peer_count (8 BE) + entries (variable: address_len 2 BE + address + port 2 BE + namespace_count 4 BE + namespaces 32*N + connected 1 + last_seen 8 BE).

9. **TimeRangeRequest (57) / TimeRangeResponse (58):** Find encoder. Verify: request = namespace (32) + start_time (8 BE) + end_time (8 BE) + limit (4 BE) = 52 bytes. Response = truncated (1) + count (4 BE) + entries (N*48: hash 32 + seq_num 8 BE + timestamp 8 BE).

**For each type:** Read the encoder code line by line, compute expected payload size from the code, compare against PROTOCOL.md table. Fix any discrepancy.

Also verify the Message Type Reference table (lines 554-618) has all 58 entries with correct Value numbers, Names, and Description text.
  </action>
  <verify>
    <automated>grep -c "NamespaceListRequest\|StorageStatusRequest\|NamespaceStatsRequest\|MetadataRequest\|BatchExistsRequest\|DelegationListRequest\|BatchReadRequest\|PeerInfoRequest\|TimeRangeRequest" db/PROTOCOL.md | xargs test 9 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - db/PROTOCOL.md section "v1.4.0 Query Extensions" contains all 9 request/response pairs (types 41-58)
    - Each request type has a payload table with correct offsets and sizes
    - Each response type has a payload table with correct offsets and sizes
    - StorageStatusResponse documented as 44 bytes matches encoder
    - NamespaceStatsResponse documented as 41 bytes matches encoder
    - TimeRangeRequest documented as 52 bytes matches encoder
    - BatchReadResponse truncated flag documented at offset 0
    - PeerInfoResponse trust-gated format documented (8 bytes untrusted, variable trusted)
    - Message Type Reference table (lines 554-618) contains all 58 entries numbered 0-58
    - No payload size in description contradicts field size sum in table
  </acceptance_criteria>
  <done>Types 41-58 in PROTOCOL.md verified against peer_manager.cpp encoder source, all byte offsets and payload sizes match, message type reference table complete with 58 entries</done>
</task>

</tasks>

<verification>
1. `wc -l db/PROTOCOL.md` returns 800+ lines (protocol doc is substantial)
2. `grep -c "Type [0-9]" db/PROTOCOL.md` shows entries for types up through 58
3. All 9 v1.4.0 query pairs are documented with payload tables
4. Message Type Reference table has 59 rows (0 through 58)
5. Spot-check: ExistsResponse = 33 bytes, StorageStatusResponse = 44 bytes, TimeRangeRequest = 52 bytes
</verification>

<success_criteria>
- All 58 message types in PROTOCOL.md have byte-level format documentation
- Every documented payload size matches the encoder source in peer_manager.cpp
- Every documented byte offset equals the cumulative sum of preceding field sizes
- The Message Type Reference table lists all 58 types with correct enum values
- No undocumented message types exist in transport_generated.h
</success_criteria>

<output>
After completion, create `.planning/phases/69-documentation-refresh/69-02-SUMMARY.md`
</output>
