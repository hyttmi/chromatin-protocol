---
phase: 08-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-storage-engine/02-VERIFICATION.md
  - .planning/phases/05-peer-system/05-VERIFICATION.md
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-06
  - DAEM-04
  - DISC-01

must_haves:
  truths:
    - "02-VERIFICATION.md exists in .planning/phases/02-storage-engine/ with YAML frontmatter (phase, verified, status, score) and Observable Truths table mapping all 4 Phase 2 success criteria"
    - "02-VERIFICATION.md cites specific source file paths and line numbers as evidence for each verified truth"
    - "02-VERIFICATION.md covers all Phase 2 requirements: STOR-01, STOR-02, STOR-03, STOR-04, STOR-05, STOR-06, DAEM-04"
    - "05-VERIFICATION.md exists in .planning/phases/05-peer-system/ with YAML frontmatter and Observable Truths table mapping all 4 Phase 5 success criteria"
    - "05-VERIFICATION.md includes gap closure coverage: Phase 6 (sync receive side) and Phase 7 (peer discovery) are cited as evidence for Phase 5 success criteria 1-4"
    - "05-VERIFICATION.md covers Phase 5 requirements DISC-01 and DISC-02 (via Phase 7 gap closure)"
  artifacts:
    - path: ".planning/phases/02-storage-engine/02-VERIFICATION.md"
      provides: "Phase 2 Storage Engine verification report"
      min_lines: 80
    - path: ".planning/phases/05-peer-system/05-VERIFICATION.md"
      provides: "Phase 5 Peer System verification report including gap closure phases"
      min_lines: 80
  key_links:
    - from: "02-VERIFICATION.md Observable Truths"
      to: "ROADMAP.md Phase 2 Success Criteria"
      via: "1:1 mapping of each success criterion to verified truth"
    - from: "05-VERIFICATION.md Observable Truths"
      to: "ROADMAP.md Phase 5 Success Criteria"
      via: "1:1 mapping, with gap closure phases (6, 7) cited as evidence"
    - from: "02-VERIFICATION.md Requirements Coverage"
      to: "REQUIREMENTS.md traceability table"
      via: "All STOR-01 through STOR-06 and DAEM-04 marked SATISFIED"
---

<objective>
Create missing verification documents for Phase 2 (Storage Engine) and Phase 5 (Peer System). These are the two phases that were never verified after execution. Phase 5 verification must include gap closure coverage from Phases 6 (sync receive) and 7 (peer discovery).

Purpose: Close verification gaps identified by the milestone audit. Every phase needs a verification doc confirming its success criteria were met.

Output: Two VERIFICATION.md files matching the established format from 01-VERIFICATION.md.
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-verification-cleanup/08-RESEARCH.md
@.planning/phases/08-verification-cleanup/08-CONTEXT.md

<interfaces>
<!-- Verification document format from existing examples -->

From .planning/phases/01-VERIFICATION.md (format template):
```yaml
---
phase: XX-name
verified: ISO-8601-timestamp
status: passed
score: N/N must-haves verified
re_verification: false
---

# Phase XX: Name Verification Report

**Phase Goal:** [from ROADMAP.md]
**Verified:** [timestamp]
**Status:** PASSED

## Goal Achievement

### Observable Truths
| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | [from success criteria] | VERIFIED | [file:line + test evidence] |

### Required Artifacts
| Artifact | Expected | Status | Details |

### Key Link Verification
| From | To | Via | Status | Details |

### Requirements Coverage
| Requirement | Source Plan | Description | Status | Evidence |

### Anti-Patterns Found
| File | Line | Pattern | Severity | Impact |

### Test Suite Summary
[test counts and results]

### Gap Summary
[gaps or "No gaps"]
```

Phase 2 Success Criteria (from ROADMAP.md):
1. A blob stored by namespace + SHA3-256 hash can be retrieved, and storing the same blob twice results in exactly one entry (content-addressed dedup)
2. Each blob stored in a namespace receives a monotonically increasing seq_num, and blobs can be retrieved by namespace + seq_num range
3. Blobs with elapsed TTL are automatically pruned by the background expiry scanner, while TTL=0 blobs persist indefinitely
4. After a simulated crash (kill -9), the node restarts and all committed data is intact with no corruption (libmdbx ACID)

Phase 2 Requirements: STOR-01, STOR-02, STOR-03, STOR-04, STOR-05, STOR-06, DAEM-04
Phase 2 Plans: 02-01 (CMake + libmdbx + Storage), 02-02 (Sequence indexing + range queries), 02-03 (TTL expiry scanner)

Phase 5 Success Criteria (from ROADMAP.md):
1. Node connects to configured bootstrap nodes on startup and receives a peer list, then connects to discovered peers
2. Two nodes with different blob sets exchange hash lists and end up with the union of both sets (bidirectional sync)
3. Expired blobs are not replicated during sync (dead data stays dead)
4. A complete chromatindb daemon starts from config, joins the network, accepts blobs, replicates them to peers, and answers queries -- end to end

Phase 5 Requirements: DISC-01, DISC-02, SYNC-01, SYNC-02, SYNC-03
Phase 5 Plans: 05-01 (Sync protocol), 05-02 (PeerManager), 05-03 (Daemon CLI + E2E)
Gap Closure: Phase 6 (SYNC-01/02/03), Phase 7 (DISC-02)

Key source files for Phase 2:
- src/storage/storage.h, src/storage/storage.cpp
- tests/storage/ directory

Key source files for Phase 5:
- src/peer/peer_manager.h, src/peer/peer_manager.cpp
- src/sync/sync_protocol.h, src/sync/sync_protocol.cpp
- src/net/server.h, src/net/server.cpp
- tests/test_daemon.cpp (E2E tests)
- tests/peer/, tests/sync/, tests/net/ directories
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 02-VERIFICATION.md for Phase 2 (Storage Engine)</name>
  <files>.planning/phases/02-storage-engine/02-VERIFICATION.md</files>
  <action>
**1. Gather evidence from Phase 2 source code:**

Read these files to collect evidence:
- `src/storage/storage.h` -- Storage class API, StoreResult struct, sub-database declarations
- `src/storage/storage.cpp` -- Implementation of store, retrieve, dedup, seq indexing, expiry, crash recovery
- `tests/storage/` -- All test files for storage module

Run the test suite to capture current test counts:
```bash
cd /home/mika/dev/chromatin-protocol && cmake --build build --target chromatindb_tests 2>&1 | tail -5 && ./build/chromatindb_tests --abort -t "[storage]" -v 2>&1 | tail -30
```

**2. Read existing verification docs for format reference:**
- `.planning/phases/01-foundation/01-VERIFICATION.md` -- Gold standard template

**3. Read Phase 2 plans for must_haves and requirement mapping:**
- `.planning/phases/02-storage-engine/02-01-PLAN.md`
- `.planning/phases/02-storage-engine/02-02-PLAN.md`
- `.planning/phases/02-storage-engine/02-03-PLAN.md`

**4. Write 02-VERIFICATION.md** following the exact format from 01-VERIFICATION.md:

```yaml
---
phase: 02-storage-engine
verified: [current ISO-8601 timestamp]
status: passed
score: 4/4 must-haves verified
re_verification: false
---
```

Sections required (in order):
1. **Phase header** -- Goal from ROADMAP, verified timestamp, status
2. **Observable Truths** -- 4 truths from Phase 2 success criteria, each with VERIFIED status and specific file:line + test evidence
3. **Required Artifacts** -- Storage module files with status and details
4. **Key Link Verification** -- Storage -> libmdbx wiring, sub-database connections
5. **Requirements Coverage** -- Map STOR-01 through STOR-06 and DAEM-04 to source plans and evidence
6. **Anti-Patterns Found** -- Scan for TODO/FIXME/HACK in storage files
7. **Human Verification Required** -- Should be "None" (all programmatic)
8. **Test Suite Summary** -- Storage test counts and pass status
9. **Gap Summary** -- Expected "No gaps"

**CRITICAL:** Every VERIFIED cell must cite specific `file_path:line_number` and test case names. Do not write generic "see storage.cpp" citations.
  </action>
  <verify>
    <automated>test -f /home/mika/dev/chromatin-protocol/.planning/phases/02-storage-engine/02-VERIFICATION.md && grep -c "VERIFIED" /home/mika/dev/chromatin-protocol/.planning/phases/02-storage-engine/02-VERIFICATION.md</automated>
  </verify>
  <done>02-VERIFICATION.md exists with YAML frontmatter, 4 Observable Truths all VERIFIED with file:line evidence, Requirements Coverage for all 7 Phase 2 requirements (STOR-01 through STOR-06 + DAEM-04).</done>
</task>

<task type="auto">
  <name>Task 2: Create 05-VERIFICATION.md for Phase 5 (Peer System) with gap closure coverage</name>
  <files>.planning/phases/05-peer-system/05-VERIFICATION.md</files>
  <action>
**1. Gather evidence from Phase 5+6+7 source code:**

Read these files to collect evidence:
- `src/peer/peer_manager.h` and `src/peer/peer_manager.cpp` -- PeerManager with sync orchestration + PEX
- `src/sync/sync_protocol.h` and `src/sync/sync_protocol.cpp` -- Sync protocol (hash-list diff)
- `src/net/server.h` and `src/net/server.cpp` -- Server with bootstrap connect + connect_once
- `tests/test_daemon.cpp` -- E2E tests (sync, peer discovery, 3-node test)
- `tests/peer/` and `tests/sync/` directories

Run the test suite for relevant tags:
```bash
cd /home/mika/dev/chromatin-protocol && ./build/chromatindb_tests --abort -t "[e2e]" -v 2>&1 | tail -30
```
```bash
cd /home/mika/dev/chromatin-protocol && ./build/chromatindb_tests --abort -t "[peer]" -v 2>&1 | tail -20
```
```bash
cd /home/mika/dev/chromatin-protocol && ./build/chromatindb_tests --abort -t "[sync]" -v 2>&1 | tail -20
```

**2. Read Phase 5 plans and gap closure phase verification docs:**
- `.planning/phases/05-peer-system/05-01-PLAN.md`
- `.planning/phases/05-peer-system/05-02-PLAN.md`
- `.planning/phases/05-peer-system/05-03-PLAN.md`
- `.planning/phases/06-complete-sync-receive-side/06-VERIFICATION.md` -- Already verified Phase 6
- Phase 7 plans: `.planning/phases/07-peer-discovery/07-01-PLAN.md`, `07-02-PLAN.md`

**3. Write 05-VERIFICATION.md** following the exact format:

```yaml
---
phase: 05-peer-system
verified: [current ISO-8601 timestamp]
status: passed
score: 4/4 must-haves verified
re_verification: false
gap_closure:
  phase_6: "SYNC-01, SYNC-02, SYNC-03 — bidirectional sync receive side completed"
  phase_7: "DISC-02 — peer exchange protocol for discovery beyond bootstrap"
---
```

Sections required:
1. **Phase header** -- Goal from ROADMAP, verified timestamp, note about gap closure phases
2. **Observable Truths** -- 4 truths from Phase 5 success criteria. CRITICAL: For criteria 1 and 2, cite Phase 7 (PEX/peer discovery) and Phase 6 (sync receive) as evidence alongside original Phase 5 implementation. Success criterion 1 ("receives a peer list, then connects to discovered peers") is closed by Phase 7's PEX implementation. Success criterion 2 ("bidirectional sync") is closed by Phase 6's receive-side sync.
3. **Required Artifacts** -- Peer system, sync, and server files
4. **Key Link Verification** -- PeerManager -> Server -> Connection wiring, SyncProtocol integration
5. **Requirements Coverage** -- Map DISC-01 (Phase 5), DISC-02 (Phase 7), SYNC-01/02/03 (Phase 6). Each must note which phase delivered it.
6. **Anti-Patterns Found** -- Note the dead HandshakeInitiator hs2 code at connection.cpp:153-179 as Info severity (cleanup addressed in Plan 02)
7. **Human Verification Required** -- None (all E2E-tested)
8. **Test Suite Summary** -- E2E, peer, and sync test counts
9. **Gap Summary** -- "No gaps. All Phase 5 success criteria verified, including gap closure from Phases 6 and 7."

**CRITICAL:** Phase 5 success criterion 1 references "receives a peer list, then connects to discovered peers" -- this was NOT complete in Phase 5 alone. Phase 7 (PEX protocol) closes this gap. The verification must trace through to Phase 7 evidence.
  </action>
  <verify>
    <automated>test -f /home/mika/dev/chromatin-protocol/.planning/phases/05-peer-system/05-VERIFICATION.md && grep -c "VERIFIED" /home/mika/dev/chromatin-protocol/.planning/phases/05-peer-system/05-VERIFICATION.md</automated>
  </verify>
  <done>05-VERIFICATION.md exists with YAML frontmatter including gap_closure section, 4 Observable Truths all VERIFIED with evidence from Phases 5+6+7, Requirements Coverage for DISC-01, DISC-02, SYNC-01, SYNC-02, SYNC-03 with phase attribution.</done>
</task>

</tasks>

<verification>
1. 02-VERIFICATION.md exists in .planning/phases/02-storage-engine/
2. 02-VERIFICATION.md has YAML frontmatter with phase, verified, status, score fields
3. 02-VERIFICATION.md has 4 Observable Truths all marked VERIFIED
4. 02-VERIFICATION.md covers all 7 Phase 2 requirements (STOR-01 through STOR-06, DAEM-04)
5. 05-VERIFICATION.md exists in .planning/phases/05-peer-system/
6. 05-VERIFICATION.md has YAML frontmatter with gap_closure section
7. 05-VERIFICATION.md has 4 Observable Truths all marked VERIFIED
8. 05-VERIFICATION.md cites Phase 6 and Phase 7 as gap closure evidence
9. 05-VERIFICATION.md covers DISC-01, DISC-02, SYNC-01, SYNC-02, SYNC-03
10. Both docs follow exact format from 01-VERIFICATION.md
</verification>

<success_criteria>
- 02-VERIFICATION.md confirms all Phase 2 success criteria met with specific evidence
- 05-VERIFICATION.md confirms all Phase 5 success criteria met including gap closure
- Both documents use the established VERIFICATION.md format exactly
- All requirement IDs from both phases are covered and marked SATISFIED
- No generic citations -- every truth backed by file:line evidence
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-cleanup/08-01-SUMMARY.md`
</output>
