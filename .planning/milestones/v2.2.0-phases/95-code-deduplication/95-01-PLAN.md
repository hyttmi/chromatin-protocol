---
phase: 95-code-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/util/endian.h
  - db/util/blob_helpers.h
  - db/tests/util/test_endian.cpp
  - db/tests/util/test_blob_helpers.cpp
  - db/sync/sync_protocol.cpp
  - db/sync/reconciliation.cpp
  - db/storage/storage.cpp
  - db/net/framing.cpp
  - db/CMakeLists.txt
autonomous: true
requirements: [DEDUP-01, DEDUP-04, DEDUP-05]

must_haves:
  truths:
    - "All inline BE encode/decode functions removed from sync_protocol.cpp, reconciliation.cpp, storage.cpp, framing.cpp"
    - "Endian helpers throw on insufficient input (span overloads)"
    - "Blob extraction helpers throw on payloads shorter than 32/64 bytes"
    - "All 615+ existing unit tests pass with no regressions"
  artifacts:
    - path: "db/util/endian.h"
      provides: "BE read/write helpers"
      contains: "namespace chromatindb::util"
    - path: "db/util/blob_helpers.h"
      provides: "Namespace/hash extraction and blob ref encoding"
      contains: "namespace chromatindb::util"
    - path: "db/tests/util/test_endian.cpp"
      provides: "Unit tests for endian helpers"
      contains: "TEST_CASE"
    - path: "db/tests/util/test_blob_helpers.cpp"
      provides: "Unit tests for blob helpers"
      contains: "TEST_CASE"
  key_links:
    - from: "db/sync/sync_protocol.cpp"
      to: "db/util/endian.h"
      via: "include and function calls"
      pattern: '#include "db/util/endian.h"'
    - from: "db/sync/reconciliation.cpp"
      to: "db/util/endian.h"
      via: "include and function calls"
      pattern: '#include "db/util/endian.h"'
    - from: "db/storage/storage.cpp"
      to: "db/util/endian.h"
      via: "include and function calls"
      pattern: '#include "db/util/endian.h"'
---

<objective>
Create the two foundational utility headers (endian.h and blob_helpers.h) with comprehensive tests, and replace all inline duplicate patterns in the sync, storage, and framing layers.

Purpose: Establishes the shared utility foundation that all subsequent deduplication work depends on. After this plan, zero inline BE encoding functions remain in sync_protocol.cpp, reconciliation.cpp, storage.cpp, or framing.cpp.

Output: 2 new utility headers, 2 new test files, 4 source files cleaned of inline duplication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-code-deduplication/95-CONTEXT.md
@.planning/phases/95-code-deduplication/95-RESEARCH.md

@db/util/hex.h
@db/crypto/thread_pool.h
@db/tests/test_helpers.h
@db/CMakeLists.txt

<interfaces>
<!-- Source of truth for existing file-local helpers being replaced -->

From db/sync/sync_protocol.cpp (lines 129-163, anonymous namespace):
```cpp
void write_u32_be(std::vector<uint8_t>& buf, uint32_t val);
void write_u64_be(std::vector<uint8_t>& buf, uint64_t val);
uint32_t read_u32_be(const uint8_t* p);
uint64_t read_u64_be(const uint8_t* p);
```

From db/sync/reconciliation.cpp (lines 15-26, anonymous namespace):
```cpp
void write_u32_be(std::vector<uint8_t>& buf, uint32_t val);
uint32_t read_u32_be(const uint8_t* p);
```

From db/storage/storage.cpp (lines 27-91, file-local static):
```cpp
static void encode_be_u64(uint64_t val, uint8_t* out);
static uint64_t decode_be_u64(const uint8_t* data);
static void encode_be_u32(uint32_t val, uint8_t* out);
static uint32_t decode_be_u32(const uint8_t* data);
```

From db/crypto/signing.h:
```cpp
static bool verify(std::span<const uint8_t> message,
                   std::span<const uint8_t> signature,
                   std::span<const uint8_t> public_key);
static constexpr size_t PUBLIC_KEY_SIZE = 2592;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create endian.h and blob_helpers.h with dedicated test files</name>
  <files>
    db/util/endian.h
    db/util/blob_helpers.h
    db/tests/util/test_endian.cpp
    db/tests/util/test_blob_helpers.cpp
    db/CMakeLists.txt
  </files>
  <read_first>
    db/util/hex.h
    db/sync/sync_protocol.cpp
    db/sync/reconciliation.cpp
    db/storage/storage.cpp
    db/net/framing.cpp
    db/CMakeLists.txt
    db/tests/test_helpers.h
  </read_first>
  <behavior>
    endian.h tests (test_endian.cpp):
    - write_u16_be: appends 2 bytes in BE order to vector (0x1234 -> [0x12, 0x34])
    - write_u32_be: appends 4 bytes in BE order to vector (0xDEADBEEF -> [0xDE, 0xAD, 0xBE, 0xEF])
    - write_u64_be: appends 8 bytes in BE order to vector
    - store_u32_be: writes 4 bytes in BE to pre-allocated buffer
    - store_u64_be: writes 8 bytes in BE to pre-allocated buffer
    - read_u16_be(span): reads 2-byte BE, throws std::out_of_range on span < 2 bytes
    - read_u32_be(span): reads 4-byte BE, throws std::out_of_range on span < 4 bytes
    - read_u64_be(span): reads 8-byte BE, throws std::out_of_range on span < 8 bytes
    - read_u32_be(const uint8_t*): reads 4-byte BE (unchecked overload)
    - read_u64_be(const uint8_t*): reads 8-byte BE (unchecked overload)
    - Round-trip: write then read for each width produces original value
    - Boundary values: 0, UINT16_MAX, UINT32_MAX, UINT64_MAX
    - Empty span throws for all read_*_be(span) overloads

    blob_helpers.h tests (test_blob_helpers.cpp):
    - extract_namespace: returns first 32 bytes as array<uint8_t,32>, throws on payload < 32 bytes
    - extract_namespace_hash: returns pair of two 32-byte arrays, throws on payload < 64 bytes
    - encode_namespace_hash: produces 64-byte vector from two 32-byte spans
    - encode_blob_ref: encodes namespace(32) + hash(32) + seq_num(8 BE) + size(4 BE) + tombstone(1) = 77 bytes
    - Round-trip: encode_namespace_hash then extract_namespace_hash produces original values
    - extract_namespace on exactly 32 bytes succeeds
    - extract_namespace_hash on exactly 64 bytes succeeds
    - extract_namespace on 31 bytes throws std::out_of_range
  </behavior>
  <action>
    1. Create `db/tests/util/` directory (does not exist yet).

    2. Create `db/util/endian.h` following the hex.h pattern (per D-04):
       - `#pragma once`, includes: `<cstdint>`, `<span>`, `<stdexcept>`, `<string>`, `<vector>`
       - Namespace: `chromatindb::util` (per D-03)
       - Vector-append writers: `write_u16_be(vector&, uint16_t)`, `write_u32_be(vector&, uint32_t)`, `write_u64_be(vector&, uint64_t)` -- all inline
       - Fixed-buffer writers: `store_u32_be(uint8_t*, uint32_t)`, `store_u64_be(uint8_t*, uint64_t)` -- for pre-allocated buffers (storage.cpp pattern)
       - Span readers (bounds-checked, per D-01): `read_u16_be(span<const uint8_t>)`, `read_u32_be(span<const uint8_t>)`, `read_u64_be(span<const uint8_t>)` -- throw `std::out_of_range` with descriptive message on insufficient bytes
       - Raw-pointer readers (unchecked, for pre-validated internal paths): `read_u32_be(const uint8_t*)`, `read_u64_be(const uint8_t*)` -- no bounds check, for callers that already validated length
       - Use the exact implementation from RESEARCH.md Architecture Patterns section
       - IMPORTANT: Do NOT add LE helpers -- per RESEARCH open question 1, LE stays internal to auth_helpers.h (YAGNI)

    3. Create `db/util/blob_helpers.h` under `chromatindb::util` (per D-03):
       - `#pragma once`, includes: `<array>`, `<cstdint>`, `<cstring>`, `<span>`, `<stdexcept>`, `<string>`, `<vector>`
       - Also include `"db/util/endian.h"` for BE encoding in encode_blob_ref
       - `extract_namespace(span<const uint8_t> payload)` -> `array<uint8_t, 32>`: extracts first 32 bytes, throws `std::out_of_range` if payload.size() < 32 (per D-01 bounds checking)
       - `extract_namespace_hash(span<const uint8_t> payload)` -> `pair<array<uint8_t,32>, array<uint8_t,32>>`: extracts namespace+hash (64 bytes), throws if payload.size() < 64
       - `encode_namespace_hash(span<const uint8_t, 32> ns, span<const uint8_t, 32> hash)` -> `vector<uint8_t>`: produces 64-byte vector
       - `encode_blob_ref(span<const uint8_t, 32> ns, span<const uint8_t, 32> hash, uint64_t seq_num, uint32_t size, bool tombstone)` -> `vector<uint8_t>`: produces 77-byte BlobNotify-format payload (namespace:32 + hash:32 + seq_num_be:8 + size_be:4 + tombstone:1). Uses `write_u64_be` and `write_u32_be` from endian.h.
       - All functions inline in header, matching hex.h pattern

    4. Create `db/tests/util/test_endian.cpp`:
       - `#include "db/util/endian.h"` and `#include <catch2/catch_test_macros.hpp>`
       - TEST_CASE sections covering all behaviors listed above
       - Use `REQUIRE_THROWS_AS(expr, std::out_of_range)` for bounds-check tests
       - Test boundary values: 0, max for each width

    5. Create `db/tests/util/test_blob_helpers.cpp`:
       - `#include "db/util/blob_helpers.h"` and `#include <catch2/catch_test_macros.hpp>`
       - TEST_CASE sections covering all behaviors listed above
       - Verify exact byte layout for encode_blob_ref (77 bytes, correct offsets)

    6. Update `db/CMakeLists.txt`: Add `tests/util/test_endian.cpp` and `tests/util/test_blob_helpers.cpp` to the `chromatindb_tests` source list (insert after existing test files, before the closing parenthesis on line 242).
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && cmake -B build -DBUILD_TESTING=ON 2>&1 | tail -5 && cmake --build build 2>&1 | tail -10 && cd build/db && ctest -R "test_endian|test_blob_helpers" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - db/util/endian.h exists and contains `namespace chromatindb::util`
    - db/util/endian.h contains `inline void write_u16_be(`, `inline void write_u32_be(`, `inline void write_u64_be(`
    - db/util/endian.h contains `inline void store_u32_be(`, `inline void store_u64_be(`
    - db/util/endian.h contains `inline uint16_t read_u16_be(std::span`, `inline uint32_t read_u32_be(std::span`, `inline uint64_t read_u64_be(std::span`
    - db/util/endian.h contains `inline uint32_t read_u32_be(const uint8_t*`, `inline uint64_t read_u64_be(const uint8_t*`
    - db/util/endian.h contains `throw std::out_of_range`
    - db/util/blob_helpers.h exists and contains `namespace chromatindb::util`
    - db/util/blob_helpers.h contains `extract_namespace(`, `extract_namespace_hash(`, `encode_namespace_hash(`, `encode_blob_ref(`
    - db/util/blob_helpers.h contains `#include "db/util/endian.h"`
    - db/tests/util/test_endian.cpp contains `TEST_CASE` and `REQUIRE_THROWS_AS`
    - db/tests/util/test_blob_helpers.cpp contains `TEST_CASE` and `REQUIRE_THROWS_AS`
    - db/CMakeLists.txt contains `tests/util/test_endian.cpp` and `tests/util/test_blob_helpers.cpp`
    - `ctest -R test_endian` exits 0 with all tests passing
    - `ctest -R test_blob_helpers` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Both utility headers exist with full test coverage. All new tests pass. CMakeLists.txt updated.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all inline BE patterns in sync, storage, and framing files</name>
  <files>
    db/sync/sync_protocol.cpp
    db/sync/reconciliation.cpp
    db/storage/storage.cpp
    db/net/framing.cpp
  </files>
  <read_first>
    db/util/endian.h
    db/sync/sync_protocol.cpp
    db/sync/reconciliation.cpp
    db/storage/storage.cpp
    db/net/framing.cpp
  </read_first>
  <action>
    Replace all file-local BE encode/decode helpers and inline shift patterns with calls to `chromatindb::util` functions from endian.h. Add `#include "db/util/endian.h"` and a `using namespace chromatindb::util;` or explicit `chromatindb::util::` prefix at each call site (prefer explicit prefix for clarity in implementation files).

    **db/sync/sync_protocol.cpp:**
    - DELETE the anonymous namespace block containing `write_u32_be`, `write_u64_be`, `read_u32_be`, `read_u64_be` (lines 129-163)
    - Add `#include "db/util/endian.h"` to includes
    - Replace all calls: `write_u32_be(buf, val)` -> `chromatindb::util::write_u32_be(buf, val)` (same signature, drop-in)
    - Replace all calls: `write_u64_be(buf, val)` -> `chromatindb::util::write_u64_be(buf, val)` (same signature, drop-in)
    - Replace all calls: `read_u32_be(p)` -> `chromatindb::util::read_u32_be(p)` where `p` is `const uint8_t*` (unchecked overload)
    - Replace all calls: `read_u64_be(p)` -> `chromatindb::util::read_u64_be(p)` where `p` is `const uint8_t*` (unchecked overload)
    - Call sites are at approximately lines 177, 180, 190, 202, 218, 234, 255, 259, 271, 272, 281, 288 (line numbers may shift after deletion)

    **db/sync/reconciliation.cpp:**
    - DELETE the anonymous namespace block containing `write_u32_be`, `read_u32_be` (lines 15-26)
    - Add `#include "db/util/endian.h"` to includes
    - Replace all calls: same pattern as sync_protocol.cpp
    - Call sites at approximately lines 257, 271, 289, 303, 308, 325, 345, 352, 385, 399

    **db/storage/storage.cpp:**
    - DELETE the static functions `encode_be_u64`, `decode_be_u64`, `encode_be_u32`, `decode_be_u32` (lines 27-91)
    - Add `#include "db/util/endian.h"` to includes
    - Replace `encode_be_u64(val, out)` -> `chromatindb::util::store_u64_be(out, val)` (NOTE: argument order reversal -- endian.h puts destination first)
    - Replace `decode_be_u64(data)` -> `chromatindb::util::read_u64_be(data)` (raw pointer overload, unchecked)
    - Replace `encode_be_u32(val, out)` -> `chromatindb::util::store_u32_be(out, val)` (NOTE: argument order reversal)
    - Replace `decode_be_u32(data)` -> `chromatindb::util::read_u32_be(data)` (raw pointer overload)
    - CAUTION: storage.cpp has ~25 call sites spread across the file. Replace ALL of them.

    **db/net/framing.cpp:**
    - Add `#include "db/util/endian.h"` to includes
    - Replace the inline 4-byte BE write at line ~32-35 with `chromatindb::util::store_u32_be(header, len)` (writing frame length to pre-allocated buffer)
    - If there is a read pattern, replace with `chromatindb::util::read_u32_be(...)` as appropriate

    After all replacements, verify zero file-local BE encode/decode functions remain in these 4 files.
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && cmake --build build 2>&1 | tail -10 && cd build/db && ctest --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sync_protocol.cpp contains `#include "db/util/endian.h"`
    - sync_protocol.cpp does NOT contain `void write_u32_be(` or `uint32_t read_u32_be(const uint8_t` as a function definition
    - reconciliation.cpp contains `#include "db/util/endian.h"`
    - reconciliation.cpp does NOT contain `void write_u32_be(` or `uint32_t read_u32_be(const uint8_t` as a function definition
    - storage.cpp contains `#include "db/util/endian.h"`
    - storage.cpp does NOT contain `static void encode_be_u64` or `static uint64_t decode_be_u64` or `static void encode_be_u32` or `static uint32_t decode_be_u32`
    - framing.cpp contains `#include "db/util/endian.h"`
    - All 615+ existing unit tests pass (ctest exits 0)
  </acceptance_criteria>
  <done>All file-local BE encode/decode helpers in sync_protocol.cpp, reconciliation.cpp, storage.cpp, and framing.cpp are deleted and replaced with chromatindb::util calls. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "static.*encode_be_u\|static.*decode_be_u\|void write_u32_be\|void write_u64_be\|uint32_t read_u32_be.*const uint8_t\*.*{" db/sync/ db/storage/` returns zero matches (all file-local helpers removed)
2. `grep -c "chromatindb::util::" db/sync/sync_protocol.cpp db/sync/reconciliation.cpp db/storage/storage.cpp db/net/framing.cpp` shows non-zero counts in all 4 files
3. `cd build/db && ctest --output-on-failure` -- all tests pass including new test_endian and test_blob_helpers
</verification>

<success_criteria>
- endian.h provides write/read helpers for u16_be, u32_be, u64_be with both span (checked) and pointer (unchecked) overloads
- blob_helpers.h provides extract_namespace, extract_namespace_hash, encode_namespace_hash, encode_blob_ref
- Zero inline BE encode/decode functions remain in sync_protocol.cpp, reconciliation.cpp, storage.cpp, framing.cpp
- All 615+ existing tests pass with no regressions
- New utility tests pass (test_endian, test_blob_helpers)
</success_criteria>

<output>
After completion, create `.planning/phases/95-code-deduplication/95-01-SUMMARY.md`
</output>
