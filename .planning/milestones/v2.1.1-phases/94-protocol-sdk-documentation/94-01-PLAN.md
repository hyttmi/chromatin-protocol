---
phase: 94-protocol-sdk-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/PROTOCOL.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "PROTOCOL.md documents tombstone-based delegation revocation workflow with propagation bounds"
    - "PROTOCOL.md documents UserEntry v2 binary format with key_version field"
    - "PROTOCOL.md documents key ring fallback decryption replacing binary search"
    - "PROTOCOL.md documents group membership revocation with forward exclusion semantics"
  artifacts:
    - path: "db/PROTOCOL.md"
      provides: "Complete protocol documentation for v2.1.1 features"
      contains: "Delegation Revocation"
    - path: "db/PROTOCOL.md"
      provides: "UserEntry v2 format spec under Client-Side Envelope Encryption"
      contains: "UserEntry"
    - path: "db/PROTOCOL.md"
      provides: "Group membership revocation under Client-Side Envelope Encryption"
      contains: "Group Membership"
  key_links:
    - from: "db/PROTOCOL.md Namespace Delegation section"
      to: "revocation workflow"
      via: "new subsection after delegation description"
      pattern: "Delegation Revocation"
    - from: "db/PROTOCOL.md Client-Side Envelope Encryption section"
      to: "UserEntry v2 + key ring decrypt + GroupEntry + group revocation"
      via: "new ### subsections within envelope encryption section"
      pattern: "UserEntry.*v2"
---

<objective>
Update PROTOCOL.md with complete documentation for delegation revocation, UserEntry v2 binary format with key versioning, key ring fallback decryption, and group membership revocation.

Purpose: PROTOCOL.md is the canonical wire-level reference for anyone building a compatible client. It must document all v2.1.1 features so a developer in any language can implement them.
Output: Updated db/PROTOCOL.md with four new subsections extending existing content per D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-protocol-sdk-documentation/94-CONTEXT.md
@.planning/phases/91-sdk-delegation-revocation/91-01-SUMMARY.md
@.planning/phases/92-kem-key-versioning/92-01-SUMMARY.md
@.planning/phases/92-kem-key-versioning/92-02-SUMMARY.md
@.planning/phases/92-kem-key-versioning/92-03-SUMMARY.md
@.planning/phases/93-group-membership-revocation/93-01-SUMMARY.md
@.planning/phases/93-group-membership-revocation/93-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delegation revocation, directory formats, and group membership sections to PROTOCOL.md</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    - db/PROTOCOL.md (full file -- understand structure and insertion points)
    - sdk/python/chromatindb/_directory.py (lines 367-421 for revoke_delegation, list_delegates; lines 584-620 for remove_member; lines 95-160 for group entry encoding/decoding)
    - sdk/python/chromatindb/client.py (lines 1031-1066 for write_to_group with refresh)
  </read_first>
  <action>
    Edit db/PROTOCOL.md to add new subsections. Per D-01, extend existing sections rather than creating new top-level sections. Per D-03, document correct behavior only -- no changelog callouts.

    **1. Delegation Revocation subsection (insert at line 439, after the existing "Revocation is done by tombstoning the delegation blob." sentence)**

    Replace the single sentence "Revocation is done by tombstoning the delegation blob." with a full subsection:

    ```
    #### Delegation Revocation

    Revocation is done by tombstoning the delegation blob. The workflow:

    1. Compute `SHA3-256(delegate_signing_pubkey)` to get the delegate's `pk_hash` (32 bytes)
    2. Call `DelegationList` to enumerate active delegations in the namespace
    3. Find the delegation entry whose `delegate_pk_hash` matches
    4. Tombstone the delegation blob via `Delete` (type 17) using the `delegation_blob_hash`

    Once the tombstone is stored, the node removes the delegate from the namespace's `delegation_map` index. Subsequent writes signed by the revoked delegate are rejected immediately.

    **Propagation bounds:** For peers connected at the time of revocation, the tombstone propagates via BlobNotify (type 59) -- near-instant delivery (sub-second on LAN). For disconnected peers, propagation occurs on the next sync round: either on reconnect (sync-on-connect) or at the safety-net interval (default 600 seconds). There is no separate revocation protocol -- revocation reuses the existing tombstone + sync mechanism.

    After revocation, `DelegationList` for the namespace no longer includes the revoked delegate's entry. The tombstone is permanent (`ttl=0`) and replicates like any other blob.
    ```

    **2. Directory subsections under "## Client-Side Envelope Encryption" (insert AFTER the "### Decryption" subsection at line 1078, BEFORE "## Prometheus Metrics Endpoint" at line 1080)**

    Do NOT create a new `##` top-level section. Instead, add the following as `###` subsections within the existing `## Client-Side Envelope Encryption` section. The directory layer is documented here because UserEntry and GroupEntry are the structures that envelope encryption operates on -- envelope_encrypt resolves recipients via UserEntry, and write_to_group resolves members via GroupEntry.

    Insert these subsections after line 1078 (end of Decryption) and before line 1080 (start of Prometheus):

    ```
    ### Directory: User Entries and Groups

    The SDK implements a directory layer over the blob store for user discovery and group management. The directory uses the admin's namespace for storage. These formats are documented here because envelope encryption depends on them: `envelope_encrypt` resolves recipient KEM public keys from UserEntry blobs, and `write_to_group` resolves group membership from GroupEntry blobs.

    #### UserEntry Binary Format (v2)

    A UserEntry blob allows users to publish their encryption public key for discovery. The format uses magic `UENT` (0x55454E54):

    ```
    [magic:4][version:1][signing_pk:2592][kem_pk:1568][name_len:2 BE][name:variable][key_version:4 BE][kem_sig:variable]
    ```

    | Offset | Size | Field | Encoding | Description |
    |--------|------|-------|----------|-------------|
    | 0 | 4 | magic | `UENT` (0x55454E54) | UserEntry format identifier |
    | 4 | 1 | version | uint8 | Format version (0x02) |
    | 5 | 2592 | signing_pk | raw bytes | ML-DSA-87 public key |
    | 2597 | 1568 | kem_pk | raw bytes | ML-KEM-1024 public key |
    | 4165 | 2 | name_len | big-endian uint16 | Display name byte length (max 256) |
    | 4167 | variable | name | UTF-8 | Display name |
    | 4167+N | 4 | key_version | big-endian uint32 | KEM key version (0 for initial, increments on rotation) |
    | 4171+N | variable | kem_sig | raw bytes | ML-DSA-87 signature of (kem_pk || key_version_be) |

    Minimum size: 4172 bytes (4 magic + 1 version + 2592 signing_pk + 1568 kem_pk + 2 name_len + 4 key_version + 1 minimum kem_sig).

    The `kem_sig` field signs the concatenation of `kem_pk` (1568 bytes) and `key_version` encoded as 4-byte big-endian. This binds the key version to the signature, preventing version downgrade attacks where an attacker replays an old UserEntry with a lower key_version.

    Version 0x01 entries (without `key_version`) are rejected. No backward compatibility is maintained for pre-rotation entries.

    ##### Key Version Semantics

    The `key_version` field tracks KEM key rotation. When a user rotates their ML-KEM-1024 keypair, they publish a new UserEntry with an incremented `key_version`. The directory cache retains only the entry with the highest `key_version` per signing key, ensuring `resolve_recipient()` always returns the latest encryption public key.

    Multiple UserEntry blobs for the same signing key may coexist in storage (old versions are not deleted). The cache deduplication uses a highest-version-wins strategy: when populating the cache, if an entry for the same signing key already exists with a `key_version` greater than or equal to the new entry's version, the new entry is skipped.

    #### GroupEntry Binary Format

    A GroupEntry blob stores a named member list. The format uses magic `GRPE` (0x47525045):

    ```
    [magic:4][version:1][name_len:2 BE][name:variable][member_count:2 BE][N x member_hash:32]
    ```

    | Offset | Size | Field | Encoding | Description |
    |--------|------|-------|----------|-------------|
    | 0 | 4 | magic | `GRPE` (0x47525045) | GroupEntry format identifier |
    | 4 | 1 | version | uint8 | Format version (0x01) |
    | 5 | 2 | name_len | big-endian uint16 | Group name byte length |
    | 7 | variable | name | UTF-8 | Group name |
    | 7+N | 2 | member_count | big-endian uint16 | Number of members |
    | 9+N | 32 each | member_hash | raw bytes | SHA3-256(signing_pk) per member |

    Minimum size: 9 bytes (4 magic + 1 version + 2 name_len + 2 member_count).

    Members are identified by `SHA3-256(signing_public_key)` -- the same 32-byte hash used as namespace IDs.

    ##### Group Membership Revocation

    To remove a member from a group:

    1. Read the current GroupEntry blob for the group name
    2. Filter out the member's `SHA3-256(signing_pk)` hash from the member list
    3. Write a new GroupEntry blob with the reduced member list

    The old GroupEntry blob remains in storage but the directory cache uses timestamp ordering (newest blob wins for same group name). The `>=` comparison ensures that a removal written in the same second as the original group creation correctly supersedes it.

    **Forward exclusion:** After removal, `write_to_group()` forces a directory cache refresh before resolving group membership. This guarantees that the removed member is excluded from the recipient stanza list of new encrypted blobs. The removed member cannot decrypt data encrypted after their removal.

    **Old data remains readable:** Removal does not re-encrypt existing data. The removed member retains their KEM secret key and can still decrypt any envelope where their `kem_pk_hash` appears in a recipient stanza. This is by design -- forward exclusion prevents access to new data, not retroactive revocation of old data.

    **No re-encryption on removal:** Re-encrypting old data for remaining members would require the admin to read and re-write every historical blob, which is impractical for large datasets and breaks content addressing. The security boundary is: removal prevents future access, not past access.
    ```

    IMPORTANT: The heading hierarchy must be:
    - `## Client-Side Envelope Encryption` (existing top-level, do NOT modify)
      - `### Directory: User Entries and Groups` (new subsection within it)
        - `#### UserEntry Binary Format (v2)`
          - `##### Key Version Semantics`
        - `#### GroupEntry Binary Format`
          - `##### Group Membership Revocation`

    Do NOT create any new `##` sections. All new content lives within the existing `## Client-Side Envelope Encryption` section.
  </action>
  <verify>
    <automated>grep -c "Delegation Revocation" db/PROTOCOL.md | grep -q "[1-9]" && grep -c "UserEntry.*Format" db/PROTOCOL.md | grep -q "[1-9]" && grep -c "Group Membership Revocation" db/PROTOCOL.md | grep -q "[1-9]" && grep -c "key_version" db/PROTOCOL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Delegation Revocation" db/PROTOCOL.md returns at least 1 match
    - grep "SHA3-256(delegate_signing_pubkey)" db/PROTOCOL.md returns a match (revocation workflow step 1)
    - grep "Propagation bounds" db/PROTOCOL.md returns a match
    - grep "near-instant.*safety-net" db/PROTOCOL.md returns a match (propagation window description)
    - grep "### Directory: User Entries and Groups" db/PROTOCOL.md returns exactly 1 match (### not ##)
    - grep "#### UserEntry Binary Format" db/PROTOCOL.md returns a match (#### level under ###)
    - grep "0x02" db/PROTOCOL.md returns matches (version byte)
    - grep "key_version" db/PROTOCOL.md returns at least 3 matches (field, semantics, signature binding)
    - grep "kem_pk.*key_version_be" db/PROTOCOL.md returns a match (signature binding)
    - grep "highest.*key_version" db/PROTOCOL.md returns a match (cache dedup)
    - grep "#### GroupEntry Binary Format" db/PROTOCOL.md returns a match (#### level under ###)
    - grep "GRPE" db/PROTOCOL.md returns a match
    - grep "##### Group Membership Revocation" db/PROTOCOL.md returns a match (##### level under ####)
    - grep "Forward exclusion" db/PROTOCOL.md returns a match
    - grep "Old data remains readable" db/PROTOCOL.md returns a match
    - grep "directory cache refresh" db/PROTOCOL.md returns a match (write_to_group refresh behavior)
    - "### Directory: User Entries and Groups" appears AFTER "### Decryption" and BEFORE "## Prometheus Metrics Endpoint" -- all within "## Client-Side Envelope Encryption"
    - No new "## Directory" top-level section exists (grep "^## Directory" returns 0 matches)
  </acceptance_criteria>
  <done>
    PROTOCOL.md contains: (1) delegation revocation workflow with propagation bounds under Namespace Delegation, (2) UserEntry v2 binary format table with key_version field and version-bound signature as #### under ### Directory within ## Client-Side Envelope Encryption, (3) key version semantics with highest-version-wins cache, (4) GroupEntry binary format as #### under ### Directory within ## Client-Side Envelope Encryption, (5) group membership revocation with forward exclusion and old-data-readable notes. No new top-level ## sections created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROTOCOL.md decryption section for key ring fallback</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    - db/PROTOCOL.md (lines 1067-1078 -- current Decryption section)
    - sdk/python/chromatindb/_envelope.py (lines 240-290 for key ring map lookup)
    - sdk/python/chromatindb/identity.py (lines 421-484 for rotate_kem and key ring)
  </read_first>
  <action>
    Edit the existing "### Decryption" section in db/PROTOCOL.md (currently at approximately line 1067) to document the key ring fallback mechanism that replaced binary search in Phase 92.

    Per D-03, document the correct current behavior only -- no mention of the old binary search approach.

    Replace the current 8-step decryption algorithm with:

    ```
    ### Decryption

    To decrypt an envelope:

    1. Parse fixed header: validate magic `CENV` (0x43454E56), version 0x01, suite 0x01 or 0x02
    2. Build a key ring map: `{SHA3-256(kem_pk): kem_secret_key}` for all KEM keypairs held by the recipient (current key and any retained historical keys from prior rotations)
    3. Scan recipient stanzas sequentially, checking each stanza's `kem_pk_hash` against the key ring map
    4. If no stanza matches any key in the ring, reject with NotARecipientError
    5. Decapsulate `kem_ciphertext` with the matched KEM secret key to recover the shared secret
    6. Derive KEK: `HKDF-SHA256(ikm=shared_secret, salt=empty, info="chromatindb-envelope-kek-v1")` producing 32 bytes
    7. Decrypt `wrapped_dek` with KEK and zero nonce to recover the 32-byte DEK
    8. If suite is 0x02: decrypt data ciphertext, then Brotli-decompress the plaintext (100 MiB decompression limit)
       If suite is 0x01: decrypt data ciphertext directly
       Associated data for the data AEAD is the full header (fixed header + all recipient stanzas)

    #### Key Ring Fallback

    After KEM key rotation, a recipient holds multiple KEM keypairs (the current key plus all retained historical keys). The key ring map enables decryption of envelopes encrypted under any historical key:

    - **Current key:** Envelopes encrypted after the sender discovered the rotation use the latest `kem_pk`. The stanza's `kem_pk_hash` matches the current key in the ring.
    - **Historical keys:** Envelopes encrypted before the sender discovered the rotation (or encrypted before rotation occurred) use an older `kem_pk`. The stanza's `kem_pk_hash` matches a retained historical key in the ring.

    The envelope binary format is unchanged -- the `kem_pk_hash` field in each stanza already identifies which key was used. No envelope version bump is required for key ring support.

    Implementations must retain all historical KEM secret keys for as long as data encrypted under those keys may need to be decrypted. Discarding a historical key permanently prevents decryption of any envelope whose stanza references that key.
    ```

    This replaces steps 2-4 (which previously described computing a single pk_hash and binary searching) with the ring map approach.
  </action>
  <verify>
    <automated>grep -c "key ring map" db/PROTOCOL.md | grep -q "[1-9]" && grep -c "Key Ring Fallback" db/PROTOCOL.md | grep -q "[1-9]" && grep -c "historical keys" db/PROTOCOL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "key ring map" db/PROTOCOL.md returns at least 1 match
    - grep "Key Ring Fallback" db/PROTOCOL.md returns a match (subsection header)
    - grep "historical key" db/PROTOCOL.md returns at least 1 match
    - grep "SHA3-256(kem_pk).*kem_secret_key" db/PROTOCOL.md returns a match (ring map definition)
    - grep "Binary search" db/PROTOCOL.md returns 0 matches (old approach removed per D-03)
    - grep "suite.*0x02" db/PROTOCOL.md returns a match (Brotli suite documented)
    - grep "envelope binary format is unchanged" db/PROTOCOL.md returns a match (no format change needed)
    - The decryption section mentions scanning stanzas against the ring map (not binary search)
  </acceptance_criteria>
  <done>
    PROTOCOL.md Decryption section documents the key ring map approach with sequential stanza scan, Key Ring Fallback subsection explains historical key retention, and the old binary search description is replaced with correct current behavior
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "Delegation Revocation" db/PROTOCOL.md` returns >= 1
2. `grep -c "### Directory: User Entries and Groups" db/PROTOCOL.md` returns 1 (### level, not ##)
3. `grep -c "#### UserEntry Binary Format" db/PROTOCOL.md` returns 1
4. `grep -c "key_version" db/PROTOCOL.md` returns >= 3
5. `grep -c "##### Group Membership Revocation" db/PROTOCOL.md` returns 1
6. `grep -c "Key Ring Fallback" db/PROTOCOL.md` returns >= 1
7. `grep -c "Forward exclusion" db/PROTOCOL.md` returns >= 1
8. `grep -c "Binary search" db/PROTOCOL.md` returns 0 (old approach removed)
9. `grep -c "^## Directory" db/PROTOCOL.md` returns 0 (no new top-level section)
10. The document has valid markdown structure (no broken headers or tables)
</verification>

<success_criteria>
PROTOCOL.md comprehensively documents:
- Delegation revocation workflow (tombstone, propagation bounds)
- UserEntry v2 binary format with key_version field and version-bound kem_sig
- Key version semantics (highest-version-wins cache, resolve_recipient)
- GroupEntry binary format
- Group membership revocation (remove_member, forward exclusion, old data readable)
- Key ring fallback decryption (ring map, historical key retention)
All new content extends existing sections per D-01 -- no new ## top-level sections. UserEntry, GroupEntry, and group revocation are ### / #### / ##### subsections within "## Client-Side Envelope Encryption". No changelog callouts per D-03.
</success_criteria>

<output>
After completion, create `.planning/phases/94-protocol-sdk-documentation/94-01-SUMMARY.md`
</output>
