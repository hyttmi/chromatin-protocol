---
phase: 37-general-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/README.md
  - db/PROTOCOL.md
autonomous: true
requirements: [CLEAN-03, CLEAN-04]

must_haves:
  truths:
    - "db/README.md documents all v0.7.0 features including sync resumption and namespace quotas"
    - "db/README.md config example includes all 5 new v0.7.0 fields"
    - "db/README.md SIGHUP section lists quota and cursor config as reloadable"
    - "db/README.md has no Performance section or references to chromatindb_bench"
    - "db/PROTOCOL.md documents all 27 message types including TrustedHello, PQRequired, and QuotaExceeded"
    - "Wire protocol message type count is 27 in both README and PROTOCOL.md"
    - "Top-level README.md remains a consistent thin pointer to db/README.md"
  artifacts:
    - path: "db/README.md"
      provides: "Complete project documentation with v0.7.0 features"
      contains: "Sync Resumption"
    - path: "db/PROTOCOL.md"
      provides: "Complete wire protocol documentation with all 27 message types"
      contains: "QuotaExceeded"
  key_links:
    - from: "db/README.md"
      to: "db/PROTOCOL.md"
      via: "message type count must match"
      pattern: "27 message types"
    - from: "db/README.md"
      to: "db/config/config.h"
      via: "config fields documented must match actual config struct"
      pattern: "full_resync_interval"
---

<objective>
Update db/README.md and db/PROTOCOL.md to accurately reflect all v0.7.0 changes. Remove stale performance data from README, add new feature bullets, update config documentation, and add missing wire protocol message types to PROTOCOL.md.

Purpose: CLEAN-03 requires README to document current features with no stale data. CLEAN-04 requires PROTOCOL.md to document all message types (currently missing 3 added in v0.5.0 and v0.7.0).
Output: Both documentation files fully up to date with v0.7.0 state.
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-general-cleanup/37-CONTEXT.md
@.planning/phases/37-general-cleanup/37-RESEARCH.md

<interfaces>
<!-- Authoritative sources for documentation updates -->

From db/schemas/transport.fbs (27 message types, None=0 through QuotaExceeded=26):
- TrustedHello (24): Lightweight handshake initiation from trusted peer
- PQRequired (25): Response indicating full PQ handshake required (peer not trusted)
- QuotaExceeded (26): Rejection when namespace quota limit reached

From db/config/config.h (v0.7.0 fields to document):
- full_resync_interval (uint32_t, default 10): Full hash-diff resync every Nth sync round
- cursor_stale_seconds (uint64_t, default 3600): Force full resync after this time gap
- namespace_quota_bytes (uint64_t, default 0): Global default byte limit per namespace (0 = unlimited)
- namespace_quota_count (uint64_t, default 0): Global default blob count limit per namespace (0 = unlimited)
- namespace_quotas (map, default {}): Per-namespace overrides {"hex_ns": {"max_bytes": N, "max_count": N}}

SIGHUP-reloadable fields (from db/peer/peer_manager.cpp):
- Existing: allowed_keys, trusted_peers, rate_limit_bytes_per_sec, rate_limit_burst, sync_namespaces
- New: full_resync_interval, cursor_stale_seconds, namespace_quota_bytes, namespace_quota_count, namespace_quotas

v0.7.0 feature summaries for README:
1. Sync Resumption: Per-peer per-namespace seq_num cursors skip unchanged namespaces. O(new) not O(total). Persist across restarts. Periodic full resync as safety net.
2. Namespace Quotas: Per-namespace byte and blob count limits at ingest. QuotaExceeded message to peers. Per-namespace overrides.

PROTOCOL.md hash-then-sign: Already correct (lines 113-125). No change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update db/README.md with v0.7.0 changes</name>
  <files>db/README.md</files>
  <action>
Read db/README.md fully, then make these changes:

1. **Remove Performance section (lines 247-301):** Delete the entire section including the "## Performance" heading, all three benchmark tables (Crypto Operations, Data Path, Network Operations), the summary paragraph, and the "### Running Benchmarks" subsection with its code block. The "## License" section should follow directly after the Features section.

2. **Update Wire Protocol section (line 132):** Change "24 message types" to "27 message types".

3. **Add config fields to JSON example (after line 97, before the closing brace):** Add these 5 fields:
```json
  "full_resync_interval": 10,
  "cursor_stale_seconds": 3600,
  "namespace_quota_bytes": 0,
  "namespace_quota_count": 0,
  "namespace_quotas": {}
```

4. **Add config field descriptions (after sync_namespaces description, before "## Signals"):** Add bullet descriptions:
- **full_resync_interval** -- full hash-diff resync every Nth sync round, overriding cursor-based skip (default: `10`)
- **cursor_stale_seconds** -- force full resync with a peer after this many seconds since last sync (default: `3600`)
- **namespace_quota_bytes** -- global default maximum bytes per namespace; the node rejects new blobs when a namespace exceeds this limit and sends QuotaExceeded to the peer (default: `0` = unlimited)
- **namespace_quota_count** -- global default maximum blob count per namespace (default: `0` = unlimited)
- **namespace_quotas** -- per-namespace quota overrides as a JSON object mapping namespace hash (64-char hex) to `{"max_bytes": N, "max_count": N}`; overrides the global defaults for listed namespaces (default: `{}` = use global defaults)

5. **Update SIGHUP section (line 120):** Change the reloadable fields list to include cursor and quota config. The new sentence should read: "Re-reads the config file and updates `allowed_keys`, `trusted_peers`, `rate_limit_bytes_per_sec`, `rate_limit_burst`, `sync_namespaces`, `full_resync_interval`, `cursor_stale_seconds`, `namespace_quota_bytes`, `namespace_quota_count`, and `namespace_quotas` without restarting the daemon."

6. **Add feature bullets (in Features section, after "Writer-Controlled TTL" bullet):** Add two new feature paragraphs following the existing pattern (bold title + em-dash + description):

**Sync Resumption** -- Per-peer per-namespace sequence number cursors track sync progress. Only namespaces with new blobs since the last sync exchange hash lists, reducing sync cost from O(total blobs) to O(new blobs). Cursors persist across node restarts via libmdbx. A periodic full hash-diff resync every Nth round (configurable) serves as a safety net against cursor drift.

**Namespace Quotas** -- Per-namespace byte and blob count limits enforced at ingest. When a write would exceed the configured quota, the node rejects the blob and sends a QuotaExceeded signal to the writing peer. Global defaults apply to all namespaces, with per-namespace overrides for differentiated limits. Quota configuration is reloadable via SIGHUP.

7. **Verify top-level README.md:** Read README.md (top-level) and confirm it still correctly points to db/README.md. No changes needed if it remains a thin pointer.
  </action>
  <verify>
    <automated>grep -c "chromatindb_bench\|Running Benchmarks\|## Performance" /home/mika/dev/chromatin-protocol/db/README.md; test $? -eq 1 || echo "FAIL: stale bench references remain"</automated>
  </verify>
  <done>db/README.md has no Performance section, documents all 27 message types, lists all v0.7.0 config fields with descriptions, SIGHUP section includes new reloadable fields, Features section includes Sync Resumption and Namespace Quotas</done>
</task>

<task type="auto">
  <name>Task 2: Update db/PROTOCOL.md with missing message types</name>
  <files>db/PROTOCOL.md</files>
  <action>
Read db/PROTOCOL.md fully, then make these changes:

1. **Update message type count (line 283):** Change "All 24 message types" to "All 27 message types".

2. **Add 3 missing rows to the message type table (after line 310, StorageFull row):** Add:

| 24 | TrustedHello | Lightweight handshake: trusted peer identity exchange (ML-DSA-87 pubkey + signature, no KEM) |
| 25 | PQRequired | Lightweight handshake rejection: responder requires full PQ handshake (empty payload) |
| 26 | QuotaExceeded | Quota signaling: namespace byte or count limit reached (empty payload) |

3. **Add Lightweight Handshake section (after the "Step 2: PQ Handshake" section, before "Step 3: Encrypted Session"):** Add a subsection describing the trusted peer handshake flow:

### Lightweight Handshake (Trusted Peers)

Connections from localhost (127.0.0.1, ::1) or addresses listed in `trusted_peers` use a simplified handshake that skips ML-KEM-1024 key exchange. This reduces connection latency for trusted LAN deployments.

```
Initiator                              Responder
    |                                      |
    |--- [raw] TrustedHello ------------->|  ML-DSA-87 pubkey (2592 bytes) + signature
    |                                      |  Responder checks trust list
    |<-- [raw] TrustedHello --------------|  ML-DSA-87 pubkey (2592 bytes) + signature
    |                                      |
    |   Both derive session keys via HKDF-SHA256:
    |     ikm    = SHA3-256(initiator_pubkey || responder_pubkey)
    |     info1  = "chromatin-init-to-resp-v1"  -->  initiator-to-responder key
    |     info2  = "chromatin-resp-to-init-v1"  -->  responder-to-initiator key
    |                                      |
    |   Session established (AEAD-encrypted from here).
```

If the responder does not recognize the initiator as trusted, it replies with `PQRequired (25)` instead of `TrustedHello`. The initiator then falls back to the full PQ handshake starting from KemPubkey.

4. **Add QuotaExceeded to Additional Interactions section (after StorageFull subsection):** Add:

### Quota Signaling

**QuotaExceeded** (`type = QuotaExceeded (26)`): Empty payload. Sent by a node when a blob write would exceed the configured per-namespace byte or count quota. Unlike StorageFull (which signals global capacity), QuotaExceeded indicates that the specific namespace has reached its limit. Other namespaces may still accept writes.

5. **Verify hash-then-sign documentation:** Confirm lines 113-125 already describe SHA3-256(namespace_id || data || ttl_le32 || timestamp_le64) correctly. Per research, this is already accurate -- no change needed.
  </action>
  <verify>
    <automated>grep -c "TrustedHello\|PQRequired\|QuotaExceeded" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md | grep -q "^3$" && echo "PASS" || echo "FAIL: not all 3 new message types documented"</automated>
  </verify>
  <done>db/PROTOCOL.md documents all 27 message types, includes lightweight handshake section, includes QuotaExceeded section, hash-then-sign documentation verified accurate</done>
</task>

</tasks>

<verification>
1. `grep "27 message types" db/README.md db/PROTOCOL.md` returns matches in both files
2. `grep -c "Performance\|chromatindb_bench\|Running Benchmarks" db/README.md` returns 0
3. `grep "full_resync_interval\|cursor_stale_seconds\|namespace_quota_bytes\|namespace_quota_count\|namespace_quotas" db/README.md` returns matches for all 5 fields
4. `grep "Sync Resumption\|Namespace Quotas" db/README.md` returns matches for both features
5. `grep "TrustedHello\|PQRequired\|QuotaExceeded" db/PROTOCOL.md` returns matches for all 3
6. Top-level README.md still correctly references db/README.md
</verification>

<success_criteria>
- db/README.md Performance section completely removed (no benchmark tables, no "Running Benchmarks")
- db/README.md Wire Protocol section says "27 message types"
- db/README.md config JSON example includes all 5 new fields
- db/README.md config descriptions cover all 5 new fields
- db/README.md SIGHUP section lists all reloadable fields including new ones
- db/README.md Features section includes Sync Resumption and Namespace Quotas
- db/PROTOCOL.md message type table has 27 rows (None=0 through QuotaExceeded=26)
- db/PROTOCOL.md has Lightweight Handshake section with flow diagram
- db/PROTOCOL.md has QuotaExceeded section in Additional Interactions
- Top-level README.md unchanged (thin pointer, still consistent)
</success_criteria>

<output>
After completion, create `.planning/phases/37-general-cleanup/37-02-SUMMARY.md`
</output>
