---
phase: 124
plan: 03
type: execute
wave: 3
depends_on: [124-01, 124-02]
files_modified:
  - cli/src/commands.cpp
  - cli/src/chunked.cpp
autonomous: true
requirements:
  - SC-124-2
  - SC-124-4
  - SC-124-6
user_setup: []

must_haves:
  truths:
    - "All 9 blob-construction sites in `cli/src/commands.cpp` route through `build_owned_blob` + `encode_blob_write_body`; zero residual `blob.namespace_id =` or `blob.pubkey.assign(` in the file."
    - "All 3 blob-construction sites in `cli/src/chunked.cpp` route through `build_owned_blob` + `encode_blob_write_body`; zero residual `blob.namespace_id =` or `blob.pubkey.assign(` in the file."
    - "Every writes-to-user-data / PUBK / DLGT / CENV / NAME send uses `MsgType::BlobWrite = 64`."
    - "Every tombstone / BOMB / revoke send KEEPS `MsgType::Delete = 17` (node emits DeleteAck=18 for these; RESEARCH Q3)."
    - "`submit_name_blob` and `submit_bomb_blob` file-local helpers refactored to internally call `build_owned_blob`, eliminating their copy-paste of the sign-then-fill sequence."
    - "`build_cdat_blob_flatbuf` and `build_tombstone_flatbuf` in chunked.cpp refactored; their return values are now BlobWriteBody-envelope bytes (not bare Blob bytes)."
    - "All tests in `cli_tests` (including [wire], [pubk], [chunked], [envelope], [contacts], [identity], [pipeline]) pass after the migration."
    - "TEMP-124 compile-fix stubs from plan 01 are replaced with real migrations — zero `// TEMP-124` comments remain."
  artifacts:
    - path: cli/src/commands.cpp
      provides: "Post-migration controller; all 9 construction sites use build_owned_blob + BlobWrite/Delete envelope correctly"
      contains: "build_owned_blob"
    - path: cli/src/chunked.cpp
      provides: "Post-migration chunked service; 3 construction sites migrated"
      contains: "build_owned_blob"
  key_links:
    - from: "cli/src/commands.cpp"
      to: "cli/src/wire.h"
      via: "`build_owned_blob` + `encode_blob_write_body` calls at 9 sites; `MsgType::BlobWrite` for writes; `MsgType::Delete` retained for tombstones"
      pattern: "build_owned_blob\\(id"
    - from: "cli/src/chunked.cpp"
      to: "cli/src/wire.h"
      via: "Helper-body rewrites call build_owned_blob + encode_blob_write_body; send paths use MsgType::BlobWrite (writes) or MsgType::Delete (tombstones)"
      pattern: "encode_blob_write_body"
---

<objective>
Migrate the 12 blob-construction sites identified by RESEARCH Q1 (9 in `cli/src/commands.cpp` at lines :513, :547, :715, :1232, :1702, :1748, :2254, :2386, :2520; 3 in `cli/src/chunked.cpp` at lines :104, :125, :321) to the post-122 envelope. This is a mechanical, site-by-site refactor: replace each `namespace_id + pubkey + sign + encode_blob` sequence with `build_owned_blob` + `encode_blob_write_body`, and switch the `MsgType` per RESEARCH Q3's binding rule (BlobWrite=64 for all writes; Delete=17 retained for tombstones). NO auto-PUBK wiring, NO error-decoder changes, NO D-06 cascade in this plan — those are plan 04.

Purpose: This plan executes the "Shared Pattern A" diff from PATTERNS.md across every site. After it lands, the CLI emits the post-122 wire format on every path, but the user's experience is unchanged (still has to `cdb publish` manually — auto-PUBK wiring comes in plan 04). Phase 124 goal halfway reached.

Output: Fully migrated `commands.cpp` + `chunked.cpp`; full CLI test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-CONTEXT.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-RESEARCH.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-PATTERNS.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-01-SUMMARY.md

# Plan 01 produced the helper; plan 03 uses it at 12 sites.
@cli/src/wire.h

# Files being migrated
@cli/src/commands.cpp
@cli/src/chunked.cpp

<interfaces>
<!-- Helper contract (from plan 01). Copy into every call site. -->

```cpp
NamespacedBlob build_owned_blob(
    const Identity& id,
    std::span<const uint8_t, 32> target_namespace,
    std::span<const uint8_t>     data,
    uint32_t                     ttl,
    uint64_t                     timestamp);

std::vector<uint8_t> encode_blob_write_body(
    std::span<const uint8_t, 32> target_namespace,
    const BlobData&              blob);
```

Mechanical collapse at EVERY one of the 12 sites (PATTERNS Shared Pattern A):
```cpp
// BEFORE:
auto signing_input = build_signing_input(ns, X_data, ttl, ts);
auto signature     = id.sign(signing_input);
BlobData blob{};
std::memcpy(blob.namespace_id.data(), ns.data(), 32);  // or blob.namespace_id = ns;
blob.pubkey.assign(id.signing_pubkey().begin(), id.signing_pubkey().end());
blob.data      = std::move(X_data);
blob.ttl       = ttl;
blob.timestamp = ts;
blob.signature = std::move(signature);
auto flatbuf   = encode_blob(blob);
if (!conn.send(MsgType::Data, flatbuf, rid)) return …;      // OR MsgType::Delete

// AFTER (writes):
auto ns_blob  = build_owned_blob(id, ns, X_data, ttl, ts);
auto envelope = encode_blob_write_body(ns_blob.target_namespace, ns_blob.blob);
if (!conn.send(MsgType::BlobWrite, envelope, rid)) return …;

// AFTER (tombstones — KEEP Delete):
auto ns_blob  = build_owned_blob(id, ns, X_data, ttl, ts);
auto envelope = encode_blob_write_body(ns_blob.target_namespace, ns_blob.blob);
if (!conn.send(MsgType::Delete, envelope, rid)) return …;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate 9 blob-construction sites in cli/src/commands.cpp; set MsgType per RESEARCH Q3 table</name>
  <files>cli/src/commands.cpp</files>
  <read_first>
    - cli/src/commands.cpp (ENTIRE FILE — ~2794 lines; all 9 sites are spread across it)
    - cli/src/wire.h (post-plan-01 — verify `build_owned_blob`, `encode_blob_write_body`, `MsgType::BlobWrite`, `MsgType::Delete` present)
    - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-PATTERNS.md §"cli/src/commands.cpp" (lines 438-621) — migration patterns and MsgType disposition table
    - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-RESEARCH.md §Q1 (line numbers + context) and §Q3 (MsgType disposition — binding)
    - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-01-SUMMARY.md (note any TEMP-124 stubs placed in this file)
  </read_first>
  <behavior>
    - Tests currently green under `[wire]` and `[pubk]` remain green after the migration.
    - No test specifically adds a migration assertion (the grep-based acceptance criteria below are the authoritative check); this is a mechanical refactor validated structurally.
    - Any `cli_tests` cases that exercise commands.cpp paths indirectly (e.g., chunked tests via `build_tombstone_flatbuf` callers) continue to pass.
  </behavior>
  <action>
For each of the 9 sites below (per RESEARCH Q1 + PATTERNS MsgType disposition table at lines 470-485), apply the BEFORE→AFTER collapse from the interfaces block above. The variations per site are only (1) the `X_data` producer, (2) the `ttl` value, (3) the target `MsgType`.

**Site list — do NOT deviate on MsgType (binding per RESEARCH Q3):**

| # | Line (pre-edit) | Function | `X_data` | `ttl` | Target MsgType |
|---|-----------------|----------|----------|-------|----------------|
| 1 | ~513 | `submit_name_blob` (file-local helper) | already-built `name_data` passed in | caller-supplied | **BlobWrite** |
| 2 | ~547 | `submit_bomb_blob` (file-local helper) | already-built `bomb_data` passed in | 0 (BOMB invariant per `project_phase123_bomb_ttl_zero.md`) | **Delete** (KEEP) |
| 3 | ~715 | `cmd::put` pipeline loop body | `envelope::encrypt(...)` output (CENV) | user-supplied or default | **BlobWrite** |
| 4 | ~1232 | `cmd::rm` single-tombstone fallback | `make_tombstone_data(hash)` | 0 | **Delete** (KEEP) |
| 5 | ~1702 | `cmd::reshare` — new CENV blob | `envelope::encrypt(...)` re-encrypted | default | **BlobWrite** |
| 6 | ~1748 | `cmd::reshare` — tombstone of old blob | `make_tombstone_data(old_hash)` | 0 | **Delete** (KEEP) |
| 7 | ~2254 | `cmd::delegate` loop body | `make_delegation_data(delegate_pk)` | caller-supplied | **BlobWrite** |
| 8 | ~2386 | `cmd::revoke` loop body | `make_tombstone_data(delegation_blob_hash)` | 0 | **Delete** (KEEP) |
| 9 | ~2520 | `cmd::publish` | `make_pubkey_data(signing_pk, kem_pk)` | 0 | **BlobWrite** |

**For each site:**
1. Locate the `BlobData blob{};` declaration and the following `blob.namespace_id = ... / blob.pubkey.assign(...) / blob.data = ... / blob.ttl = ... / blob.timestamp = ... / blob.signature = ...` block.
2. Replace the entire 8-10 line block with the `auto ns_blob = build_owned_blob(id, ns, X_data, ttl, ts); auto envelope = encode_blob_write_body(ns_blob.target_namespace, ns_blob.blob);` pair. Use the ACTUAL variable names in scope — don't rename (`ns`, `hash`, `target_hash`, `name_bytes`, etc., stay as-is). If the local is a `std::array<uint8_t, 32>`, pass it as `std::span<const uint8_t, 32>(ns.data(), 32)` or rely on implicit conversion.
3. Replace the subsequent `auto flatbuf = encode_blob(blob);` line with nothing (the envelope IS the payload now).
4. In the `conn.send(...)` call following, replace `MsgType::Data` with `MsgType::BlobWrite` at sites 1, 3, 5, 7, 9. Leave `MsgType::Delete` unchanged at sites 2, 4, 6, 8 (retain per RESEARCH Q3). Replace the payload argument (previously `flatbuf`) with `envelope`.
5. Remove any now-unused imports of `encode_blob` — but DO NOT remove `encode_blob` from `cli/src/wire.{h,cpp}` itself; `decode_blob` is still used to parse `ReadResponse` payloads, and `encode_blob` may still be exported as a symmetry pair. Verify with `grep -n "encode_blob(" cli/src/commands.cpp` post-edit — should return 0 calls in this file.
6. Remove TEMP-124 compile-fix stubs from plan 01 (if any) — replace each with the real migration.

**Ack-type checks (do NOT change in this plan):** the 6 `DeleteAck` expectation sites at :560, :1261, :1775, :2404 and chunked.cpp's `:425, :451` stay pointing at `DeleteAck` — node still emits it for TransportMsgType_Delete regardless of the BlobWriteBody-shaped payload. RESEARCH Q3 evidence.

**Site 2 (`submit_bomb_blob`):** the input `bomb_data` passed to the helper is already built via `make_bomb_data` by the caller. After migration, the helper shrinks to:
```cpp
static std::optional<std::array<uint8_t, 32>> submit_bomb_blob(
    const Identity& id,
    std::span<const uint8_t, 32> ns,
    std::span<const uint8_t> bomb_data,
    Connection& conn,
    uint32_t rid,
    uint64_t timestamp) {

    auto ns_blob  = build_owned_blob(id, ns, bomb_data, /*ttl=*/0, timestamp);
    auto envelope = encode_blob_write_body(ns_blob.target_namespace, ns_blob.blob);
    if (!conn.send(MsgType::Delete, envelope, rid)) return std::nullopt;
    auto resp = conn.recv();
    // existing ack handling unchanged — accepts DeleteAck or WriteAck at :560 (see comment there)
    …
}
```
Preserve existing return semantics and ack handling exactly.

**Site 9 (`cmd::publish`):** this IS the canonical PUBK writer. Its migration is mechanical identical to the others; plan 04 will additionally skip auto-PUBK for this command (the "chicken-and-egg" note in RESEARCH Open Q #1). For THIS plan, just migrate the construction + send — do not add auto-PUBK wiring.

**Site 3 (`cmd::put` pipeline):** the existing send is `conn.send_async(MsgType::Data, flatbuf, rid)` — change to `conn.send_async(MsgType::BlobWrite, envelope, rid)`. Keep `send_async` (pipelined write path; NOT affected by the synchronous-probe rule — that only applies to `ensure_pubk`, added in plan 04).

**Do NOT in this task:**
- Wire `ensure_pubk` into any command (plan 04).
- Change any `ErrorResponse` handling (plan 04).
- Add D-06 cascade in `cmd::rm_batch` (plan 04).
- Modify `chunked.cpp` (task 2 of this plan).
- Remove or change `MsgType::Delete = 17` (retained per RESEARCH Q3).

**D-XX traceability:** D-03 (helper consolidation at 9 sites), D-03b (FlatBuffer emission via `encode_blob_write_body`), D-04 (BlobWrite=64 at all non-tombstone sites), D-04a (no `MsgType::Data` any more), D-04b interpretation per RESEARCH Q3 (tombstones keep `Delete=17`). Addresses T-124-03 (old-format blob — `MsgType::Data=8` references eliminated).
  </action>
  <verify>
    <automated>cmake --build build -j$(nproc) --target cli_tests 2>&1 | tail -30 && ./build/cli/cli_tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "blob\.namespace_id\s*=|blob\.namespace_id\.data\(\)" cli/src/commands.cpp` returns 0.
    - `grep -cE "blob\.pubkey\.assign|blob\.pubkey\.data\(\)" cli/src/commands.cpp` returns 0.
    - `grep -cE "MsgType::Data" cli/src/commands.cpp` returns 0 (none of the 9 write sites use `Data` post-migration; also validates plan 01 enum deletion reached the command file).
    - `grep -c "build_owned_blob" cli/src/commands.cpp` returns ≥ 9 (one call per site; may be higher if helpers wrap it).
    - `grep -c "encode_blob_write_body" cli/src/commands.cpp` returns ≥ 9.
    - `grep -cE "MsgType::Delete" cli/src/commands.cpp` returns ≥ 4 (sites 2, 4, 6, 8 — at minimum, possibly more if other `Delete` sends are present).
    - `grep -cE "MsgType::BlobWrite" cli/src/commands.cpp` returns ≥ 5 (sites 1, 3, 5, 7, 9 — at minimum).
    - `grep -c "TEMP-124" cli/src/commands.cpp` returns 0 (stubs gone).
    - `./build/cli/cli_tests` full suite exits 0.
  </acceptance_criteria>
  <done>
    - All 9 sites in `commands.cpp` migrated per the MsgType disposition table.
    - No residual `blob.namespace_id` / `blob.pubkey` / `MsgType::Data` / `TEMP-124` references.
    - Full CLI test suite green, including `[wire]`, `[pubk]`, `[identity]`, `[envelope]`, `[contacts]`, `[chunked]`, `[pipeline]`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate 3 blob-construction sites in cli/src/chunked.cpp; helper return types + send MsgType per RESEARCH Q3</name>
  <files>cli/src/chunked.cpp</files>
  <read_first>
    - cli/src/chunked.cpp (full file ~745 lines; the 3 sites are `build_cdat_blob_flatbuf` at :86-111, `build_tombstone_flatbuf` at :120-133, and the manifest build in `put_chunked` at :318-329)
    - cli/src/wire.h (post-plan-01)
    - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-PATTERNS.md §"cli/src/chunked.cpp" (lines 622-709) — exact diff blocks including the helper-body changes and the send-site MsgType table
    - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-01-SUMMARY.md (note any TEMP-124 stubs placed in this file)
  </read_first>
  <behavior>
    - `[chunked]`-tagged tests pass after migration (`cli/tests/test_chunked.cpp`).
    - `./build/cli/cli_tests` full suite exits 0.
  </behavior>
  <action>
Apply the three helper/site refactors from PATTERNS §"cli/src/chunked.cpp" verbatim. Summary:

**Site 1 — `build_cdat_blob_flatbuf` body (current lines 94-111)** per PATTERNS §"Construction site 1":
- Replace the 12-line `blob.namespace_id / blob.pubkey / blob.data / blob.ttl / blob.timestamp / blob.signature / encode_blob(blob)` sequence with:
  ```cpp
  auto ns_blob = build_owned_blob(id, ns, blob_data, ttl, timestamp);
  return encode_blob_write_body(ns_blob.target_namespace, ns_blob.blob);
  ```
- The helper's RETURN SEMANTICS change from "bare Blob FlatBuffer bytes" to "BlobWriteBody envelope bytes." This is a Callee-contract change — callers (in the same file) now pass the return value directly to `conn.send(MsgType::BlobWrite, <returned>, rid)`. Scan all callers of `build_cdat_blob_flatbuf` and verify they now feed `MsgType::BlobWrite`. Per PATTERNS send-site table at lines 698-704:

| Line | Current MsgType | Target MsgType |
|------|-----------------|----------------|
| ~227 (Phase A CDAT send) | Data | **BlobWrite** |
| ~245 (retry CDAT send) | Data | **BlobWrite** |
| ~331 (manifest send) | Data | **BlobWrite** |

**Site 2 — `build_tombstone_flatbuf` body (current lines 120-133)** per PATTERNS §"Construction site 2":
- Same structural change: `make_tombstone_data(target) → build_owned_blob(id, ns, td, 0, ts) → encode_blob_write_body(...)`. Return type semantically changes to BlobWriteBody envelope bytes.
- Callers at `rm_chunked` per-chunk tombstones (~:393) and manifest tombstone (~:446) keep sending via `MsgType::Delete` (RESEARCH Q3 KEEP). The payload fed to `conn.send_async(MsgType::Delete, <returned>, rid)` is now a BlobWriteBody, not a raw Blob — which is what the post-122 node dispatcher accepts for TransportMsgType_Delete.
- The `DeleteAck` checks at :425 and :451 remain unchanged — node still emits DeleteAck for TransportMsgType_Delete regardless of payload shape (RESEARCH Q3 evidence).

**Site 3 — manifest build in `put_chunked` (current lines 318-329)** per PATTERNS §"Construction site 3":
- Replace the manual `blob.namespace_id / blob.pubkey / …` block with:
  ```cpp
  auto ns_blob = build_owned_blob(id, ns, manifest_blob_data, ttl, timestamp);
  auto mfb     = encode_blob_write_body(ns_blob.target_namespace, ns_blob.blob);
  ```
- The subsequent `conn.send_async(MsgType::Data, mfb, rid)` becomes `conn.send_async(MsgType::BlobWrite, mfb, rid)`.

**Remove any `TEMP-124` stubs** placed in `chunked.cpp` during plan 01 to allow the CMake target to link.

**Do NOT in this task:**
- Change the `build_cdat_blob_flatbuf` or `build_tombstone_flatbuf` function signatures (return type `std::vector<uint8_t>` stays; only the semantic meaning of the bytes changes — document this with a one-line comment next to each helper: `// Post-124: returns BlobWriteBody envelope bytes (not bare Blob bytes)`.
- Wire `ensure_pubk` into `put_chunked` (plan 04).
- Add retry or cascade logic (plan 04 / out of scope).
- Modify `rm_chunked`'s per-chunk tombstone fan-out logic — only the payload construction changes.

**D-XX traceability:** D-03 / D-03b / D-04 / D-04b. Addresses T-124-03 (old-format blob — fully eliminated across CLI). Preserves T-124-04's (orphan chunks) exposure surface for plan 04's D-06 cascade to fix.
  </action>
  <verify>
    <automated>cmake --build build -j$(nproc) --target cli_tests 2>&1 | tail -20 && ./build/cli/cli_tests "[chunked]" 2>&1 | tail -20 && ./build/cli/cli_tests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "blob\.namespace_id\s*=|blob\.namespace_id\.data\(\)" cli/src/chunked.cpp` returns 0.
    - `grep -cE "blob\.pubkey\.assign|blob\.pubkey\.data\(\)" cli/src/chunked.cpp` returns 0.
    - `grep -cE "MsgType::Data" cli/src/chunked.cpp` returns 0.
    - `grep -c "build_owned_blob" cli/src/chunked.cpp` returns ≥ 3.
    - `grep -c "encode_blob_write_body" cli/src/chunked.cpp` returns ≥ 3.
    - `grep -cE "MsgType::BlobWrite" cli/src/chunked.cpp` returns ≥ 3 (the 3 CDAT + manifest send sites).
    - `grep -cE "MsgType::Delete" cli/src/chunked.cpp` returns ≥ 2 (rm_chunked per-chunk + manifest tombstone sends — retained).
    - `grep -c "TEMP-124" cli/src/chunked.cpp` returns 0.
    - `./build/cli/cli_tests "[chunked]"` exits 0.
    - `./build/cli/cli_tests` full suite exits 0 with zero regressions.
  </acceptance_criteria>
  <done>
    - 3 sites in `chunked.cpp` migrated.
    - Helper return-type semantics updated (BlobWriteBody envelope, documented in comment).
    - CDAT/CPAR writes on `MsgType::BlobWrite`; tombstones on `MsgType::Delete`.
    - All chunked tests pass; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → node wire at every blob write site | BlobWriteBody bytes cross; node's FlatBuffer Verifier + signature verify are gates. |
| Helper usage at 12 sites | Each call MUST pass the correct `target_namespace` for owner vs delegate vs cross-namespace intent — programmer error at any one site is a potential ACL bug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-03 | Tampering | old-format blob acceptance in CLI | mitigate | All 12 sites migrated to `build_owned_blob` (5-field schema) + `encode_blob_write_body`. `MsgType::Data = 8` was deleted in plan 01 enum; this plan removes all references. Acceptance greps enforce. |
| T-124-03b | Tampering | residual `encode_blob(blob)` send-path usage | mitigate | Acceptance criterion `grep -c "encode_blob(" cli/src/commands.cpp` returns 0 (encode_blob remains a symbol but is never called from a send path; decode_blob still parses ReadResponse). |
| T-124-site-mix | Confused Deputy | mis-choosing MsgType at one of the 12 sites | mitigate | Binding MsgType disposition table (RESEARCH Q3 + PATTERNS) is copied into the task action verbatim — per-site decision is mechanical, not interpretive. Acceptance greps count `BlobWrite` (≥5) and `Delete` (≥4) at commands.cpp to catch drift. |
| T-124-delegate-helper | Spoofing | helper composition at delegate sites | mitigate | `build_owned_blob` structurally sets `signer_hint = SHA3(id.signing_pubkey())` regardless of the `target_namespace` argument. A delegate write (e.g., delegate revoking an existing delegation they hold) passes the OWNER's namespace as `target_namespace` and their OWN signing key as `id` — signer_hint reflects the delegate. Node verifies via `delegation_map` lookup. No code path can accidentally spoof the owner's signer_hint from the CLI side. |
| T-124-chunked-orphan | Availability | orphan chunks after `rm_chunked` manifest | defer | Plan 04's D-06 cascade closes this — this plan doesn't change the behavior, only the wire format of the existing cascade (per-chunk tombstones + manifest tombstone). |
</threat_model>

<verification>
- `./build/cli/cli_tests` exits 0 with 0 failures across all tags.
- `grep -rnE "blob\.namespace_id\s*=|blob\.pubkey\.assign" cli/src/` returns 0 lines (phase-wide invariant).
- `grep -rn "MsgType::Data" cli/src/` returns 0 lines.
- `grep -rn "TEMP-124" cli/src/` returns 0 lines.
- The CLI binary `cdb` builds (`cmake --build build -j$(nproc) --target cdb` — delegate actual execution to the user per `feedback_delegate_tests_to_user.md`).
</verification>

<success_criteria>
Plan 03 succeeds when:
- [ ] 9 sites in `commands.cpp` + 3 sites in `chunked.cpp` use `build_owned_blob` + `encode_blob_write_body`.
- [ ] MsgType disposition matches RESEARCH Q3: BlobWrite=64 at sites 1/3/5/7/9 in commands.cpp + all 3 CDAT/manifest sites in chunked.cpp; Delete=17 at sites 2/4/6/8 in commands.cpp + rm_chunked per-chunk + manifest tombstone in chunked.cpp.
- [ ] All pre-existing Catch2 tests pass (including `[chunked]`).
- [ ] No residual `namespace_id`/`pubkey` blob assignment in either file.
- [ ] No `TEMP-124` stubs remain.
- [ ] Addresses SC-124-2 (signer_hint-only emission at every site), SC-124-4 (delegate signer_hint correctness — structurally ensured by helper), SC-124-6 (all pre-existing tests pass).
</success_criteria>

<output>
After completion, create `.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-03-SUMMARY.md` with: a table of the 12 sites with pre-/post-edit line numbers + MsgType change, a grep-verification block showing the invariants hold, the test suite output summary, and a note confirming zero TEMP-124 stubs remain.
</output>
