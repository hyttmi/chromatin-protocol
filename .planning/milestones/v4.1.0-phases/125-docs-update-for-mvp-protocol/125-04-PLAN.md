---
phase: 125-docs-update-for-mvp-protocol
plan: 04
type: execute
wave: 3
depends_on:
  - 125-01-PLAN
  - 125-02-PLAN
  - 125-03-PLAN
files_modified:
  - cli/src/main.cpp
  - db/schemas/transport.fbs
  - db/wire/transport_generated.h
  - db/peer/message_dispatcher.cpp
  - db/tests/net/test_protocol.cpp
  - db/tests/net/test_framing.cpp
  - db/tests/net/test_connection.cpp
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
user_setup: []
must_haves:
  truths:
    - "cli/src/main.cpp no longer emits the `(Phase 123)` token in user-visible help text for cdb rm"
    - "TransportMsgType_Data = 8 is deleted from db/schemas/transport.fbs"
    - "db/wire/transport_generated.h is regenerated via flatc (not hand-edited) and committed"
    - "All remaining db/tests/net/*.cpp references to TransportMsgType_Data are migrated to TransportMsgType_BlobWrite or deleted if they only exercised the pre-122 dispatcher branch"
    - "The // TransportMsgType_Data direct-write branch was DELETED memo at db/peer/message_dispatcher.cpp:1388 is removed"
    - "Full Catch2 suite passes after all edits (cli_tests ≥ 197614 assertions / 98 cases baseline; chromatindb_tests [peer] ≥ 506 assertions / 77 cases baseline)"
    - "No other user-visible string in cli/src/ contains a `(Phase N)` token (grep returns zero hits)"
    - "No non-generated source file references BlobData.namespace_id, BlobData.pubkey (as struct members), or an active MsgType::Data write path"
  artifacts:
    - path: "cli/src/main.cpp"
      provides: "User-visible help text free of phase leaks"
      pattern: "batched BOMB tombstone(?!.*\\(Phase)"
    - path: "db/schemas/transport.fbs"
      provides: "TransportMsgType enum without Data = 8"
    - path: "db/wire/transport_generated.h"
      provides: "flatc-regenerated generated header without TransportMsgType_Data"
    - path: "db/peer/message_dispatcher.cpp"
      provides: "No pre-122 dispatcher memo at :1388"
  key_links:
    - from: "db/schemas/transport.fbs"
      to: "db/wire/transport_generated.h"
      via: "flatc --cpp regeneration"
      pattern: "TransportMsgType_BlobWrite"
    - from: "db/tests/net/test_*.cpp"
      to: "db/wire/transport_generated.h"
      via: "Test sites migrate to TransportMsgType_BlobWrite or are removed"
      pattern: "TransportMsgType_BlobWrite|// migrated from TransportMsgType_Data"
---

<objective>
Close the pre-122 vestige gaps in source code that the documentation plans
(1-3) cannot reach. Four concrete targets:

1. **D-12:** Fix the `"(Phase 123)"` user-visible string leak in `cli/src/main.cpp:619`
   (cdb rm help text). Deferred from Phase 124 specifically for 125.
2. **D-11:** Delete `TransportMsgType_Data = 8` from `db/schemas/transport.fbs`,
   regenerate `db/wire/transport_generated.h` via `flatc`, migrate or remove
   the ~16 test-site references across `db/tests/net/test_connection.cpp`,
   `test_framing.cpp`, `test_protocol.cpp`, and remove the pre-122-dispatcher
   memo at `db/peer/message_dispatcher.cpp:1388`.
3. **D-10:** Final scan for any remaining inline-comment references to the
   removed pre-122 fields/artifacts (`BlobData.namespace_id`, `BlobData.pubkey`,
   old signing-input shape); fix in place.
4. **Test gate:** Full Catch2 suite green after all edits — the full-suite
   pass is the regression detector for accidental code deletion during the
   test-site migration.

This plan runs STRICTLY AFTER Plans 1-3 (documentation) to preserve clean
wave-boundaries, and STRICTLY BEFORE Plan 5 (comment hygiene) because Plan 5
touches the same files — running Plan 4 first means the comment-hygiene pass
doesn't step on mid-flight schema changes.

Purpose: Eliminate the last pre-122 structural artifacts from the shipping
source tree. After Plan 4 completes, the `Data = 8` enum value is a pure
archaeological footnote — no code, no tests, no comments reference it as an
active thing.

Output: Schema + generated header + 5 source/test files updated; test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-docs-update-for-mvp-protocol/125-CONTEXT.md
@.planning/phases/125-docs-update-for-mvp-protocol/125-RESEARCH.md
@.planning/phases/125-docs-update-for-mvp-protocol/125-VALIDATION.md

Source files to edit:
@cli/src/main.cpp
@db/schemas/transport.fbs
@db/wire/transport_generated.h
@db/peer/message_dispatcher.cpp
@db/tests/net/test_protocol.cpp
@db/tests/net/test_framing.cpp
@db/tests/net/test_connection.cpp

Reference for flatc invocation:
@db/CMakeLists.txt

<interfaces>
<!-- D-12 target: one string literal in cli/src/main.cpp around line 619. -->
<!-- BEFORE -->
"batched BOMB tombstone (Phase 123). Exit 2 if no targets given.\n"
<!-- AFTER -->
"batched BOMB tombstone. Exit 2 if no targets given.\n"

<!-- D-11 test-site inventory (from research §3c). Each site is an individual call: migrate to BlobWrite or delete the test case. -->
db/tests/net/test_connection.cpp:  lines 55, 166, 200, 308, 340, 498, 524, 582, 646, 930, 938 (11 hits)
db/tests/net/test_framing.cpp:     lines 222, 234 (2 hits)
db/tests/net/test_protocol.cpp:    lines 73, 76, 265 (3 hits)

<!-- Per-site decision rule (apply individually): -->
<!-- If the test is exercising the general message-dispatch path (type-agnostic framing, -->
<!-- AEAD round-trip, request_id echo, chunked sub-frame boundary) and just happens to use  -->
<!-- TransportMsgType_Data as a filler type → migrate to TransportMsgType_BlobWrite. -->
<!-- If the test is specifically testing the pre-122 direct-write dispatcher branch  -->
<!-- (e.g. "on receipt of Data=8, dispatch to legacy writer") → DELETE the test case. -->
<!-- The current node has no dispatcher branch for Data=8 (since Phase 122), so any test -->
<!-- asserting branch behavior for Data=8 is exercising dead code by definition. -->

<!-- D-11 memo target. Research says the exact comment is at db/peer/message_dispatcher.cpp:1388. -->
// TransportMsgType_Data direct-write branch was DELETED — no backward compat.
<!-- ACTION: delete the whole line. -->

<!-- Test suite baselines (from 125-VALIDATION.md D3):
  ./build/cli/tests/cli_tests: ≥ 197614 assertions / 98 cases
  ./build/db/db/chromatindb_tests "[peer]": ≥ 506 assertions / 77 cases
-->

<!-- flatc invocation: per feedback_delegate_tests_to_user, the USER runs builds. Plan 4 specifies
     the exact command so the user or executor (if reachable infra) can run it identically: -->
<!-- From repo root, after at least one `cmake -B build` run so FetchContent has materialized flatc: -->
  build/_deps/flatbuffers-build/flatc --cpp -o db/wire db/schemas/transport.fbs
<!-- NOTE: flatc may emit additional files (transport_generated.h is the one we care about for this schema). -->
<!-- NEVER hand-edit transport_generated.h. If flatc is not present, STOP and run `cmake -B build` first. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-12 fix — strip "(Phase 123)" from cdb rm help text at cli/src/main.cpp:619</name>
  <files>cli/src/main.cpp</files>
  <action>
Open `cli/src/main.cpp`. Locate the help-text switch for `cdb rm` (around line 619 per STATE.md Phase 124 deferred-items record).

**Surgical edit:** find the literal string
```
"batched BOMB tombstone (Phase 123). Exit 2 if no targets given.\n"
```
and replace it with
```
"batched BOMB tombstone. Exit 2 if no targets given.\n"
```

Only the `(Phase 123)` token (and the single space that preceded it) is removed. The rest of the sentence is intact. No other edits in this task.

**Guard against other leaks:** after the edit, grep ALL of `cli/src/` for remaining `(Phase [0-9]+)` tokens in user-visible strings (string literals inside cout/cerr/printf/stderr/stdout/log calls or inside struct initializers flagged as `help_text`, `description`, `error_msg`, etc.):
```
grep -rnE '"[^"]*\(Phase [0-9]+\)[^"]*"' cli/src/
```
If any other hits exist (expected: none per Assumption A7), fix each with the same "strip the token, keep the sentence" pattern. If a hit is in a comment rather than a string literal, it's Plan 5's scope — leave it.
  </action>
  <verify>
    <automated>bash -c '! grep -qE "\"[^\"]*\\(Phase [0-9]+\\)[^\"]*\"" cli/src/main.cpp &amp;&amp; grep -qF "batched BOMB tombstone. Exit 2" cli/src/main.cpp &amp;&amp; ! grep -rEq "\"[^\"]*\\(Phase [0-9]+\\)[^\"]*\"" cli/src/'</automated>
  </verify>
  <done>cli/src/main.cpp:619 no longer contains the "(Phase 123)" token; no other user-visible string in cli/src/ contains a (Phase N) token.</done>
</task>

<task type="auto">
  <name>Task 2: D-11 — delete Data=8 from transport.fbs, regenerate transport_generated.h via flatc, migrate test-site references, remove dispatcher memo</name>
  <files>db/schemas/transport.fbs, db/wire/transport_generated.h, db/peer/message_dispatcher.cpp, db/tests/net/test_connection.cpp, db/tests/net/test_framing.cpp, db/tests/net/test_protocol.cpp</files>
  <action>
Multi-file structural change. Execute in this order — each step builds on the previous.

**Step A: Delete `Data = 8` from schema.**
Open `db/schemas/transport.fbs`. Locate the `TransportMsgType` enum (research §3c references line 14). Delete the row that reads `Data = 8,` (or equivalent FlatBuffers-idiomatic form). FlatBuffers enum values are explicit numerics, so the gap at position 8 is harmless (the enum does not need renumbering). All other rows stay exactly as-is.

**Step B: Regenerate `db/wire/transport_generated.h` via `flatc`.**
From the repo root:
```
build/_deps/flatbuffers-build/flatc --cpp -o db/wire db/schemas/transport.fbs
```
If `build/_deps/flatbuffers-build/flatc` does not exist, the user has not run `cmake -B build` yet — stop and prompt the user to run it once, then retry. Do NOT hand-edit the generated file. After regen, `git diff db/wire/transport_generated.h` must show: `TransportMsgType_Data = 8,` removed, `TransportMsgType_Data` removed from `EnumNamesTransportMsgType` table at line ~109, and `EnumValuesTransportMsgType()` array updated. All other generated content identical.

**Step C: Delete the dispatcher memo.**
Open `db/peer/message_dispatcher.cpp`. Locate line 1388 (research §3c gives the exact line). Delete the single-line comment:
```
// TransportMsgType_Data direct-write branch was DELETED — no backward compat.
```
Leave surrounding code untouched.

**Step D: Per-site test migration — 16 sites, 3 files. Apply the per-site decision rule from `<interfaces>`.**

For each file, locate every occurrence of `TransportMsgType_Data` and decide:
- **If the test is exercising general transport/framing behavior** (AEAD round-trip, request_id echo, chunked sub-frame boundary, message-length encoding) and `TransportMsgType_Data` is just a filler type with no path-specific assertions → replace with `TransportMsgType_BlobWrite`. Add a trailing comment at the change site: `// migrated from TransportMsgType_Data (Phase 122)` — NOTE: Plan 5 will strip the phase tag from this comment, but for Plan 4's review history the tag is useful for the reviewer. Plan 5 inherits the cleanup.
- **If the test is specifically asserting on the Data=8 dispatcher branch** (lookup table entries for type 8, "on receipt of Data=8, dispatch to X", enum-name-printout tests expecting "Data" in output) → DELETE the test case. Add a single commit message line explaining which test case was deleted and why (e.g., "Delete `[protocol] dispatcher routes Data=8` — branch removed in Phase 122, test was exercising dead code").

For each of the 16 sites, record the decision (migrate vs delete) in the summary. Per-file quick pass:

- `db/tests/net/test_protocol.cpp` (3 hits at 73, 76, 265):
  Read each site; research §3c notes these are likely dispatcher-branch tests. Apply rule.
- `db/tests/net/test_framing.cpp` (2 hits at 222, 234):
  Research §3c suggests these are general framing tests. Likely migrate.
- `db/tests/net/test_connection.cpp` (11 hits at 55, 166, 200, 308, 340, 498, 524, 582, 646, 930, 938):
  Mix of framing, keepalive, chunked sub-frame, request_id correlation tests. Read each; most likely migrate; any that assert dispatcher-specific routing for Data=8 get deleted.

After all 16 sites are handled, verify zero `TransportMsgType_Data` hits remain in the three test files:
```
! grep -q "TransportMsgType_Data" db/tests/net/test_connection.cpp \
  && ! grep -q "TransportMsgType_Data" db/tests/net/test_framing.cpp \
  && ! grep -q "TransportMsgType_Data" db/tests/net/test_protocol.cpp
```

**Step E: Full-tree scan for any remaining `TransportMsgType_Data` outside the removed-schema location.**
```
! grep -rn "TransportMsgType_Data" db/ cli/
```
The generated header is already regenerated; if any stale hit remains it's either a missed test site or a non-test source file. Fix.

**Step F (delegated to user per feedback_delegate_tests_to_user):**
Ask the user to run:
```
cmake --build build -j$(nproc)
./build/cli/tests/cli_tests --reporter compact
./build/db/db/chromatindb_tests "[peer]" --reporter compact
```
Record baseline assertion counts: `cli_tests` ≥ 197614 / 98 cases; `chromatindb_tests "[peer]"` ≥ 506 / 77 cases. If test migration deleted any cases, the case count may drop; each deletion must be accounted for in the summary. Assertion count MAY drop slightly if deleted cases carried assertions; MUST NOT drop unexpectedly (accidental code break). Any unexpected drop = investigation + fix before declaring Task 2 done.
  </action>
  <verify>
    <automated>bash -c 'S=db/schemas/transport.fbs; H=db/wire/transport_generated.h; M=db/peer/message_dispatcher.cpp; ! grep -q "Data = 8" $S &amp;&amp; ! grep -q "Data *= *8" $S &amp;&amp; ! grep -q "TransportMsgType_Data" $H &amp;&amp; ! grep -q "TransportMsgType_Data direct-write branch was DELETED" $M &amp;&amp; ! grep -rq "TransportMsgType_Data" db/tests/net/'</automated>
  </verify>
  <done>transport.fbs has no Data=8 row; transport_generated.h regenerated via flatc (not hand-edited); dispatcher memo deleted; all 16 test sites migrated or deleted. User-delegated test run passes baselines.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 — final scan for lingering pre-122 field references in comments and non-generated source</name>
  <files>cli/src/, db/ (all non-test .cpp and .h files)</files>
  <action>
Sweep the source tree for comments or strings that still reference the REMOVED pre-122 artifacts. Per D-10's scope (wider than D-11's schema delete): comments and prose, not just code.

**Patterns to search (use Grep tool):**

A. `BlobData.namespace_id` as a struct field (the removed embedded field):
```
grep -rEn "BlobData\.namespace_id\b" cli/src/ db/ --include='*.cpp' --include='*.h'
```
Expected: the only hit is the wire.h:158-165 comment that DOCUMENTS the rename (and says "byte output IDENTICAL to pre-rename (Phase 122 D-01 invariant)"). That comment is load-bearing — keep the content but strip the phase prefix (already covered by Plan 5's D-13 pass; if it stays in Plan 4 that's fine, Plan 5 will handle). Any OTHER hit is a stale comment — rewrite to reference `signer_hint` instead.

B. `BlobData.pubkey` as an embedded per-blob 2592-byte pubkey field:
```
grep -rEn "BlobData\.pubkey\b|\.pubkey\s*\(embedded|per-blob pubkey|2592-byte embedded" cli/src/ db/ --include='*.cpp' --include='*.h'
```
Expected: zero hits per research §3c. If any hit exists, it's a stale comment describing the removed field. Fix each: either delete the whole comment (if it only explained the removed field) or rewrite to describe the current `signer_hint` model.

C. Old signing-input shape:
```
grep -rEn "namespace_id \|\| data \|\| ttl \|\| timestamp" cli/src/ db/ --include='*.cpp' --include='*.h'
```
Expected: zero functional hits in code (per research §3c — the current code uses `build_signing_input` with `target_namespace`). Any stale comment gets fixed: rename `namespace_id` → `target_namespace` in the comment.

D. `MsgType::Data` references (not already covered by Task 2):
```
grep -rEn "MsgType::Data|type *= *Data\b|Data\s*=\s*8" cli/src/ db/ --include='*.cpp' --include='*.h'
```
Expected: zero hits post-Task-2 (Task 2 deleted all schema + test sites; Plan 1 marked the PROTOCOL.md entry DELETED but didn't delete the row — that's Plan 5 / next pass). Any remaining hit is an active-code references — must be fixed.

**For each finding:**
- Read the file + line context.
- Classify: stale comment (rewrite or delete), stale string literal (rewrite), stale active code (investigate — should have been caught by Task 2).
- Apply the fix.

**Record ALL findings** (including zero-hit confirmations) in the task's SUMMARY output so Plan 5 knows what's already been handled.

This task is a safety net — if research §3c is fully accurate, the task should find zero actionable hits. If it finds any, Plan 5 doesn't need to re-check these specific patterns.
  </action>
  <verify>
    <automated>bash -c '! grep -rEnq "BlobData\\.pubkey\\b" cli/src/ db/ --include="*.cpp" --include="*.h" &amp;&amp; ! grep -rEnq "namespace_id \\|\\| data \\|\\| ttl \\|\\| timestamp" cli/src/ db/ --include="*.cpp" --include="*.h" &amp;&amp; ! grep -rEnq "MsgType::Data\\b" cli/src/ db/ --include="*.cpp" --include="*.h"'</automated>
  </verify>
  <done>All three D-10 patterns scan clean across cli/src/ and db/ non-generated source. Any findings fixed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — Plan 4 makes no new trust-boundary changes. It deletes a pre-122 enum value (`Data = 8`) whose dispatcher branch was already removed in Phase 122. The schema change propagates via `flatc` regeneration, which is a mechanical translation. Test migration preserves behavior: each site is either re-expressed over `BlobWrite` (no semantic change) or deleted (dead test).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-05 | Tampering | Hand-edited `transport_generated.h` could diverge from schema | mitigate | Task 2 Step B mandates `flatc` regeneration; direct-edit is forbidden and guarded by the verify grep (`! grep -q "TransportMsgType_Data" db/wire/transport_generated.h` only passes after flatc run). Generated header is committed so reviewers see the diff. |
| T-125-06 | Denial of Service | Accidentally deleting a test case that was the last coverage for a legitimate path | mitigate | Task 2 Step D requires per-site read before delete: only cases asserting on the Data=8 dispatcher branch get deleted (which is dead code by definition). Full-suite test run after all edits is the regression detector; baselines from 125-VALIDATION.md D3 catch any unexpected assertion-count drop. |

No new attack surface introduced.
</threat_model>

<verification>
VALIDATION.md D2 greps (content absence — post-Plan-4 state):
- `! grep -q "Data *= *8" db/schemas/transport.fbs`
- `! grep -q "TransportMsgType_Data" db/wire/transport_generated.h`
- `! grep -rq "TransportMsgType_Data" db/tests/net/`
- `! grep -rEq "\\(Phase [0-9]+\\)" cli/src/main.cpp` (string-literal scope — comments are Plan 5)
- `! grep -q "TransportMsgType_Data direct-write branch was DELETED" db/peer/message_dispatcher.cpp`

VALIDATION.md D3 (behavioral preservation — delegated to user):
- `./build/cli/tests/cli_tests` exits 0, assertion count ≥ 197614 (baseline)
- `./build/db/db/chromatindb_tests "[peer]"` exits 0, assertion count ≥ 506 (baseline)
- If any test case was deleted in Step D, the summary must document the exact case name(s) and justify (asserted on removed dispatcher branch).

Sanity: generated header must be the flatc output, not a hand-edit. `git diff db/wire/transport_generated.h` should show ONLY the rows touching `TransportMsgType_Data` / the enum-names/values tables.
</verification>

<success_criteria>
Plan 4 succeeds when:
- `cli/src/main.cpp:619` help text has no `(Phase 123)` token; no other string literal in `cli/src/` carries any `(Phase N)` token.
- `db/schemas/transport.fbs` has no `Data = 8` row.
- `db/wire/transport_generated.h` is flatc-regenerated (not hand-edited) and reflects the schema change; committed alongside the .fbs change.
- All 16 test-site references to `TransportMsgType_Data` across `db/tests/net/` are either migrated to `TransportMsgType_BlobWrite` (when the test was type-agnostic) or deleted (when the test specifically asserted on the removed Data=8 dispatcher branch). Each deleted case is recorded with justification.
- `db/peer/message_dispatcher.cpp:1388` memo is deleted.
- D-10 scan results: no remaining active-code or comment references to `BlobData.namespace_id` (as a struct field), `BlobData.pubkey` (as an embedded per-blob field), or the old signing-input shape. Surviving documentation-style comments (e.g., wire.h:158-165 rename note) are flagged for Plan 5's phase-prefix strip but kept for Plan 4.
- Full Catch2 suite green (user-delegated run): `cli_tests` ≥ 197614 assertions / 98 cases baseline preserved (minus any explicitly-deleted cases, justified in summary); `chromatindb_tests "[peer]"` ≥ 506 / 77 baseline preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/125-docs-update-for-mvp-protocol/125-04-SUMMARY.md` capturing:
- Exact list of 16 test-site decisions (migrate vs delete) with file:line and rationale
- `git diff --stat db/wire/transport_generated.h` output showing flatc regeneration
- D-10 scan findings (zero-hits or list of fixes applied)
- User-delegated test run results: assertion counts + case counts for both `cli_tests` and `chromatindb_tests "[peer]"`
- Any anomalies (unexpected assertion-count drops, new build warnings introduced by the schema change)
</output>
