---
phase: 125-docs-update-for-mvp-protocol
plan: 05
type: execute
wave: 4
depends_on:
  - 125-04-PLAN
files_modified:
  - cli/src/
  - cli/tests/
  - db/engine/
  - db/storage/
  - db/net/
  - db/peer/
  - db/sync/
  - db/wire/
  - db/crypto/
  - db/config/
  - db/tests/
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
user_setup: []
must_haves:
  truths:
    - "Zero `// Phase N` (including `/// Phase N` and `// Phase N / X-Y`) prefixes remain in .cpp/.h/.fbs source files across cli/src/, cli/tests/, and db/"
    - "Every load-bearing WHY comment carrying a phase prefix has been kept (prefix stripped, substance preserved); e.g. `// CONC-04 (Phase 121): post back to ioc_` → `// CONC-04: post back to ioc_`"
    - "Every comment citing a requirement ID, ADR, bug ticket, TSAN finding, or non-obvious invariant is preserved"
    - "Every comment that explains obvious code (e.g. `// increment i` above `i++;`) is deleted per CLAUDE.md default-no-comments policy"
    - "Every commented-out code block is deleted (commented-out code is always cruft per CONTEXT.md Claude's Discretion default)"
    - "Test-case names (Catch2 tags like [phase122], TEST_CASE names) are NOT modified — D-13 is scoped to comments, not identifiers"
    - "Full Catch2 suite green after the pass: cli_tests ≥ 197614 assertions / 98 cases (baseline preserved from Plan 4 output); chromatindb_tests [peer] ≥ 506 / 77 baseline preserved"
    - "ASAN build passes — accidental code deletion via comment-strip regex surfaces as use-after-free or other sanitizer findings"
  artifacts:
    - path: "cli/src/"
      provides: "Comment-hygiene pass complete; no phase breadcrumbs; no obvious-code comments; no commented-out code"
      pattern: "// Phase [0-9]"
      contains_count: 0
    - path: "db/"
      provides: "Comment-hygiene pass complete across all subsystems"
      pattern: "// Phase [0-9]"
      contains_count: 0
  key_links:
    - from: "cli/src/commands.cpp"
      to: "test suite green after hygiene pass"
      via: "Behavior preserved; only comments changed"
      pattern: "tests pass"
---

<objective>
The final cleanup pass: strip `// Phase N` historical breadcrumbs (D-13) and
prune comments that explain obvious code (D-14) across the entire source tree.
Preserve every comment that carries load-bearing WHY.

**Running order rationale:** Plan 5 is strictly the last plan in Phase 125.
It MUST come after:
- Plans 1-3 (docs): so that doc work doesn't step on source files.
- Plan 4 (pre-122 vestige cleanup + schema regen + test migration): because
  Plan 4's test-site edits add the same files to the touch list; running
  Plan 5 first would mean Plan 4's `// migrated from TransportMsgType_Data
  (Phase 122)` review-history comments get stripped before the reviewer
  sees them, reducing traceability.

**Running order rationale for Plan 4 → Plan 5:** the comment-hygiene
per-file read-decide-edit loop is slow and wide; running it after the
structural edits settle means each file sees only one sweeping review.

**Primary risk:** accidental code deletion during comment stripping. A regex
that swallows a non-comment line (e.g. `//` inside a string literal, or a
`//` that opened a multi-line `/* ... */` comment) produces a silent code
break. Mitigation is per-file manual review + full Catch2 suite green at
the end + ASAN build.

**Scope bound:** D-13 and D-14 are hygiene, NOT refactoring. If a file needs
structural rework (e.g. function decomposition, name changes), that's
out of scope for Plan 5 — log it as a deferred item and move on.

Purpose: Close the remaining comment-hygiene gap. Plan 5 brings the source
tree into compliance with CLAUDE.md "default to no comments" + in-source
phase-number-rot avoidance.

Output: Cleaner source tree; full test suite green; ASAN clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-docs-update-for-mvp-protocol/125-CONTEXT.md
@.planning/phases/125-docs-update-for-mvp-protocol/125-RESEARCH.md
@.planning/phases/125-docs-update-for-mvp-protocol/125-VALIDATION.md

Top comment-density files to read before starting (research §3b):
@cli/src/commands.cpp
@cli/src/chunked.cpp
@cli/src/connection.cpp
@cli/src/wire.cpp
@cli/src/wire.h
@db/engine/engine.cpp
@db/storage/storage.cpp
@db/storage/storage.h
@db/peer/message_dispatcher.cpp
@db/peer/sync_orchestrator.cpp
@db/net/connection.cpp
@db/sync/sync_protocol.cpp
@db/wire/codec.cpp

Test files with decorative phase tags (candidates for aggressive strip):
@db/tests/engine/test_engine.cpp
@db/tests/peer/test_peer_manager.cpp
@db/tests/storage/test_storage.cpp
@db/tests/config/test_config.cpp
@db/tests/test_helpers.h

<policy>
CLAUDE.md (via user memory):
  "Default to writing no comments. Only add one when the WHY is non-obvious:
   a hidden constraint, a subtle invariant, a workaround for a specific bug."

CONTEXT.md D-13: strip `// Phase N` historical breadcrumbs. Leave surrounding
sentence intact if it carries WHY; delete the whole comment if the phase
prefix IS the whole body. Keep requirement IDs (CONC-04, D-05, SC#4, T-124-XX).

CONTEXT.md D-14: prune comments that explain obvious code. Default: also
delete commented-out code blocks.

Research §3b Keep rules:
  - KEEP: non-obvious WHY (hidden invariant, AEAD nonce order, libmdbx quirk,
    TSAN finding, protocol-level rejection reason)
  - KEEP: strand/threading assertions (CONC-04 comments — strip only (Phase N))
  - KEEP: AEAD nonce order invariants (pre-existing Phase 84 crash-class notes)
  - KEEP: BOMB ttl=0 invariant notes (protocol rule — strip prefix, add PROTOCOL.md anchor)
  - KEEP: MDBX mmap quirks (used_data_bytes vs used_bytes geometry)
  - KEEP: FlatBuffer ForceDefaults(true) determinism notes
  - STRIP: comments that re-state the next line in English ("// increment counter" above "counter_++;")
  - STRIP: comments that cite a phase but nothing else (pure history)
  - DELETE: commented-out code blocks
  - RE-WRITE: comments that carry load-bearing WHY but lead with a phase prefix
    (strip the prefix; keep substance)
</policy>

<inventory>
From research §3a — `// Phase [0-9]+` exact-pattern counts (grep-verified):
- Total: 313 occurrences across 49 files
- Extended `/// Phase N` and `... Phase N ...`: 452 occurrences across 67 files

Top offenders (stripping these catches ~80% of hits):
- db/tests/engine/test_engine.cpp: 78 (test annotations — aggressive strip OK)
- db/tests/peer/test_peer_manager.cpp: 77 (same)
- cli/src/commands.cpp: 19 (mix — per-line decision required)
- db/tests/storage/test_storage.cpp: 15 (test annotations)
- db/tests/config/test_config.cpp: 9 (test annotations)
- db/peer/sync_orchestrator.cpp: 8 (impl breadcrumbs — per-line)
- db/tests/test_helpers.h: 5 (helper-fn phase tags)
- db/peer/message_dispatcher.cpp: 5 (dispatcher routing)
- db/peer/error_codes.h: 5 (error-code constants)
</inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-13 pass — strip `// Phase N` tokens from test files (aggressive) and source files (per-line WHY preservation)</name>
  <files>cli/src/, cli/tests/, db/engine/, db/storage/, db/net/, db/peer/, db/sync/, db/wire/, db/crypto/, db/config/, db/tests/</files>
  <action>
Per-subsystem commit strategy (one commit per area for reviewable history, per D-10 pattern):

**Subsystem pass order:**
1. `db/tests/` (largest — test annotations, mostly mechanical strip)
2. `cli/tests/`
3. `cli/src/` (mix, per-line decisions)
4. `db/engine/` (high-density, load-bearing WHYs common)
5. `db/storage/`
6. `db/net/`
7. `db/peer/`
8. `db/sync/`
9. `db/wire/`
10. `db/crypto/`
11. `db/config/`

For each file within a subsystem:

**Step 1: Discover.** Grep for every `// Phase [0-9]+` variant (including `/// Phase N`, `// ... Phase N ...`, `// Phase N / X-Y: ...`). Note each line.

**Step 2: Per-line classify.**
Apply the mechanical rule from CONTEXT.md D-13:
- **Whole-body-is-phase** (e.g. `// Phase 123`, `/// Phase 122`) → DELETE the whole comment.
- **Phase prefix + content** (e.g. `// Phase 123: Resolve a NAME to its current winner's target content_hash`) → strip the `Phase 123: ` (and any `/` or ` / ` glue); keep the rest. Result: `// Resolve a NAME to its current winner's target content_hash`.
- **Phase N / REQ-ID prefix** (e.g. `// Phase 119 / D-14: reject files above the hard 1 TiB cap up-front`) → strip only the phase token; preserve the requirement ID. Result: `// D-14: reject files above the hard 1 TiB cap up-front`.
- **CONC-04 (Phase 121)**-style inline-parenthetical → strip the `(Phase N)` parenthetical only. Result: `// CONC-04: post back to ioc_ before Storage access.`
- **Test annotations** (in `cli/tests/` and `db/tests/`) → aggressive strip acceptable. A comment like `// Phase 122: verify owner_pubkeys DBI schema` can become `// verify owner_pubkeys DBI schema` OR be deleted entirely if the test name already conveys the intent.

**Step 3: Apply edits file-by-file.** Use the Edit tool for targeted line edits; use Read first per Edit tool contract. Do NOT use sed/awk for bulk regex strips (risk of swallowing non-comment lines — research §7 warns specifically about this).

**Step 4: Per-subsystem quick verify.** After finishing each subsystem, grep the subsystem for `// Phase [0-9]+` and `/// Phase [0-9]+`; expect zero hits.

**Step 5: Per-subsystem quick-test smoke.** After each subsystem's commit, delegate to user:
```
./build/cli/tests/cli_tests --reporter compact
```
Exit 0 + assertion count preserved. Any regression = rollback the subsystem's commit and re-pass more carefully.

**Preserved in all passes (Catch2 identifier scope NOT in D-13):**
- `TEST_CASE` names and tags (e.g., `[phase122]`, `[engine][phase123]`) — these are identifiers, not comments.
- String literals (even if they contain "Phase 123" text — user-visible strings were handled in Plan 4 Task 1; any remaining hits are intentional).

**Final verification for D-13 scope:**
After all 11 subsystem passes, the repo-wide grep MUST return zero hits:
```
! grep -rEq "// Phase [0-9]+|/// Phase [0-9]+" cli/src/ cli/tests/ db/
```
  </action>
  <verify>
    <automated>bash -c '! grep -rEq "// Phase [0-9]+|/// Phase [0-9]+" cli/src/ cli/tests/ db/ --include="*.cpp" --include="*.h" --include="*.fbs"'</automated>
  </verify>
  <done>Zero `// Phase N` (including `///` and inline parenthetical) remain in .cpp/.h/.fbs source across cli/src/, cli/tests/, and db/. Per-subsystem commits preserve reviewable history. Per-subsystem smoke test runs passed.</done>
</task>

<task type="auto">
  <name>Task 2: D-14 pass — prune obvious-code comments + commented-out code across cli/src/ and db/ (high-density files first)</name>
  <files>cli/src/commands.cpp, cli/src/chunked.cpp, cli/src/connection.cpp, cli/src/wire.cpp, cli/src/wire.h, db/engine/engine.cpp, db/storage/storage.cpp, db/storage/storage.h, db/peer/message_dispatcher.cpp, db/net/connection.cpp, db/sync/sync_protocol.cpp, db/wire/codec.cpp</files>
  <action>
Per-file read → decide → edit. Twelve files chosen by research §3b as the highest-density comment files (~40% of repo LOC). Bounded scope: if this task runs long, commit what's done and defer remaining files to a follow-up — do NOT speed-run file 12 after files 1-11 consumed the context budget.

**Per-file protocol:**

**Step 1: Read the full file.** Load context before editing.

**Step 2: Decide on each comment.** For every comment in the file, classify:

- **KEEP (non-obvious WHY):** The comment explains:
  - A subtle invariant (e.g. "per-connection send queue prevents AEAD nonce desync — see Phase 84 history" → keep substance, strip any lingering phase prefix from Task 1)
  - A hidden constraint (e.g. "libmdbx txn must commit before sending WriteAck — reordering breaks crash-recovery semantics")
  - A workaround for a specific bug (e.g. "co_await value-copy workaround for C++20 coroutine-frame reference lifetime — ASAN-caught")
  - A protocol-level rejection reason that the reader can't infer from method name
  - A TSAN finding, AEAD nonce ordering, MDBX mmap geometry, ForceDefaults determinism note
  - A reference to a requirement ID, ADR, or bug ticket (CONC-04, SC#4, T-124-XX, D-05)
- **STRIP (obvious):** The comment re-states the next line in English.
  Examples to delete:
  ```cpp
  // increment i
  i++;
  ```
  ```cpp
  // Return the result
  return result;
  ```
  ```cpp
  /// The constructor.
  Foo::Foo() = default;
  ```
- **DELETE (commented-out code):** Any block of `//`-prefixed lines that parses as valid C++ tokens (semicolons, braces, function calls, ≥2 lines in a row). Per CONTEXT.md Claude's Discretion default: commented-out code is always cruft. Delete on sight.

**Step 3: Apply edits.** Use Edit tool. No regex sed — per-comment decisions required.

**Step 4: Per-file smoke.** After each file's edit commit, delegate to user:
```
./build/cli/tests/cli_tests --reporter compact
```
(If the file is in `db/`, also delegate `./build/db/db/chromatindb_tests --reporter compact` at minimum for the touched subsystem's tag.) Exit 0 + assertion counts preserved. Any drop → investigate (comment strip accidentally consumed code) → rollback → re-pass.

**Hard rules (from research §3b "Risk assessment — comments that LOOK obvious but aren't"):**

1. **KEEP** all `// CONC-04` / `// SC#N` / `// T-124-XX` / `// D-05` annotations (requirement IDs — strip phase prefixes only, keep IDs).
2. **KEEP** AEAD nonce ordering notes in `db/net/connection.cpp` ("per-connection send queue prevents AEAD nonce desync").
3. **KEEP** BOMB ttl=0 invariant notes in `db/engine/engine.cpp` ("BOMB is permanent like a single tombstone, exempt from max_ttl") — strip Phase 123 prefix; rewrite to add a PROTOCOL.md anchor: `// BOMB is permanent like a single tombstone, exempt from max_ttl. See PROTOCOL.md#bomb-blob-format.`
4. **KEEP** MDBX mmap quirk notes (`used_bytes` vs `used_data_bytes` geometry).
5. **KEEP** FlatBuffer `ForceDefaults(true)` determinism notes in `cli/src/wire.cpp`.
6. **KEEP** coroutine-frame value-copy notes (ASAN lifetime-fix history).
7. **KEEP** `engine.cpp` Step 0a/0b/0c/0d/0e annotations — they ARE load-bearing; the 11-step ingest pipeline structure relies on this numbering. DO NOT strip.

**Scope cap:** If rewriting a comment is tempting ("I could explain this better"), STOP — Plan 5 is cleaning, not rewriting. Either keep the original or delete it; no rewrites.

**Scope discipline:** 12 files is the plan; if each file consumes ~3% context, Plan 5's Task 2 is ~35% context total. If the executor hits ~45% context and only 8 files are done, COMMIT the 8, write a deferred-items note to `.planning/phases/125-docs-update-for-mvp-protocol/125-05-DEFERRED.md` naming the remaining 4 files, and terminate Task 2 cleanly. Do NOT sprint the last 4 files at degraded quality.
  </action>
  <verify>
    <automated>bash -c 'cd /home/mika/dev/chromatin-protocol &amp;&amp; [[ -f cli/src/commands.cpp ]] &amp;&amp; grep -qi "//\|/\\*" cli/src/commands.cpp &amp;&amp; echo "file-has-comments-still" &amp;&amp; ! grep -rEq "// *increment [a-z_]+\$|// *return [a-z_]+\$" cli/src/ db/ --include="*.cpp" --include="*.h"'</automated>
  </verify>
  <done>All 12 high-density files reviewed per-comment. Load-bearing WHYs preserved (with phase prefixes already stripped by Task 1). Obvious-code comments + commented-out code deleted. Per-file smoke tests pass assertion-count baseline. Any deferred files recorded in 125-05-DEFERRED.md.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate + ASAN build — catches accidental code deletion from Tasks 1-2</name>
  <files>(no file edits — build + test run only)</files>
  <action>
Final regression gate for the entire Plan 5 pass. Delegated to user per `feedback_delegate_tests_to_user`.

**Step A: Full Catch2 suite (both binaries) under default (Release-ish) build.**
```
cmake --build build -j$(nproc)
./build/cli/tests/cli_tests
./build/db/db/chromatindb_tests
```
- `cli_tests`: exit 0, assertion count ≥ 197614, case count ≥ 98 (baselines from 125-VALIDATION.md D3).
- `chromatindb_tests`: full suite (not just `[peer]`) — this is a broader gate than Plan 4's. Capture assertion + case count; record in summary.

**Any regression** (exit nonzero, assertion count drop, case count drop that wasn't accounted for in Plan 4 or Task 2 above):
1. Run under verbose: `./build/cli/tests/cli_tests --reporter console --verbosity high` (or `--break` for single-case drill-down).
2. Identify which file/commit introduced the regression.
3. Rollback that single commit; re-pass with tighter per-comment discipline.
4. Repeat Task 3 until green.

**Step B: ASAN build.**
```
cmake -B build-asan -DSANITIZER=asan
cmake --build build-asan -j$(nproc)
./build-asan/cli/tests/cli_tests
./build-asan/db/db/chromatindb_tests "[peer]"
```
ASAN catches silent code deletions that happen to compile but produce UB at runtime (use-after-free, heap-buffer-overflow). Per research §7 "Regression risk from D-14": ASAN is the secondary mitigation to test-suite's primary.

**Step C: Grep validation (per 125-VALIDATION.md D2):**
```
! grep -rEq "// Phase [0-9]+|/// Phase [0-9]+" cli/src/ cli/tests/ db/ --include="*.cpp" --include="*.h" --include="*.fbs"
! grep -rEq "\"[^\"]*\\(Phase [0-9]+\\)[^\"]*\"" cli/src/ db/
```
Both must return empty. (Plan 4 cleared `cli/src/` user-visible strings; Plan 5 clears all source comments.)

**Step D: Sanity grep — preserved patterns MUST still exist.**
These grep hits confirm we didn't over-strip load-bearing WHY comments:
```
grep -q "CONC-04" db/engine/engine.cpp || echo "MISSING CONC-04 — over-stripped"
grep -q "AEAD nonce" db/net/connection.cpp || echo "MISSING AEAD nonce invariant — over-stripped"
grep -qi "used_data_bytes\|mmap" db/storage/storage.cpp || echo "MISSING MDBX mmap note — over-stripped"
grep -q "ForceDefaults" cli/src/wire.cpp || echo "MISSING ForceDefaults determinism note — over-stripped"
```
None of these should print a MISSING line. If any do, Task 1 or Task 2 over-reached — rollback and re-pass that specific file.
  </action>
  <verify>
    <automated>bash -c '! grep -rEq "// Phase [0-9]+|/// Phase [0-9]+" cli/src/ cli/tests/ db/ --include="*.cpp" --include="*.h" --include="*.fbs" &amp;&amp; grep -q "CONC-04" db/engine/engine.cpp &amp;&amp; grep -qi "AEAD nonce" db/net/connection.cpp'</automated>
  </verify>
  <done>Full Catch2 suite green (cli_tests + chromatindb_tests both binaries); ASAN build green for the touched subsystems' tests; repo-wide phase-breadcrumb grep returns zero; preserved load-bearing WHY patterns (CONC-04, AEAD nonce, MDBX mmap, ForceDefaults) confirmed still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — Plan 5 modifies only comments. No code paths change. No new trust boundaries, no new data flows, no new input validation, no new auth paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-07 | Denial of Service | Over-aggressive D-14 strip accidentally deletes a load-bearing WHY comment; a future contributor re-introduces a bug that the comment would have prevented (e.g., AEAD nonce ordering regression) | mitigate | Task 2 "Hard rules" enumerate the specific load-bearing comments that MUST be preserved. Task 3 Step D grep-confirms the preservation invariants still hold after the pass. Any over-strip is caught either by grep-D or by test regression (since the invariants cite root causes that have tests). |
| T-125-08 | Tampering | A comment-strip regex accidentally consumes a non-comment line (e.g., `//` inside a string literal, or a `//` that opened a `/* ... */` comment) producing a silent code break | mitigate | Task 1 and Task 2 BOTH forbid sed/awk bulk regex — per-line edits via Edit tool with Read-first contract. Full Catch2 suite + ASAN build in Task 3 are the regression net. Per research §7: this is THE primary hygiene-pass risk, and the tests are the primary detector. |

No new attack surface introduced.
</threat_model>

<verification>
Primary (D-13 scope, per 125-VALIDATION.md D2):
- `! grep -rEq "// Phase [0-9]+" cli/src/ cli/tests/ db/ --include="*.cpp" --include="*.h" --include="*.fbs"`
- `! grep -rEq "/// Phase [0-9]+" cli/src/ cli/tests/ db/ --include="*.cpp" --include="*.h" --include="*.fbs"`

Primary (D-14 scope, spot-check):
- `! grep -rEq "// *increment [a-z_]+\$|// *return [a-z_]+\$" cli/src/ db/ --include="*.cpp" --include="*.h"` (obvious-code pattern absence)
- No commented-out code blocks ≥3 lines (manual spot-check during per-file review)

Preservation (Task 3 Step D):
- `grep -q "CONC-04" db/engine/engine.cpp`
- `grep -qi "AEAD nonce" db/net/connection.cpp`
- `grep -qi "used_data_bytes\|mmap geometry\|mmap quirk" db/storage/storage.cpp`
- `grep -q "ForceDefaults" cli/src/wire.cpp`

Test gate (per 125-VALIDATION.md D3):
- `./build/cli/tests/cli_tests`: exit 0, assertion count ≥ 197614 (baseline after Plan 4), case count ≥ 98.
- `./build/db/db/chromatindb_tests`: exit 0, full-suite pass. Baselines captured in Plan 4 summary; Plan 5 must not regress them.
- `./build-asan/.../cli_tests` + `./build-asan/.../chromatindb_tests "[peer]"`: exit 0 under ASAN.

Accept criteria: all primary absence greps return empty, all preservation greps hit, test + ASAN gates green.
</verification>

<success_criteria>
Plan 5 succeeds when:
- Zero `// Phase N` / `/// Phase N` breadcrumbs remain in `.cpp`, `.h`, `.fbs` files across `cli/src/`, `cli/tests/`, and `db/` (including `db/tests/`).
- Comments citing requirement IDs (CONC-04, SC#4, T-124-XX, D-05) are PRESERVED — phase prefixes removed, substance intact.
- Comments describing non-obvious invariants (AEAD nonce ordering, MDBX mmap geometry, FlatBuffer ForceDefaults determinism, coroutine-frame value-copy lifetimes, BOMB ttl=0 protocol rule, 11-step ingest pipeline step numbers, TSAN findings) are PRESERVED.
- Comments explaining obvious code and commented-out code blocks are DELETED per CLAUDE.md policy.
- Catch2 `TEST_CASE` names and tags (e.g. `[phase122]`) are NOT modified (D-13 is comment scope, not identifier scope).
- Full Catch2 suite green: `cli_tests` ≥ 197614 assertions / 98 cases baseline preserved; `chromatindb_tests` full-suite exit 0 with baselines captured in summary.
- ASAN build of touched subsystems' tests exits 0 (sanity check for silent code deletion).
- Any files deferred due to context budget are recorded in `125-05-DEFERRED.md` with exact file list, so a follow-up phase can close the remaining gap.
</success_criteria>

<output>
After completion, create `.planning/phases/125-docs-update-for-mvp-protocol/125-05-SUMMARY.md` capturing:
- Per-subsystem commit list with hit count before/after for `// Phase N`
- Per-file D-14 findings: lines deleted, commented-out blocks removed, load-bearing comments preserved
- Task 3 test gate results: `cli_tests` + `chromatindb_tests` assertion/case counts (full suite, not just `[peer]`)
- ASAN build results
- Preservation-grep confirmation (CONC-04, AEAD nonce, MDBX, ForceDefaults)
- Any deferred files with rationale (if Task 2 context-capped)
- Final repo-wide grep proof: zero `// Phase N` across all three source trees
</output>
