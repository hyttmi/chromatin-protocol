---
phase: 64-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - db/README.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "Root README.md shows v1.3.0 version"
    - "db/README.md documents request_id pipelining as a feature"
    - "db/README.md documents blob existence check (ExistsRequest) as a feature"
    - "db/README.md documents node capability discovery (NodeInfoRequest) as a feature"
    - "db/README.md describes the concurrent dispatch model (inline vs coroutine vs offload)"
    - "db/README.md states 40 message types in the wire protocol paragraph"
    - "db/README.md states 551 unit tests"
  artifacts:
    - path: "README.md"
      provides: "Updated version string"
      contains: "v1.3.0"
    - path: "db/README.md"
      provides: "Updated feature list, dispatch model, type count, test count"
      contains: "request_id"
  key_links:
    - from: "db/README.md"
      to: "db/PROTOCOL.md"
      via: "wire protocol reference link"
      pattern: "PROTOCOL.md"
---

<objective>
Update README.md (version bump) and db/README.md (new features, dispatch model, updated counts) to reflect v1.3.0 capabilities.

Purpose: Operators and contributors need accurate documentation of new features (request pipelining, blob existence check, node info query) and the concurrent dispatch architecture.
Output: Updated README.md with v1.3.0 version; updated db/README.md with 3 new feature entries, dispatch model description, and corrected counts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-documentation/64-CONTEXT.md
@db/peer/peer_manager.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root README.md version string</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — shows "v1.0.0 | Development: v1.1.0")
  </read_first>
  <action>
Per D-06: Keep root README.md as a thin pointer to db/README.md. Only change the version line.

Change line 5 from:
```
**Current release: v1.0.0** | **Development: v1.1.0**
```

To:
```
**Current release: v1.3.0**
```

Remove the "Development" portion — v1.3.0 is the shipping version for this milestone. Do not add feature bullets or any other content.
  </action>
  <verify>
    <automated>grep "v1.3.0" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md line 5 contains "**Current release: v1.3.0**"
    - README.md does NOT contain "v1.0.0" or "v1.1.0"
    - README.md does NOT contain "Development:"
    - README.md total line count remains under 15 lines (thin pointer)
  </acceptance_criteria>
  <done>Root README.md shows v1.3.0 with no duplicated feature content</done>
</task>

<task type="auto">
  <name>Task 2: Update db/README.md with v1.3.0 features, dispatch model, and counts</name>
  <files>db/README.md</files>
  <read_first>
    - db/README.md (current state — has pre-existing unstaged fix changing "30 message types" to "36 message types")
    - db/peer/peer_manager.cpp lines 523-538 (dispatch model comment — source of truth for categories)
  </read_first>
  <action>
Update db/README.md with the following changes. The file has a pre-existing unstaged fix (message count 30->36) — this task updates that to 40.

**1. Update wire protocol message count** (in "Wire Protocol" section, around line 192).

Change the sentence that currently reads (after the pre-existing fix):
```
The protocol defines 36 message types covering handshake, keepalive, blob storage, sync (with range-based set reconciliation), peer exchange, deletion, pub/sub, storage signaling, trusted peer handshake, namespace quota enforcement, and sync rate limiting.
```

To:
```
The protocol defines 40 message types covering handshake, keepalive, blob storage, sync (with range-based set reconciliation), peer exchange, deletion, pub/sub, storage signaling, trusted peer handshake, namespace quota enforcement, sync rate limiting, client queries, and node capability discovery.
```

Per D-10: update from 36 to 40 (adding ExistsRequest 37, ExistsResponse 38, NodeInfoRequest 39, NodeInfoResponse 40).

**2. Update unit test count** (in "Testing > Unit Tests" section, around line 64).

Change:
```
469 unit tests covering all subsystems
```

To:
```
551 unit tests covering all subsystems
```

Per D-11: The actual test count is 551 (verified via `ctest -N`).

**3. Add three new feature entries** (in "Features" section, after the "Inactivity Timeout" entry which is the last feature). Follow the existing pattern of **bold name** + em-dash + one-paragraph description. Per D-09, add these three entries:

```markdown
**Request Pipelining** -- Clients assign a `request_id` to each request message and the node echoes it on the corresponding response. Multiple requests can be sent without waiting, and responses are matched by `request_id` regardless of arrival order. The `request_id` is a `uint32` in the transport envelope, scoped per connection.

**Blob Existence Check** -- Clients send an ExistsRequest with a namespace and blob hash to check whether a blob exists without transferring its data. The node responds with a single-byte existence flag and the echoed blob hash. Tombstoned blobs are reported as not found.

**Node Capability Discovery** -- Clients send a NodeInfoRequest to retrieve the node's software version, git hash, uptime, peer count, namespace count, total blobs, storage usage, and a list of supported message types. SDKs use the supported types list for feature detection.
```

**4. Add concurrent dispatch model description** (per D-05). Insert a new feature entry after "Thread Pool Crypto Offload" (which is currently the feature that discusses the thread pool). This positions the dispatch model near the related threading content:

```markdown
**Concurrent Request Dispatch** -- Client requests are dispatched according to their cost. Lightweight operations (Subscribe, Unsubscribe, StorageFull, QuotaExceeded) execute inline on the IO thread. Storage queries (ReadRequest, ListRequest, StatsRequest, ExistsRequest, NodeInfoRequest) run as coroutines on the IO thread. Heavy operations (Data, Delete) offload cryptographic verification to the thread pool and transfer back to the IO thread before sending responses, maintaining AEAD nonce safety.
```
  </action>
  <verify>
    <automated>grep -c "Request Pipelining\|Blob Existence Check\|Node Capability Discovery\|Concurrent Request Dispatch" db/README.md | xargs test 4 -eq</automated>
  </verify>
  <acceptance_criteria>
    - db/README.md contains "**Request Pipelining**"
    - db/README.md contains "**Blob Existence Check**"
    - db/README.md contains "**Node Capability Discovery**"
    - db/README.md contains "**Concurrent Request Dispatch**"
    - db/README.md contains "The protocol defines 40 message types"
    - db/README.md contains "551 unit tests"
    - db/README.md does NOT contain "469 unit tests"
    - db/README.md does NOT contain "36 message types" (updated to 40)
    - db/README.md contains "request_id"
    - db/README.md contains "ExistsRequest"
    - db/README.md contains "NodeInfoRequest"
    - db/README.md contains "inline on the IO thread" (dispatch model)
    - db/README.md contains "AEAD nonce safety" (dispatch model)
  </acceptance_criteria>
  <done>db/README.md has 4 new feature entries (pipelining, existence, node info, dispatch model), 40 message types, and 551 unit tests</done>
</task>

</tasks>

<verification>
- `grep "v1.3.0" README.md` returns a match
- `grep "40 message types" db/README.md` returns a match
- `grep "551 unit tests" db/README.md` returns a match
- `grep -c "Request Pipelining\|Blob Existence\|Node Capability\|Concurrent Request Dispatch" db/README.md` returns 4
- `grep "request_id" db/README.md` returns at least one match
- `grep "inline on the IO thread" db/README.md` returns a match
</verification>

<success_criteria>
README.md shows v1.3.0 version; db/README.md documents all v1.3.0 features (request pipelining, blob existence check, node capability discovery, concurrent dispatch model) with accurate message type count (40) and test count (551)
</success_criteria>

<output>
After completion, create `.planning/phases/64-documentation/64-02-SUMMARY.md`
</output>
