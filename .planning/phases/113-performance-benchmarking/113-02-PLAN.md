---
phase: 113-performance-benchmarking
plan: 02
type: execute
wave: 1
depends_on: ["113-01"]
files_modified:
  - tools/benchmark_report.md
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04]
gap_closure: true

must_haves:
  truths:
    - "Throughput benchmark produces blobs/sec at concurrency 1, 10, and 100"
    - "Latency benchmark produces p50/p95/p99 per-operation round-trip times"
    - "Large blob benchmark produces write+read MiB/sec at 1, 10, 50, and 100 MiB"
    - "Mixed workload benchmark produces small-query latency degradation percentage"
    - "All results are recorded in tools/benchmark_report.md with git hash and build type metadata"
  artifacts:
    - path: "tools/benchmark_report.md"
      provides: "Benchmark results for all 4 PERF workloads with system metadata"
      contains: "PERF-01"
  key_links:
    - from: "tools/relay_perf_test.sh"
      to: "tools/benchmark_report.md"
      via: "script execution produces report file"
      pattern: "benchmark_report\\.md"
---

<objective>
Execute the benchmark orchestration script and produce the performance baseline report that closes all 4 PERF requirement gaps.

Purpose: Phase 113's tooling (relay_perf_test.sh) was created in Plan 01 but the benchmark was never actually run -- the checkpoint:human-verify gate was auto-approved without evidence. All 4 PERF requirements explicitly require results "recorded in benchmark report." This gap closure plan runs the benchmark and validates the output.

Output: `tools/benchmark_report.md` containing throughput, latency, large blob, and mixed workload baseline results with environment metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-performance-benchmarking/113-CONTEXT.md
@.planning/phases/113-performance-benchmarking/113-VERIFICATION.md
@.planning/phases/113-performance-benchmarking/113-01-SUMMARY.md

<interfaces>
<!-- Tooling already verified complete in VERIFICATION.md (Level 3 pass). -->

tools/relay_perf_test.sh (230 lines, executable, bash -n clean):
- Builds Release binaries (cmake -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF)
- Generates node config (port 4280) + relay config (port 4281, rate_limit=0, request_timeout=0, max_blob_size=0)
- Starts node, waits for UDS socket (30s timeout)
- Starts relay, waits for TCP accept (15s timeout)
- Runs: python3 relay_benchmark.py --relay-url http://127.0.0.1:4281 --output tools/benchmark_report.md
- Appends Environment metadata table (Git hash, Build type, CPU, OS, Date)
- Cleanup trap kills node+relay and removes temp dir

tools/relay_benchmark.py (1139 lines):
- Defaults: 100 iterations, 5 warmup, concurrency 1/10/100
- Blob sizes: 1/10/50/100 MiB
- Mixed duration: 30s
- Produces markdown report with sections: ## PERF-01, ## PERF-02, ## PERF-03, ## PERF-04

Expected report structure after successful run:
- ## PERF-01: Throughput — blobs/sec table at concurrency 1, 10, 100
- ## PERF-02: Latency — p50/p95/p99 for write, read, exists, stats operations
- ## PERF-03: Large Blob — write+read MiB/sec at 1, 10, 50, 100 MiB
- ## PERF-04: Mixed Workload — baseline vs under-load latency + degradation %
- ## Environment — Git hash, Build type, CPU, OS, Date (appended by shell script)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute benchmark and produce report</name>
  <files>tools/benchmark_report.md</files>
  <read_first>
    tools/relay_perf_test.sh
    tools/relay_benchmark.py
  </read_first>
  <action>
Run the performance benchmark orchestration script to produce `tools/benchmark_report.md`.

**Step 1: Check for existing Release build to save time.**
```bash
ls -la build-release/chromatindb build-release/chromatindb_relay 2>/dev/null
```
If both binaries exist, use `--skip-build` flag. Otherwise, run full build (adds ~2-5 minutes).

**Step 2: Execute the benchmark script.**
If binaries exist:
```bash
cd /home/mika/dev/chromatin-protocol && bash tools/relay_perf_test.sh --skip-build
```
If binaries do not exist:
```bash
cd /home/mika/dev/chromatin-protocol && bash tools/relay_perf_test.sh
```

Expected runtime: 10-20 minutes. The script will:
1. Verify Python dependencies (httpx, flatbuffers, oqs)
2. Build Release binaries if needed (cmake + make)
3. Start chromatindb node on port 4280 with UDS socket in /tmp/relay_perf_test.XXXXXX/
4. Start chromatindb_relay on port 4281
5. Run relay_benchmark.py with all 4 PERF workloads using defaults (100 iterations, 5 warmup, concurrency 1/10/100, blob sizes 1/10/50/100 MiB, 30s mixed)
6. Append Environment metadata (git hash, build type Release, CPU, OS, date) to report
7. Clean up (kill processes, remove temp dir)

**Step 3: Verify report file was created.**
```bash
test -f tools/benchmark_report.md && echo "REPORT EXISTS" || echo "REPORT MISSING"
wc -l tools/benchmark_report.md
```

**Step 4: Verify report contains all required sections.**
```bash
grep -c "PERF-01" tools/benchmark_report.md
grep -c "PERF-02" tools/benchmark_report.md
grep -c "PERF-03" tools/benchmark_report.md
grep -c "PERF-04" tools/benchmark_report.md
grep -c "Git hash" tools/benchmark_report.md
grep -c "Build type" tools/benchmark_report.md
```

**If the script fails:** Check node.log and relay.log in the temp directory (printed by the script on failure). Common issues: port 4280/4281 in use (kill stale processes), missing Python deps. If the script exits non-zero, do NOT create a fake report -- diagnose and fix the issue.

**If a specific workload fails:** The benchmark tool runs all 4 workloads sequentially. If one fails (e.g., 100 MiB blob times out), the script exits non-zero and no report is produced. Check relay.log for errors. Consider if relay config needs adjustment (max_blob_size_bytes is already 0=unlimited, request_timeout_seconds is already 0=disabled).
  </action>
  <verify>
    <automated>test -f /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && grep -q "PERF-01" /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && grep -q "PERF-02" /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && grep -q "PERF-03" /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && grep -q "PERF-04" /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && grep -q "Git hash" /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && grep -q "Build type" /home/mika/dev/chromatin-protocol/tools/benchmark_report.md && echo "ALL SECTIONS PRESENT" || echo "VERIFICATION FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - tools/benchmark_report.md exists (test -f returns 0)
    - File contains "PERF-01" (throughput section present)
    - File contains "PERF-02" (latency section present)
    - File contains "PERF-03" (large blob section present)
    - File contains "PERF-04" (mixed workload section present)
    - File contains "Git hash" (environment metadata present)
    - File contains "Build type" with "Release" value
    - File contains "CPU" in environment section
    - File contains "blobs/sec" or "blob" in PERF-01 section (throughput data exists, not just header)
    - File contains "p50" or "p95" or "p99" in PERF-02 section (latency percentiles exist)
    - File contains "MiB" in PERF-03 section (large blob data exists)
    - File is more than 50 lines (substantive report, not stub)
  </acceptance_criteria>
  <done>tools/benchmark_report.md exists with all 4 PERF workload results and environment metadata. The benchmark was actually executed against a live Release-built relay and node.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify benchmark results are reasonable</name>
  <what-built>Performance baseline report generated by running all 4 PERF workloads (throughput at concurrency 1/10/100, latency p50/p95/p99, large blob 1-100 MiB write+read, mixed workload degradation) against a Release-built relay.</what-built>
  <how-to-verify>
1. Read the benchmark report:
   ```
   cat tools/benchmark_report.md
   ```

2. Check PERF-01 (Throughput): Are blobs/sec numbers present for concurrency 1, 10, and 100? Numbers should be higher than ASAN baseline (Phase 112 saw 244-766 blobs/sec under ASAN; Release should be 2-10x faster).

3. Check PERF-02 (Latency): Are p50/p95/p99 millisecond values present for write, read, exists, and stats operations?

4. Check PERF-03 (Large Blob): Are MiB/sec values present for 1, 10, 50, and 100 MiB blob sizes?

5. Check PERF-04 (Mixed Workload): Is there a baseline latency, under-load latency, and degradation percentage?

6. Check Environment section at bottom: git hash, "Release" build type, CPU model, OS, date.

7. Sanity: No error messages or stack traces in the report. Numbers are non-zero and plausible.
  </how-to-verify>
  <resume-signal>Type "approved" if the report contains all 4 PERF sections with plausible numbers and environment metadata, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- tools/benchmark_report.md exists with non-trivial content (>50 lines)
- Report contains sections for PERF-01 (throughput), PERF-02 (latency), PERF-03 (large blob), PERF-04 (mixed)
- Each section contains actual numeric data (not placeholders or errors)
- Report contains Environment table with git hash, build type (Release), CPU, OS, date
- No relay code was modified (only tools/benchmark_report.md created as output)
</verification>

<success_criteria>
- tools/benchmark_report.md contains throughput blobs/sec at concurrency 1, 10, and 100 (PERF-01)
- tools/benchmark_report.md contains latency p50/p95/p99 values (PERF-02)
- tools/benchmark_report.md contains large blob write+read MiB/sec at 1, 10, 50, and 100 MiB (PERF-03)
- tools/benchmark_report.md contains mixed workload degradation percentage (PERF-04)
- tools/benchmark_report.md contains git hash and "Release" build type in Environment section
- Benchmark was run against actual Release-built relay (not fabricated data)
</success_criteria>

<output>
After completion, create `.planning/phases/113-performance-benchmarking/113-02-SUMMARY.md`
</output>
