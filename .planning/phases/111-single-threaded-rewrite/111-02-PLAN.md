---
phase: 111-single-threaded-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - relay/http/http_router.h
  - relay/http/http_router.cpp
  - relay/http/handlers_data.h
  - relay/http/handlers_data.cpp
  - relay/http/handlers_query.h
  - relay/http/handlers_query.cpp
  - relay/http/handlers_pubsub.h
  - relay/http/handlers_pubsub.cpp
  - relay/http/http_server.h
  - relay/http/http_server.cpp
  - relay/http/http_connection.h
  - relay/http/http_connection.cpp
  - relay/http/response_promise.h
  - relay/http/token_store.h
  - relay/core/uds_multiplexer.h
  - relay/core/uds_multiplexer.cpp
  - relay/core/authenticator.h
  - relay/core/authenticator.cpp
  - relay/core/metrics_collector.h
  - relay/core/metrics_collector.cpp
  - relay/core/request_router.h
  - relay/core/subscription_tracker.h
  - relay/core/write_tracker.h
autonomous: true
requirements:
  - CONC-02
  - CONC-03
  - CONC-04

must_haves:
  truths:
    - "No std::mutex exists in any relay production header or source file"
    - "No asio::strand type alias or member exists in any relay production code"
    - "No co_await asio::post(strand, ...) calls exist in any relay handler"
    - "ML-DSA-87 verify in auth handler offloads to thread pool via offload() with transfer-back"
    - "UdsMultiplexer::send() executes directly without strand posting"
    - "ResponsePromise uses ioc executor instead of strand executor"
    - "All shared state access comments say single-threaded, not strand"
  artifacts:
    - path: "relay/http/http_router.h"
      provides: "HttpRouter without strand, register_auth_routes with pool+ioc params"
    - path: "relay/core/uds_multiplexer.h"
      provides: "UdsMultiplexer without strand member"
    - path: "relay/http/handlers_data.h"
      provides: "DataHandlers with ioc reference instead of strand"
    - path: "relay/core/authenticator.h"
      provides: "Authenticator without acl_mutex_"
  key_links:
    - from: "relay/http/http_router.cpp"
      to: "relay/util/thread_pool.h"
      via: "offload() call for ML-DSA-87 verify"
      pattern: "co_await relay::util::offload"
    - from: "relay/http/http_router.cpp"
      to: "relay/relay_main.cpp"
      via: "transfer-back after offload"
      pattern: "co_await asio::post.*ioc.*use_awaitable"
---

<objective>
Strip all strand, mutex, and atomic synchronization from relay production code. Wire the
ML-DSA-87 offload at its single call site. Convert all components to the single-threaded
access model.

Purpose: After Plan 01 changed relay_main.cpp to single-threaded, the component signatures
still expect strands/mutexes. This plan makes all components match the new model so the relay
compiles again.

Output: All 23 relay production files updated to single-threaded model, offload() wired for auth verify
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-single-threaded-rewrite/111-CONTEXT.md
@.planning/phases/111-single-threaded-rewrite/111-RESEARCH.md
@.planning/phases/111-single-threaded-rewrite/111-01-SUMMARY.md

@relay/relay_main.cpp
@relay/util/thread_pool.h
</context>

<interfaces>
<!-- Plan 01 outputs that this plan consumes -->
From relay/util/thread_pool.h (created in Plan 01):
```cpp
namespace chromatindb::relay::util {
template <typename F> requires std::invocable<F>
asio::awaitable<std::invoke_result_t<F>> offload(asio::thread_pool& pool, F&& fn);
}
```

From relay/relay_main.cpp (modified in Plan 01) -- new wiring calls:
```cpp
// register_auth_routes(router, authenticator, token_store, offload_pool, ioc);
// UdsMultiplexer(ioc, cfg.uds_path, identity, request_router, std::move(dispatch));
// DataHandlers(uds_mux, request_router, promise_map, ..., max_blob_size, request_timeout, ioc);
// QueryHandlerDeps{uds_mux, request_router, promise_map, ioc, &request_timeout};
// PubSubHandlers(subscription_tracker, uds_mux, token_store, ioc);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Strip strands/mutexes from HTTP layer headers and wire offload</name>
  <files>
    relay/http/http_router.h
    relay/http/http_router.cpp
    relay/http/handlers_data.h
    relay/http/handlers_data.cpp
    relay/http/handlers_query.h
    relay/http/handlers_query.cpp
    relay/http/handlers_pubsub.h
    relay/http/handlers_pubsub.cpp
    relay/http/http_server.h
    relay/http/http_server.cpp
    relay/http/http_connection.h
    relay/http/http_connection.cpp
    relay/http/response_promise.h
    relay/http/token_store.h
  </files>
  <read_first>
    - relay/http/http_router.h
    - relay/http/http_router.cpp
    - relay/http/handlers_data.h
    - relay/http/handlers_data.cpp
    - relay/http/handlers_query.h
    - relay/http/handlers_query.cpp
    - relay/http/handlers_pubsub.h
    - relay/http/handlers_pubsub.cpp
    - relay/http/http_server.h
    - relay/http/http_server.cpp
    - relay/http/http_connection.h
    - relay/http/http_connection.cpp
    - relay/http/response_promise.h
    - relay/http/token_store.h
    - relay/util/thread_pool.h
    - .planning/phases/111-single-threaded-rewrite/111-RESEARCH.md
  </read_first>
  <action>
Apply the following changes to each file. The RESEARCH.md "Complete Inventory of Changes" section has the exact current/target for every line. Use that as the authoritative reference.

**--- relay/http/http_router.h ---**
Per D-07, D-09:
1. Delete `using Strand = asio::strand<asio::io_context::executor_type>;` (line 29)
2. Delete `void set_strand(Strand* s) { strand_ = s; }` (line 70)
3. Delete `Strand* strand_ = nullptr;` (line 73)
4. Update `register_auth_routes` signature: change `(HttpRouter& router, core::Authenticator& authenticator, TokenStore& token_store, HttpRouter::Strand& strand)` to `(HttpRouter& router, core::Authenticator& authenticator, TokenStore& token_store, asio::thread_pool& pool, asio::io_context& ioc)`
5. Add `#include <asio.hpp>` if not already present (it is -- keep it)
6. Update the class doc comment: remove "Auth middleware (check_auth) posts to strand before TokenStore::lookup" -- replace with "Auth middleware checks TokenStore directly (single-threaded model)."

**--- relay/http/http_router.cpp ---**
Per D-07, D-09, D-03 (offload wiring):
1. Add `#include "relay/util/thread_pool.h"` to includes
2. Delete the `using Strand = ...` type alias (line 91)
3. **Rewrite `check_auth` async function (lines 93-126):** Remove strand parameter. Remove `co_await asio::post(strand, asio::use_awaitable);` (line 110). The function body stays the same but without the strand post -- it accesses TokenStore directly since we're on the event loop thread. The function signature becomes: `static asio::awaitable<HttpResponse> check_auth(const HttpRequest& req, TokenStore& token_store, HttpSessionState** out_session)` (no strand param).
4. **Rewrite `dispatch_async` (lines 180-224):** Remove the `if (strand_)` branch (lines 193-199). Always call `check_auth(req, token_store, &session)` directly (no strand, the new 3-param version). Remove the `else` fallback to `check_auth_sync`.
5. **Rewrite `register_auth_routes` (lines 280-379):** Change signature to `(HttpRouter& router, core::Authenticator& authenticator, TokenStore& token_store, asio::thread_pool& pool, asio::io_context& ioc)`.

   In the POST /auth/challenge lambda:
   - Capture `&ioc` instead of `&strand`
   - Remove `co_await asio::post(strand, asio::use_awaitable);` (line 296). ChallengeStore::add is now direct (single-threaded).

   In the POST /auth/verify lambda:
   - Capture `&pool, &ioc` instead of `&strand`
   - Remove `co_await asio::post(strand, asio::use_awaitable);` before ChallengeStore::consume (line 344). Consume is direct.
   - **Wire offload() for ML-DSA-87 verify (per D-03).** Replace:
     ```cpp
     auto result = authenticator.verify(*challenge_bytes, *pubkey_bytes, *sig_bytes);
     ```
     With:
     ```cpp
     // Offload ML-DSA-87 verification to thread pool (per D-03)
     auto result = co_await relay::util::offload(pool, [&]() {
         return authenticator.verify(*challenge_bytes, *pubkey_bytes, *sig_bytes);
     });
     // CRITICAL: Transfer back to event loop before accessing shared state
     co_await asio::post(ioc, asio::use_awaitable);
     ```
   - The TokenStore::create_session call after verify stays as-is (already on event loop after transfer-back).

**--- relay/http/handlers_data.h ---**
Per D-07, D-09, D-10:
1. Delete `using Strand = asio::strand<asio::io_context::executor_type>;` (line 50)
2. Remove `#include <atomic>` (no longer needed)
3. Change constructor: replace `Strand& strand` parameter with `asio::io_context& ioc`
4. Change `const std::atomic<uint32_t>& max_blob_size_` to `const uint32_t& max_blob_size_`
5. Change `const std::atomic<uint32_t>& request_timeout_` to `const uint32_t& request_timeout_`
6. Replace `Strand& strand_` member with `asio::io_context& ioc_`
7. Update class doc comment: remove "All handlers post to strand_" -- replace with "All handlers execute on the single event loop thread."

**--- relay/http/handlers_data.cpp ---**
Per D-07:
1. In constructor: accept `asio::io_context& ioc` instead of `Strand& strand`, store as `ioc_(ioc)` instead of `strand_(strand)`
2. Delete ALL `co_await asio::post(strand_, asio::use_awaitable);` calls (4 occurrences in handle_blob_write, handle_blob_read, handle_blob_delete, handle_batch_read)
3. Change `promises_.create_promise(relay_rid, strand_)` to `promises_.create_promise(relay_rid, ioc_.get_executor())` (executor for ResponsePromise timer)
4. Change `max_blob_size_.load(std::memory_order_relaxed)` to just `max_blob_size_` (plain uint32_t& dereference, 1-2 occurrences)
5. Change `request_timeout_.load(std::memory_order_relaxed)` to just `request_timeout_` (plain uint32_t& dereference)

**--- relay/http/handlers_query.h ---**
Per D-07, D-09:
1. Delete `using Strand = asio::strand<asio::io_context::executor_type>;` (line 58)
2. In `QueryHandlerDeps` struct: delete `Strand* strand = nullptr;` member (line 67)
3. Change `const std::atomic<uint32_t>* request_timeout` to `const uint32_t* request_timeout`
4. Remove `#include <atomic>` if only used for the above
5. Update the doc comment "Strand type alias for query handler strand confinement" -- delete that comment entirely

**--- relay/http/handlers_query.cpp ---**
Per D-07:
1. In the `forward_query` helper function: remove `Strand& strand` parameter if present
2. Delete ALL `co_await asio::post(strand, asio::use_awaitable);` calls (likely in forward_query and route lambdas)
3. Change `promises.create_promise(relay_rid, strand)` to `promises.create_promise(relay_rid, deps.ioc.get_executor())` -- use ioc from QueryHandlerDeps
4. Change `deps.request_timeout->load(std::memory_order_relaxed)` to `*deps.request_timeout` (plain uint32_t* dereference)
5. Remove all lambda captures of `strand_ptr` or `strand` -- replace with ioc reference from deps where needed

**--- relay/http/handlers_pubsub.h ---**
Per D-07, D-09:
1. Delete `using Strand = asio::strand<asio::io_context::executor_type>;` (line 37)
2. Change constructor: replace `Strand& strand` parameter with `asio::io_context& ioc`
3. Replace `Strand& strand_` member with `asio::io_context& ioc_`
4. Update doc comments: remove "All handlers post to strand_" -- replace with "All handlers execute on the single event loop thread."

**--- relay/http/handlers_pubsub.cpp ---**
Per D-07:
1. In constructor: accept `asio::io_context& ioc` instead of `Strand& strand`, store as `ioc_(ioc)`
2. Delete ALL `co_await asio::post(strand_, asio::use_awaitable);` calls (3 occurrences: handle_subscribe, handle_unsubscribe, handle_events_auth)

**--- relay/http/http_server.h ---**
Per D-08, D-10:
1. Delete `#include <mutex>` (line 9)
2. Delete `mutable std::mutex tls_mutex_;` (line 68)
3. Change `std::atomic<uint32_t> max_connections_;` to `uint32_t max_connections_;` (line 70)
4. Change `std::atomic<uint32_t> active_connections_{0};` to `uint32_t active_connections_{0};` (line 71)
5. Change constructor parameter: `const std::atomic<bool>& stopping` stays as-is (stopping remains atomic)
6. Update comment "TLS context: shared_ptr + mutex for SIGHUP reload" to "TLS context: shared_ptr, SIGHUP reload on event loop thread."

**--- relay/http/http_server.cpp ---**
Per D-08:
1. Delete ALL `std::lock_guard<std::mutex> lock(tls_mutex_);` calls (3 occurrences: in init_tls, reload_tls, and handle_new_connection or accept_loop)
2. Change `active_connections_.load(std::memory_order_relaxed)` to just `active_connections_` (if used)
3. Change `max_connections_.load(std::memory_order_relaxed)` to just `max_connections_`
4. Change `max_connections_.store(value, ...)` to `max_connections_ = value` in set_max_connections()
5. In `active_connections()` method: return `active_connections_` directly (if it was using .load())

**--- relay/http/http_connection.h ---**
Per D-10:
1. Change `std::atomic<uint32_t>& active_connections_` member (line 67) to `uint32_t& active_connections_`
2. Change constructor parameter to match: `uint32_t& active_connections`
3. Update `ConnectionGuard` class (lines 80-93): change `std::atomic<uint32_t>& counter_` to `uint32_t& counter_`. Replace `counter_.fetch_add(1, std::memory_order_relaxed)` with `++counter_`. Replace `counter_.fetch_sub(1, std::memory_order_relaxed)` with `--counter_`.
4. Remove `#include <atomic>` if no other atomics remain in this file. (Check: `std::atomic<bool>& stopping_` is NOT in this file, so safe to remove if only active_connections used it.)

**--- relay/http/http_connection.cpp ---**
Constructor parameter type change follows header. No functional changes beyond the type.

**--- relay/http/response_promise.h ---**
Comment update only:
1. Change line 83 "Access serialized via strand -- all callers must be on the strand." to "Access from single event loop thread -- no synchronization needed."

**--- relay/http/token_store.h ---**
Comment update only:
1. Change "Access serialized via strand -- all callers must be on the strand." to "Access from single event loop thread -- no synchronization needed."
  </action>
  <verify>
    <automated>! grep -rn 'using Strand' relay/http/ --include='*.h' --include='*.cpp' && ! grep -rn 'strand_' relay/http/ --include='*.h' --include='*.cpp' && ! grep -rn 'set_strand' relay/http/ --include='*.h' --include='*.cpp' && ! grep -rn 'tls_mutex_' relay/http/ --include='*.h' --include='*.cpp' && echo "HTTP layer clean"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `using Strand` in relay/http/ headers and sources
    - Zero occurrences of `strand_` member in relay/http/ headers and sources
    - Zero occurrences of `set_strand` in relay/http/
    - Zero occurrences of `tls_mutex_` in relay/http/
    - Zero occurrences of `co_await asio::post(strand` in relay/http/ sources
    - relay/http/http_router.h contains `register_auth_routes(HttpRouter&, core::Authenticator&, TokenStore&, asio::thread_pool&, asio::io_context&)`
    - relay/http/http_router.cpp contains `co_await relay::util::offload(pool,` (ML-DSA-87 offload)
    - relay/http/http_router.cpp contains `co_await asio::post(ioc, asio::use_awaitable)` (transfer-back)
    - relay/http/handlers_data.h contains `asio::io_context& ioc_` (NOT `Strand& strand_`)
    - relay/http/handlers_data.h contains `const uint32_t& max_blob_size_` (NOT atomic)
    - relay/http/handlers_data.cpp contains `ioc_.get_executor()` for promise creation
    - relay/http/handlers_query.h QueryHandlerDeps has NO `strand` field
    - relay/http/handlers_pubsub.h contains `asio::io_context& ioc_` (NOT `Strand& strand_`)
    - relay/http/http_server.h contains `uint32_t max_connections_` (NOT atomic)
    - relay/http/http_server.h contains `uint32_t active_connections_` (NOT atomic)
    - relay/http/http_server.h does NOT contain `#include <mutex>`
    - relay/http/http_connection.h ConnectionGuard uses `uint32_t&` (NOT atomic)
    - relay/http/response_promise.h contains "single event loop thread" comment
    - relay/http/token_store.h contains "single event loop thread" comment
  </acceptance_criteria>
  <done>All HTTP layer files stripped of strand/mutex/atomic synchronization. ML-DSA-87 offload wired via offload() with transfer-back. ResponsePromise uses ioc executor.</done>
</task>

<task type="auto">
  <name>Task 2: Strip strands/mutexes from core layer and update comments</name>
  <files>
    relay/core/uds_multiplexer.h
    relay/core/uds_multiplexer.cpp
    relay/core/authenticator.h
    relay/core/authenticator.cpp
    relay/core/metrics_collector.h
    relay/core/metrics_collector.cpp
    relay/core/request_router.h
    relay/core/subscription_tracker.h
    relay/core/write_tracker.h
  </files>
  <read_first>
    - relay/core/uds_multiplexer.h
    - relay/core/uds_multiplexer.cpp
    - relay/core/authenticator.h
    - relay/core/authenticator.cpp
    - relay/core/metrics_collector.h
    - relay/core/metrics_collector.cpp
    - relay/core/request_router.h
    - relay/core/subscription_tracker.h
    - relay/core/write_tracker.h
    - .planning/phases/111-single-threaded-rewrite/111-RESEARCH.md
  </read_first>
  <action>
**--- relay/core/uds_multiplexer.h ---**
Per D-07, D-09, D-10:
1. Delete `using Strand = asio::strand<asio::io_context::executor_type>;` (line 31)
2. Change constructor: remove `Strand& strand` parameter. New signature: `UdsMultiplexer(asio::io_context& ioc, std::string uds_path, const identity::RelayIdentity& identity, RequestRouter& router, SessionDispatch dispatch)`
3. Delete `Strand& strand_` member (line 111)
4. Change `const std::atomic<uint32_t>* request_timeout_` to `const uint32_t* request_timeout_`
5. Change `set_request_timeout` parameter from `const std::atomic<uint32_t>*` to `const uint32_t*`

**--- relay/core/uds_multiplexer.cpp ---**
Per D-07:
1. In constructor: remove strand parameter, remove `strand_(strand)` initializer
2. Change `socket_(strand_)` to `socket_(ioc_)` in constructor initializer list
3. Change ALL `asio::co_spawn(strand_, ...)` to `asio::co_spawn(ioc_, ...)` (5 occurrences: in start(), connect_loop after handshake, connect_loop for cleanup, and reconnect points)
4. Change ALL `asio::steady_timer timer(strand_)` to `asio::steady_timer timer(ioc_)` (2 occurrences: in connect_loop backoff and cleanup_loop)
5. Change `asio::local::stream_protocol::socket(strand_)` to `asio::local::stream_protocol::socket(ioc_)` (in reconnect code where new socket is created)
6. **Simplify send() method:** Remove the `asio::post(strand_, [this, msg = std::move(transport_msg)]() mutable { ... });` wrapping. Instead, execute the body directly:
   ```cpp
   bool UdsMultiplexer::send(std::vector<uint8_t> transport_msg) {
       if (!connected_) return false;
       send_queue_.push_back(std::move(transport_msg));
       if (!draining_) {
           draining_ = true;
           asio::co_spawn(ioc_, [this]() -> asio::awaitable<void> {
               co_await drain_send_queue();
           }, asio::detached);
       }
       return true;
   }
   ```
   (Note: use `this` directly since send() is always called from the event loop thread.)
7. Change `request_timeout_->load(std::memory_order_relaxed)` to `*request_timeout_` (plain uint32_t* dereference, all occurrences)

**--- relay/core/authenticator.h ---**
Per D-08:
1. Delete `#include <mutex>` (line 6)
2. Delete `mutable std::mutex acl_mutex_;` (line 60)
3. Update doc comment on `reload_allowed_keys`: change "Thread-safe via mutex" to "Called from event loop thread (single-threaded model)."
4. Update doc comment on `verify`: remove "Blocking -- call from thread pool" -- replace with "CPU-heavy -- offloaded to thread pool in http_router.cpp."

**--- relay/core/authenticator.cpp ---**
Per D-08:
1. Delete ALL `std::lock_guard<std::mutex> lock(acl_mutex_);` calls (3 occurrences: in verify, reload_allowed_keys, has_acl)

**--- relay/core/metrics_collector.h ---**
Per D-10:
1. Change all 8 `std::atomic<uint64_t>` fields in `RelayMetrics` struct to plain `uint64_t`:
   - `uint64_t http_connections_total{0};`
   - `uint64_t http_disconnections_total{0};`
   - `uint64_t messages_received_total{0};`
   - `uint64_t messages_sent_total{0};`
   - `uint64_t auth_failures_total{0};`
   - `uint64_t rate_limited_total{0};`
   - `uint64_t errors_total{0};`
   - `uint64_t request_timeouts_total{0};`
2. Remove `#include <atomic>` from metrics_collector.h (check if any other atomic usage remains -- `const std::atomic<bool>& stopping_` does use atomic, but that's included from relay_main which keeps stopping as atomic. If `<atomic>` is needed for stopping_, keep it.)
3. Change `MetricsCollector(const std::atomic<bool>& stopping)` -- keep as-is (stopping remains atomic per relay_main.cpp decision)
4. Update `RelayMetrics` doc comment: remove "All fields are atomic for safe concurrent access from multiple sessions" -- replace with "All fields accessed from single event loop thread. No synchronization needed."

**--- relay/core/metrics_collector.cpp ---**
1. Change all `metrics_.*.load(std::memory_order_relaxed)` to direct reads (8 occurrences in format_prometheus)
2. Change all `metrics_.*.fetch_add(1, std::memory_order_relaxed)` to `++metrics_.*` if any exist in this file (counter increments may be in other files that include metrics_collector.h -- those files will naturally work with plain uint64_t since ++ works on both atomic and plain types)

**--- relay/core/request_router.h ---**
Comment update only:
1. Change "Access serialized via strand -- all callers must be on the strand." to "Access from single event loop thread -- no synchronization needed."

**--- relay/core/subscription_tracker.h ---**
Comment update only:
1. Change "Access serialized via strand -- all callers must be on the strand." to "Access from single event loop thread -- no synchronization needed."

**--- relay/core/write_tracker.h ---**
Comment update only:
1. Change "Access serialized via strand -- all callers must be on the strand." to "Access from single event loop thread -- no synchronization needed."

**After all changes, also search for any remaining `.fetch_add`, `.fetch_sub`, `.load(`, `.store(` patterns in relay production code (NOT tests) that reference RelayMetrics fields or the converted uint32_t settings, and convert those to plain operations.** Use grep to find them:
```bash
grep -rn 'fetch_add\|fetch_sub\|\.load(\|\.store(' relay/ --include='*.h' --include='*.cpp' | grep -v tests/
```
Convert any remaining hits that apply to converted types. Leave `stopping` atomic operations alone (stopping stays atomic).
  </action>
  <verify>
    <automated>! grep -rn 'std::mutex' relay/core/ relay/http/ --include='*.h' --include='*.cpp' | grep -v tests/ | grep -v 'test_' && ! grep -rn 'using Strand' relay/core/ --include='*.h' --include='*.cpp' && ! grep -rn 'strand_' relay/core/ --include='*.h' --include='*.cpp' | grep -v tests/ && ! grep -rn 'acl_mutex_' relay/ --include='*.h' --include='*.cpp' && echo "Core layer clean"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `std::mutex` in relay production code (relay/core/ and relay/http/, excluding tests/)
    - Zero occurrences of `using Strand` in relay/core/ headers
    - Zero occurrences of `strand_` member in relay/core/ production code
    - Zero occurrences of `acl_mutex_` anywhere in relay/
    - relay/core/uds_multiplexer.h constructor has NO `Strand&` parameter
    - relay/core/uds_multiplexer.cpp send() does NOT use `asio::post(strand_,`
    - relay/core/uds_multiplexer.cpp uses `asio::co_spawn(ioc_,` (NOT `strand_`)
    - relay/core/authenticator.h does NOT contain `#include <mutex>` or `std::mutex`
    - relay/core/authenticator.cpp does NOT contain `std::lock_guard`
    - relay/core/metrics_collector.h RelayMetrics uses `uint64_t` (NOT `std::atomic<uint64_t>`) for all 8 counters
    - relay/core/request_router.h contains "single event loop thread"
    - relay/core/subscription_tracker.h contains "single event loop thread"
    - relay/core/write_tracker.h contains "single event loop thread"
  </acceptance_criteria>
  <done>All core layer files stripped of strand/mutex/atomic synchronization. UDS send simplified. Comments updated to reflect single-threaded model.</done>
</task>

</tasks>

<verification>
After both tasks complete, run comprehensive grep audit on ALL relay production code:
```bash
# Must find zero matches (excluding tests):
grep -rn 'std::mutex\|asio::strand\|make_strand\|set_strand\|acl_mutex_\|tls_mutex_' \
  relay/ --include='*.h' --include='*.cpp' | grep -v tests/ | grep -v test_
```

Expected: zero lines of output.

```bash
# Confirm offload is wired:
grep -rn 'relay::util::offload' relay/ --include='*.cpp'
```
Expected: one match in relay/http/http_router.cpp.

```bash
# Confirm transfer-back is present:
grep -rn 'co_await asio::post(ioc' relay/http/http_router.cpp
```
Expected: one match (transfer-back after offload).
</verification>

<success_criteria>
1. Zero std::mutex in relay production code
2. Zero asio::strand type aliases or members in relay production code
3. Zero co_await asio::post(strand, ...) calls anywhere
4. ML-DSA-87 verify offloaded via relay::util::offload() with transfer-back in http_router.cpp
5. UdsMultiplexer::send() is direct (no strand posting)
6. All ResponsePromise creation uses ioc executor
7. All "serialized via strand" comments updated to "single event loop thread"
8. All atomic uint32_t/uint64_t fields converted to plain types (except stopping)
</success_criteria>

<output>
After completion, create `.planning/phases/111-single-threaded-rewrite/111-02-SUMMARY.md`
</output>
