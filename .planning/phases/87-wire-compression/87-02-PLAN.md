---
phase: 87-wire-compression
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - db/PROTOCOL.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "ROADMAP.md Phase 87 describes SDK envelope compression, not node-side wire compression"
    - "REQUIREMENTS.md COMP-01 through COMP-04 describe SDK-only behavior"
    - "PROTOCOL.md documents cipher suite 0x02 = ML-KEM-ChaCha-Brotli in the envelope encryption section"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Rewritten Phase 87 section with SDK-only scope"
      contains: "SDK Envelope Compression"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Rewritten COMP-01 through COMP-04 with SDK-only descriptions"
      contains: "SDK compresses plaintext"
    - path: "db/PROTOCOL.md"
      provides: "Suite 0x02 documentation in envelope encryption section"
      contains: "0x02"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 87 references COMP-01..04"
      pattern: "COMP-01.*COMP-02.*COMP-03.*COMP-04"
---

<objective>
Rewrite Phase 87 documentation to reflect the SDK-only envelope compression scope. Update ROADMAP.md (rename phase, rewrite goal/success criteria), REQUIREMENTS.md (rewrite COMP-01..04), and PROTOCOL.md (add suite=0x02 documentation).

Purpose: Align all documentation with the scope pivot from "Wire Compression" to "SDK Envelope Compression" (per D-12, D-13).
Output: Updated ROADMAP.md, REQUIREMENTS.md, PROTOCOL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-wire-compression/87-CONTEXT.md
@db/PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ROADMAP.md Phase 87 and REQUIREMENTS.md COMP-01..04</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/87-wire-compression/87-CONTEXT.md
  </read_first>
  <action>
**ROADMAP.md changes:**

Find the `### Phase 87: Wire Compression` section (around line 189) and replace it entirely with:

```markdown
### Phase 87: SDK Envelope Compression
**Goal**: SDK compresses plaintext with Brotli before envelope encryption (suite=0x02) and decompresses after decryption, with decompression bomb protection. No node-side changes.
**Depends on**: Phase 86
**Requirements**: COMP-01, COMP-02, COMP-03, COMP-04
**Success Criteria** (what must be TRUE):
  1. SDK `encrypt_envelope()` compresses plaintext with Brotli (quality 6) before AEAD encryption when plaintext >= 256 bytes; cipher suite byte 0x02 in the envelope header signals compression
  2. SDK skips compression for plaintext under 256 bytes or when compressed output >= original size (expansion fallback); these use suite=0x01
  3. SDK `decrypt_envelope()` enforces a 100 MiB decompressed output cap via streaming decompressor, rejecting decompression bombs before full decompression completes
  4. SDK handles both suite=0x01 (uncompressed) and suite=0x02 (Brotli) envelopes transparently; PROTOCOL.md documents the new suite
**Plans**: 2 plans
```

Also update the Plans list below to:
```markdown
Plans:
- [ ] 87-01-PLAN.md -- SDK envelope compression implementation + tests
- [ ] 87-02-PLAN.md -- Documentation: ROADMAP, REQUIREMENTS, PROTOCOL.md update
```

Also update the bullet point in the phase list (around line 189):
```markdown
- [ ] **Phase 87: SDK Envelope Compression** - Brotli compress-before-encrypt in SDK envelope layer with decompression bomb protection
```

Also update the progress table at the bottom to show `0/2` plans for Phase 87.

**REQUIREMENTS.md changes:**

Find the `### Compression` section and replace the four COMP requirements:

Replace:
```markdown
- [ ] **COMP-01**: Node compresses sync/data message payloads with Brotli before AEAD encryption (flag byte per message)
- [ ] **COMP-02**: Node skips compression for payloads under 256 bytes or already-encrypted envelope data
- [ ] **COMP-03**: Node enforces decompressed output cap at MAX_BLOB_DATA_SIZE to prevent decompression bombs
- [ ] **COMP-04**: SDK compresses outbound and decompresses inbound payloads matching node behavior
```

With:
```markdown
- [ ] **COMP-01**: SDK compresses plaintext in `encrypt_envelope()` with Brotli (quality 6) before AEAD encryption; cipher suite byte 0x02 signals compression
- [ ] **COMP-02**: SDK skips compression for plaintext under 256 bytes; falls back to suite=0x01 if compressed output >= original size
- [ ] **COMP-03**: SDK enforces 100 MiB decompressed output cap in `decrypt_envelope()` via streaming decompressor to prevent decompression bombs
- [ ] **COMP-04**: SDK `decrypt_envelope()` handles both suite=0x01 and suite=0x02 transparently; PROTOCOL.md documents suite=0x02
```

In the `## Out of Scope` section, add a new bullet:
```markdown
- Node-side wire compression (node-to-node traffic is encrypted envelope data, incompressible by design)
```
  </action>
  <verify>
    <automated>grep -c "SDK Envelope Compression" /home/mika/dev/chromatin-protocol/.planning/ROADMAP.md && grep -c "SDK compresses plaintext" /home/mika/dev/chromatin-protocol/.planning/REQUIREMENTS.md && grep -c "Node compresses sync/data" /home/mika/dev/chromatin-protocol/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md contains `### Phase 87: SDK Envelope Compression` (not `Wire Compression`)
    - ROADMAP.md Phase 87 Goal mentions "SDK compresses plaintext" not "Wire traffic is compressed"
    - ROADMAP.md Phase 87 Success Criteria mention "encrypt_envelope()" and "suite=0x02"
    - ROADMAP.md Phase 87 Plans line shows "2 plans"
    - ROADMAP.md bullet shows "SDK Envelope Compression" not "Wire Compression"
    - REQUIREMENTS.md COMP-01 starts with "SDK compresses plaintext" not "Node compresses"
    - REQUIREMENTS.md COMP-02 mentions "256 bytes" and "expansion fallback"
    - REQUIREMENTS.md COMP-03 mentions "100 MiB" and "streaming decompressor"
    - REQUIREMENTS.md COMP-04 mentions "suite=0x01 and suite=0x02"
    - REQUIREMENTS.md Out of Scope contains "Node-side wire compression"
    - `grep "Node compresses sync/data" .planning/REQUIREMENTS.md` returns 0 matches (old text gone)
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 87 fully rewritten for SDK-only scope. REQUIREMENTS.md COMP-01..04 rewritten. Old node-side wire compression language removed. Out of Scope updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROTOCOL.md with suite=0x02 documentation</name>
  <files>
    db/PROTOCOL.md
  </files>
  <read_first>
    db/PROTOCOL.md
    .planning/phases/87-wire-compression/87-CONTEXT.md
  </read_first>
  <action>
Read the full envelope encryption section of PROTOCOL.md (lines 949-1053). Make the following targeted updates:

**1. Update the cipher suite table in the Fixed Header section (around line 978).**

Change:
```
| 5 | 1 | suite | uint8 | Cipher suite (0x01 = ML-KEM-1024 + ChaCha20-Poly1305) |
```

To:
```
| 5 | 1 | suite | uint8 | Cipher suite: 0x01 = ML-KEM-1024 + ChaCha20-Poly1305, 0x02 = ML-KEM-1024 + ChaCha20-Poly1305 + Brotli |
```

**2. Add a new subsection after the "Decryption" subsection (after line 1053) titled "Compression (Suite 0x02)":**

```markdown
### Compression (Suite 0x02)

Suite `0x02` (ML-KEM-1024 + ChaCha20-Poly1305 + Brotli) adds Brotli compression before AEAD encryption. The compression is an SDK-only concern -- the node stores and replicates envelopes opaquely and is unaware of the suite byte.

**Encryption flow (suite 0x02):**

1. If plaintext size >= 256 bytes, compress with Brotli (quality 6)
2. If compressed output < original size, use the compressed data and write suite=0x02 into the header
3. If compressed output >= original size (expansion), fall back to suite=0x01 with the original plaintext
4. If plaintext < 256 bytes, always use suite=0x01 (skip compression)
5. The `compress` parameter defaults to true; callers can opt out with `compress=False`

The suite byte is written into the envelope header BEFORE AEAD associated data computation. This is critical: the full header (including suite byte) is used as AD for both DEK wrapping and data encryption.

**Decryption flow (suite 0x02):**

1. Parse the suite byte at offset 5
2. If suite=0x01: decrypt normally (no decompression)
3. If suite=0x02: decrypt AEAD, then decompress the plaintext with Brotli
4. Decompression uses a streaming `Decompressor` with output size cap at 100 MiB (`MAX_BLOB_DATA_SIZE`). If decompressed output exceeds this limit, decryption fails with `DecompressionError` before full allocation.

**Cipher suite registry:**

| Suite | Name | Description |
|-------|------|-------------|
| 0x01 | ML-KEM-ChaCha | ML-KEM-1024 encap + HKDF-SHA256 KEK + ChaCha20-Poly1305 (uncompressed) |
| 0x02 | ML-KEM-ChaCha-Brotli | Same as 0x01, but plaintext is Brotli-compressed before encryption |
| 0x03-0xFF | Reserved | Future cipher suites |

**Backward compatibility:** An SDK that only knows suite=0x01 will reject suite=0x02 envelopes with `MalformedEnvelopeError`. Newer SDKs handle both transparently. This is by design -- the suite byte enables forward-compatible format evolution.
```

**3. Update the Decryption section step 1 (around line 1045).**

Change:
```
1. Parse fixed header: validate magic `CENV` (0x43454E56), version 0x01, suite 0x01
```

To:
```
1. Parse fixed header: validate magic `CENV` (0x43454E56), version 0x01, suite 0x01 or 0x02
```
  </action>
  <verify>
    <automated>grep -c "0x02.*Brotli" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md && grep -c "ML-KEM-ChaCha-Brotli" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md && grep "suite 0x01 or 0x02" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - PROTOCOL.md contains `### Compression (Suite 0x02)` section heading
    - PROTOCOL.md contains `ML-KEM-ChaCha-Brotli` in the cipher suite registry table
    - PROTOCOL.md fixed header table mentions both 0x01 and 0x02 for suite byte
    - PROTOCOL.md decryption step 1 says "suite 0x01 or 0x02" (not just "suite 0x01")
    - PROTOCOL.md compression section mentions "100 MiB" decompression cap
    - PROTOCOL.md compression section mentions "quality 6"
    - PROTOCOL.md compression section mentions "256 bytes" threshold
    - PROTOCOL.md compression section mentions "DecompressionError"
    - PROTOCOL.md contains "Suite `0x02`" or "suite=0x02"
    - PROTOCOL.md cipher suite registry has 0x01, 0x02, and 0x03-0xFF reserved
  </acceptance_criteria>
  <done>
    PROTOCOL.md envelope encryption section updated with suite=0x02 documentation. Cipher suite table expanded. Decryption steps updated. New "Compression (Suite 0x02)" subsection documents encryption/decryption flow, threshold, expansion fallback, bomb protection, and cipher suite registry.
  </done>
</task>

</tasks>

<verification>
1. `grep "SDK Envelope Compression" .planning/ROADMAP.md` -- phase renamed
2. `grep "SDK compresses plaintext" .planning/REQUIREMENTS.md` -- requirements rewritten
3. `grep "ML-KEM-ChaCha-Brotli" db/PROTOCOL.md` -- suite=0x02 documented
4. `grep "Node compresses sync/data" .planning/REQUIREMENTS.md` -- should return nothing (old text removed)
5. `grep "Wire Compression" .planning/ROADMAP.md` -- should return nothing in Phase 87 title (old title removed)
</verification>

<success_criteria>
- ROADMAP.md Phase 87 fully describes SDK envelope compression scope
- REQUIREMENTS.md COMP-01..04 describe SDK-only behavior, no node references
- PROTOCOL.md has complete suite=0x02 documentation with cipher suite registry
- All old "Wire Compression" / "Node compresses" language is gone from Phase 87 context
</success_criteria>

<output>
After completion, create `.planning/phases/87-wire-compression/87-02-SUMMARY.md`
</output>
