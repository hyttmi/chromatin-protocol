---
phase: 100-cleanup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/
  - sdk/python/
  - db/tests/relay/
  - CMakeLists.txt
  - db/CMakeLists.txt
  - .gitignore
  - dist/install.sh
  - dist/config/relay.json
  - dist/systemd/chromatindb-relay.service
  - db/PROTOCOL.md
  - db/README.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "Old relay/ directory no longer exists in the repo"
    - "Old sdk/python/ directory no longer exists in the repo"
    - "No stale build references to old relay/SDK remain — cmake -S . -B build configures successfully with only the db/ target"
    - "No doc references to old SDK Python specifics remain"
    - "dist/ files reference the new relay binary without old CLI args"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Root CMake without old relay targets"
      contains: "add_subdirectory(db)"
    - path: "db/CMakeLists.txt"
      provides: "db build without relay test files or relay_lib linkage"
    - path: "dist/install.sh"
      provides: "Install script updated for new relay"
    - path: "dist/config/relay.json"
      provides: "Relay config template with max_send_queue field"
    - path: "dist/systemd/chromatindb-relay.service"
      provides: "Systemd unit for new relay"
  key_links:
    - from: "CMakeLists.txt"
      to: "db/"
      via: "add_subdirectory"
      pattern: "add_subdirectory\\(db\\)"
---

<objective>
Delete the old relay/ and sdk/python/ directories and scrub every stale reference from the build system, docs, dist files, and .gitignore. After this plan, the repo builds cleanly with only the db/ component and no trace of the old relay or Python SDK.

Purpose: Clean break from the old architecture. The old relay used per-client UDS connections and linked chromatindb_lib. The old SDK is being replaced by direct WebSocket+JSON clients. All traces must be removed before the new relay can be scaffolded in relay/.

Output: A repo that compiles and passes all db/ tests with no relay or SDK artifacts remaining. relay_identity.h/cpp saved to a temporary location for Plan 100-02 to copy into the new relay tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-cleanup-foundation/100-CONTEXT.md
@.planning/phases/100-cleanup-foundation/100-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save relay identity, delete old relay and SDK directories, remove old relay tests</name>
  <read_first>
    - relay/identity/relay_identity.h
    - relay/identity/relay_identity.cpp
    - db/tests/relay/test_relay_config.cpp
    - db/tests/relay/test_relay_identity.cpp
    - db/tests/relay/test_message_filter.cpp
    - db/tests/relay/test_relay_session.cpp
  </read_first>
  <files>
    relay/identity/relay_identity.h
    relay/identity/relay_identity.cpp
    relay/
    sdk/python/
    db/tests/relay/
  </files>
  <action>
    1. Copy relay/identity/relay_identity.h and relay/identity/relay_identity.cpp to a temporary holding location at /tmp/chromatindb-relay-identity/ so they are preserved for Plan 100-02 (per D-01: copy into new relay tree, do NOT share from db/).

    2. Delete the entire old relay/ directory:
       ```
       rm -rf relay/
       ```
       This removes 10 source files (~1,500 LOC): relay_main.cpp, config/relay_config.h, config/relay_config.cpp, identity/relay_identity.h, identity/relay_identity.cpp, core/message_filter.h, core/message_filter.cpp, core/relay_session.h, core/relay_session.cpp, CMakeLists.txt. (per D-01/CLEAN-01)

    3. Delete the entire old sdk/python/ directory:
       ```
       rm -rf sdk/python/
       ```
       If the sdk/ directory is now empty, also delete it:
       ```
       rmdir sdk/ 2>/dev/null || true
       ```
       This removes ~4,600 LOC + 656 tests. (per CLEAN-02)

    4. Delete the old relay test files in db/tests/relay/ (per D-02):
       ```
       rm -rf db/tests/relay/
       ```
       This removes 4 test files (836 LOC): test_relay_config.cpp, test_relay_identity.cpp, test_message_filter.cpp, test_relay_session.cpp.
  </action>
  <verify>
    <automated>test ! -d relay/ && test ! -d sdk/python/ && test ! -d db/tests/relay/ && test -f /tmp/chromatindb-relay-identity/relay_identity.h && test -f /tmp/chromatindb-relay-identity/relay_identity.cpp && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d relay/` succeeds (old relay directory gone)
    - `test ! -d sdk/python/` succeeds (old SDK directory gone)
    - `test ! -d db/tests/relay/` succeeds (old relay tests gone)
    - `test -f /tmp/chromatindb-relay-identity/relay_identity.h` succeeds (identity saved)
    - `test -f /tmp/chromatindb-relay-identity/relay_identity.cpp` succeeds (identity saved)
  </acceptance_criteria>
  <done>Old relay/, sdk/python/, and db/tests/relay/ directories fully deleted. relay_identity source saved to /tmp for Plan 100-02.</done>
</task>

<task type="auto">
  <name>Task 2: Clean stale references from build system, docs, dist, and .gitignore</name>
  <read_first>
    - CMakeLists.txt
    - db/CMakeLists.txt
    - .gitignore
    - dist/install.sh
    - dist/config/relay.json
    - dist/systemd/chromatindb-relay.service
    - db/PROTOCOL.md
    - db/README.md
  </read_first>
  <files>
    CMakeLists.txt
    db/CMakeLists.txt
    .gitignore
    dist/install.sh
    dist/config/relay.json
    dist/systemd/chromatindb-relay.service
    db/PROTOCOL.md
    db/README.md
  </files>
  <action>
    **CMakeLists.txt (root)** — per D-04:
    1. Remove the "Relay component" section (line 162-165): delete `add_subdirectory(relay)`.
    2. Remove the "Relay binary" section (lines 173-177): delete `add_executable(chromatindb_relay relay/relay_main.cpp)` and `target_link_libraries(chromatindb_relay PRIVATE chromatindb_relay_lib)`.
    3. Keep the comment structure clean — no orphaned section headers.
    4. Do NOT add the new relay yet — that happens in Plan 100-02.

    **db/CMakeLists.txt** — per D-04:
    1. Remove the 3 relay test file references (lines 247-249):
       - `tests/relay/test_relay_config.cpp`
       - `tests/relay/test_relay_identity.cpp`
       - `tests/relay/test_message_filter.cpp`
    2. Remove `chromatindb_relay_lib` from `target_link_libraries(chromatindb_tests ...)` (line 258). Keep `chromatindb_lib` and `Catch2::Catch2WithMain`.

    **.gitignore:**
    1. Remove `sdk/python/.venv` entry (line 8).
    2. Keep the `__pycache__/`, `*.egg-info/`, `*.pyc` entries — they are general Python ignores that may be useful.

    **dist/install.sh:**
    1. Update `usage()` to take only one argument: `Usage: install.sh [--uninstall] <chromatindb>` (remove `<chromatindb_relay>` — relay is not installable yet, will be re-added when ready).
    2. In `do_uninstall()`: keep `chromatindb-relay.service` stop/disable/rm lines (they clean up old installations).
    3. In `do_install()`: remove `RELAY_BIN="$2"` and the relay binary validation/install. Remove `chromatindb_relay` install line. Remove relay keygen block (lines 87-90). Change argument count check from `$# -ne 2` to `$# -ne 1` and `do_install "$1"`.
    4. Keep relay.json config install and relay systemd service install — they deploy config for the new relay.

    **dist/config/relay.json:**
    Replace contents with updated config template for the new relay:
    ```json
    {
      "bind_address": "0.0.0.0",
      "bind_port": 4201,
      "uds_path": "/run/chromatindb/node.sock",
      "identity_key_path": "/var/lib/chromatindb/relay.key",
      "max_send_queue": 256,
      "log_level": "info",
      "log_file": "/var/log/chromatindb/relay.log"
    }
    ```

    **dist/systemd/chromatindb-relay.service:**
    Update ExecStart to match the new relay binary CLI:
    ```
    ExecStart=/usr/local/bin/chromatindb_relay --config /etc/chromatindb/relay.json
    ```
    (Remove the `run` subcommand — the new relay will use `--config` directly, no subcommands in Phase 100.)

    **db/PROTOCOL.md:**
    1. Rename the "SDK Client Notes" section (around line 1020) to "Client Implementation Notes".
    2. Remove the Python-specific paragraph about `chromatindb.exceptions.ConnectionError` inheriting from `ProtocolError` (line 1042-1043) — Python SDK is deleted.
    3. Reword the Pong/request_id note (line 1030) to be generic: "The node sends Pong responses with request_id = 0, regardless of the request_id in the Ping. Client implementations should use a dedicated Ping send path that does not rely on request_id correlation."
    4. Reword FlatBuffer non-determinism note (line 1050) to: "ML-DSA-87 signatures are non-deterministic -- signing the same data twice produces different signatures. Since the serialized blob includes the signature, writing identical data produces a different blob_hash each time. Client implementations should verify individual fields (data, ttl, timestamp) rather than comparing serialized bytes."
    5. Keep the directory layer section (line 1172+) — it documents the blob format, not SDK code.

    **db/README.md:**
    1. Line 163: Change "allowed_client_keys" description from mentioning "through relay" to "via UDS" — clients connect through relay to the node's UDS.
    2. Line 188: Change "relay connects via this path" to "external services connect via this path".
    3. Line 264: Change "from relays/clients" to "via UDS" — keep meaning, remove relay-specific wording.
    4. Line 455: Change "SDKs use the supported types list" to "Clients use the supported types list".
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && cmake -S . -B /tmp/chromatindb-build-test 2>&1 | tail -5 && cmake --build /tmp/chromatindb-build-test --target chromatindb_tests 2>&1 | tail -3 && echo "BUILD OK" && rm -rf /tmp/chromatindb-build-test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'add_subdirectory(relay)' CMakeLists.txt` returns 0
    - `grep -c 'chromatindb_relay' CMakeLists.txt` returns 0
    - `grep -c 'test_relay_config' db/CMakeLists.txt` returns 0
    - `grep -c 'test_relay_identity' db/CMakeLists.txt` returns 0
    - `grep -c 'test_message_filter' db/CMakeLists.txt` returns 0
    - `grep -c 'chromatindb_relay_lib' db/CMakeLists.txt` returns 0
    - `grep -c 'sdk/python/.venv' .gitignore` returns 0
    - `grep -c 'max_send_queue' dist/config/relay.json` returns 1
    - `grep -c 'SDK Client Notes' db/PROTOCOL.md` returns 0
    - `grep -c 'Client Implementation Notes' db/PROTOCOL.md` returns 1
    - `grep -c 'SDKs use' db/README.md` returns 0
    - `cmake -S . -B /tmp/chromatindb-build-verify` succeeds (exit code 0)
  </acceptance_criteria>
  <done>All stale references to old relay and SDK removed from build system, docs, dist, and .gitignore. cmake configure succeeds with only db/ target. db/ tests compile without relay_lib dependency.</done>
</task>

</tasks>

<verification>
1. `test ! -d relay/ && test ! -d sdk/python/ && test ! -d db/tests/relay/` — all old directories gone
2. `cmake -S . -B build && cmake --build build --target chromatindb_tests` — builds cleanly without relay
3. `cd build && ctest --output-on-failure` — all 647 db/ tests pass
4. `grep -r 'chromatindb_relay_lib' CMakeLists.txt db/CMakeLists.txt` — returns empty
5. `grep -c 'SDK Client Notes' db/PROTOCOL.md` — returns 0
</verification>

<success_criteria>
- Old relay/ directory deleted (CLEAN-01)
- Old sdk/python/ directory deleted (CLEAN-02)
- All stale references removed from build system, docs, dist, and .gitignore (CLEAN-03)
- relay_identity.h/cpp preserved at /tmp/chromatindb-relay-identity/ for Plan 100-02
- cmake configure and build succeed with only db/ target
- All existing 647 db/ unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/100-cleanup-foundation/100-01-SUMMARY.md`
</output>
