---
phase: 19-documentation-release
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified: [db/version.h]
autonomous: true
requirements: [DOC-04]

must_haves:
  truths:
    - "version.h reports 0.4.0"
    - "All tests pass with the new version"
  artifacts:
    - path: "db/version.h"
      provides: "Version 0.4.0 constants"
      contains: "VERSION_MINOR \"4\""
  key_links:
    - from: "db/version.h"
      to: "db/main.cpp"
      via: "#include version.h for version subcommand output"
      pattern: "VERSION"
---

<objective>
Bump chromatindb version from 0.1.0 to 0.4.0 and verify all tests pass.

Purpose: Mark the v0.4.0 Production Readiness milestone as shipped with a version number that reflects all features delivered in Phases 16-19.

Output: db/version.h updated to 0.4.0, full test suite green
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@db/version.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.4.0 and verify tests</name>
  <files>db/version.h</files>
  <action>
First, run the full test suite to confirm the baseline is green BEFORE making any changes:

```bash
cd build && cmake .. && make -j$(nproc) && ctest --output-on-failure
```

If baseline passes, edit `db/version.h`:
- Change `VERSION_MINOR` from `"1"` to `"4"`
- Leave `VERSION_MAJOR` as `"0"` and `VERSION_PATCH` as `"0"`
- The `VERSION` constexpr string will automatically become `"0.4.0"` via macro concatenation

After the edit, rebuild and run the full test suite again:

```bash
cd build && cmake .. && make -j$(nproc) && ctest --output-on-failure
```

Both test runs must pass with 0 failures. The version bump is a single-character change (`"1"` to `"4"`) in one file.

If any test fails, do NOT commit the version bump. Investigate and fix the test failure first.
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol/build && cmake .. -DCMAKE_BUILD_TYPE=Release 2>&1 | tail -1 && make -j$(nproc) 2>&1 | tail -3 && ctest --output-on-failure 2>&1 | tail -5 && grep -q '"4"' /home/mika/dev/chromatin-protocol/db/version.h && echo "VERSION_CHECK=PASS" || echo "VERSION_CHECK=FAIL"</automated>
  </verify>
  <done>
    - db/version.h contains VERSION_MINOR "4" (producing version string "0.4.0")
    - Full test suite passes with 0 failures after the version bump
    - `chromatindb version` would output "0.4.0"
  </done>
</task>

</tasks>

<verification>
1. `grep VERSION_MINOR db/version.h` shows `"4"`
2. `ctest --output-on-failure` in build directory shows all tests passing
3. No other files modified besides db/version.h
</verification>

<success_criteria>
- db/version.h reports version 0.4.0
- All 255+ tests pass with the new version
- The version bump is the final act of the v0.4.0 milestone
</success_criteria>

<output>
After completion, create `.planning/phases/19-documentation-release/19-02-SUMMARY.md`
</output>
