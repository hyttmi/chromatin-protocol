---
phase: 999.5-binary-ws-frame-inconsistency-for-json-responses
verified: 2026-04-12T07:38:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
---

# Phase 999.5: Binary WS Frame Inconsistency Verification Report

**Phase Goal:** ReadResponse/BatchReadResponse sent as binary WS frames (opcode 0x02) but payload is JSON text. Either use text frames for JSON or send actual binary. Current approach forces clients to special-case two types for no benefit.
**Verified:** 2026-04-12T07:38:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All JSON responses (including ReadResponse and BatchReadResponse) are sent as text WebSocket frames | VERIFIED | `route_response()` calls `session->send_json(*json_opt)` unconditionally at line 564 of `uds_multiplexer.cpp`. No branching on type 32 or 54. |
| 2 | No binary WS frame send path exists in relay code | VERIFIED | `grep -rn "send_binary\|is_binary_response" relay/` returns zero matches across all production and test files. |
| 3 | write_frame() unconditionally uses OPCODE_TEXT for outbound frames | VERIFIED | `ws_session.cpp` line 260: `auto frame = encode_frame(OPCODE_TEXT, data);` — no opcode variable, no marker detection block. |
| 4 | Relay builds and all existing tests pass | VERIFIED | `chromatindb_relay_tests`: All tests passed (2596 assertions in 241 test cases). `relay_smoke_test` and `chromatindb_relay` binary build clean. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `relay/ws/ws_session.h` | WsSession public API without send_binary | VERIFIED | Public section has only `write_frame()` and `send_json()`. No `send_binary` declaration. `void send_json(const nlohmann::json& j)` present at line 75. |
| `relay/ws/ws_session.cpp` | Simplified write_frame without binary marker detection | VERIFIED | `write_frame()` at line 257-263: 6 lines total, `encode_frame(OPCODE_TEXT, data)` with no opcode branching. No `send_binary` implementation anywhere in file. |
| `relay/translate/translator.h` | Translator API without is_binary_response | VERIFIED | Header exports only `TranslateResult`, `json_to_binary()`, and `binary_to_json()`. `is_binary_response()` function gone entirely. |
| `relay/core/uds_multiplexer.cpp` | route_response using send_json unconditionally | VERIFIED | `route_response()` lines 505-565: single `session->send_json(*json_opt)` call, no if/else on `is_binary_response`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `relay/core/uds_multiplexer.cpp` | `relay/ws/ws_session.h` | `session->send_json()` call in `route_response` | WIRED | Confirmed at `uds_multiplexer.cpp` line 564: `session->send_json(*json_opt);` — no binary branch precedes it. |

### Data-Flow Trace (Level 4)

Not applicable — this phase is a code deletion/simplification. No new data rendering paths were introduced. All data flows through the existing `send_json()` path which was already verified wired.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 241 relay tests pass | `./build/relay/tests/chromatindb_relay_tests` | All tests passed (2596 assertions in 241 test cases) | PASS |
| Full relay binary builds | `cmake --build build --target relay relay_smoke_test` | Built successfully, no errors | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| WSTEXT-01 | 999.5-01-PLAN.md | send_binary() removed from WsSession — no binary WS frame send method exists | SATISFIED | `relay/ws/ws_session.h` has no `send_binary` declaration. `ws_session.cpp` has no `send_binary` implementation. |
| WSTEXT-02 | 999.5-01-PLAN.md | is_binary_response() removed from translator.h — no binary response type detection exists | SATISFIED | `relay/translate/translator.h` contains only `TranslateResult`, `json_to_binary`, `binary_to_json`. |
| WSTEXT-03 | 999.5-01-PLAN.md | route_response() in uds_multiplexer.cpp calls send_json() unconditionally for all response types | SATISFIED | `uds_multiplexer.cpp` line 564: unconditional `session->send_json(*json_opt)`. |
| WSTEXT-04 | 999.5-01-PLAN.md | write_frame() in ws_session.cpp uses OPCODE_TEXT directly with no binary marker prefix detection | SATISFIED | `ws_session.cpp` line 260: `encode_frame(OPCODE_TEXT, data)` — no marker detection, no `opcode` variable. |

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments in modified files. No stub implementations.

**Additional check — OPCODE_BINARY preserved correctly:** `OPCODE_BINARY` constant is defined only in `relay/ws/ws_frame.h` (line 16) and used in `relay/tests/test_ws_frame.cpp` for receive-side frame encoding tests. It is absent from `ws_session.cpp` — the send path is clean. This matches the plan's discretion note: keep constant for receive-side validation.

### Human Verification Required

None. All aspects of this change are mechanically verifiable:
- Symbol removal verified by grep
- Unconditional send_json verified by code inspection
- Build and test pass verified by execution

### Gaps Summary

No gaps. All four must-have truths are fully verified. The binary WS frame send path has been completely removed from the relay codebase. All JSON responses including ReadResponse (type 32) and BatchReadResponse (type 54) now use text frames via `send_json()`. The 241 relay tests pass, confirming no regressions.

---

_Verified: 2026-04-12T07:38:00Z_
_Verifier: Claude (gsd-verifier)_
