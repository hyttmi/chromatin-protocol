---
phase: 112-asan-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/lsan_suppressions.txt
  - tools/relay_asan_test.sh
autonomous: true
requirements: [VER-02, VER-03]

must_haves:
  truths:
    - "LSAN suppression file exists with patterns for liboqs, OpenSSL, and Asio shutdown leaks"
    - "ASAN test script builds both chromatindb and chromatindb_relay with -DSANITIZER=asan"
    - "ASAN test script starts a node and relay, runs benchmark at 1/10/100 concurrency, sends SIGHUP and SIGTERM"
    - "ASAN test script parses stderr for ASAN error markers and exits with clear pass/fail code"
  artifacts:
    - path: "relay/lsan_suppressions.txt"
      provides: "LSAN suppression patterns for accepted shutdown leaks"
      contains: "leak:OQS_SIG_new"
    - path: "tools/relay_asan_test.sh"
      provides: "Complete ASAN test harness script"
      min_lines: 100
  key_links:
    - from: "tools/relay_asan_test.sh"
      to: "relay/lsan_suppressions.txt"
      via: "LSAN_OPTIONS=suppressions=relay/lsan_suppressions.txt"
      pattern: "LSAN_OPTIONS.*suppressions.*relay/lsan_suppressions"
    - from: "tools/relay_asan_test.sh"
      to: "tools/relay_benchmark.py"
      via: "python3 tools/relay_benchmark.py invocation"
      pattern: "relay_benchmark\\.py"
---

<objective>
Create the LSAN suppression file and ASAN test harness script that will exercise the relay under AddressSanitizer at 1, 10, and 100 concurrent HTTP clients with signal handling verification.

Purpose: Provide the tooling infrastructure for proving VER-02 (ASAN-clean under concurrent load) and VER-03 (SIGHUP/SIGTERM correctness). The test harness must be repeatable and CI-friendly with clear pass/fail exit codes.

Output: `relay/lsan_suppressions.txt` (LSAN suppression file per D-06/D-08) and `tools/relay_asan_test.sh` (automated ASAN test harness per D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-asan-verification/112-CONTEXT.md
@.planning/phases/112-asan-verification/112-RESEARCH.md

<interfaces>
<!-- Existing suppression file pattern (from sanitizers/lsan.supp): -->
<!-- Each line is either a comment (# ...) or a suppression (leak:function_name) -->
<!-- Relay LSAN file carries forward liboqs + Asio patterns and adds OpenSSL -->

From relay/config/relay_config.h:
```cpp
struct RelayConfig {
    std::string bind_address = "0.0.0.0";
    uint32_t bind_port = 4201;
    std::string uds_path;
    std::string identity_key_path;
    std::string log_level = "info";
    std::string log_file;
    uint32_t max_send_queue = 256;
    std::string cert_path;
    std::string key_path;
    uint32_t max_connections = 1024;
    std::vector<std::string> allowed_client_keys;
    std::string metrics_bind;
    uint32_t rate_limit_messages_per_sec = 0;
    uint32_t request_timeout_seconds = 10;
    uint32_t max_blob_size_bytes = 0;
    bool tls_enabled() const;
};
```

Node config (from db/config/config.h):
- `uds_path`: Unix domain socket path (empty = disabled, must be absolute)
- `bind_address`: "host:port" for TCP
- `log_level`: trace|debug|info|warn|error

CMake SANITIZER flag (from CMakeLists.txt line 31):
```cmake
if(SANITIZER STREQUAL "asan")
    add_compile_options(-fsanitize=address -fno-omit-frame-pointer -fno-optimize-sibling-calls)
    add_link_options(-fsanitize=address)
endif()
```

Benchmark CLI (from tools/relay_benchmark.py):
```
--relay-url URL          (default: http://localhost:4201)
--concurrency-levels STR (default: "1,10,100")
--iterations INT         (default: 100)
--warmup INT             (default: 5)
--blob-sizes STR         (default: large; "" to skip)
--output PATH            (default: tools/benchmark_report.md)
--mixed-duration INT     (default: 30)
```

SIGHUP log markers from relay_main.cpp:
- "received SIGHUP -- reloading configuration"
- "rate_limit reloaded: {value}" (or "rate_limit reloaded: disabled")
- "SIGHUP config reload failed: {error}"

SIGTERM log markers from relay_main.cpp:
- "received signal {N}, graceful shutdown ({N} sessions)"
- "relay stopped"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LSAN suppression file and ASAN test harness script</name>
  <files>relay/lsan_suppressions.txt, tools/relay_asan_test.sh</files>
  <read_first>
    - sanitizers/lsan.supp (existing suppression patterns to carry forward)
    - CMakeLists.txt lines 25-50 (SANITIZER=asan build flags)
    - relay/relay_main.cpp lines 435-542 (SIGTERM + SIGHUP handlers + ioc.run)
    - tools/relay_benchmark.py lines 998-1067 (CLI arguments)
    - relay/config/relay_config.h (config fields for test JSON)
    - db/config/config.h lines 40-55 (node config fields for uds_path)
    - tests/integration/configs/node1.json (example node config)
  </read_first>
  <action>
Create two files:

**1. `relay/lsan_suppressions.txt`** (per D-06, D-08):

Relay-specific LeakSanitizer suppression file for accepted shutdown leaks. Carry forward patterns from `sanitizers/lsan.supp` and add relay-specific ones:

```
# relay/lsan_suppressions.txt
# Relay-specific LeakSanitizer suppressions for accepted shutdown leaks.
# Usage: LSAN_OPTIONS=suppressions=relay/lsan_suppressions.txt
#
# Policy (D-06/D-07): Only suppress third-party library cleanup.
# Any new leaks from relay/ code are bugs and must be fixed.

# liboqs: ML-DSA-87 signature context allocated once, not freed at exit.
leak:OQS_SIG_new
leak:OQS_MEM_malloc

# OpenSSL: global SSL library state and TLS contexts.
leak:OPENSSL_init_ssl
leak:SSL_CTX_new
leak:OPENSSL_init_crypto

# Asio: coroutine frame allocations from in-flight coroutines at shutdown.
# SIGTERM triggers ioc.stop() after 2s drain, some coroutines may be suspended.
leak:awaitable_handler
leak:awaitable_frame

# Asio: resolver results for UDS reconnect coroutines.
leak:basic_resolver_results
leak:resolve_query_op
```

**2. `tools/relay_asan_test.sh`** (per D-01, D-02, D-03, D-04, D-05):

Bash test harness script that:

a) **Setup** (~lines 1-40):
   - `#!/usr/bin/env bash` with `set -euo pipefail`
   - Define `REPO_ROOT` as the script's grandparent dir (same as integration tests pattern)
   - Define `BUILD_DIR="$REPO_ROOT/build-asan"` 
   - Create temp dir `WORK_DIR=$(mktemp -d /tmp/relay_asan_test.XXXXXX)` for configs, logs, data
   - Define cleanup trap: `trap cleanup EXIT` that kills relay/node PIDs, removes temp dir
   - Define log() and fail() and pass() helper functions (match tests/integration/helpers.sh style)

b) **Build** (~lines 41-65):
   - Run `cmake -B "$BUILD_DIR" -S "$REPO_ROOT" -DSANITIZER=asan -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=OFF`
   - Run `cmake --build "$BUILD_DIR" --target chromatindb chromatindb_relay -j$(nproc)`
   - Per D-02: both binaries ASAN-instrumented (Pitfall 5 from RESEARCH.md)
   - Fail fast if build fails

c) **Generate test configs** (~lines 66-110):
   - Node config JSON at `$WORK_DIR/node.json`:
     ```json
     {
       "bind_address": "127.0.0.1:0",
       "uds_path": "$WORK_DIR/chromatindb.sock",
       "log_level": "info"
     }
     ```
     (port 0 = ephemeral, matching project convention; uds_path is absolute)
   - Relay config JSON at `$WORK_DIR/relay.json`:
     ```json
     {
       "bind_address": "127.0.0.1",
       "bind_port": 4201,
       "uds_path": "$WORK_DIR/chromatindb.sock",
       "identity_key_path": "$WORK_DIR/relay.key",
       "log_level": "info",
       "rate_limit_messages_per_sec": 0,
       "request_timeout_seconds": 0,
       "max_blob_size_bytes": 0,
       "max_connections": 1024
     }
     ```
     (rate_limit and request_timeout disabled per RESEARCH.md Pitfall 1)
   - Generate relay identity key: run `$BUILD_DIR/chromatindb_relay --config "$WORK_DIR/relay.json"` briefly or use `openssl` to create a placeholder if the relay auto-generates. Actually, the relay generates identity on first run from `identity_key_path`. Use existing pattern from relay_identity.cpp — just let the relay create it.

d) **Start node** (~lines 111-140):
   - Set `ASAN_OPTIONS="detect_leaks=1:halt_on_error=0:print_stats=0:log_path=/dev/stderr"`
   - Set `LSAN_OPTIONS="suppressions=$REPO_ROOT/sanitizers/lsan.supp"` (node uses existing file)
   - Start: `$BUILD_DIR/chromatindb --config "$WORK_DIR/node.json" --data-dir "$WORK_DIR/node_data" 2>"$WORK_DIR/node_stderr.log" &`
   - Save `NODE_PID=$!`
   - Wait for UDS socket to appear: loop checking `[ -S "$WORK_DIR/chromatindb.sock" ]` with 30s timeout
   - Fail fast if node doesn't start

e) **Start relay** (~lines 141-170):
   - Set `ASAN_OPTIONS="detect_leaks=1:halt_on_error=0:print_stats=0:log_path=/dev/stderr"`
   - Set `LSAN_OPTIONS="suppressions=$REPO_ROOT/relay/lsan_suppressions.txt"` (relay uses new file per D-08)
   - Start: `$BUILD_DIR/chromatindb_relay --config "$WORK_DIR/relay.json" 2>"$WORK_DIR/relay_stderr.log" &`
   - Save `RELAY_PID=$!`
   - Wait for relay to accept connections: loop with `curl -sf http://127.0.0.1:4201/health` or a simple TCP check `bash -c "echo > /dev/tcp/127.0.0.1/4201" 2>/dev/null` with 15s timeout
   - Fail fast if relay doesn't start

f) **Run benchmark at concurrency 1, 10, 100** (~lines 171-200, per D-01, D-02):
   - Command: `python3 "$REPO_ROOT/tools/relay_benchmark.py" --relay-url http://127.0.0.1:4201 --concurrency-levels "1,10,100" --iterations 20 --warmup 3 --blob-sizes "" --output /dev/null --mixed-duration 10`
   - Skip large blobs (too slow under ASAN per RESEARCH.md anti-pattern)
   - Reduced iterations (20 not 100) per RESEARCH.md Pitfall 1
   - Capture exit code; if non-zero, log warning but continue (benchmark errors may be ASAN slowdown, not relay bugs)

g) **SIGHUP signal test** (~lines 201-240, per D-03, D-04):
   - Write modified config with rate_limit_messages_per_sec=999 using atomic mv pattern:
     ```bash
     python3 -c "
     import json, sys
     with open(sys.argv[1]) as f: cfg = json.load(f)
     cfg['rate_limit_messages_per_sec'] = 999
     with open(sys.argv[2], 'w') as f: json.dump(cfg, f, indent=2)
     " "$WORK_DIR/relay.json" "$WORK_DIR/relay.json.tmp"
     mv "$WORK_DIR/relay.json.tmp" "$WORK_DIR/relay.json"
     ```
   - Send `kill -HUP $RELAY_PID`
   - Sleep 2 seconds for reload
   - Check relay stderr for "rate_limit reloaded: 999" — if found, PASS; if not found, FAIL
   - Check relay stderr for "SIGHUP config reload failed" — if found, FAIL
   - Check relay is still running (`kill -0 $RELAY_PID`) — if not, FAIL (crash)

h) **SIGTERM graceful shutdown test** (~lines 241-280, per D-03, D-05):
   - Send `kill -TERM $RELAY_PID`
   - Wait up to 10 seconds for relay to exit: `wait $RELAY_PID` with timeout loop
   - If still alive after 10s, FAIL and `kill -9`
   - If exited, PASS
   - Check relay stderr for "relay stopped" — confirms clean shutdown path

i) **Parse ASAN output** (~lines 281-330):
   - Initialize `ASAN_ERRORS=0`
   - Check relay_stderr.log for `ERROR: AddressSanitizer` — if found, `ASAN_ERRORS=1`, print the error block (20 lines of context)
   - Check node_stderr.log for `ERROR: AddressSanitizer` — if found, `ASAN_ERRORS=1`, print the error block
   - Check relay_stderr.log for `ERROR: LeakSanitizer` — if found, check SUMMARY line for non-zero leaks after suppression. If un-suppressed leaks exist, `ASAN_ERRORS=1` per D-07
   - Check node_stderr.log for `ERROR: LeakSanitizer` similarly

j) **Summary and exit** (~lines 331-360):
   - Print summary table: each test phase (build, benchmark, SIGHUP, SIGTERM, ASAN-clean) with PASS/FAIL
   - If any FAIL, exit 1
   - If all PASS, print "ALL TESTS PASSED" and exit 0

Also kill the node in the cleanup trap if still alive. The cleanup trap must handle the case where PIDs are unset (early failure).

Make the script executable with `chmod +x tools/relay_asan_test.sh`.
  </action>
  <verify>
    <automated>test -f relay/lsan_suppressions.txt && grep -q 'leak:OQS_SIG_new' relay/lsan_suppressions.txt && test -x tools/relay_asan_test.sh && grep -q 'SANITIZER=asan' tools/relay_asan_test.sh && grep -q 'relay_benchmark.py' tools/relay_asan_test.sh && grep -q 'kill -HUP' tools/relay_asan_test.sh && grep -q 'kill -TERM' tools/relay_asan_test.sh && grep -q 'LSAN_OPTIONS' tools/relay_asan_test.sh && echo "PASS: both files exist with required content" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - relay/lsan_suppressions.txt contains "leak:OQS_SIG_new" (liboqs suppression)
    - relay/lsan_suppressions.txt contains "leak:OQS_MEM_malloc" (liboqs suppression)
    - relay/lsan_suppressions.txt contains "leak:OPENSSL_init_ssl" (OpenSSL suppression)
    - relay/lsan_suppressions.txt contains "leak:SSL_CTX_new" (OpenSSL suppression)
    - relay/lsan_suppressions.txt contains "leak:awaitable_handler" (Asio suppression)
    - relay/lsan_suppressions.txt contains "leak:awaitable_frame" (Asio suppression)
    - tools/relay_asan_test.sh is executable (chmod +x)
    - tools/relay_asan_test.sh contains "-DSANITIZER=asan" (ASAN build)
    - tools/relay_asan_test.sh contains "--target chromatindb chromatindb_relay" (builds both per Pitfall 5)
    - tools/relay_asan_test.sh contains "relay_benchmark.py" (invokes load driver per D-01)
    - tools/relay_asan_test.sh contains "--concurrency-levels" with "1,10,100" (per D-02)
    - tools/relay_asan_test.sh contains "--blob-sizes" with empty string (skip large blobs under ASAN)
    - tools/relay_asan_test.sh contains "kill -HUP" (SIGHUP test per D-03/D-04)
    - tools/relay_asan_test.sh contains "rate_limit reloaded: 999" (SIGHUP verification)
    - tools/relay_asan_test.sh contains "kill -TERM" (SIGTERM test per D-03/D-05)
    - tools/relay_asan_test.sh contains "LSAN_OPTIONS=suppressions=" (leak suppression per D-08)
    - tools/relay_asan_test.sh contains "ERROR: AddressSanitizer" (ASAN output parsing)
    - tools/relay_asan_test.sh contains "ERROR: LeakSanitizer" (LSAN output parsing)
    - tools/relay_asan_test.sh exits 0 only if all checks pass, exits 1 on any failure
    - tools/relay_asan_test.sh has a cleanup trap that kills node/relay PIDs and removes temp dir
  </acceptance_criteria>
  <done>Both relay/lsan_suppressions.txt and tools/relay_asan_test.sh exist. The suppression file has patterns for liboqs, OpenSSL, and Asio. The test script builds ASAN binaries, starts node+relay, runs benchmark at 1/10/100 concurrency, sends SIGHUP (verifies rate limit change), sends SIGTERM (verifies clean exit), parses ASAN stderr for errors, and exits with clear pass/fail.</done>
</task>

<task type="auto">
  <name>Task 2: Run ASAN test harness and verify clean output</name>
  <files>tools/relay_asan_test.sh, relay/lsan_suppressions.txt</files>
  <read_first>
    - tools/relay_asan_test.sh (the script just created)
    - relay/lsan_suppressions.txt (the suppression file just created)
  </read_first>
  <action>
Execute the ASAN test harness script created in Task 1:

```bash
cd /home/mika/dev/chromatin-protocol
bash tools/relay_asan_test.sh
```

**If the script exits 0**: Phase 112 requirements are met. Record the output.

**If the script exits non-zero due to script bugs** (syntax errors, wrong paths, timing issues):
- Fix the script (`tools/relay_asan_test.sh`) or suppression file (`relay/lsan_suppressions.txt`) as needed.
- Common issues to watch for:
  - Node or relay startup timeout: increase wait time or check config paths
  - Benchmark connection errors: increase iterations or timeout in the script
  - SIGHUP log check: relay may log slightly differently, grep pattern may need adjustment
  - Config file race: ensure the atomic mv pattern works correctly
- Re-run until the script itself works correctly.

**If the script exits non-zero due to ASAN errors** (real memory safety bugs per D-09):
- Read the ASAN error reports from the temp dir stderr logs
- Fix the bugs in relay/ or db/ code as needed per D-09
- For simple bugs (use-after-free, buffer overrun): fix inline
- For complex architectural bugs per D-10: stop and document for discussion
- Re-run the script after fixes to confirm clean

**If the script exits non-zero due to un-suppressed LSAN leaks** (per D-07):
- Determine if the leak is from third-party code (add to suppression file) or relay code (fix the bug)
- Re-run after fix

The goal is the script exits 0 with all checks PASS. The script output serves as the verification evidence for VER-02 and VER-03.
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && bash tools/relay_asan_test.sh && echo "PASS: ASAN test harness clean" || echo "FAIL: ASAN test harness found issues"</automated>
  </verify>
  <acceptance_criteria>
    - tools/relay_asan_test.sh exits with code 0
    - Script output contains "PASS" for benchmark test (concurrency 1, 10, 100)
    - Script output contains "PASS" for SIGHUP test (rate limit change observed)
    - Script output contains "PASS" for SIGTERM test (clean exit)
    - Script output contains "PASS" for ASAN-clean check (no ERROR: AddressSanitizer in stderr)
    - No modifications to relay/ production code needed (ideal) OR all fixes are committed with explanation
  </acceptance_criteria>
  <done>The ASAN test harness runs end-to-end and exits 0. The relay is proven ASAN-clean at 1, 10, and 100 concurrent HTTP clients (VER-02). SIGHUP config reload and SIGTERM graceful shutdown work correctly under the single-threaded model (VER-03). Any bugs found are fixed and committed.</done>
</task>

</tasks>

<verification>
Phase 112 verification criteria:

1. **VER-02**: `tools/relay_asan_test.sh` exits 0 with benchmark completing at concurrency 1, 10, and 100 with zero `ERROR: AddressSanitizer` reports in relay stderr
2. **VER-03**: `tools/relay_asan_test.sh` exits 0 with SIGHUP rate limit change confirmed ("rate_limit reloaded: 999" in relay log) and SIGTERM clean exit confirmed (relay exits within 10s, "relay stopped" in log)
3. **LSAN**: Only suppressed (third-party) leaks in exit report, no un-suppressed leaks from relay code
</verification>

<success_criteria>
- `bash tools/relay_asan_test.sh` exits 0
- relay/lsan_suppressions.txt exists with liboqs, OpenSSL, Asio suppression patterns
- Relay runs ASAN-clean at 1, 10, 100 concurrent HTTP clients with full auth + data flow
- SIGHUP mid-load causes rate limit reload without crash
- SIGTERM causes clean drain and exit within 10 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/112-asan-verification/112-01-SUMMARY.md`
</output>
