---
phase: 124
plan: 05
type: execute
wave: 5
depends_on: [124-01, 124-02, 124-03, 124-04]
files_modified:
  - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md
autonomous: false
requirements:
  - SC-124-1
  - SC-124-4
  - SC-124-5
  - SC-124-7
user_setup:
  - service: chromatindb-node
    why: "D-07 + D-08 E2E matrix requires both `local` (127.0.0.1) and `home` (192.168.1.73) nodes running the post-122+123 binary with wiped data dirs. User is responsible for build + deploy per memory feedback_delegate_tests_to_user.md."
    env_vars: []
    dashboard_config:
      - task: "Build cli/db binaries from Phase 124 tip"
        location: "user's local workstation: `cmake --build build -j$(nproc) --target cdb chromatindb`"
      - task: "Wipe local node data dir + restart"
        location: "user's local workstation: stop existing chromatindb, remove ~/.chromatindb/* or configured data dir, restart chromatindb"
      - task: "Deploy post-122+123+124 node binary to 192.168.1.73"
        location: "user SSHes to home node, replaces binary + wipes data dir, restarts"
      - task: "Verify ~/.cdb/config.json has `nodes.home = 192.168.1.73` and `nodes.local = 127.0.0.1`"
        location: "user's workstation"

must_haves:
  truths:
    - "User has built and deployed the post-124 `cdb` CLI binary + `chromatindb` node binary to BOTH the local workstation AND the home node at 192.168.1.73, with each node's data dir freshly wiped (per Phase 122 D-11 no-backward-compat invariant)."
    - "Claude executes every item of the D-08 E2E matrix (items 1–7) directly from Claude's shell using `cdb --node local <cmd>` and `cdb --node home <cmd>` — no SSH required except for node-restart diagnostics, which the user owns."
    - "`124-E2E.md` artifact exists and documents each of the 7 matrix items with: the exact command(s) run, observed stdout/stderr, a pass/fail verdict, and any blocker observed."
    - "All 7 D-08 items pass on BOTH nodes. Any failure blocks phase completion (per D-08a)."
    - "Error-string wording tests (SC-124-7 items 3 and 4 in VALIDATION.md): triggering `ERROR_PUBK_FIRST_VIOLATION` (0x07) via a delegate-first-write and `ERROR_PUBK_MISMATCH` (0x08) via a second identity writing PUBK to the same namespace both surface the exact D-05 strings."
  artifacts:
    - path: .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md
      provides: "D-08 matrix execution log with pass/fail verdicts per item, both nodes"
      contains: "## Item 1"
  key_links:
    - from: ".planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md"
      to: "D-08 matrix (CONTEXT items 1-7)"
      via: "One H2 section per item with the exact commands and observations"
      pattern: "Item [1-7]"
    - from: "Claude's shell"
      to: "chromatindb nodes at 127.0.0.1 and 192.168.1.73"
      via: "`cdb --node local` / `cdb --node home` invocations"
      pattern: "cdb --node (local|home)"
---

<objective>
Execute and document the D-08 E2E verification matrix (7 items) against both local and home (`192.168.1.73`) nodes running the post-122+123+124 binaries. This plan is the phase-completion gate (D-08a): any failing item blocks phase-done. All items are live-infra checks; all of Claude's work here is orchestration, invocation, and artifact capture — no source code changes.

This plan has ONE human checkpoint (the user's redeploy) followed by a sequence of autonomous Claude-run verification tasks. Claude does NOT wait on the user for the E2E execution itself (per `feedback_self_verify_checkpoints.md`) — the user is responsible for the build/deploy (per `feedback_delegate_tests_to_user.md`), Claude is responsible for running the matrix.

Purpose: Prove SC-124-1 (auto-PUBK fires), SC-124-4 (delegate writes work), SC-124-5 (put --name / get <name> / batched rm + D-06 cascade all work E2E), and SC-124-7 (full live matrix green). Without this plan passing, phase 124 is not done.

Output: `124-E2E.md` with all 7 items PASSED against both nodes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-CONTEXT.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-RESEARCH.md
@.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-04-SUMMARY.md
@~/.cdb/config.json

# Reference
@~/.claude/projects/-home-mika-dev-chromatin-protocol/memory/reference_live_node.md
@~/.claude/projects/-home-mika-dev-chromatin-protocol/memory/feedback_self_verify_checkpoints.md
@~/.claude/projects/-home-mika-dev-chromatin-protocol/memory/feedback_delegate_tests_to_user.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User redeploys post-124 cdb + chromatindb binaries to both local and home nodes with wiped data dirs</name>
  <files>(none — human-action checkpoint; the user builds + deploys binaries outside the repo's source tree)</files>
  <what-built>
Phase 124 plans 01–04 have delivered: wire schema migration, pubk_presence module, 12 blob-construction migrations, auto-PUBK wiring, D-05 error decoder, and D-06 BOMB cascade. All unit tests are green. The CLI binary emits the post-122+123 wire format exclusively.

Before Claude can run the D-08 live-infra matrix, the user must: (a) build both binaries from the Phase 124 tip, (b) wipe the local node's data dir and restart the local node on the post-124 binary, (c) SSH or otherwise deploy the post-124 `chromatindb` node binary to 192.168.1.73 and wipe its data dir + restart.

Per `feedback_delegate_tests_to_user.md`, orchestrator-level build/test/deploy runs belong to the user. Claude does not build or deploy.
  </what-built>
  <action>
**USER ACTIONS (outside Claude's scope — this task is a human-action checkpoint):**

1. From Phase 124 tip (commit HEAD after plan 04 summary), build both binaries:
   ```bash
   cmake --build build -j$(nproc) --target cdb chromatindb
   ```

2. **Local node:**
   - Stop existing chromatindb (e.g., `systemctl --user stop chromatindb` or kill the process).
   - Wipe its data dir: `rm -rf ~/.chromatindb/*` (or wherever config.json points; back up first if unsure).
   - Restart chromatindb using the newly built binary.
   - Sanity: `cdb --node local info` should succeed (shows node identity).

3. **Home node (192.168.1.73):**
   - Copy `build/db/chromatindb` to the home node via `scp` (user's preferred method).
   - SSH to home node, stop the running daemon, wipe the data dir, install the new binary, restart.
   - Sanity from workstation: `cdb --node home info` should succeed.

4. Confirm `~/.cdb/config.json` on the workstation has `"home": "192.168.1.73"` and `"local": "127.0.0.1"` under `nodes`.

After the user completes those steps, they type `approved` (or similar) to signal Claude to proceed with the matrix execution in tasks 2–7.

**CLAUDE ACTIONS in this task: none.** Wait for the resume signal. Do not run any `cdb` commands until the user signals.
  </action>
  <how-to-verify>
    After the user signals `approved`, Claude's first sanity check (which kicks off task 2) is to run `cdb --node local info` and `cdb --node home info`. Both MUST return without error and show each node's identity (namespace hash). If either fails, Claude re-raises the checkpoint with the error output instead of proceeding.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human-action checkpoint; verification happens in task 2's first sanity step via `cdb --node local info && cdb --node home info`.</automated>
  </verify>
  <done>
User signals `approved`. Claude has not run any `cdb` commands in this task — execution starts at task 2.
  </done>
  <resume-signal>
Type "approved" once both nodes are running the post-124 binary with wiped data dirs, OR describe any deployment blocker.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create 124-E2E.md skeleton + run Item 1 (SC#7 literal flow on both nodes)</name>
  <files>.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</files>
  <read_first>
    - .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-CONTEXT.md §D-08 (lines ~94-104) — the 7 matrix items verbatim
    - ~/.cdb/config.json (verify `nodes.local` = 127.0.0.1 and `nodes.home` = 192.168.1.73)
  </read_first>
  <action>
**Sanity gate first** (the verification deferred from task 1): run `cdb --node local info` and `cdb --node home info`. If EITHER fails, stop and re-raise the task-1 checkpoint with the error output. Only proceed when both succeed.

Then create `.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md` with this skeleton:

```markdown
# Phase 124 — D-08 E2E Matrix Execution Log

**Executed:** <ISO-8601 timestamp>
**Node versions:** post-122 + post-123 + post-124 (user-deployed)
**Data dirs:** freshly wiped on both nodes

**Nodes:**
- `local`: 127.0.0.1 (workstation)
- `home`: 192.168.1.73

## Summary

| Item | Description | local | home | Notes |
|------|-------------|-------|------|-------|
| 1 | SC#7 literal flow | pending | pending | |
| 2 | Cross-node sync | n/a | pending | single run; not per-node |
| 3 | BOMB propagation | n/a | pending | |
| 4 | Chunked >500 MiB | pending | pending | |
| 5 | Delegate --share @contact | n/a | pending | |
| 6 | --replace BOMB-of-1 | pending | pending | |
| 7 | D-06 cascade | pending | pending | |

(Update verdicts as items execute.)

---

## Item 1: SC#7 literal flow, both nodes

**Definition (CONTEXT D-08 #1):** `cdb publish` → `cdb put file` → `cdb get <hash>` → `cdb put --name foo file` → `cdb get foo` → `cdb rm <hash1> <hash2>` (batched) → `cdb ls`.

<!-- Claude fills this section after running. Capture both nodes. -->
```

**Then run Item 1 twice — once with `--node local`, once with `--node home`.** Use a temp file for the test data (e.g., `/tmp/124-test-$(date +%s).bin` containing 1 KiB of random bytes via `dd if=/dev/urandom of=... bs=1024 count=1`).

Run each command, capture stdout + stderr + exit code. Fill in `124-E2E.md` with one sub-section per node, showing the exact command and observed output.

**Verifications for Item 1:**
- Every command exits 0.
- `cdb put` outputs a 64-hex-char hash; the blob is later retrievable.
- `cdb get <hash>` writes byte-identical content (compute `sha3sum` of original and output, compare).
- `cdb put --name foo <file>` succeeds; `cdb get foo` retrieves byte-identical content.
- Batched `cdb rm <h1> <h2>` emits a single BOMB (verify via `cdb ls --raw --type BOMB` showing 1 entry).
- `cdb ls` hides PUBK, CDAT, BOMB, DLGT types by default.
- **SC-124-1 proof:** against the fresh-wiped node, `cdb put` (the SECOND command in the sequence, AFTER `cdb publish`) succeeds. Repeat the full sequence with a fresh identity or fresh namespace to confirm auto-PUBK also works when `cdb publish` is NOT run first — that IS the auto-PUBK test. Document both forms in the log.

Mark item 1 with verdict `PASS` / `FAIL` per node. If any step fails, capture the full error output AND mark the phase as blocked; do NOT proceed to later items until item 1 is green on both nodes.
  </action>
  <verify>
    <automated>test -f .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md && grep -c "## Item 1" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - `124-E2E.md` file exists with the skeleton summary table and at least Item 1 filled in for both `local` and `home` nodes.
    - Item 1 shows verdict `PASS` on both nodes OR documents blocker and leaves summary table reflecting the actual state.
    - Byte-identical verification is evidence-based (commands + outputs captured).
  </acceptance_criteria>
  <done>Item 1 PASS on both nodes; log populated.</done>
</task>

<task type="auto">
  <name>Task 3: Run Items 2 + 3 — cross-node sync (propagation in both directions) + BOMB propagation</name>
  <files>.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</files>
  <action>
**Item 2 — Cross-node sync:**
1. `cdb --node local put /tmp/xfer.bin` → hash X.
2. Wait for sync interval (node defaults; up to ~30s depending on config — give it 60s).
3. `cdb --node home ls --raw | grep <hash X>` — MUST show the blob present on home.
4. Reverse: `cdb --node home put /tmp/xfer2.bin` → hash Y; wait; `cdb --node local ls --raw | grep <hash Y>` present.
5. Rm propagation: `cdb --node local rm <hash X>` (single); wait; `cdb --node home ls --raw | grep <hash X>` should NOT return the content (tombstoned).

Document commands + observations in `124-E2E.md` §"Item 2". Verdict per direction.

**Item 3 — BOMB propagation:**
1. `cdb --node local put /tmp/b1.bin; cdb --node local put /tmp/b2.bin; cdb --node local put /tmp/b3.bin` → collect 3 hashes.
2. `cdb --node local rm <h1> <h2> <h3>` — batched, emits one BOMB.
3. Wait for sync.
4. `cdb --node home ls --raw --type BOMB` should show 1 BOMB entry.
5. Optional: inspect the BOMB payload (if `cdb get <bomb_hash> --raw` or similar works) to verify count=3 and the 3 target hashes.

Document in §"Item 3". Verdict PASS/FAIL.
  </action>
  <verify>
    <automated>grep -c "## Item 2" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md && grep -c "## Item 3" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - Both items documented with commands, observations, verdicts.
    - Summary table updated with results.
    - If either item fails, the failure is captured verbatim in the log and marked BLOCKER.
  </acceptance_criteria>
  <done>Items 2 and 3 PASS documented in E2E log.</done>
</task>

<task type="auto">
  <name>Task 4: Run Item 4 — Chunked >500 MiB roundtrip (both nodes)</name>
  <files>.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</files>
  <action>
1. Generate a 750 MiB test file: `dd if=/dev/urandom of=/tmp/big_750M.bin bs=1M count=750` (or use an existing large file).
2. `cdb --node local put /tmp/big_750M.bin` — triggers chunked path (CDAT chunks + CPAR manifest). Capture manifest hash.
3. Wait for sync.
4. `cdb --node home get <manifest_hash> --out /tmp/big_recv.bin` — downloads via chunked reassembly.
5. Verify byte-identical: `sha3sum /tmp/big_750M.bin /tmp/big_recv.bin` both produce the same 32-byte hex.
6. Clean up: `cdb --node local rm <manifest_hash>` (should cascade via D-06 in the single-target case — or rely on the existing rm_chunked single-target cascade). Wait; verify chunks gone via `cdb --node home ls --raw --type CDAT | wc -l` showing a reduced count (or zero if no other chunks present).

Document in §"Item 4". The auto-PUBK + chunked combined path is a strong SC-124-1 + CHUNK-01 through CHUNK-04 regression indicator.
  </action>
  <verify>
    <automated>grep -c "## Item 4" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - Byte-identical recovery after cross-node chunked transfer.
    - Document any retry backoff or transient errors; single successful final transfer counts as PASS.
  </acceptance_criteria>
  <done>Item 4 PASS.</done>
</task>

<task type="auto">
  <name>Task 5: Run Item 5 — Delegate --share @contact E2E</name>
  <files>.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</files>
  <action>
Prerequisites: need TWO identities, with one acting as owner (PUBK published on home node) and the other acting as delegate (registered via `cdb delegate`).

1. If not already set up, create a secondary identity (e.g., in `/tmp/delegate-cdb/`): initialize a second `cdb` identity directory via whatever setup flow the CLI supports (`cdb --home /tmp/delegate-cdb identity new` or similar — check `cmd::info` / `main.cpp:cmd_dispatch` for the right subcommand).
2. Primary identity: `cdb --node home publish` (owner PUBK to home).
3. Primary identity: `cdb --node home contact add delegate <delegate_ns_hex>`; `cdb --node home delegate @delegate` — registers delegation.
4. From delegate identity: `cdb --home /tmp/delegate-cdb --node local put --share @owner /tmp/delegate-test.bin` — writes a CENV targeted at owner's namespace via delegate's signing key.
5. Verify: `cdb --node home ls --raw` should show the new blob (appearing in owner's namespace, signed by delegate per `delegation_map`). Decrypt via owner identity: `cdb --node home get <hash>` — byte-identical content.

Document in §"Item 5". If this flow is too complex for a single invocation (e.g., multi-identity setup is painful), scope down to a simpler delegate scenario that still validates the `delegation_map` lookup end-to-end; document the simplification explicitly in the log.
  </action>
  <verify>
    <automated>grep -c "## Item 5" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - Delegate write reaches the owner's namespace; owner can decrypt + read.
    - If the multi-identity setup is prohibitively difficult, the log must explain the scoped-down version and what was covered.
  </acceptance_criteria>
  <done>Item 5 PASS (or PARTIAL-PASS with clear scope note).</done>
</task>

<task type="auto">
  <name>Task 6: Run Item 6 — --replace BOMB-of-1 + Item 7 — D-06 cascade live</name>
  <files>.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</files>
  <action>
**Item 6 — --replace:**
1. `cdb --node local put --name foo /tmp/v1.bin` → hash V1 + NAME(foo → V1).
2. `cdb --node local put --name foo --replace /tmp/v2.bin` → hash V2 + NAME(foo → V2 seq++), plus a BOMB-of-1 tombstoning V1 (per Phase 123 D-15).
3. `cdb --node local get foo --out /tmp/foo_recv.bin` — MUST return v2 content (sha3sum /tmp/v2.bin == sha3sum /tmp/foo_recv.bin).
4. `cdb --node local ls --raw --type BOMB` — shows at least one BOMB entry; inspect to confirm count=1 and target == V1 hash.

Document in §"Item 6". Repeat against `--node home` for completeness.

**Item 7 — D-06 cascade live:**
1. Put a chunked file (reuse `/tmp/big_750M.bin` or a new 600 MiB file): `cdb --node local put /tmp/big600.bin` → manifest hash M.
2. Put 2 small plain blobs: `cdb --node local put /tmp/p1.bin` → P1, `cdb --node local put /tmp/p2.bin` → P2.
3. Batched rm with cascade: `cdb --node local rm M P1 P2` — this is the D-06 cascade path. Observe stderr: `cascade: 1 manifests fully tombstoned, 0 manifests failed to fetch`.
4. Wait for sync.
5. Verify on home: `cdb --node home ls --raw --type CDAT` should NOT contain the CDAT chunk hashes from M's manifest (they were cascaded into the BOMB). Run `cdb --node home ls --raw --type BOMB` to confirm one BOMB entry with target count == 1 + #chunks + 2 (manifest + chunks + 2 plain blobs); document the exact count observed.
6. `cdb --node home get <any_chunk_hash>` should fail (tombstoned).

Document in §"Item 7".

**SC-124-7 error-string triggers (additional coverage from VALIDATION.md):**
- Trigger `ERROR_PUBK_FIRST_VIOLATION` (0x07): find a way to force a delegate-first-write against a fresh namespace. Simplest: create a new identity C that isn't yet PUBK'd on home; have C attempt a write with `--share @some_contact`; the auto-PUBK skip for delegate writes (D-01a) means the node sees a non-PUBK first write → emits 0x07. CLI MUST display the D-05 string: `Error: namespace not yet initialized on node 192.168.1.73. Auto-PUBK failed; try running 'cdb publish' first.` Document this trigger under a new §"Error-string triggers" subsection.
- Trigger `ERROR_PUBK_MISMATCH` (0x08): two identities try to register PUBK to the same namespace. First `cdb --node local publish` (from identity A). Then, from identity B with the SAME `namespace_id` (would require a key collision or a deliberate namespace override — typically not possible without crafting keys) OR: publish from A to home, then from some third machine publish a different signing key to the same namespace — hard to orchestrate. If the trigger path requires key manipulation beyond a normal operator workflow, document the CODE PATH that surfaces 0x08 via a brief trace of `decode_error_response` with a mocked 0x08 payload instead of a live trigger. Acceptable per VALIDATION "manual (trigger)" column.
  </action>
  <verify>
    <automated>grep -c "## Item 6" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md && grep -c "## Item 7" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - Items 6 and 7 both documented with commands and verdicts.
    - Error-string trigger subsection present; at least one (0x07 or 0x08) is live-observed with the D-05 string matching exactly.
  </acceptance_criteria>
  <done>Items 6, 7 PASS; error-string subsection present.</done>
</task>

<task type="auto">
  <name>Task 7: Update 124-E2E.md summary table + mark phase-done verdict</name>
  <files>.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</files>
  <action>
Update the summary table at the top of `124-E2E.md` with final verdicts for every (item × node) cell.

If ALL 7 items are PASS on both applicable nodes, add a final section:

```markdown
## Phase Gate

**Verdict:** PASS
**Blockers:** none
**SC coverage:** SC-124-1 ✓ (auto-PUBK fires), SC-124-2 ✓ (signer_hint-only), SC-124-3 ✓ (signing canonical form), SC-124-4 ✓ (delegate), SC-124-5 ✓ (put --name / get / rm batched), SC-124-6 ✓ (unit tests green), SC-124-7 ✓ (live E2E).
**Proceed to `/gsd-verify-work 124`.**
```

If ANY item is FAIL, add:

```markdown
## Phase Gate

**Verdict:** FAIL
**Blockers:**
- Item N: <description of what failed and the evidence>

Phase 124 is NOT done. Route to `/gsd-plan-phase 124 --gaps` to close the gaps or to triage via `/gsd-discuss-phase` if the failure exposes a design gap.
```

Update STATE.md (best-effort, or delegate to orchestrator) to note: "Phase 124 E2E complete (pass)" or "Phase 124 E2E blocked on items <N,M>".
  </action>
  <verify>
    <automated>grep -c "## Phase Gate" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md && grep -cE "Verdict:\s*(PASS|FAIL)" .planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary table fully populated with PASS/FAIL per cell.
    - `## Phase Gate` section present with a verdict.
    - If PASS: phase-done; if FAIL: blockers enumerated for gap-closure planning.
  </acceptance_criteria>
  <done>Summary populated and phase gate verdict recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's shell ↔ local node | TCP to 127.0.0.1 via `cdb`; trusted environment. |
| Claude's shell ↔ home node | TCP to 192.168.1.73 via `cdb`; both ends are user-owned infrastructure. |
| User's redeploy step | Outside Claude's scope; a mis-deployed node (stale binary, non-wiped data dir) would produce false-negative E2E results. Mitigation: the user checkpoint is explicit and the first E2E step (`cdb --node X info`) verifies the node is reachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-e2e-stale-binary | Integrity | User deploys the WRONG binary | mitigate | Task 1 checkpoint names the exact build target and wipe step; Claude's first item 1 execution in task 2 includes `cdb --node X info` which shows the node's identity — if `info` fails or shows unexpected output, the user is asked to redeploy. |
| T-124-01 live | Spoofing (race) | Peer writes PUBK during auto-PUBK window | accept-surface | Item 1's fresh-namespace path tests the happy-path auto-PUBK; if a race fires, `ERROR_PUBK_MISMATCH` surfaces via D-05 as the expected user-facing string. Logged in E2E as "observed: <string>" if it fires — not a phase blocker unless it fires consistently. |
| T-124-e2e-cleanup | Availability | Leftover state from wiped-but-not-fully-wiped data dirs | accept | User responsibility per D-07; if residual state causes a false failure, Claude asks for re-wipe. |
| T-124-05 live | Information Disclosure | An error path outputs a phase-leaking string | mitigate | Task 6's SC-124-7 subsection explicitly captures the 0x07 / 0x08 trigger output; any leak of `PUBK_FIRST_VIOLATION` / `PUBK_MISMATCH` / phase numbers in observed CLI output is a blocker. Plan 04's acceptance greps already prevent this in source; this plan verifies live. |
</threat_model>

<verification>
- `.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-E2E.md` exists.
- All 7 matrix items documented with PASS verdicts.
- `## Phase Gate` section has `Verdict: PASS`.
- Error-string observation (at least 0x07 trigger live, 0x08 either live or with documented code-trace fallback) matches D-05 wording exactly.
</verification>

<success_criteria>
Plan 05 succeeds when:
- [ ] User checkpoint acknowledged; both nodes running post-124 binary with wiped data dirs.
- [ ] 124-E2E.md exists with all 7 D-08 items documented per node.
- [ ] All 7 items PASS on all applicable nodes.
- [ ] Error-string SC-124-7 observation captured.
- [ ] `## Phase Gate` verdict is PASS.
- [ ] Addresses SC-124-1, SC-124-4, SC-124-5, SC-124-7.
</success_criteria>

<output>
After completion, create `.planning/phases/124-cli-adaptation-to-new-mvp-protocol/124-05-SUMMARY.md` with: the E2E verdict (PASS/FAIL), links to `124-E2E.md` for details, and the phase-gate decision.
</output>
