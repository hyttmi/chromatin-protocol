---
phase: 122
plan: 07
type: execute
wave: 6
depends_on: [3, 4, 5, 6]
files_modified:
  - db/tests/engine/test_engine.cpp
  - db/tests/sync/test_sync_protocol.cpp
  - db/tests/storage/test_storage.cpp
  - db/tests/wire/test_codec.cpp
  - db/tests/engine/test_verify_signer_hint.cpp
autonomous: true
requirements_addressed:
  - SC#7
  - "D-12 (test sweep wrap-up)"
  - "VALIDATION test anchor: phase122/regression: no stale references"
  - "VALIDATION test anchor: phase122/regression: max_maps bumped"
  - "VALIDATION test anchor: phase122/engine: verify path resolves pubkey via signer_hint"
user_setup: []

must_haves:
  truths:
    - "All pre-existing test files (test_engine.cpp 77 cases, test_sync_protocol.cpp 33, test_storage.cpp 105, test_codec.cpp) compile and pass after schema-cascade fixes."
    - "test_verify_signer_hint.cpp covers the post-122 engine verify path: owner_pubkeys lookup -> ML-DSA verify; no_delegation path for unknown signer_hint; namespace_mismatch with revised post-122 semantics (SHA3(resolved_pubkey) != target_namespace)."
    - "Grep regression: zero references to `blob.namespace_id` or `blob.pubkey` in db/{engine,sync,wire,storage,peer,tests}."
    - "max_maps verification: storage.cpp has `operate_params.max_maps = 10`."
    - "Full Catch2 suite passes (all phases, not just [phase122]) — proves the cascade caused no regressions."
  artifacts:
    - path: "db/tests/engine/test_engine.cpp"
      provides: "77 pre-existing TEST_CASEs updated for post-122 BlobData shape + ingest signature"
    - path: "db/tests/sync/test_sync_protocol.cpp"
      provides: "33 pre-existing TEST_CASEs updated for post-122 NamespacedBlob + sync wire format"
    - path: "db/tests/storage/test_storage.cpp"
      provides: "105 pre-existing TEST_CASEs updated for D-10 parameter rename"
    - path: "db/tests/wire/test_codec.cpp"
      provides: "Any remaining schema-cascade fixes (mostly done in Plan 03 but defensive cleanup here)"
    - path: "db/tests/engine/test_verify_signer_hint.cpp"
      provides: "New TEST_CASEs for the post-122 verify path semantics (SC#6 explicit coverage)"
      contains: "[phase122][engine][verify]"
  key_links:
    - from: "Test cascade fixes"
      to: "Compiler errors as guide"
      via: "Land Plan 04 + Plan 05 -> compiler shows every stale reference -> mechanically fix"
      pattern: "blob\\.namespace_id|blob\\.pubkey"
    - from: "Grep regression gate"
      to: "Phase exit"
      via: "! grep -rnE 'blob\\.namespace_id|blob\\.pubkey' db/ --include='*.cpp' --include='*.h' --exclude='*_generated.h' --exclude='*.fbs'"
      pattern: "blob\\.namespace_id|blob\\.pubkey"
---

<objective>
Sweep the schema cascade: fix every remaining test file that broke from Plan 04's BlobData shape change + ingest signature widening + Plan 05's sync wire format change. The compiler is the friend here — `cmake --build build --target chromatindb_tests 2>&1` lists every site to fix. Each fix is mechanical: change `blob.namespace_id` -> `target_namespace`, `blob.pubkey` -> resolved-via-storage; update `engine.ingest(blob, ...)` callers to `engine.ingest(target_namespace, blob, ...)`; update `decode_blob_transfer` consumers to the new namespaced container.

Also add `test_verify_signer_hint.cpp` for explicit SC#6 coverage (the post-122 verify path semantics — owner_pubkeys lookup, unknown signer_hint -> no_delegation, namespace_mismatch with revised post-122 meaning).

Final regression gates:
1. Zero stale references to `blob.namespace_id` / `blob.pubkey` in db/ (production + tests).
2. `max_maps = 10` confirmed in storage.cpp.
3. Full Catch2 suite passes — no regressions in pre-existing functionality.

Purpose: Phase 122 is the protocol break landing all at once. This plan closes out the cascade so the suite is clean and shippable. Without this, the test suite is in a "compiles in lib but fails in tests" limbo.

Output: Full chromatindb_tests suite passes; grep regression gate clean; phase ready for the verify-work pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-CONTEXT.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-RESEARCH.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-PATTERNS.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-VALIDATION.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-04-SUMMARY.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-05-SUMMARY.md
@.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-06-SUMMARY.md

<interfaces>
<!-- Mechanical replacement table for the cascade -->

| Pre-122 pattern                              | Post-122 replacement                                            |
|----------------------------------------------|-----------------------------------------------------------------|
| `blob.namespace_id`                          | `target_namespace` (a span/array passed in by caller)          |
| `blob.pubkey` (in tests, fed to verify path) | `resolved_pubkey` (looked up via storage_.get_owner_pubkey)    |
| `engine.ingest(blob)` / `engine.ingest(blob, conn)` | `engine.ingest(target_ns, blob)` / `engine.ingest(target_ns, blob, conn)` |
| `engine.delete_blob(req)` / `(req, conn)`    | `engine.delete_blob(target_ns, req, conn)`                      |
| `sync.ingest_blobs(blobs, conn)`             | `sync.ingest_blobs(ns_blobs, conn)` where ns_blobs is `vector<NamespacedBlob>` |
| `decode_blob_transfer(payload)` returning `vector<BlobData>` | returns `vector<NamespacedBlob>`                  |
| `make_signed_blob(id, "x")` etc.             | unchanged signature; internals updated by Plan 04 helpers       |
| `make_signed_delegation(...)`                | unchanged signature                                             |
| `BlobData{}.namespace_id`                    | does not exist; use `BlobData{}.signer_hint` if relevant         |
| `BlobData{}.pubkey`                          | does not exist                                                   |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cascade-sweep test_engine.cpp + test_storage.cpp + test_sync_protocol.cpp + test_codec.cpp</name>
  <behavior>
    - All pre-existing TEST_CASEs in test_engine.cpp / test_storage.cpp / test_sync_protocol.cpp / test_codec.cpp compile and pass against the post-122 codec/engine/storage/sync APIs.
    - chromatindb_tests target builds cleanly.
    - Full suite runs with all cases passing (no skips, no failures).
  </behavior>
  <read_first>
    - db/tests/engine/test_engine.cpp (full file — large, ~1500 lines)
    - db/tests/sync/test_sync_protocol.cpp (full file)
    - db/tests/storage/test_storage.cpp (full file — large, ~2000 lines)
    - db/tests/wire/test_codec.cpp (full file — Plan 03 already updated some; this is mop-up)
    - db/tests/test_helpers.h (post-Plan-04 — confirm helpers are post-122 shape)
    - db/engine/engine.h (post-Plan-04 — confirm widened signature)
    - db/storage/storage.h (post-Plan-02 — confirm renamed parameters)
    - db/sync/sync_protocol.h (post-Plan-05 — confirm NamespacedBlob)
    - .planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-RESEARCH.md § Pitfall #4 (test_helpers shape churn)
    - .planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-RESEARCH.md § Open Question #4 (the test_engine.cpp:89 namespace-mismatch case rescoped)
  </read_first>
  <action>
    The compiler is the guide. Run:
    ```bash
    cmake --build build -j$(nproc) --target chromatindb_tests 2>&1 | tee /tmp/build.log
    grep -nE "error:" /tmp/build.log | head -100
    ```

    Mechanical fix loop (per error site):

    (a) `blob.namespace_id` references: replace with `target_namespace` (where target_namespace is a local already in scope, OR pass it in via the test setup). For tests that BUILD a blob and then ingest it, use `id.namespace_id()` as the target_namespace argument to engine.ingest.

    (b) `blob.pubkey` references: in test verify paths, the resolved pubkey is now stored in owner_pubkeys after a PUBK is ingested. Tests that previously did `blob.pubkey = some_corrupted_value;` to provoke namespace_mismatch should be rescoped per RESEARCH.md Open Question #4:
    - Old test: "BlobEngine rejects namespace mismatch" by setting `blob.namespace_id = 0xFF...`
    - New equivalent: ingest a blob whose `signer_hint` does not exist in owner_pubkeys AND no matching delegation -> expect `IngestError::no_delegation`.
    - OR: ingest a PUBK first, then submit a non-PUBK blob whose target_namespace is DIFFERENT from the PUBK's -> the engine first hits PUBK-first violation on the new namespace.
    - Use whichever rescoping preserves the test's INTENT.

    (c) `engine.ingest(blob)` / `engine.ingest(blob, conn)` -> `engine.ingest(target_ns, blob, conn)`. The target_ns argument is `std::span<const uint8_t, 32>(id.namespace_id())` for owner-write tests, or whichever namespace the test exercises.

    (d) `engine.delete_blob(req)` / `(req, conn)` -> `engine.delete_blob(target_ns, req, conn)`.

    (e) `sync.ingest_blobs(blobs, conn)` and `decode_blob_transfer(payload)` consumers: convert to NamespacedBlob container. For tests that build a vector of blobs, wrap each as `{id.namespace_id(), blob}` (or the equivalent `NamespacedBlob{ns, blob}`).

    (f) Storage::has_valid_delegation / count_delegations parameter-name consumers: D-10 renamed `namespace_id` -> `ns` (or whatever Plan 02 chose). Tests that name an arg by parameter (rare in C++ but possible) need updating; positional callers are fine.

    (g) Tombstone, delegation, and other tests that reference `blob.namespace_id` semantically (e.g. asserting a tombstone was stored under a namespace): change to assert against `target_namespace` since the namespace is now the call-site parameter, not a blob field.

    Repeat the build-fix loop until `cmake --build build -j$(nproc) --target chromatindb_tests 2>&1 | tail -3 | grep -q "Built target chromatindb_tests"`.

    Then run the full test suite:
    ```bash
    ./build/chromatindb_tests --reporter compact 2>&1 | tee /tmp/test.log | tail -20
    grep -E "(failed|FAILED)" /tmp/test.log
    ```

    Expected: zero failures. If a test fails for a reason unrelated to the schema cascade (pre-existing flake), document and decide: fix it here or document as out-of-scope.

    Per `feedback_no_backward_compat.md`: do not preserve "compatibility" wrappers; do not leave commented-out old test code. Each test either makes sense post-122 or is deleted.

    Per `feedback_no_duplicate_code.md`: if a fix pattern is repeated 5+ times, extract a small helper (e.g. `inline std::span<const uint8_t, 32> ns_span(const NodeIdentity& id) { return std::span<const uint8_t, 32>(id.namespace_id()); }`) into test_helpers.h.
  </action>
  <verify>
    <automated>cmake --build build -j$(nproc) --target chromatindb_tests 2>&1 | tail -3 | grep -q "Built target chromatindb_tests" && ./build/chromatindb_tests --reporter compact 2>&1 | tail -5 | grep -qE "All tests passed|^[0-9]+ assertions in [0-9]+ test cases" && ! ./build/chromatindb_tests --reporter compact 2>&1 | grep -qE "^[^0]+ failed"</automated>
  </verify>
  <done>
    - chromatindb_tests builds with no errors
    - Full test suite passes (zero failures)
    - test_engine.cpp's 77 pre-existing cases all green
    - test_sync_protocol.cpp's 33 pre-existing cases all green
    - test_storage.cpp's 105 pre-existing cases all green
    - test_codec.cpp's cases all green
    - The "namespace_mismatch" pre-existing test rescoped per RESEARCH.md Open Question #4 (now exercises no_delegation or pubk_first_violation depending on intent)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_verify_signer_hint.cpp for explicit SC#6 verify-path coverage</name>
  <behavior>
    - 3-4 TEST_CASEs covering the post-122 verify path: owner_pubkeys lookup hit (owner write succeeds), owner_pubkeys lookup miss + no delegation (rejection), owner_pubkeys lookup hit but cross-check fails (revised namespace_mismatch).
    - All cases pass under chromatindb_tests `[phase122][engine][verify]` filter.
  </behavior>
  <read_first>
    - db/tests/engine/test_engine.cpp:79-114 (analog test cases — namespace mismatch + invalid signature scaffolding)
    - db/engine/engine.cpp (post-Plan-04 — confirm verify-path Step 2 + Step 3 shape)
    - db/storage/storage.h (post-Plan-02 — register_owner_pubkey + get_owner_pubkey)
    - .planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-PATTERNS.md § "db/tests/engine/test_verify_signer_hint.cpp (NEW or extend existing)" (line 27)
    - .planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-VALIDATION.md § "phase122/engine: verify path resolves pubkey via signer_hint" row (SC#6)
  </read_first>
  <action>
    Create `db/tests/engine/test_verify_signer_hint.cpp` with at minimum these 3 TEST_CASEs (each tagged `"[phase122][engine][verify]"`):

    **Test 1**: `"owner_pubkeys lookup hit -> verify succeeds"`
    - Ingest a PUBK first, then ingest a regular blob signed by the same identity.
    - Assert: regular blob's verify path goes through owner_pubkeys lookup -> match -> ML-DSA verify succeeds -> stored.
    - Sanity: `REQUIRE(storage.has_owner_pubkey(target_ns)); REQUIRE(result.accepted);`

    **Test 2**: `"unknown signer_hint with no delegation -> no_delegation"`
    - Ingest A's PUBK to establish A's namespace.
    - Build a blob with B's identity (signer_hint = SHA3(B_pk)) but submitted to A's namespace (target_namespace = A's ns).
    - There's no PUBK for B in store + no delegation for B in A's namespace -> verify must reject with `IngestError::no_delegation`.

    **Test 3**: `"delegation_map hit (delegate) -> verify succeeds"`
    - Ingest A's PUBK, then publish a delegation from A to D.
    - Build a blob signed by D targeting A's namespace (`make_delegate_blob(A, D, "x")`).
    - Submit; expect accepted (delegation_map fallback resolves D's pubkey).

    **Test 4 (optional)**: `"namespace_mismatch revised — owner_pubkeys hit but SHA3(pubkey) != target_namespace"`
    - This is a defense-in-depth test for the engine's owner-branch cross-check (engine.cpp Step 2 owner branch's `if (hint_check == target_namespace)`).
    - The case is degenerate to construct because owner_pubkeys is populated by the engine itself with a verified PUBK whose hint already matches; manual corruption would require mutating the DBI directly. For phase 122 this can be SKIPPED unless trivial — document the skip in the file as "covered by integrity invariant; explicit test deferred until DBI corruption fault-injection harness exists."

    Wire `db/tests/engine/test_verify_signer_hint.cpp` into `db/CMakeLists.txt`.

    Build + run:
    ```
    cmake --build build -j$(nproc) --target chromatindb_tests
    ./build/chromatindb_tests "[phase122][engine][verify]" --reporter compact
    ```
  </action>
  <verify>
    <automated>test -f db/tests/engine/test_verify_signer_hint.cpp && grep -c "TEST_CASE" db/tests/engine/test_verify_signer_hint.cpp | awk '$1 >= 3 {exit 0} {exit 1}' && grep -q "test_verify_signer_hint.cpp" db/CMakeLists.txt && ./build/chromatindb_tests "[phase122][engine][verify]" --reporter compact 2>&1 | tail -3 | grep -qE "All tests passed|passed"</automated>
  </verify>
  <done>
    - test_verify_signer_hint.cpp exists with ≥ 3 TEST_CASEs
    - SC#6 directly tested: post-122 verify path goes through owner_pubkeys lookup + delegation fallback + signature verify
    - CMakeLists.txt updated
    - All 3 cases pass under standard build
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final regression gates — grep sweep, max_maps verification, full suite green</name>
  <behavior>
    - Zero references to `blob.namespace_id` or `blob.pubkey` in db/{engine,sync,wire,storage,peer,tests}.
    - storage.cpp has `operate_params.max_maps = 10`.
    - Full Catch2 suite passes with no failures.
    - All Phase 122 test anchors from VALIDATION.md are green (every row's automated command exits 0).
  </behavior>
  <read_first>
    - .planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-VALIDATION.md (full file — every test anchor must be green)
    - db/storage/storage.cpp:170-200 (max_maps area — sanity-check post-Plan-02 value)
  </read_first>
  <action>
    Execute the regression battery:

    (a) Grep regression — must return zero hits in production + test code (excluding generated headers and .fbs):
    ```bash
    ! grep -rnE "blob\.namespace_id|blob\.pubkey" db/ --include="*.cpp" --include="*.h" --exclude="*_generated.h" --exclude="*.fbs"
    ```
    If any hit remains, fix it (mechanical: probably a missed test case or a comment that still references the old field). The grep gate must be CLEAN.

    (b) max_maps verification:
    ```bash
    grep -nE "operate_params\.max_maps\s*=\s*10" db/storage/storage.cpp
    ```
    Must return exactly one match (the line bumped in Plan 02).

    (c) Full Catch2 suite green:
    ```bash
    cmake --build build -j$(nproc) --target chromatindb_tests
    ./build/chromatindb_tests --reporter compact 2>&1 | tee /tmp/full_test.log
    tail -10 /tmp/full_test.log
    ```
    Expected line: `All tests passed (N assertions in M test cases)` — no failures.

    (d) All `[phase122]` test anchors green (covers SC#1-7 + D-12 + D-13):
    ```bash
    ./build/chromatindb_tests "[phase122]" --reporter compact 2>&1 | tail -3
    ```

    (e) TSAN build still green (sanity):
    ```bash
    cmake --build build-tsan -j$(nproc) --target chromatindb_tests 2>&1 | tail -3
    ./build-tsan/chromatindb_tests "[tsan]" --reporter compact 2>&1 | tail -3
    ```
    No data-race warnings.

    (f) The full VALIDATION.md test-anchor table — for each row, verify the Automated Command exits 0:
    - phase122/schema: Blob has no namespace_id field
    - phase122/schema: Blob has signer_hint [32]
    - phase122/codec: build_signing_input absorbs target_namespace byte-identical (golden vector)
    - phase122/engine: PUBK-first rejects non-PUBK on fresh namespace
    - phase122/sync: PUBK-first rejects sync-replicated non-PUBK on fresh namespace
    - phase122/engine: PUBK after PUBK idempotent when signing key matches
    - phase122/engine: PUBK with different signing key rejected with PUBK_MISMATCH
    - phase122/engine: non-PUBK after PUBK succeeds
    - phase122/tsan: cross-namespace PUBK race first-wins
    - phase122/storage: owner_pubkeys DBI register/get/has/count
    - phase122/engine: verify path resolves pubkey via signer_hint
    - phase122/engine: delegate-replay cross-namespace rejected
    - phase122/regression: no stale references to blob.namespace_id or blob.pubkey
    - phase122/regression: max_maps bumped to ≥ 10

    For each: invoke the command from VALIDATION.md, expect exit 0. Mark the row's Status as ✅ green in the executor's summary.

    (g) Document any blob-size measurement (manual-only verification from VALIDATION.md): use a quick test print or a Catch2 sanity test to confirm encoded blob size shrunk by ≥ 2592 bytes vs pre-122. The size check in test_schema_phase122.cpp (Plan 03) covers part of this; record the actual byte counts in the summary.
  </action>
  <verify>
    <automated>! grep -rnE "blob\.namespace_id|blob\.pubkey" db/ --include="*.cpp" --include="*.h" --exclude="*_generated.h" --exclude="*.fbs" 2>/dev/null && grep -qE "operate_params\.max_maps\s*=\s*10" db/storage/storage.cpp && ./build/chromatindb_tests --reporter compact 2>&1 | tail -3 | grep -qE "All tests passed|^[0-9]+ assertions" && ./build/chromatindb_tests "[phase122]" --reporter compact 2>&1 | tail -3 | grep -qE "All tests passed|passed"</automated>
  </verify>
  <done>
    - Grep regression returns zero hits in db/ (excluding generated + schema files)
    - storage.cpp shows max_maps = 10
    - Full chromatindb_tests suite passes (no failures, no skips)
    - `[phase122]` filter passes (all phase-specific cases green)
    - TSAN build remains green
    - All 14 VALIDATION.md test-anchor rows are ✅ green; status table updated in summary
    - Manual verification (blob size shrink) recorded with actual byte counts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test cascade -> production correctness | Cascade fixes are mechanical; the suite is the proof — full green = all production paths exercised by pre-existing test cases work post-refactor. |
| Grep regression -> shipping readiness | The grep gate is the structural assertion that no stale field accesses remain in source — a runtime-correct cascade can still fail this if a comment or string literal references the old field. Fix all hits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-11 | Tampering (cascade leaves stale reference) | Test files / production code | mitigate | Grep gate forces zero `blob.namespace_id`/`blob.pubkey` hits; CI/local check before phase exit. |
| T-122-12 | Integrity (suite passes but missed test rescoping) | test_engine.cpp namespace_mismatch case | mitigate | Cascade explicitly rescopes per RESEARCH.md Open Question #4; test_verify_signer_hint.cpp adds explicit positive-coverage cases for the post-122 verify path. |
| T-122-06 | DoS — pre-existing test for max_maps boundary | Storage env init | mitigate | max_maps = 10 verified by direct grep + the existing tests open the env successfully (proves no MDBX_DBS_FULL on 8 named DBIs). |
</threat_model>

<verification>
- chromatindb_tests builds cleanly
- Full suite passes (no failures)
- `[phase122]` filter passes
- Grep regression returns zero stale references
- storage.cpp max_maps = 10
- TSAN build green
- All VALIDATION.md test anchors marked ✅
- Phase 122 ready for /gsd-verify-work
</verification>

<success_criteria>
- SC#7 met: production + test code has zero references to removed fields
- D-12 (all clauses) test sweep green
- VALIDATION.md anchors all green
- Phase 122 protocol break landed cleanly across schema, codec, storage, engine, sync, dispatcher, and tests
- Suite passes; phase ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/122-schema-signing-cleanup-strip-namespace-and-compress-pubkey/122-07-SUMMARY.md` including:
- Status table for all 14 VALIDATION.md test anchors
- Final blob-size measurement (post-122 minimal blob bytes vs pre-122)
- Confirmation that phase is ready for /gsd-verify-work
</output>
