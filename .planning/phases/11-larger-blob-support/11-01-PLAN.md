---
phase: 11-larger-blob-support
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - db/net/framing.h
  - db/engine/engine.h
  - db/engine/engine.cpp
  - tests/engine/test_engine.cpp
  - tests/net/test_framing.cpp
autonomous: true
requirements: [BLOB-01, BLOB-02, BLOB-05]

must_haves:
  truths:
    - MAX_BLOB_DATA_SIZE is a constexpr set to 100 MiB (104857600 bytes)
    - MAX_FRAME_SIZE is raised to 110 MiB (115343360 bytes) to accommodate 100 MiB blobs plus protocol overhead
    - Blobs with data.size() > MAX_BLOB_DATA_SIZE are rejected at ingest as Step 0 before any other validation
    - IngestError enum has oversized_blob variant
    - Blobs with data.size() == MAX_BLOB_DATA_SIZE are accepted (boundary case)
    - read_frame throws for frame sizes > MAX_FRAME_SIZE
    - recv_raw returns nullopt for frame sizes > MAX_FRAME_SIZE (existing behavior, verified with new limit)
  artifacts:
    - db/net/framing.h
    - db/engine/engine.h
    - db/engine/engine.cpp
    - tests/engine/test_engine.cpp
    - tests/net/test_framing.cpp
  key_links:
    - BlobEngine::ingest uses net::MAX_BLOB_DATA_SIZE for Step 0 size check
    - framing.h defines both MAX_FRAME_SIZE and MAX_BLOB_DATA_SIZE as co-located protocol constants
---

<objective>
Define protocol constants for 100 MiB blob support and add size validation at ingest and transport layers.

Purpose: Establish the hard limits that protect nodes from memory exhaustion (BLOB-01, BLOB-05) and ensure transport can carry max-size blobs (BLOB-02). This is the foundation all other plans build on.
Output: Updated constants + size validation in engine ingest + verified frame limits
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-larger-blob-support/11-CONTEXT.md
@.planning/phases/11-larger-blob-support/11-RESEARCH.md

@db/net/framing.h
@db/net/framing.cpp
@db/net/connection.cpp
@db/engine/engine.h
@db/engine/engine.cpp
@tests/engine/test_engine.cpp
@tests/net/test_framing.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MAX_BLOB_DATA_SIZE constant and raise MAX_FRAME_SIZE</name>
  <files>db/net/framing.h, tests/net/test_framing.cpp</files>
  <behavior>
    - Test: MAX_BLOB_DATA_SIZE equals 104857600 (100 * 1024 * 1024)
    - Test: MAX_FRAME_SIZE equals 115343360 (110 * 1024 * 1024)
    - Test: MAX_FRAME_SIZE > MAX_BLOB_DATA_SIZE (frame must be larger to fit overhead)
    - Test: write_frame + read_frame round-trip succeeds for a payload near MAX_BLOB_DATA_SIZE (use a small proxy, e.g., 1 MiB, to verify the framing logic still works -- actual 100 MiB allocation in tests is wasteful)
    - Test: read_frame throws runtime_error for a buffer with declared length > MAX_FRAME_SIZE
  </behavior>
  <action>
1. In `db/net/framing.h`:
   - Change `constexpr uint32_t MAX_FRAME_SIZE = 16 * 1024 * 1024;` to `constexpr uint32_t MAX_FRAME_SIZE = 110 * 1024 * 1024;` (110 MiB = 115,343,360)
   - Add below it: `constexpr uint64_t MAX_BLOB_DATA_SIZE = 100ULL * 1024 * 1024;` (100 MiB). Use uint64_t to prevent overflow in downstream calculations.
   - Add a static_assert: `static_assert(MAX_FRAME_SIZE > MAX_BLOB_DATA_SIZE, "frame must accommodate max blob plus overhead");`

2. In `tests/net/test_framing.cpp`:
   - Add test cases verifying the constant values
   - Add test verifying read_frame rejects oversized frames (craft a buffer with a 4-byte BE header declaring a length > MAX_FRAME_SIZE, verify it throws runtime_error)
   - Add test verifying write_frame + read_frame round-trip with a 1 MiB payload (confirms framing still works at larger sizes)
  </action>
  <verify>
    <automated>cd build && cmake --build . --target chromatindb_tests 2>&1 && ctest --test-dir . -R "framing" --output-on-failure 2>&1</automated>
  </verify>
  <done>MAX_FRAME_SIZE is 110 MiB, MAX_BLOB_DATA_SIZE is 100 MiB as constexpr protocol invariants. Framing tests verify the constants and reject oversized frames. All existing framing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add oversized_blob rejection as Step 0 in BlobEngine::ingest</name>
  <files>db/engine/engine.h, db/engine/engine.cpp, tests/engine/test_engine.cpp</files>
  <behavior>
    - Test: Blob with data.size() == MAX_BLOB_DATA_SIZE + 1 is rejected with IngestError::oversized_blob
    - Test: Blob with data.size() == MAX_BLOB_DATA_SIZE is NOT rejected for size (may fail later validation, that's fine)
    - Test: Blob with data.size() == 0 is NOT rejected for size (empty data is valid)
    - Test: oversized_blob rejection happens before signature verification (verify by providing invalid signature -- if we get oversized_blob error, not invalid_signature, the ordering is correct)
    - Test: IngestResult error_detail for oversized_blob includes the actual size
  </behavior>
  <action>
1. In `db/engine/engine.h`:
   - Add `oversized_blob` to the IngestError enum (after `malformed_blob`, before `storage_error`)

2. In `db/engine/engine.cpp`:
   - Add `#include "db/net/framing.h"` at the top (for MAX_BLOB_DATA_SIZE)
   - Insert Step 0 BEFORE the existing Step 1 (structural checks) in `BlobEngine::ingest`:
     ```cpp
     // Step 0: Size check (cheapest possible -- one integer comparison)
     if (blob.data.size() > net::MAX_BLOB_DATA_SIZE) {
         spdlog::warn("Ingest rejected: blob data size {} exceeds max {}",
                      blob.data.size(), net::MAX_BLOB_DATA_SIZE);
         return IngestResult::rejection(IngestError::oversized_blob,
             "blob data size " + std::to_string(blob.data.size()) +
             " exceeds max " + std::to_string(net::MAX_BLOB_DATA_SIZE));
     }
     ```
   - Renumber existing comments: "Step 1" becomes "Step 1", "Step 2" becomes "Step 2", etc. (they stay the same, Step 0 is added above them)

3. In `tests/engine/test_engine.cpp`:
   - Add test cases for oversized blob rejection
   - For the "before signature verification" ordering test: create a blob with data.size() > MAX_BLOB_DATA_SIZE and an invalid/empty signature. Verify IngestError::oversized_blob is returned (not malformed_blob or invalid_signature).
   - For the boundary test: create a blob with data.size() == MAX_BLOB_DATA_SIZE and valid pubkey size. It should NOT be rejected for size (it will be rejected for namespace_mismatch or invalid_signature, which proves size check passed).
  </action>
  <verify>
    <automated>cd build && cmake --build . --target chromatindb_tests 2>&1 && ctest --test-dir . -R "engine" --output-on-failure 2>&1</automated>
  </verify>
  <done>IngestError::oversized_blob exists. Blobs exceeding 100 MiB are rejected as Step 0 before any other validation. Error detail includes actual size. All existing engine tests pass.</done>
</task>

</tasks>

<verification>
- `cd build && cmake .. && cmake --build . 2>&1` -- clean build with no warnings
- `cd build && ctest --output-on-failure 2>&1` -- ALL 185+ tests pass (existing + new)
- MAX_BLOB_DATA_SIZE and MAX_FRAME_SIZE are constexpr in framing.h
- Oversized blobs rejected before signature verification
</verification>

<success_criteria>
- Protocol constants defined: MAX_BLOB_DATA_SIZE = 100 MiB, MAX_FRAME_SIZE = 110 MiB
- Engine rejects oversized blobs as Step 0 (before structural checks, namespace, signature)
- Transport rejects oversized frames before buffer allocation (existing behavior, verified with new limit)
- All new and existing tests pass
- Clean build with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/11-larger-blob-support/11-01-SUMMARY.md`
</output>
