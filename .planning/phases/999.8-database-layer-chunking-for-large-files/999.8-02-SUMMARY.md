---
phase: 999.8-database-layer-chunking-for-large-files
plan: 02
subsystem: database
tags: [chunking, engine, manifest, large-files, atomic-storage]

# Dependency graph
requires:
  - phase: 999.8-01
    provides: "MANIFEST_MAGIC, CHUNK_SIZE, MAX_CHUNK_COUNT, is_manifest, parse_manifest, make_manifest_data, PrecomputedBlob, store_blobs_atomic"
provides:
  - "BlobEngine::store_chunked() -- splits data into 1 MiB chunks, signs, creates manifest, stores atomically"
  - "BlobEngine::read_chunked() -- fetches manifest, reassembles chunks, returns original data"
  - "SignFn type alias for chunk signing callback"
  - "ChunkedReadResult struct with success/error/chunks_found/chunks_expected"
  - "PROTOCOL.md CHNK manifest magic prefix documentation"
affects: [999.8-03, relay-chunking, sdk-chunking]

# Tech tracking
tech-stack:
  added: []
  patterns: ["coroutine-based chunk signing via crypto::offload", "atomic multi-blob store for chunked writes"]

key-files:
  created: []
  modified:
    - db/engine/engine.h
    - db/engine/engine.cpp
    - db/tests/engine/test_chunking.cpp
    - db/PROTOCOL.md

key-decisions:
  - "store_chunked uses crypto::offload per-chunk for signing (ML-DSA-87 is non-deterministic, each chunk gets unique signature)"
  - "read_chunked is synchronous (not a coroutine) -- no crypto offload needed, matching existing get_blob pattern"
  - "Manifest result check uses last entry in store_blobs_atomic results (manifest is appended last)"

patterns-established:
  - "Chunked API pattern: store_chunked returns manifest blob_hash as handle, read_chunked takes manifest_hash"
  - "SignFn callback pattern: engine delegates signing to caller, enabling flexible key management"

requirements-completed: [CHUNK-04, CHUNK-05, CHUNK-06]

# Metrics
duration: 29min
completed: 2026-04-12
---

# Phase 999.8 Plan 02: Engine Chunking API Summary

**BlobEngine gains store_chunked/read_chunked with 1 MiB chunk splitting, atomic manifest storage, and PROTOCOL.md CHNK magic prefix documentation**

## Performance

- **Duration:** 29 min
- **Started:** 2026-04-12T07:37:52Z
- **Completed:** 2026-04-12T08:06:52Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Implemented store_chunked(): splits data into 1 MiB chunks, signs each via crypto::offload callback, builds CHNK manifest, stores all atomically via store_blobs_atomic with capacity/quota enforcement
- Implemented read_chunked(): fetches manifest by hash, parses chunk list, fetches each chunk sequentially, concatenates data with error reporting (chunks_found vs chunks_expected)
- Added namespace constraint test (D-08) verifying all chunks and manifest share same namespace
- Documented CHNK manifest magic prefix (0x43484E4B) in PROTOCOL.md with full format spec
- 28 total [chunking] tests, 226 assertions, all passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Engine store_chunked() and read_chunked() methods** - `122f85b9` (feat) -- TDD GREEN: implemented both methods, all 27 tests pass
2. **Task 2: Integration tests + PROTOCOL.md update** - `60e533b6` (test) -- Added D-08 namespace constraint test, documented CHNK in PROTOCOL.md

_Note: TDD RED phase was from cherry-picked commit a2153480 (original: a11368ae) which added 9 engine-level integration tests with stub implementations._

## Files Created/Modified
- `db/engine/engine.h` - Added SignFn type alias, ChunkedReadResult struct, store_chunked/read_chunked declarations
- `db/engine/engine.cpp` - Implemented store_chunked (chunk splitting, signing, manifest, atomic store) and read_chunked (manifest parse, chunk fetch, reassembly)
- `db/tests/engine/test_chunking.cpp` - 10 engine-level integration tests covering round-trip, edge cases, errors, TTL, namespace constraint
- `db/PROTOCOL.md` - Added CHNK manifest magic prefix documentation in Additional Interactions section

## Decisions Made
- store_chunked uses crypto::offload per-chunk for ML-DSA-87 signing -- each chunk signing is a co_await, consistent with existing ingest pattern
- read_chunked is synchronous (not a coroutine) -- Storage::get_blob is synchronous, no crypto offload needed
- Manifest result check uses the last entry from store_blobs_atomic (manifest is appended after all chunks)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Worktree did not have Plan 01 changes -- cherry-picked commits 7f2b8532 and 63f7dc26 to bring in chunking foundation
- std::array vs std::span comparison error in namespace constraint test -- fixed with std::memcmp (consistent with existing test patterns)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Engine chunking API complete: store_chunked() and read_chunked() fully functional
- Ready for Plan 03 (handler integration) to wire chunking into message dispatch
- PROTOCOL.md documents manifest format for relay/SDK implementations

## Self-Check: PASSED

All files exist, all commits found, all content checks pass:
- db/engine/engine.h: store_chunked declared (1 match)
- db/engine/engine.h: read_chunked declared (1 match)
- db/tests/engine/test_chunking.cpp: 28 TEST_CASE entries
- db/PROTOCOL.md: CHNK documented (1 match)
- Commits: 122f85b9, 60e533b6 both found

---
*Phase: 999.8-database-layer-chunking-for-large-files*
*Completed: 2026-04-12*
