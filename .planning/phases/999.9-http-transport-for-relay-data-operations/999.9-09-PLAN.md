---
phase: 999.9-http-transport-for-relay-data-operations
plan: 09
type: execute
wave: 6
depends_on:
  - 999.9-08
files_modified:
  - relay/ws/ws_frame.h
  - relay/ws/ws_frame.cpp
  - relay/ws/ws_handshake.h
  - relay/ws/ws_handshake.cpp
  - relay/ws/ws_session.h
  - relay/ws/ws_session.cpp
  - relay/ws/ws_acceptor.h
  - relay/ws/ws_acceptor.cpp
  - relay/ws/session_manager.h
  - relay/ws/session_manager.cpp
  - relay/tests/test_ws_frame.cpp
  - relay/tests/test_ws_handshake.cpp
  - relay/core/session.h
  - relay/core/session.cpp
  - relay/tests/test_session.cpp
autonomous: false
requirements:
  - HTTP-29
  - HTTP-30

must_haves:
  truths:
    - "relay/ws/ directory no longer exists"
    - "No source file in relay/ includes any ws/ header"
    - "WS test files are deleted"
    - "core::Session send queue class removed or repurposed (not used by HTTP transport)"
    - "Relay compiles, links, and all remaining tests pass"
  artifacts:
    - path: "relay/ws/"
      provides: "DELETED -- entire directory removed"
      contains: "N/A"
  key_links:
    - from: "relay/CMakeLists.txt"
      to: "relay/ws/"
      via: "No ws/ references remain in build system"
      pattern: "ws/"
---

<objective>
Delete all WebSocket code and verify the relay compiles cleanly without it. This is the clean break per D-02.

Purpose: With all HTTP transport working (Plans 01-08), the WS code is dead. Deleting it reduces the codebase by ~2400 lines, eliminates maintenance burden, and confirms no hidden dependencies remain.
Output: relay/ws/ directory deleted, WS test files deleted, core::Session evaluated for removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.9-http-transport-for-relay-data-operations/999.9-CONTEXT.md
@.planning/phases/999.9-http-transport-for-relay-data-operations/999.9-08-SUMMARY.md

@relay/CMakeLists.txt (from Plan 08 -- should already have ws/ sources removed)
@relay/ws/ (files to delete)
@relay/core/session.h (send queue -- evaluate if still needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete WebSocket code and clean up dead references</name>
  <files>relay/ws/ (all files), relay/tests/test_ws_frame.cpp, relay/tests/test_ws_handshake.cpp, relay/core/session.h, relay/core/session.cpp, relay/tests/test_session.cpp</files>
  <read_first>relay/CMakeLists.txt (verify ws/ not in sources), relay/core/session.h</read_first>
  <action>
    Per D-02, D-33, D-34.

    **Step 1: Delete relay/ws/ directory entirely**:
    ```
    rm -rf relay/ws/
    ```
    This removes: ws_frame.h/cpp, ws_handshake.h/cpp, ws_session.h/cpp, ws_acceptor.h/cpp, session_manager.h/cpp, .gitkeep.

    **Step 2: Delete WS test files**:
    ```
    rm relay/tests/test_ws_frame.cpp
    rm relay/tests/test_ws_handshake.cpp
    ```

    **Step 3: Evaluate core::Session**:
    core::Session was the WS per-client send queue (bounded deque + drain coroutine). In the HTTP model:
    - Request/response: handler coroutine writes response directly (no queue needed)
    - SSE: SseWriter has its own event queue + drain loop
    - So core::Session is no longer used by any HTTP component.

    If nothing else references core::Session, delete session.h, session.cpp, and test_session.cpp.
    If something references it (check with grep), keep it.

    **Step 4: Scan for stale references**:
    ```
    grep -r "ws/" relay/ --include="*.h" --include="*.cpp"
    grep -r "WsSession\|WsAcceptor\|SessionManager\|ws_frame\|ws_handshake" relay/ --include="*.h" --include="*.cpp"
    ```
    Fix any remaining references (there should be none after Plan 08, but verify).

    **Step 5: Clean CMakeLists.txt**:
    Verify relay/CMakeLists.txt has NO ws/ entries in sources or test targets.
    Verify relay/tests/CMakeLists.txt has NO ws test targets.

    **Step 6: Remove OpenSSL SHA-1 comment** from CMakeLists.txt -- SHA-1 was for WebSocket accept key computation. The relay still uses OpenSSL for TLS, so the dependency stays, but update the comment.

    **Step 7: Full rebuild and test**:
    Clean build to ensure no stale object files from ws/ sources.
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && test ! -d relay/ws && test ! -f relay/tests/test_ws_frame.cpp && test ! -f relay/tests/test_ws_handshake.cpp && cd build && cmake .. -DBUILD_TESTING=ON 2>&1 | tail -5 && cmake --build . -j$(nproc) 2>&1 | tail -10 && cd relay/tests && ctest -j$(nproc) --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - relay/ws/ directory does not exist
    - relay/tests/test_ws_frame.cpp does not exist
    - relay/tests/test_ws_handshake.cpp does not exist
    - grep -r "ws/" relay/ --include="*.h" --include="*.cpp" returns no results
    - grep -r "WsSession\|WsAcceptor\|ws_frame\|ws_handshake" relay/ --include="*.h" --include="*.cpp" returns no results
    - relay/CMakeLists.txt does not contain "ws/"
    - Full relay compiles from clean build
    - All remaining relay tests pass
  </acceptance_criteria>
  <done>All WebSocket code deleted. ~2400 lines removed. Zero stale references. Relay compiles cleanly. All remaining tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify HTTP relay builds and WS code is fully removed</name>
  <files>relay/http/, relay/CMakeLists.txt</files>
  <action>Human verifies that the complete HTTP relay builds, all WS code is removed, and the relay is ready for integration testing.</action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol/build && cmake --build . -j$(nproc) 2>&1 | tail -5 && cd relay/tests && ctest -j$(nproc) --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Relay binary compiles with zero errors
    - All relay tests pass
    - relay/ws/ directory does not exist
    - relay/http/ directory contains all new HTTP transport files
  </acceptance_criteria>
  <done>Human confirmed the HTTP relay builds cleanly, all tests pass, and WebSocket code is fully removed.</done>
</task>

</tasks>

<verification>
- relay/ws/ directory does not exist
- Zero WS references in codebase
- Clean compile from scratch
- All remaining tests pass
</verification>

<success_criteria>
WebSocket code fully deleted. Relay compiles and passes all tests with only HTTP transport. Clean break achieved per D-02.
</success_criteria>

<output>
After completion, create `.planning/phases/999.9-http-transport-for-relay-data-operations/999.9-09-SUMMARY.md`
</output>
