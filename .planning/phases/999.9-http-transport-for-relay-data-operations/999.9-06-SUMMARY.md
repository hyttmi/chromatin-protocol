---
phase: 999.9-http-transport-for-relay-data-operations
plan: 06
subsystem: relay
tags: [http, query, translator, json, uds, coroutine]

# Dependency graph
requires:
  - phase: 999.9-03
    provides: HTTP router with auth middleware and route registration
  - phase: 999.9-04
    provides: ResponsePromise awaitable for async UDS response bridging
provides:
  - 12 query HTTP endpoints using translator for JSON<->binary conversion
  - Async route support in HttpRouter (add_async_route, dispatch_async)
  - Query string parser and path segment extraction utilities
affects: [999.9-07, 999.9-08, relay integration tests]

# Tech tracking
tech-stack:
  added: []
  patterns: [async-route-handler, forward-query-pipeline]

key-files:
  created:
    - relay/http/handlers_query.h
    - relay/http/handlers_query.cpp
    - relay/tests/test_handlers_query.cpp
  modified:
    - relay/http/http_router.h
    - relay/http/http_router.cpp
    - relay/http/http_connection.cpp
    - relay/CMakeLists.txt
    - relay/tests/CMakeLists.txt

key-decisions:
  - "Extended HttpRouter with AsyncHandler variant and dispatch_async to support coroutine query handlers that co_await UDS responses"
  - "Mapped /stats/{ns} and /namespace-stats/{ns} to same namespace_stats_request handler (D-11 and D-18 are redundant)"
  - "Added /namespace-list endpoint for NamespaceListRequest even though not explicitly in D-11 through D-21"

patterns-established:
  - "forward_query pattern: build JSON -> json_to_binary -> register_request -> TransportCodec::encode -> UDS send -> co_await ResponsePromise -> binary_to_json -> HttpResponse::json(200, result)"
  - "Async route registration via add_async_route for handlers needing co_await"

requirements-completed: [HTTP-10, HTTP-11, HTTP-15, HTTP-16, HTTP-17, HTTP-18, HTTP-19, HTTP-20, HTTP-21, HTTP-31]

# Metrics
duration: 16min
completed: 2026-04-13
---

# Phase 999.9 Plan 06: Query Endpoints Summary

**12 HTTP query endpoints (list, stats, exists, batch_exists, node-info, peer-info, storage-status, namespace-stats, metadata, delegations, time-range, namespace-list) using translator JSON<->binary pipeline with ResponsePromise async UDS bridging**

## Performance

- **Duration:** 16 min
- **Started:** 2026-04-13T06:31:30Z
- **Completed:** 2026-04-13T06:47:32Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- All 12 query endpoints implemented with proper parameter parsing, hex validation, and error handling
- Extended HttpRouter with async handler support (add_async_route, dispatch_async) so query handlers can co_await UDS responses
- 47 unit tests with 95 assertions covering utility functions, JSON construction, and translator integration

## Task Commits

Each task was committed atomically:

1. **Task 1: Query endpoint handlers** - `f1102246` (feat)
2. **Task 2: Query handler unit tests** - `6d5995f8` (test)

## Files Created/Modified
- `relay/http/handlers_query.h` - Query handler declarations, utility function signatures, QueryHandlerDeps struct
- `relay/http/handlers_query.cpp` - All 12 query route implementations with forward_query helper
- `relay/tests/test_handlers_query.cpp` - 47 unit tests for parsing, validation, JSON construction, translator integration
- `relay/http/http_router.h` - Added AsyncHandler type and dispatch_async method
- `relay/http/http_router.cpp` - Implemented async dispatch with refactored auth middleware
- `relay/http/http_connection.cpp` - Changed dispatch to dispatch_async for coroutine support
- `relay/CMakeLists.txt` - Added handlers_query.cpp to library sources
- `relay/tests/CMakeLists.txt` - Added test_handlers_query target

## Decisions Made
- Extended HttpRouter with AsyncHandler variant (std::function<asio::awaitable<HttpResponse>>) rather than creating a separate async router, to keep all route registration in one place
- HttpConnection now uses dispatch_async for all requests (async dispatch handles both sync and async handlers transparently)
- Mapped both /stats/{ns} and /namespace-stats/{ns} to namespace_stats_request -- D-11 and D-18 describe the same query
- Added /namespace-list endpoint for completeness even though not in the D-10 through D-21 list
- forward_query uses 30-second default timeout, overridable via SIGHUP-reloadable atomic

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Extended HttpRouter with async handler support**
- **Found during:** Task 1 (Query endpoint handlers)
- **Issue:** HttpRouter::Handler returns HttpResponse synchronously, but query handlers need co_await for ResponsePromise. Cannot complete task without coroutine support.
- **Fix:** Added AsyncHandler variant to Route, add_async_route() method, and dispatch_async() coroutine. Refactored auth middleware into shared helper. Updated HttpConnection to use dispatch_async.
- **Files modified:** relay/http/http_router.h, relay/http/http_router.cpp, relay/http/http_connection.cpp
- **Verification:** Existing test_http_router tests pass (33 assertions). New query handlers compile and link.
- **Committed in:** f1102246 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential for async query forwarding. No scope creep -- extends existing router rather than replacing it.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All query routes registered and functional
- forward_query pattern established for any future query-type endpoints
- dispatch_async available for any future async HTTP handlers (e.g., data upload/download endpoints)

---
*Phase: 999.9-http-transport-for-relay-data-operations*
*Completed: 2026-04-13*

## Self-Check: PASSED
- All 3 created files exist
- Both task commits (f1102246, 6d5995f8) verified in git log
