---
phase: 999.9-http-transport-for-relay-data-operations
verified: 2026-04-13T12:15:00Z
status: passed
score: 10/10 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 8/10
  gaps_closed:
    - "SSE streaming wired into HttpConnection (detect X-SSE-Session-Id, create SseWriter, drain loop)"
    - "Subscription cleanup on idle reap (reap_idle returns reaped IDs, remove_client called)"
    - "Test data bug fixed (66-char hex -> 64-char in test_handlers_data.cpp)"
  gaps_remaining: []
  regressions: []
---

# Phase 999.9: HTTP Transport for Relay Data Operations Verification Report

**Phase Goal:** Replace entire WebSocket relay transport with HTTP + SSE. All request/response operations become HTTP endpoints with raw binary bodies for blob data and JSON for queries. Pub/sub notifications use SSE. WebSocket code deleted entirely.
**Verified:** 2026-04-13T12:15:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (Plan 10)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | No WebSocket code remains (relay/ws/ deleted, no WS includes) | VERIFIED | relay/ws/ directory absent. Zero grep hits for ws_frame/ws_handshake/WsSession/WsAcceptor. relay/CMakeLists.txt has no ws/ entries. |
| 2 | HTTP server exists and handles connections (HttpServer accept loop) | VERIFIED | relay/http/http_server.h/cpp present and substantive. relay_main.cpp co_spawns accept_loop(). TLS dual-mode wired. |
| 3 | Auth endpoints work (POST /auth/challenge, POST /auth/verify -> bearer token) | VERIFIED | register_auth_routes() called. Routes registered in http_router.cpp. Test coverage in test_http_router.cpp. |
| 4 | Data endpoints exist (POST /blob, GET /blob/{ns}/{hash}, DELETE /blob/{ns}/{hash}) | VERIFIED | handlers_data.cpp implements all three. Routes registered via register_data_routes(). Full UDS request-response flow with ResponsePromise. |
| 5 | Query endpoints exist (list, stats, exists, batch/exists, node-info, peer-info, etc.) | VERIFIED | handlers_query.cpp registers 12 query routes. register_query_routes() called in relay_main.cpp. |
| 6 | SSE notification stream is live (GET /events streams events to clients) | VERIFIED | HttpConnection::handle() detects X-SSE-Session-Id in response headers at line 224-244. run_sse_mode() creates SseWriter on coroutine stack, sets session->sse_writer, runs drain loop. relay_main.cpp dispatch.send_json pushes to sse_writer (line 181). |
| 7 | Subscribe/unsubscribe endpoints exist and forward to node | VERIFIED | handlers_pubsub.cpp implements handle_subscribe and handle_unsubscribe. Both use u16BE namespace encoding and forward to UDS. Routes registered. |
| 8 | relay_main.cpp uses HttpServer (not WsAcceptor) | VERIFIED | relay_main.cpp creates HttpServer, calls accept_loop(). No WsAcceptor reference anywhere. |
| 9 | /metrics and /health in main HTTP router | VERIFIED | register_metrics_route() and register_health_route() called in relay_main.cpp. Public routes registered in http_router.cpp. |
| 10 | Tests pass: all relay test suites green | VERIFIED | User confirmed all tests pass. test_handlers_data.cpp ns_hex fixed to 64 chars (verified: len=64, is_valid_hex=True). TODO comment removed from relay_main.cpp idle reaper. |

**Score:** 10/10 truths verified

### Gap Closure Verification

#### Gap 1: SSE Streaming Wired (was FAILED, now VERIFIED)

`relay/http/http_connection.h` now declares:
- `core::SubscriptionTracker& tracker_` member (line 65)
- `core::UdsMultiplexer& uds_` member (line 66)
- `asio::awaitable<void> run_sse_mode(uint64_t session_id)` method (line 57)

`relay/http/http_connection.cpp` now:
- Detects `X-SSE-Session-Id` header after dispatch at lines 222-229
- Enters SSE mode branch at lines 231-244: sends response headers, calls `run_sse_mode(sse_sid)`
- `run_sse_mode()` at lines 266-318: creates `SseWriter` with `WriteFn` bound to stream, sets `session->sse_writer = &writer`, co_awaits `writer.run()`, clears pointer on close, calls `tracker_.remove_client(session_id)`, forwards Unsubscribe to node

`relay/http/http_server.h` updated: constructor takes `core::SubscriptionTracker& tracker` and `core::UdsMultiplexer& uds` parameters. Both stored as member references.

`relay/relay_main.cpp` line 344: HttpServer constructed with `subscription_tracker` and `uds_mux` forwarded.

#### Gap 2: Subscription Cleanup on Idle Reap (was FAILED, now VERIFIED)

`relay/http/token_store.h` line 56: `reap_idle` now returns `std::vector<uint64_t>` (reaped session IDs).

`relay/http/token_store.cpp` `reap_idle()`: collects reaped session IDs into vector; calls `sse_writer->close()` before removal if SSE writer is active.

`relay/relay_main.cpp` idle reaper (lines 401-416): `reap_idle` return value captured as `reaped_ids`; iterates and calls `subscription_tracker.remove_client(sid)` for each reaped session; forwards Unsubscribe to node for empty namespaces. TODO comment is gone (zero grep hits for "TODO.*subscription" in relay_main.cpp).

`relay/tests/test_token_store.cpp`: assertions updated for new return type (`reaped.size() == 3`, `reaped.empty()`).

#### Gap 3: Test Data Bug Fixed (was PARTIAL, now VERIFIED)

`relay/tests/test_handlers_data.cpp` line 52: ns_hex is now `"aAbBcCdDeEfF00112233445566778899aAbBcCdDeEfF00112233445566778899"` — confirmed length=64, valid hex, mixed case. All 23 test_handlers_data tests pass.

### Key Link Verification (Updated)

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| relay_main.cpp | relay/http/http_server.h | HttpServer created and accept_loop co_spawned | WIRED | Line 344: HttpServer created with tracker + uds. Line 387: co_spawn(http_server.accept_loop()) |
| relay_main.cpp | relay/http/token_store.h | TokenStore replaces SessionManager | WIRED | Line 232: TokenStore token_store created and passed to HttpServer |
| relay_main.cpp | relay/http/handlers_data.h | register_data_routes() called | WIRED | Line 329: register_data_routes(router, data_handlers) |
| relay_main.cpp | relay/http/handlers_query.h | register_query_routes() called | WIRED | Line 334: register_query_routes(router, query_deps) |
| relay_main.cpp | relay/http/handlers_pubsub.h | register_pubsub_routes() called | WIRED | Line 339: register_pubsub_routes(router, pubsub_handlers) |
| relay_main.cpp | relay/core/uds_multiplexer.h | UdsMultiplexer.set_response_promises() | WIRED | Line 315: uds_mux.set_response_promises(&promise_map) |
| UdsMultiplexer | ResponsePromiseMap | route_response checks promise map | WIRED | uds_multiplexer.cpp: response_promises_->resolve() checked first |
| handlers_pubsub.cpp | http_connection.cpp | SSE mode activation via X-SSE-Session-Id | WIRED | http_connection.cpp lines 222-244 detect X-SSE-Session-Id and call run_sse_mode() |
| token_store.cpp | subscription_tracker | Session removal cleans up subscriptions | WIRED | reap_idle returns IDs; relay_main.cpp idle reaper calls subscription_tracker.remove_client() per ID |
| http_connection.cpp | subscription_tracker | SSE close cleans up subscriptions | WIRED | run_sse_mode() calls tracker_.remove_client(session_id) after writer.run() returns |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| HTTP-01 | 999.9-01 | HTTP/1.1 request parser | SATISFIED | http_parser.h/cpp, 35 test cases pass |
| HTTP-02 | 999.9-01 | HttpResponse builder | SATISFIED | http_response.h header-only, covered by router tests |
| HTTP-03 | 999.9-03 | Coroutine HTTP server, TLS | SATISFIED | http_server.h/cpp, accept_loop(), init_tls() |
| HTTP-04 | 999.9-01 | Session tokens via RAND_bytes | SATISFIED | token_store.cpp uses RAND_bytes, 23 test cases pass |
| HTTP-05 | 999.9-03 | Challenge-response auth, bearer token | SATISFIED | register_auth_routes() in http_router.cpp |
| HTTP-06 | 999.9-03 | Bearer token auth middleware, 401 rejection | SATISFIED | dispatch_async() checks auth for non-public routes |
| HTTP-07 | 999.9-05 | POST /blob raw binary write | SATISFIED | handle_blob_write() in handlers_data.cpp. REQUIREMENTS.md checkbox corrected. |
| HTTP-08 | 999.9-05 | GET /blob/{ns}/{hash} raw binary read | SATISFIED | handle_blob_read() returns HttpResponse::binary(). REQUIREMENTS.md checkbox corrected. |
| HTTP-09 | 999.9-05 | DELETE /blob/{ns}/{hash} tombstone | SATISFIED | handle_blob_delete() in handlers_data.cpp. REQUIREMENTS.md checkbox corrected. |
| HTTP-10 | 999.9-06 | GET /list/{namespace} | SATISFIED | /list/ route in handlers_query.cpp |
| HTTP-11 | 999.9-06 | GET /stats/{namespace} | SATISFIED | /stats/ route in handlers_query.cpp |
| HTTP-12 | 999.9-02 | SessionDispatch decouples UDS from WS | SATISFIED | core/session_dispatch.h, used in relay_main.cpp |
| HTTP-13 | 999.9-04 | ResponsePromise awaitable | SATISFIED | response_promise.h, wired in uds_multiplexer.cpp |
| HTTP-14 | 999.9-05 | POST /batch/read | SATISFIED | handle_batch_read() in handlers_data.cpp. REQUIREMENTS.md checkbox corrected. |
| HTTP-15 | 999.9-06 | GET /exists/{namespace}/{hash} | SATISFIED | /exists/ route in handlers_query.cpp |
| HTTP-16 | 999.9-06 | GET /node-info | SATISFIED | /node-info route |
| HTTP-17 | 999.9-06 | GET /peer-info | SATISFIED | /peer-info route |
| HTTP-18 | 999.9-06 | GET /storage-status | SATISFIED | /storage-status route |
| HTTP-19 | 999.9-06 | GET /metadata/{ns}/{hash} | SATISFIED | /metadata/ route |
| HTTP-20 | 999.9-06 | GET /delegations/{namespace} | SATISFIED | /delegations/ route |
| HTTP-21 | 999.9-06 | GET /time-range/{namespace} | SATISFIED | /time-range/ route |
| HTTP-22 | 999.9-07 | POST /subscribe | SATISFIED | handle_subscribe() with SubscriptionTracker |
| HTTP-23 | 999.9-07 | POST /unsubscribe | SATISFIED | handle_unsubscribe() with SubscriptionTracker |
| HTTP-24 | 999.9-07 | GET /events SSE stream | SATISFIED | SSE mode wired end-to-end. HttpConnection detects X-SSE-Session-Id and runs SseWriter drain loop. |
| HTTP-25 | 999.9-07 | SSE heartbeats, disconnect cleanup | SATISFIED | SseWriter heartbeat runs in drain loop. Disconnect cleanup in run_sse_mode (SSE close path) + idle reaper. |
| HTTP-26 | 999.9-08 | relay_main.cpp uses HttpServer | SATISFIED | HttpServer replaces WsAcceptor throughout |
| HTTP-27 | 999.9-08 | /metrics and /health in main HTTP server | SATISFIED | register_metrics_route() and register_health_route() wired |
| HTTP-28 | 999.9-08 | SIGHUP reloads HTTP settings | SATISFIED | SIGHUP handler in relay_main.cpp reloads TLS, ACL, rate limit, timeouts |
| HTTP-29 | 999.9-09 | All WS code deleted | SATISFIED | relay/ws/ directory absent, all WS files confirmed deleted |
| HTTP-30 | 999.9-09 | No WS includes remain | SATISFIED | Zero grep results for ws/ includes |
| HTTP-31 | 999.9-06 | GET /namespace-stats/{namespace} | SATISFIED | /namespace-stats/ route in handlers_query.cpp |

### Anti-Patterns — Re-check

All three blockers from initial verification are resolved:

| File | Line | Pattern | Severity | Resolution |
|------|------|---------|----------|------------|
| relay/http/http_connection.cpp | 222-244 | SSE mode detection (was: missing) | RESOLVED | X-SSE-Session-Id detection and run_sse_mode() now in place |
| relay/relay_main.cpp | 401-416 | TODO subscription cleanup (was: WARNING) | RESOLVED | TODO removed; remove_client() called in idle reaper |
| relay/tests/test_handlers_data.cpp | 52 | 66-char hex (was: WARNING) | RESOLVED | Fixed to 64-char, all 23 tests pass |

Remaining informational items (not blockers, unchanged):
- relay/core/uds_multiplexer.cpp: "WS path (legacy)" comment is cosmetic/misleading — INFO only
- relay/relay_main.cpp: static notification_counter in lambda — INFO only, not a correctness issue

### Human Verification Required

#### 1. End-to-End SSE Stream

**Test:** Start relay against a running node. Subscribe a session to a namespace via POST /subscribe. In a second session, POST a blob to that namespace. Verify SSE event arrives on the first session's GET /events stream.
**Expected:** `data: {"namespace": ..., "blob_hash": ..., ...}` event appears within 1 second of the POST.
**Why human:** Requires running relay + node UDS socket and real ML-DSA-87 auth. Cannot test without external service.

#### 2. Idle Session Reap + Subscription Cleanup

**Test:** Create a session, subscribe to a namespace. Wait for the 10-minute idle timeout (or reduce it temporarily). Verify that after reap, the SubscriptionTracker reports 0 subscribers for that namespace and the node receives an Unsubscribe message.
**Why human:** Requires live relay + node and time manipulation or config change.

---

_Verified: 2026-04-13T12:15:00Z_
_Verifier: Claude (gsd-verifier)_
