---
phase: 90-observability-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/PROTOCOL.md
  - README.md
  - sdk/python/README.md
  - sdk/python/docs/getting-started.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "PROTOCOL.md documents SyncNamespaceAnnounce (type 62) with wire format and semantics"
    - "PROTOCOL.md message type table includes type 62"
    - "PROTOCOL.md documents BlobNotify namespace filtering behavior"
    - "PROTOCOL.md documents the /metrics endpoint configuration"
    - "README.md has an Observability section with metrics config and Prometheus scrape example"
    - "README.md feature list mentions compression, filtering, multi-relay, and observability"
    - "SDK README mentions Brotli compression as transparent default for encrypted blobs"
    - "Getting-started tutorial includes metrics configuration example for operators"
  artifacts:
    - path: "db/PROTOCOL.md"
      provides: "Type 62 wire spec, namespace filtering docs, /metrics operational section"
      contains: "SyncNamespaceAnnounce"
    - path: "README.md"
      provides: "Observability section, updated feature references"
      contains: "Observability"
    - path: "sdk/python/README.md"
      provides: "Brotli compression documentation"
      contains: "Brotli"
    - path: "sdk/python/docs/getting-started.md"
      provides: "Metrics config and compression notes"
      contains: "metrics_bind"
  key_links:
    - from: "README.md Observability section"
      to: "db/PROTOCOL.md /metrics section"
      via: "cross-reference"
      pattern: "PROTOCOL.md"
    - from: "sdk/python/README.md compression note"
      to: "db/PROTOCOL.md compression section"
      via: "protocol reference"
      pattern: "suite.*0x02"
---

<objective>
Refresh all documentation to reflect the complete v2.1.0 feature set: namespace filtering, compression, multi-relay failover, and observability.

Purpose: Developers and operators need accurate documentation covering SyncNamespaceAnnounce wire format, BlobNotify namespace filtering, Prometheus /metrics endpoint setup, Brotli compression behavior, and multi-relay failover -- all features shipped in Phases 86-90.

Output: Updated PROTOCOL.md, README.md, SDK README, and getting-started tutorial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-observability-documentation/90-CONTEXT.md
@.planning/phases/90-observability-documentation/90-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROTOCOL.md -- add SyncNamespaceAnnounce, namespace filtering, and /metrics docs</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    db/PROTOCOL.md
    db/net/message_types.h (for type 62 constant)
    db/peer/peer_manager.cpp (lines 472-529 for encode_namespace_list and SyncNamespaceAnnounce sending)
    .planning/phases/90-observability-documentation/90-RESEARCH.md (Documentation Gap Analysis section)
  </read_first>
  <action>
    Per D-12, update PROTOCOL.md with three additions. All are in-place edits to existing structure (per D-11).

    **1. Add type 62 to Message Type Reference table (line ~706)**

    After the row for type 61 (BlobFetchResponse), add:
    ```
    | 62 | SyncNamespaceAnnounce | Namespace replication scope announcement: count + namespace IDs (peer-internal, blocked by relay) |
    ```

    **2. Add SyncNamespaceAnnounce section**

    Add a new section after the "Targeted Blob Fetch" section (after the BlobFetch/BlobFetchResponse documentation, before "Reconcile-on-Connect"). Title: `### SyncNamespaceAnnounce (Type 62)`.

    Content must include:

    ```markdown
    ### SyncNamespaceAnnounce (Type 62)

    After the PQ handshake completes, both peers exchange SyncNamespaceAnnounce messages declaring which namespaces they replicate. This scopes all subsequent push notifications and reconciliation to the intersection of both peers' namespace sets.

    **Direction:** Peer <-> Peer (bidirectional, sent by both sides after handshake)

    **Wire format:**

    | Offset | Size | Field | Description |
    |--------|------|-------|-------------|
    | 0 | 2 | count (uint16 BE) | Number of namespace IDs (0 = replicate all) |
    | 2 | N * 32 | namespace_ids | 32-byte namespace hashes |

    **Semantics:**

    - **Empty set (count=0):** Peer replicates all namespaces. All BlobNotify messages are forwarded; reconciliation covers all namespaces.
    - **Non-empty set:** Peer only replicates the listed namespaces. BlobNotify for namespaces not in the set is suppressed. Reconciliation (Phase A/B/C) is scoped to the intersection of both peers' announced sets.
    - **Inline dispatch:** SyncNamespaceAnnounce is processed immediately (not queued to the sync inbox). The announced set is stored per-peer and takes effect for the next BlobNotify or sync round.
    - **SIGHUP re-announce:** When the operator reloads `sync_namespaces` via SIGHUP, the node re-sends SyncNamespaceAnnounce to all connected TCP peers with the updated set.
    - **Relay blocking:** The relay blocks type 62 from clients. SyncNamespaceAnnounce is a peer-internal protocol message.

    **Example:** A node configured with `sync_namespaces: ["a1b2..."]` sends `[0x00, 0x01, <32 bytes of a1b2...>]` after handshake. A node with empty `sync_namespaces` sends `[0x00, 0x00]` (replicate all).
    ```

    **3. Add BlobNotify namespace filtering note**

    In the existing BlobNotify section (around line 219-237), after the "Sync suppression" paragraph, add a new paragraph:

    ```markdown
    **Namespace filtering:** BlobNotify is only sent to peers whose announced namespace set (via SyncNamespaceAnnounce, type 62) includes the blob's namespace. Peers with an empty announced set (replicate-all) receive all notifications. This filtering is evaluated per-peer on every ingest event.
    ```

    **4. Add /metrics Operational Section**

    Add a new top-level section `## Prometheus Metrics Endpoint` before the final section of the document (or after the Cipher Suite Registry if that is the last section). Content:

    ```markdown
    ## Prometheus Metrics Endpoint

    The node optionally exposes a Prometheus-compatible HTTP endpoint for automated monitoring. This is NOT a general-purpose HTTP server -- it responds only to `GET /metrics` with Prometheus text exposition format 0.0.4.

    ### Configuration

    | Field | Type | Default | Description |
    |-------|------|---------|-------------|
    | `metrics_bind` | string | `""` (disabled) | HTTP bind address for /metrics endpoint. Format: `host:port`. Example: `"127.0.0.1:9090"`. |

    When `metrics_bind` is empty (default), no HTTP listener starts. The endpoint is opt-in.

    **SIGHUP reloadable:** Changing `metrics_bind` via SIGHUP starts, stops, or restarts the listener dynamically.

    ### Exposed Metrics

    All metrics use the `chromatindb_` prefix. No labels (flat namespace).

    **Counters (monotonic since startup):**

    | Metric | Description |
    |--------|-------------|
    | `chromatindb_ingests_total` | Successful blob ingestions |
    | `chromatindb_rejections_total` | Failed blob ingestions (validation errors) |
    | `chromatindb_syncs_total` | Completed sync rounds |
    | `chromatindb_rate_limited_total` | Rate limit disconnections |
    | `chromatindb_peers_connected_total` | Total peer connections since startup |
    | `chromatindb_peers_disconnected_total` | Total peer disconnections since startup |
    | `chromatindb_cursor_hits_total` | Namespaces skipped via cursor match |
    | `chromatindb_cursor_misses_total` | Namespaces requiring full hash diff |
    | `chromatindb_full_resyncs_total` | Full resync rounds triggered |
    | `chromatindb_quota_rejections_total` | Namespace quota exceeded rejections |
    | `chromatindb_sync_rejections_total` | Sync rate limit rejections |

    **Gauges (current state):**

    | Metric | Description |
    |--------|-------------|
    | `chromatindb_peers_connected` | Current number of connected peers |
    | `chromatindb_blobs_stored` | Total blobs across all namespaces |
    | `chromatindb_storage_bytes` | Current storage usage in bytes |
    | `chromatindb_namespaces` | Number of active namespaces |
    | `chromatindb_uptime_seconds` | Node uptime in seconds |

    ### HTTP Response Format

    **`GET /metrics` -- 200 OK:**
    ```
    Content-Type: text/plain; version=0.0.4; charset=utf-8
    ```

    Each metric includes `# HELP` and `# TYPE` annotations per the Prometheus text exposition format.

    **Any other request -- 404 Not Found:**
    ```
    Content-Length: 0
    Connection: close
    ```

    ### Prometheus Scrape Configuration

    ```yaml
    scrape_configs:
      - job_name: 'chromatindb'
        static_configs:
          - targets: ['127.0.0.1:9090']
        scrape_interval: 15s
    ```
    ```
  </action>
  <verify>
    <automated>grep -c "SyncNamespaceAnnounce" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md && grep -c "metrics_bind" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md && grep -c "Namespace filtering" /home/mika/dev/chromatin-protocol/db/PROTOCOL.md</automated>
  </verify>
  <acceptance_criteria>
    - db/PROTOCOL.md message type table contains a row with `| 62 | SyncNamespaceAnnounce |`
    - db/PROTOCOL.md contains `### SyncNamespaceAnnounce (Type 62)` section header
    - db/PROTOCOL.md SyncNamespaceAnnounce section contains wire format table with `count (uint16 BE)` and `namespace_ids`
    - db/PROTOCOL.md SyncNamespaceAnnounce section mentions "Empty set (count=0)" and "replicate all"
    - db/PROTOCOL.md SyncNamespaceAnnounce section mentions "SIGHUP re-announce"
    - db/PROTOCOL.md SyncNamespaceAnnounce section mentions "Relay blocking"
    - db/PROTOCOL.md BlobNotify section contains "Namespace filtering" paragraph
    - db/PROTOCOL.md contains `## Prometheus Metrics Endpoint` section header
    - db/PROTOCOL.md Prometheus section contains `metrics_bind` config field documentation
    - db/PROTOCOL.md Prometheus section lists all 11 counter metric names with `chromatindb_` prefix
    - db/PROTOCOL.md Prometheus section lists all 5 gauge metric names
    - db/PROTOCOL.md Prometheus section contains Prometheus scrape config YAML example
  </acceptance_criteria>
  <done>PROTOCOL.md documents type 62 in both message table and dedicated section with wire format, BlobNotify namespace filtering paragraph added, /metrics operational section with config table, metric list, and scrape example.</done>
</task>

<task type="auto">
  <name>Task 2: README.md, SDK README, and getting-started tutorial updates</name>
  <files>README.md, sdk/python/README.md, sdk/python/docs/getting-started.md</files>
  <read_first>
    README.md
    sdk/python/README.md
    sdk/python/docs/getting-started.md
  </read_first>
  <action>
    Per D-13, D-14, D-15, update three documentation files with v2.1.0 features.

    **README.md (per D-13):**

    1. **Update Architecture > Node bullet** (line 9): After "Encrypted at rest with ChaCha20-Poly1305." add: "Prometheus /metrics endpoint for operational monitoring."

    2. **Add namespace filtering note** in Sync Model section. After item 2 ("Node B sends BlobNotify to all connected peers"), modify to: "Node B sends BlobNotify to peers whose announced namespaces include the blob's namespace"

    3. **Add Observability section** after the "Keepalive" section (after line 42), before "Crypto Stack":

    ```markdown
    ### Observability

    Optional Prometheus-compatible HTTP `/metrics` endpoint. Exposes node health metrics (peer count, blob count, storage bytes, sync stats, uptime) in Prometheus text exposition format.

    Enable in config:
    ```json
    {
      "metrics_bind": "127.0.0.1:9090"
    }
    ```

    Prometheus scrape configuration:
    ```yaml
    scrape_configs:
      - job_name: 'chromatindb'
        static_configs:
          - targets: ['127.0.0.1:9090']
        scrape_interval: 15s
    ```

    Disabled by default (empty `metrics_bind`). Localhost-only when enabled. SIGHUP-reloadable. See [PROTOCOL.md](db/PROTOCOL.md) for the full metric list.
    ```

    4. **Update Architecture > SDK bullet** (line 11): Change from "Python SDK available (`pip install chromatindb`)." to "Python SDK available (`pip install chromatindb`). Supports multi-relay failover and transparent Brotli compression."

    5. **Update Architecture > Relay bullet** (line 10): After "Bridges client connections to the node over Unix domain sockets." add: "Auto-reconnects to node on UDS failure with subscription replay."

    **SDK README (per D-14):**

    1. **Add Encryption > Compression subsection** after the Encryption Operations table (after line 85, before the Directory & Groups section). Insert:

    ```markdown
    **Compression:**

    Encrypted blobs are automatically compressed with Brotli before AEAD encryption when plaintext is >= 256 bytes (envelope suite 0x02). This is transparent -- `write_encrypted()` compresses by default, `read_encrypted()` decompresses automatically. Compression reduces storage and bandwidth for text-heavy payloads. Pass `compress=False` to `encrypt_envelope()` to disable.

    - Suite 0x01: uncompressed (plaintext < 256 bytes, or compression disabled)
    - Suite 0x02: Brotli-compressed before encryption (quality 6, 100 MiB decompression cap)
    ```

    **Getting-started tutorial (per D-15):**

    1. **Add Observability section** before the "Next Steps" section (before line 427). Insert:

    ```markdown
    ## Monitoring with Prometheus

    For operators running chromatindb nodes, the built-in Prometheus endpoint
    exposes health metrics for automated monitoring.

    Enable it in your node's config.json:

    ```json
    {
      "metrics_bind": "127.0.0.1:9090"
    }
    ```

    Then configure Prometheus to scrape it:

    ```yaml
    scrape_configs:
      - job_name: 'chromatindb'
        static_configs:
          - targets: ['127.0.0.1:9090']
    ```

    Available metrics include peer count, blob count, storage bytes, sync
    rounds, ingestion counters, and uptime. See
    [PROTOCOL.md](../../db/PROTOCOL.md) for the full list.

    The endpoint is disabled by default (empty `metrics_bind`) and listens on
    localhost only when enabled. Reload with SIGHUP to toggle without restart.
    ```

    2. **Add compression note** in the Encrypted blobs section (if there is one) or in the Next Steps section. Add a bullet:
    ```markdown
    - Encrypted blobs are automatically Brotli-compressed before encryption for payloads >= 256 bytes (transparent, enabled by default)
    ```
  </action>
  <verify>
    <automated>grep -c "Observability" /home/mika/dev/chromatin-protocol/README.md && grep -c "Brotli" /home/mika/dev/chromatin-protocol/sdk/python/README.md && grep -c "metrics_bind" /home/mika/dev/chromatin-protocol/sdk/python/docs/getting-started.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `### Observability` section header
    - README.md Observability section contains `"metrics_bind": "127.0.0.1:9090"` JSON example
    - README.md Observability section contains `scrape_configs` YAML example
    - README.md Observability section mentions "SIGHUP-reloadable"
    - README.md Architecture > SDK bullet mentions "multi-relay failover" and "Brotli compression"
    - README.md Architecture > Relay bullet mentions "Auto-reconnects"
    - README.md Sync Model mentions "announced namespaces" in the BlobNotify step
    - sdk/python/README.md contains "Brotli" string
    - sdk/python/README.md contains "suite 0x02" or "Suite 0x02"
    - sdk/python/README.md contains "compress=False" mention
    - sdk/python/docs/getting-started.md contains `## Monitoring with Prometheus` section header
    - sdk/python/docs/getting-started.md contains `"metrics_bind": "127.0.0.1:9090"` JSON example
    - sdk/python/docs/getting-started.md contains "Brotli" or "compressed" mention
  </acceptance_criteria>
  <done>README.md has Observability section with Prometheus config, SDK README documents Brotli compression, getting-started tutorial includes metrics setup and compression note. All v2.1.0 features documented.</done>
</task>

</tasks>

<verification>
1. PROTOCOL.md has type 62 in message table: `grep "62.*SyncNamespaceAnnounce" db/PROTOCOL.md`
2. PROTOCOL.md has /metrics section: `grep "Prometheus Metrics Endpoint" db/PROTOCOL.md`
3. README.md has Observability: `grep "Observability" README.md`
4. SDK README has Brotli: `grep "Brotli" sdk/python/README.md`
5. Tutorial has metrics: `grep "metrics_bind" sdk/python/docs/getting-started.md`
</verification>

<success_criteria>
- PROTOCOL.md documents SyncNamespaceAnnounce (type 62) with wire format table and full semantics
- PROTOCOL.md documents BlobNotify namespace filtering behavior
- PROTOCOL.md documents /metrics endpoint with config, metric list, and Prometheus scrape example
- README.md has Observability section, updated feature descriptions
- SDK README documents transparent Brotli compression for encrypted blobs
- Getting-started tutorial includes metrics configuration and compression note
</success_criteria>

<output>
After completion, create `.planning/phases/90-observability-documentation/90-02-SUMMARY.md`
</output>
