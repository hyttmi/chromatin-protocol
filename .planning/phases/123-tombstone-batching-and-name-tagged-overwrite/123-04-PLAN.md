---
phase: 123
plan: 04
type: execute
wave: 4
depends_on: [1, 2, 3]
files_modified:
  - db/tests/peer/test_list_by_magic.cpp
  - db/CMakeLists.txt
autonomous: true
requirements:
  - SC#1  # NAME magic integration end-to-end
  - SC#4  # cdb get enumeration via ListRequest type_filter (validated at transport layer)
  - SC#7  # no regression to Phase 122 ingest or sync paths
tags: [integration, transport, list_by_magic, regression]

must_haves:
  truths:
    - "db/tests/peer/test_list_by_magic.cpp proves Phase 117's existing ListRequest + type_filter correctly returns NAME blobs when filtered by NAME_MAGIC prefix"
    - "Test ingests a mix of blob types (content, NAME, BOMB, TOMB, DELEG) then issues ListRequest(ns, type_filter=NAME_MAGIC) and asserts only NAME-typed entries come back"
    - "Test proves the same for BOMB_MAGIC (filter returns only BOMB entries)"
    - "No new TransportMsgType was added anywhere in this phase — `git diff db/schemas/transport.fbs` returns empty for Phase 123 changes"
    - "No FlatBuffers schema touched — `git diff db/schemas/` returns empty for Phase 123 changes"
    - "Phase 122 regression: `[phase122]` subset still passes after all Phase 123 edits (user runs `./build/db/chromatindb_tests '[phase122]'` and pastes green output as the final sign-off)"
    - "Full suite runs green end-to-end: user runs `./build/db/chromatindb_tests` and pastes totals showing 0 failures"
  artifacts:
    - path: "db/tests/peer/test_list_by_magic.cpp"
      provides: "[phase123][transport][list] TEST_CASEs proving existing Phase 117 ListRequest + type_filter handles NAME and BOMB magic prefixes correctly"
    - path: "db/CMakeLists.txt"
      provides: "Registration of test_list_by_magic.cpp if existing GLOB does not cover it"

<threat_model>
Phase 123 Plan 04 threat surface is validation/regression — no new production code added. The plan proves:

- **T-123-07 (Encrypted blob bodies not leaked via ListRequest):** seq_map is plaintext `[hash:32][type:4]` per RESEARCH.md Summary. ListRequest returns only those index entries — no blob payload. Test verifies response structure matches Phase 117's documented response shape.
- **Phase 122 regression:** All PUBK-first / BlobWrite / owner_pubkeys tests must still pass. No mitigations to add — this is a verification task. Any regression surfaces as a test failure; if the failure is in Phase 122 territory, Phase 123 has made an unintended change.
</threat_model>

<tasks>

<task id="123-04-01">
  <objective>Create `db/tests/peer/test_list_by_magic.cpp` proving Phase 117's ListRequest + type_filter correctly enumerates NAME and BOMB blobs.</objective>
  <read_first>
    - db/peer/message_dispatcher.cpp:486-499, 520-549 (ListRequest + type_filter handling)
    - db/storage/storage.cpp:463-471 and :875-933 (seq_map indexing, prefix-filter cursor pattern)
    - db/tests/test_helpers.h (make_name_blob, make_bomb_blob from Plan 01; make_signed_blob / make_tombstone_blob / make_delegation_blob existing)
    - db/tests/peer/ (existing peer tests for structural template — reuse fixture shape, e.g. test_peer_manager.cpp or test_blob_push.cpp)
    - .planning/phases/117-blob-type-indexing-ls-filtering/117-*-SUMMARY.md if present (for context on what Phase 117's type_filter returns)
  </read_first>
  <action>
    Create `db/tests/peer/test_list_by_magic.cpp` with at least these TEST_CASEs:

    1. `TEST_CASE("ListRequest type_filter=NAME_MAGIC returns only NAME blobs", "[phase123][transport][list]")`:
       - Setup: one namespace. Register owner PUBK. Ingest 4 blob types: one content blob (`make_signed_blob`), one NAME blob (`make_name_blob`), one BOMB blob (`make_bomb_blob`, count=0 to avoid side-effects), one tombstone (`make_tombstone_blob`).
       - Issue `ListRequest(ns, type_filter=NAME_MAGIC)` via whatever test-level harness exists (mirror existing peer tests for the send path).
       - REQUIRE the response contains exactly 1 entry, and that entry's hash matches the NAME blob's content_hash.

    2. `TEST_CASE("ListRequest type_filter=BOMB_MAGIC returns only BOMB blobs", "[phase123][transport][list]")`:
       - Same setup as (1). Issue `ListRequest(ns, type_filter=BOMB_MAGIC)`. REQUIRE exactly 1 entry, matching the BOMB blob.

    3. `TEST_CASE("ListRequest type_filter=NAME_MAGIC returns zero entries when no NAME blobs exist", "[phase123][transport][list]")`:
       - Setup: namespace with only content blobs and tombstones. Issue ListRequest(ns, type_filter=NAME_MAGIC). REQUIRE empty response.

    4. (Optional, if ListRequest supports no filter) `TEST_CASE("ListRequest without type_filter returns all blob types", "[phase123][transport][list]")`:
       - Setup as (1). Issue ListRequest(ns) with no filter. REQUIRE ≥4 entries.

    **Harness choice (pre-resolved from plan-checker feedback):** If the existing test infrastructure does not have a simple in-process ListRequest harness, the test drops to the Storage layer: call `storage_.get_blob_refs_since(ns, /*since_seq=*/0, /*max=*/1000)` (Phase 117 API — returns `std::vector<BlobRef>` with each entry carrying `blob_type` as a 4-byte array) and filter the result in-memory by comparing `ref.blob_type` against `wire::NAME_MAGIC` / `wire::BOMB_MAGIC`. This still proves D-10's behavioral contract (correct prefix-filter enumeration) and the transport layer is implicitly exercised by Phase 117's existing dispatcher tests. Prefer the transport harness if one exists — grep `db/tests/peer/` for `ListRequest` usage — otherwise use `get_blob_refs_since` + in-memory filter.

    Check whether the existing GLOB in db/CMakeLists.txt picks up `db/tests/peer/test_list_by_magic.cpp` — if not, add an explicit entry.
  </action>
  <acceptance_criteria>
    - `test -f db/tests/peer/test_list_by_magic.cpp` returns 0.
    - `grep -c "TEST_CASE" db/tests/peer/test_list_by_magic.cpp` returns ≥3.
    - `grep -q "NAME_MAGIC\\|BOMB_MAGIC" db/tests/peer/test_list_by_magic.cpp` succeeds.
    - `grep -q "\\[phase123\\]\\[transport\\]\\[list\\]" db/tests/peer/test_list_by_magic.cpp` succeeds.
    - `cmake --build build -j$(nproc) --target chromatindb_tests` exits 0.
  </acceptance_criteria>
  <done>
    Test file exists, compiles, 3+ TEST_CASEs cover NAME and BOMB enumeration via existing ListRequest + type_filter. Ask user: "Please run `./build/db/chromatindb_tests '[phase123][transport][list]' --reporter compact` and paste the result."
  </done>
</task>

<task id="123-04-02">
  <objective>Final regression + schema-purity gates. Document in SUMMARY.</objective>
  <read_first>
    - .planning/phases/123-tombstone-batching-and-name-tagged-overwrite/ (all prior plan SUMMARY.md files 01/02/03)
    - .planning/phases/123-tombstone-batching-and-name-tagged-overwrite/123-VALIDATION.md (for the full 16-anchor map — confirm all anchors are green)
    - db/schemas/ (confirm no changes)
  </read_first>
  <action>
    Run the purity gates and capture outputs into `123-04-SUMMARY.md`:

    1. Schema purity: `git diff db/schemas/ | wc -l` should be 0 — no FlatBuffers changes in Phase 123.
    2. TransportMsgType purity: `git log --oneline --since='<phase start>' --all | xargs -I{} git show {} -- db/schemas/transport.fbs | grep "TransportMsgType" | wc -l` should be 0 (no commits touched the enum).
    3. No duplicate payload construction: `grep -rn "0x4E.*0x41.*0x4D.*0x45\\|0x42.*0x4F.*0x4D.*0x42" db/ cli/ --include='*.cpp' --include='*.h' | grep -v "codec\\.h\\|codec\\.cpp\\|wire\\.h\\|wire\\.cpp" | wc -l` should be 0 (magic bytes only appear in the codec + wire headers; not duplicated in commands.cpp, engine.cpp, or tests).
    4. Regression directive: user runs `./build/db/chromatindb_tests "[phase122]"` and pastes totals. Must show 0 failures.
    5. Regression directive: user runs full suite `./build/db/chromatindb_tests` and pastes totals. Must show 0 failures.
    6. VALIDATION.md anchor map: each of the 16 anchors from `123-VALIDATION.md` must have a referenced test file that exists. Grep for each anchor tag; confirm presence. If any anchor is unattached, surface it in SUMMARY.

    Per `feedback_delegate_tests_to_user.md`, the orchestrator does not run the suite — the user does. Plan 04 outputs the commands and collects the results in the SUMMARY.
  </action>
  <acceptance_criteria>
    - SUMMARY.md is created and committed.
    - All 3 automated purity gates (schema, transport, duplicate-magic) pass automatically (no user input).
    - SUMMARY.md contains the user-run command strings for regression + full-suite with explicit "paste result here" placeholders.
    - SUMMARY.md lists all 16 VALIDATION.md anchors and confirms their test-file existence.
  </acceptance_criteria>
  <done>
    123-04-SUMMARY.md committed. Automated gates green. User-run directives documented. Ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<verification>
- Automated gates (Task 02): schema purity + TransportMsgType purity + no-duplicate-magic-bytes all pass.
- User-run verification directives clearly stated in SUMMARY.md with command strings.
- All 16 VALIDATION.md test anchors have an existing test file (any unattached anchor blocks phase close).
- `git diff db/schemas/` is empty from Phase 123 start.
</verification>

<success_criteria>
- test_list_by_magic.cpp exercises ListRequest + type_filter for NAME + BOMB prefix enumeration.
- Zero FlatBuffers / TransportMsgType changes in Phase 123.
- Magic bytes only in codec.h/wire.h — no duplication.
- All 16 VALIDATION.md anchors attached to test files.
- Phase 122 regression verified green (by user).
- Full suite verified green (by user).
</success_criteria>

<output>
`.planning/phases/123-tombstone-batching-and-name-tagged-owrite/123-04-SUMMARY.md` (note typo — fix path to `overwrite` as in the directory name) documenting:
- Regression gate results (placeholders for user-pasted totals).
- Purity gate results (automated).
- 16-anchor map with file references.
- Phase-level closeout: "Phase 123 ready for /gsd-verify-work. CLI E2E deferred to Phase 124 per CONTEXT.md <domain>. Live-node deploy post-124 per user preference."
</output>
