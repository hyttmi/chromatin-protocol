---
phase: 74-packaging-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/PROTOCOL.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "PROTOCOL.md HKDF salt field says empty salt, not SHA3-256(pubkeys)"
    - "PROTOCOL.md has an SDK Client Notes section documenting nonce start, Pong behavior, endianness, and exception hierarchy"
    - "No remnant of the incorrect salt description remains in the PQ handshake section"
  artifacts:
    - path: "db/PROTOCOL.md"
      provides: "Corrected protocol documentation with SDK notes"
      contains: "salt   = (empty)"
  key_links:
    - from: "db/PROTOCOL.md"
      to: "sdk/python/chromatindb/_handshake.py"
      via: "HKDF salt documentation matches implementation"
      pattern: "empty.*salt|salt.*empty"
---

<objective>
Fix the HKDF salt discrepancy in PROTOCOL.md and add an SDK Client Notes section documenting protocol implementation details that SDK developers need to know.

Purpose: The HKDF salt documentation says SHA3-256(pubkeys) but the C++ implementation uses an empty salt. SDK developers reading PROTOCOL.md will implement the wrong thing without this fix. The SDK Client Notes section captures hard-won implementation knowledge from Phases 70-73.
Output: Corrected db/PROTOCOL.md with accurate HKDF salt and new SDK Client Notes section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-packaging-documentation/74-CONTEXT.md
@db/PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix HKDF salt discrepancy in PQ handshake section</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    db/PROTOCOL.md
  </read_first>
  <action>
Fix the HKDF salt discrepancy in db/PROTOCOL.md (per D-10). There are exactly two occurrences to fix:

**Occurrence 1 (line 69, in the ASCII handshake diagram):**
Change:
```
    |     salt   = SHA3-256(initiator_signing_pubkey || responder_signing_pubkey)
```
To:
```
    |     salt   = (empty)
```

**Occurrence 2 (line 91, in the prose paragraph after the diagram):**
Change the sentence:
```
The HKDF salt is `SHA3-256(initiator_signing_pubkey || responder_signing_pubkey)`. At this point, the initiator does not yet know the responder's signing pubkey, so the responder's portion is initially unknown and filled in after the auth exchange.
```
To:
```
The HKDF salt is empty (zero-length). The shared secret from ML-KEM-1024 provides sufficient entropy as the sole IKM input.
```

The old sentence about "initially unknown and filled in after the auth exchange" is misleading (it described a workaround for the incorrect salt design that was never implemented). Remove it entirely.

Do NOT modify the Lightweight Handshake (Trusted Peers) section -- line 111 uses ikm (not salt) and is correct as-is.

Do NOT modify any other sections of PROTOCOL.md.
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && grep -n 'SHA3-256(initiator_signing_pubkey' db/PROTOCOL.md | wc -l | awk '{if ($1 == 0) print "old salt removed: OK"; else {print "FAIL: old salt still present ("$1" occurrences)"; exit 1}}' && grep -c 'salt.*= (empty)' db/PROTOCOL.md | awk '{if ($1 >= 1) print "new salt in diagram: OK"; else {print "FAIL: new salt not found"; exit 1}}' && grep -c 'HKDF salt is empty' db/PROTOCOL.md | awk '{if ($1 >= 1) print "new salt in prose: OK"; else {print "FAIL: new salt prose not found"; exit 1}}' && grep -c 'initially unknown' db/PROTOCOL.md | awk '{if ($1 == 0) print "misleading text removed: OK"; else {print "FAIL: misleading text still present"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of "SHA3-256(initiator_signing_pubkey" in db/PROTOCOL.md (grep returns 0)
    - At least 1 occurrence of 'salt   = (empty)' in the handshake diagram
    - At least 1 occurrence of "HKDF salt is empty" in prose
    - Zero occurrences of "initially unknown" (misleading sentence removed)
    - Line 111 (Trusted Peers ikm) is unchanged -- still contains "SHA3-256(initiator_pubkey || responder_pubkey)" as ikm (not salt)
    - The rest of the handshake diagram (KemPubkey, KemCiphertext, AuthSignature, info strings) is unchanged
  </acceptance_criteria>
  <done>HKDF salt in PROTOCOL.md matches C++ implementation (empty salt) in both the diagram and prose, with no remnant of the incorrect SHA3-256(pubkeys) description</done>
</task>

<task type="auto">
  <name>Task 2: Add SDK Client Notes section to PROTOCOL.md</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    db/PROTOCOL.md
  </read_first>
  <action>
Add an "SDK Client Notes" section to db/PROTOCOL.md (per D-11). Insert it as a new top-level section (## heading) at the end of the file, before any trailing whitespace.

The section documents implementation details that SDK developers need to know, discovered during Python SDK development (Phases 70-73):

```markdown
## SDK Client Notes

Implementation details for SDK developers connecting to chromatindb via relay.

### AEAD Nonce Counters

Post-handshake AEAD nonce counters start at 1 in both directions, not 0. Nonce 0 is consumed by the AuthSignature messages (Message 3 and Message 4) during the PQ handshake. Each direction maintains an independent counter.

### Ping/Pong Behavior

The C++ relay sends Pong responses with `request_id = 0`, regardless of the request_id sent in the Ping. SDK implementations should use a dedicated send path for Ping that does not rely on request_id correlation. A FIFO pending queue for Pong responses works well.

### Wire Format Endianness

The protocol uses mixed endianness:
- **Big-endian:** Frame length prefix (4 bytes), most wire format fields
- **Little-endian:** Auth payload `pubkey_size` field (4 bytes), canonical signing input fields `ttl` (4 bytes) and `timestamp` (8 bytes)

SDK implementations must encode/decode each field with the correct byte order.

### Exception Hierarchy

The Python SDK defines `chromatindb.exceptions.ConnectionError` which inherits from `ProtocolError`, not from Python's builtin `ConnectionError`. This is intentional -- SDK connection errors are protocol-level events, not OS-level socket errors. Code that catches `builtins.ConnectionError` will not catch SDK connection timeouts.

### FlatBuffers Determinism

FlatBuffers serialization is not deterministic across languages. The same logical message may produce different bytes in Python vs C++. Always use the server-returned `blob_hash` (from WriteAck) rather than computing it client-side from the serialized FlatBuffer.

### ML-DSA-87 Signature Non-determinism

ML-DSA-87 signatures are non-deterministic -- signing the same data twice produces different signatures. Since the FlatBuffer blob includes the signature, this means writing identical data produces a different `blob_hash` each time. SDK tests should verify individual fields (data, ttl, timestamp) rather than comparing serialized bytes.
```

Do NOT modify any existing sections. Append only.
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && grep -c '## SDK Client Notes' db/PROTOCOL.md | awk '{if ($1 == 1) print "section exists: OK"; else {print "FAIL: section count is "$1; exit 1}}' && grep -q 'nonce counters start at 1' db/PROTOCOL.md && echo "nonce note: OK" && grep -q 'request_id = 0' db/PROTOCOL.md && echo "pong note: OK" && grep -q 'Big-endian.*Frame length' db/PROTOCOL.md && echo "endianness note: OK" && grep -q 'inherits from.*ProtocolError.*not.*builtin' db/PROTOCOL.md && echo "exception note: OK" && grep -q 'FlatBuffers serialization is not deterministic' db/PROTOCOL.md && echo "flatbuffers note: OK" && grep -q 'ML-DSA-87 signatures are non-deterministic' db/PROTOCOL.md && echo "signature note: OK"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 occurrence of "## SDK Client Notes" heading in db/PROTOCOL.md
    - Contains "nonce counters start at 1" (AEAD nonce note per D-11)
    - Contains "request_id = 0" (Pong behavior note per D-11)
    - Contains "Big-endian" and "Little-endian" (endianness note per D-11)
    - Contains "inherits from" and "ProtocolError" and "builtin" (exception hierarchy note per D-11)
    - Contains "FlatBuffers serialization is not deterministic" (FlatBuffers note)
    - Contains "ML-DSA-87 signatures are non-deterministic" (signature note)
    - Section is at the end of the file (no existing sections modified)
  </acceptance_criteria>
  <done>PROTOCOL.md has SDK Client Notes section with all 6 implementation notes covering nonce counters, Pong behavior, endianness, exception hierarchy, FlatBuffers determinism, and ML-DSA-87 non-determinism</done>
</task>

</tasks>

<verification>
1. `grep 'SHA3-256(initiator_signing_pubkey' db/PROTOCOL.md` returns empty (old salt gone)
2. `grep 'salt.*= (empty)' db/PROTOCOL.md` finds the corrected diagram
3. `grep 'HKDF salt is empty' db/PROTOCOL.md` finds the corrected prose
4. `grep '## SDK Client Notes' db/PROTOCOL.md` finds exactly 1 heading
5. All 6 SDK client notes present (nonce, pong, endianness, exception, flatbuffers, signature)
</verification>

<success_criteria>
PROTOCOL.md accurately documents the HKDF empty salt (matching C++ implementation) and includes a comprehensive SDK Client Notes section that prevents future SDK implementers from hitting the same protocol edge cases discovered during Python SDK development.
</success_criteria>

<output>
After completion, create `.planning/phases/74-packaging-documentation/74-02-SUMMARY.md`
</output>
