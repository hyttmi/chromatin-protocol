---
phase: 25-transport-optimization
plan: 01
subsystem: config, transport
tags: [asio, flatbuffers, ip-validation, sighup]

requires:
  - phase: 24-encryption-at-rest
    provides: stable codebase foundation
provides:
  - trusted_peers config field with JSON parsing and IP validation
  - TrustedHello=24 and PQRequired=25 transport message types
  - PeerManager is_trusted_address() for IP-based trust checks
  - SIGHUP reload of trusted_peers with validation
affects: [25-02, connection, handshake]

tech-stack:
  added: []
  patterns:
    - "validate_trusted_peers: Asio IP parsing for config validation (no regex)"
    - "Canonical IP storage: parse to asio::ip::address then to_string() for matching"

key-files:
  created: []
  modified:
    - db/config/config.h
    - db/config/config.cpp
    - db/schemas/transport.fbs
    - db/wire/transport_generated.h
    - db/peer/peer_manager.h
    - db/peer/peer_manager.cpp
    - tests/config/test_config.cpp

key-decisions:
  - "Use asio::ip::make_address() for IP validation instead of regex -- handles IPv4, IPv6, and mapped addresses correctly"
  - "Port detection heuristic: check for IPv6 bracket-colon pattern and IPv4 trailing colon-digits pattern to give specific error messages"
  - "Store trusted_peers as canonical strings via addr.to_string() for reliable matching"

patterns-established:
  - "validate_trusted_peers: parse with Asio, reject on parse failure with port-aware error messages"

requirements-completed: [TOPT-03]

duration: 15min
completed: 2026-03-15
---

# Phase 25 Plan 01: Config Infrastructure + Transport Schema Summary

**trusted_peers config field with IP validation, TrustedHello/PQRequired transport types, and PeerManager trust-check infrastructure**

## Performance

- **Duration:** 15 min
- **Started:** 2026-03-15T04:23:00Z
- **Completed:** 2026-03-15T04:38:33Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Config struct has trusted_peers field, parsed from JSON arrays with full IP validation
- validate_trusted_peers rejects ports (with helpful error mentioning port removal) and non-IP strings
- TransportMsgType enum extended with TrustedHello=24 and PQRequired=25
- PeerManager stores trusted_peers set, initializes from config with canonical IP form
- is_trusted_address checks loopback (covers IPv4 127.0.0.1 and IPv6 ::1) and configured trusted IPs
- SIGHUP reload updates trusted_peers with validation-first pattern
- 10 new test cases (16 assertions) for trusted_peers, all passing
- 307 total tests, 1174 assertions, zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Add trusted_peers to Config and transport schema types** - `00be93b` (feat, TDD)
2. **Task 2: Add trusted_peers SIGHUP reload and is_trusted_address to PeerManager** - `bc86074` (feat)

## Files Created/Modified
- `db/config/config.h` - Added trusted_peers field and validate_trusted_peers declaration
- `db/config/config.cpp` - Added validate_trusted_peers() with Asio IP parsing and JSON parsing for trusted_peers
- `db/schemas/transport.fbs` - Added TrustedHello=24 and PQRequired=25 enum entries
- `db/wire/transport_generated.h` - Regenerated by flatc
- `db/peer/peer_manager.h` - Added trusted_peers_ set and is_trusted_address() method
- `db/peer/peer_manager.cpp` - Added constructor init, is_trusted_address impl, reload_config extension
- `tests/config/test_config.cpp` - Added 10 test cases for trusted_peers parsing and validation

## Decisions Made
- Used asio::ip::make_address() for IP validation instead of regex -- handles IPv4, IPv6, and mapped addresses correctly
- Port detection uses heuristic: bracket-colon for IPv6 ports, trailing colon-digits for IPv4 ports
- Canonical IP form via addr.to_string() ensures matching works for IPv4-mapped IPv6

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Config infrastructure complete, ready for Plan 02 (lightweight handshake protocol)
- TrustedHello and PQRequired message types available for handshake branching
- is_trusted_address() ready for Connection to call via trust-check function

---
*Phase: 25-transport-optimization*
*Completed: 2026-03-15*
