---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/config.h
  - src/config/config.cpp
  - tests/config/test_config.cpp
autonomous: true
requirements: [DAEM-01]
gap_closure: true

must_haves:
  truths:
    - "TTL is a protocol constant (604800 seconds / 7 days), not user-configurable"
    - "Config JSON files with a default_ttl key are ignored (field not loaded)"
    - "All existing tests pass after the change"
  artifacts:
    - path: "src/config/config.h"
      provides: "Protocol constant for blob TTL"
      contains: "constexpr.*604800"
    - path: "src/config/config.h"
      provides: "Config struct without default_ttl field"
    - path: "src/config/config.cpp"
      provides: "Config loading without TTL parsing"
    - path: "tests/config/test_config.cpp"
      provides: "Tests validating TTL is not configurable"
  key_links:
    - from: "src/config/config.h"
      to: "wire/codec.h"
      via: "protocol constant used when creating blobs"
      pattern: "BLOB_TTL_SECONDS"
---

<objective>
Close UAT gap: Make TTL a protocol-enforced constant instead of a user-configurable setting.

Purpose: User reported that TTL should be strict (7-day for data blobs), not overridable via config. TTL is a protocol invariant, not a preference.
Output: Config struct without default_ttl field, new constexpr protocol constant, updated tests.
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-UAT.md
@.planning/debug/ttl-configurable-should-be-enforced.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make TTL a protocol constant and remove from Config</name>
  <files>src/config/config.h, src/config/config.cpp, tests/config/test_config.cpp</files>
  <action>
1. In `src/config/config.h`:
   - Add a protocol constants section ABOVE the Config struct:
     ```cpp
     /// Protocol constants (not user-configurable).
     constexpr uint32_t BLOB_TTL_SECONDS = 604800;  // 7 days
     ```
   - Remove the `uint32_t default_ttl = 604800;` field from the Config struct (line 16).

2. In `src/config/config.cpp`:
   - Remove line 30: `cfg.default_ttl = j.value("default_ttl", cfg.default_ttl);`
   - No other changes needed. The JSON parser will simply ignore any "default_ttl" key in the config file.

3. In `tests/config/test_config.cpp`:
   - In "Default config has sensible values" test: Remove the `REQUIRE(cfg.default_ttl == 604800);` assertion (line 15). Add a new assertion: `REQUIRE(chromatin::config::BLOB_TTL_SECONDS == 604800);` to verify the constant exists.
   - In "load_config with non-existent file returns defaults" test: Remove `REQUIRE(cfg.default_ttl == 604800);` (line 26).
   - In "load_config with valid JSON populates all fields" test: Remove `"default_ttl": 86400,` from the JSON string (line 39). Remove `REQUIRE(cfg.default_ttl == 86400);` (line 52).
   - In "load_config with partial JSON uses defaults for missing fields" test: Remove `REQUIRE(cfg.default_ttl == 604800);` (line 73).
   - In "parse_args with --config loads from file" test: Remove `"default_ttl": 3600` from the JSON string (line 94). Remove `REQUIRE(cfg.default_ttl == 3600);` (line 101).
   - Add a new test case:
     ```cpp
     TEST_CASE("BLOB_TTL_SECONDS is a protocol constant", "[config]") {
         REQUIRE(chromatin::config::BLOB_TTL_SECONDS == 604800);  // 7 days
         // Verify it's not part of Config struct (compile-time guarantee)
         // The fact that Config has no default_ttl field means it can't be overridden
     }
     ```

4. Build and run all tests to confirm nothing breaks: `cmake --build build -j$(nproc) && ctest --test-dir build --output-on-failure`
  </action>
  <verify>
    <automated>cmake --build build -j$(nproc) 2>&1 | tail -3 && ctest --test-dir build --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Config struct has no default_ttl field (compile error if any code tries cfg.default_ttl)
  - BLOB_TTL_SECONDS constexpr exists in chromatin::config namespace with value 604800
  - Config JSON loading ignores any "default_ttl" key silently
  - All tests pass (count should remain 64 total since we replace TTL-override assertions with constant assertion)
  </done>
</task>

</tasks>

<verification>
- `cmake --build build -j$(nproc)` compiles cleanly (no errors, no warnings about TTL)
- `ctest --test-dir build --output-on-failure` shows all tests passing
- `grep -n "default_ttl" src/config/config.h src/config/config.cpp` returns no matches
- `grep -n "BLOB_TTL_SECONDS" src/config/config.h` shows the constexpr definition
</verification>

<success_criteria>
TTL is a compile-time protocol constant. No runtime configuration can change it. All 64 tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
