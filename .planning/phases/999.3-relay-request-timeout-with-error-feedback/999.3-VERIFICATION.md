---
phase: 999.3-relay-request-timeout-with-error-feedback
verified: 2026-04-11T20:24:45Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 999.3: Relay Request Timeout with Error Feedback — Verification Report

**Phase Goal:** RequestRouter lets requests sit pending 60s before silent cleanup. Add configurable per-request timeout (e.g. 10s) that sends `{"type": "error", "code": "timeout"}` to the client when the node doesn't respond.
**Verified:** 2026-04-11T20:24:45Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

Plan 01 truths (TIMEOUT-01 through TIMEOUT-04):

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | PendingRequest stores original_type byte alongside session and request IDs | VERIFIED | `request_router.h` line 16: `uint8_t original_type = 0;` in PendingRequest struct; `request_router.cpp` lines 5-14: stored at registration |
| 2 | purge_stale accepts an optional callback invoked per stale entry before erasure | VERIFIED | `request_router.h` lines 40-42: two overloads; `request_router.cpp` lines 43-64: callback invoked in erase_if, delegates to nullptr when omitted |
| 3 | Error code 6 (timeout) is defined in error_codes.h and mapped in translator | VERIFIED | `error_codes.h` line 15: `constexpr uint8_t ERROR_TIMEOUT = 0x06;`, line 26: case in error_code_string; `translator.cpp`: `{6, "timeout"}` in ERROR_CODES array |
| 4 | RelayConfig has request_timeout_seconds field with default 10 and correct parsing | VERIFIED | `relay_config.h` line 24: `uint32_t request_timeout_seconds = 10;`; `relay_config.cpp` line 55: `j.value("request_timeout_seconds", ...)` |

Plan 02 truths (TIMEOUT-05 through TIMEOUT-07):

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 5 | Stale requests receive a JSON error with type=error, code=timeout, and original_type before cleanup | VERIFIED | `uds_multiplexer.cpp` lines 636-661: send_timeout_error builds `{type:error, code:timeout, original_type:<name>, request_id:<N>}` and sends via session->send_json |
| 6 | Cleanup interval is dynamically computed as timeout/2 (min 1s) when timeout enabled, 60s when disabled | VERIFIED | `uds_multiplexer.cpp` lines 698-731: `std::max(timeout_sec / 2, 1u)` when enabled, `60u` when disabled; threshold matches |
| 7 | request_timeout_seconds is SIGHUP-reloadable via shared atomic | VERIFIED | `relay_main.cpp` line 204: `std::atomic<uint32_t> request_timeout{cfg.request_timeout_seconds}`; line 360: SIGHUP store; line 233: `uds_mux.set_request_timeout(&request_timeout)` |
| 8 | errors_total and request_timeouts_total Prometheus counters increment on timeout | VERIFIED | `uds_multiplexer.cpp` lines 657-659: both `errors_total` and `request_timeouts_total` incremented; `metrics_collector.h` line 25: counter declared; `metrics_collector.cpp` lines 203-206: Prometheus exposition |
| 9 | Session-gone case is handled gracefully (no crash when client disconnects before timeout fires) | VERIFIED | `uds_multiplexer.cpp` line 638: `if (!session) return;` guard before any send |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact | Purpose | Status | Evidence |
|----------|---------|--------|---------|
| `db/peer/error_codes.h` | ERROR_TIMEOUT constant | VERIFIED | `ERROR_TIMEOUT = 0x06` at line 15; `case ERROR_TIMEOUT: return "timeout"` at line 26 |
| `relay/core/request_router.h` | Extended PendingRequest + callback purge_stale | VERIFIED | `original_type` field at line 16; 3-arg `register_request` at line 28; callback overload at lines 40-41 |
| `relay/core/request_router.cpp` | Implementation | VERIFIED | `original_type` stored at line 13; callback invoked at lines 50-52; no-callback overload delegates at line 63 |
| `relay/config/relay_config.h` | request_timeout_seconds config field | VERIFIED | `uint32_t request_timeout_seconds = 10` at line 24 |
| `relay/config/relay_config.cpp` | JSON parsing | VERIFIED | `j.value("request_timeout_seconds", cfg.request_timeout_seconds)` at line 55 |
| `relay/translate/translator.cpp` | Timeout error code mapping | VERIFIED | `{6, "timeout"}` in ERROR_CODES array at line 723 |
| `relay/core/uds_multiplexer.h` | set_request_timeout/set_metrics setters, send_timeout_error | VERIFIED | All three methods declared (lines 46, 49, 92); private members at lines 117, 120 |
| `relay/core/uds_multiplexer.cpp` | Dynamic cleanup_loop + send_timeout_error implementation | VERIFIED | send_timeout_error at lines 636-661; cleanup_loop at lines 698-731 |
| `relay/core/metrics_collector.h` | request_timeouts_total counter | VERIFIED | `std::atomic<uint64_t> request_timeouts_total{0}` at line 25 |
| `relay/core/metrics_collector.cpp` | Prometheus exposition | VERIFIED | `chromatindb_relay_request_timeouts_total` counter block at lines 203-206 |
| `relay/relay_main.cpp` | Atomic declaration, UdsMultiplexer wiring, SIGHUP reload | VERIFIED | `std::atomic<uint32_t> request_timeout` at line 204; `set_request_timeout` wired at line 233; SIGHUP store at line 360 |
| `relay/tests/test_request_router.cpp` | 5 new tests for original_type and callback behavior | VERIFIED | 5 TEST_CASEs starting at lines 203, 213, 244, 265, 279 |
| `relay/tests/test_relay_config.cpp` | 4 new tests for request_timeout_seconds | VERIFIED | 4 TEST_CASEs: defaults-to-10, parsed-from-JSON, 0-valid, 1-valid |
| `relay/ws/ws_session.cpp` | 3-arg register_request call passes wire_type | VERIFIED | Line 590: `router_->register_request(session_id_, client_rid, wire_type)` |

---

### Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|---------|
| `relay/core/request_router.h` | `db/peer/error_codes.h` | ERROR_TIMEOUT constant | WIRED | `error_codes.h` defines `ERROR_TIMEOUT = 0x06`; used in router tests |
| `relay/translate/translator.cpp` | `relay/translate/type_registry.h` | type_to_string in decode_error_response | WIRED | `type_to_string` called in decode_error_response |
| `relay/core/uds_multiplexer.cpp` | `relay/core/request_router.h` | purge_stale callback with PendingRequest | WIRED | `router_.purge_stale(threshold, [this](const PendingRequest& pending) { send_timeout_error(pending); })` at lines 719-722 |
| `relay/core/uds_multiplexer.cpp` | `relay/ws/session_manager.h` | get_session for sending timeout error JSON | WIRED | `sessions_.get_session(pending.client_session_id)` at line 637 |
| `relay/core/uds_multiplexer.cpp` | `relay/translate/type_registry.h` | type_to_string for original_type name in JSON | WIRED | `translate::type_to_string(pending.original_type)` at line 644 |
| `relay/relay_main.cpp` | `relay/core/uds_multiplexer.h` | set_request_timeout passes atomic pointer | WIRED | `uds_mux.set_request_timeout(&request_timeout)` at line 233 |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies infrastructure code (request routing, cleanup loop, metrics). No components that render user-visible dynamic data from a database.

The data flow that matters is behavioral: client request → register_request (stores original_type) → cleanup_loop fires → purge_stale callback → send_timeout_error → client receives JSON. All links verified above.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full relay test suite (242 tests) | `relay/tests/chromatindb_relay_tests` | All tests passed (2601 assertions in 242 test cases) | PASS |
| request_router tag tests | `relay/tests/chromatindb_relay_tests "[request_router]"` | All tests passed (68 assertions in 16 test cases) | PASS |
| relay_config tag tests | `relay/tests/chromatindb_relay_tests "[relay_config]"` | All tests passed (13 assertions in 11 test cases) | PASS |
| Relay binary compiles | `cmake --build . --target chromatindb_relay` | Build succeeded | PASS |

---

### Requirements Coverage

All 7 requirements are declared in plan frontmatter and present in REQUIREMENTS.md:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| TIMEOUT-01 | Plan 01 | PendingRequest stores original_type byte, register_request accepts type parameter | SATISFIED | `request_router.h`/`.cpp`: field + 3-arg signature |
| TIMEOUT-02 | Plan 01 | purge_stale accepts optional callback invoked per stale entry with full PendingRequest | SATISFIED | Both overloads in `request_router.h`; 5 tests covering all callback behaviors |
| TIMEOUT-03 | Plan 01 | ERROR_TIMEOUT (0x06) defined in error_codes.h and mapped in relay translator | SATISFIED | `error_codes.h` line 15; `translator.cpp` line 723 |
| TIMEOUT-04 | Plan 01 | RelayConfig has request_timeout_seconds field (default 10, 0=disabled, SIGHUP-reloadable) | SATISFIED | `relay_config.h` line 24; `relay_config.cpp` line 55; `relay_main.cpp` SIGHUP at line 360 |
| TIMEOUT-05 | Plan 02 | Cleanup loop interval is dynamic: timeout/2 when enabled, 60s when disabled | SATISFIED | `uds_multiplexer.cpp` cleanup_loop lines 706-716 |
| TIMEOUT-06 | Plan 02 | Stale requests receive JSON error {type:error, code:timeout, original_type:<name>} before cleanup | SATISFIED | `uds_multiplexer.cpp` send_timeout_error lines 636-661 |
| TIMEOUT-07 | Plan 02 | Prometheus errors_total and request_timeouts_total counters increment on timeout | SATISFIED | `metrics_collector.h` line 25; `metrics_collector.cpp` lines 203-206; both incremented in send_timeout_error |

No orphaned requirements found. REQUIREMENTS.md section "Request Timeout (Phase 999.3)" lists exactly TIMEOUT-01 through TIMEOUT-07, all marked `[x]` complete. Traceability table at lines 99-105 maps all 7 to Phase 999.3 with status Complete.

---

### Anti-Patterns Found

No anti-patterns detected.

- No TODO/FIXME/placeholder comments in any modified file
- No stub implementations (return null / return {} / empty handlers)
- No hardcoded empty data flowing to output
- send_timeout_error null-guards the session before use (correct defensive pattern, not a stub)
- purge_stale null-guards the callback before invocation (correct optional behavior, not a stub)

---

### Human Verification Required

None. All behaviors are mechanically verifiable:

- Error code values are constants — verified by grep
- JSON field names are string literals — verified by grep
- Callback invocation is tested by unit tests — verified by test run
- Interval computation formula is deterministic — verified by code reading
- SIGHUP atomic store is wired at the correct callsite — verified by grep

The only remaining question (live end-to-end: does a real client actually receive the timeout JSON after 10s?) is covered by the existing smoke test infrastructure (relay_smoke_test from Phase 106), not a gap requiring this verification report to flag.

---

## Summary

Phase 999.3 achieved its goal. The RequestRouter no longer silently discards stale requests — it now notifies clients with a structured JSON error `{"type":"error","code":"timeout","original_type":"<name>","request_id":<N>}` when their request exceeds the configurable `request_timeout_seconds` threshold (default 10s, 0=disabled). The implementation is:

- **Foundation layer (Plan 01):** ERROR_TIMEOUT constant, PendingRequest.original_type field, callback-based purge_stale overload, request_timeout_seconds config field — all contracts delivered and unit-tested.
- **Integration layer (Plan 02):** Dynamic cleanup_loop interval (timeout/2 min 1s), send_timeout_error helper, SIGHUP-reloadable atomic, Prometheus request_timeouts_total counter — all wired end-to-end.
- **Quality:** 242 relay tests passing (2601 assertions), relay binary compiles clean, no regressions from 3-arg register_request signature change (fixed in Plan 01 execution).

---

_Verified: 2026-04-11T20:24:45Z_
_Verifier: Claude (gsd-verifier)_
