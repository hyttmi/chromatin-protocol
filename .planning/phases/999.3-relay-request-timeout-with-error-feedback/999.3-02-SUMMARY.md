---
phase: 999.3-relay-request-timeout-with-error-feedback
plan: 02
subsystem: relay
tags: [timeout, prometheus, sighup, cleanup-loop, error-feedback]

requires:
  - phase: 999.3-01
    provides: "ERROR_TIMEOUT error code, PendingRequest.original_type, callback purge_stale overload, request_timeout_seconds config field"
provides:
  - "Dynamic cleanup_loop with configurable timeout interval"
  - "send_timeout_error helper sending JSON error to clients on request timeout"
  - "SIGHUP-reloadable request_timeout_seconds via shared atomic"
  - "request_timeouts_total Prometheus counter"
affects: [relay-testing, relay-operations, relay-monitoring]

tech-stack:
  added: []
  patterns:
    - "Atomic pointer injection for SIGHUP-reloadable config (matches rate_limit_rate pattern)"
    - "Dynamic timer interval computed from atomic value each iteration"

key-files:
  created: []
  modified:
    - relay/core/metrics_collector.h
    - relay/core/metrics_collector.cpp
    - relay/core/uds_multiplexer.h
    - relay/core/uds_multiplexer.cpp
    - relay/relay_main.cpp

key-decisions:
  - "Both errors_total and request_timeouts_total incremented on timeout (general + specific tracking)"
  - "Cleanup interval = timeout/2 (min 1s) when enabled, 60s silent purge when disabled"
  - "Forward-declare RelayMetrics struct in uds_multiplexer.h to avoid header coupling"

patterns-established:
  - "set_request_timeout(const atomic*) + set_metrics(RelayMetrics*) setter injection pattern for UdsMultiplexer"

requirements-completed: [TIMEOUT-05, TIMEOUT-06, TIMEOUT-07]

duration: 4min
completed: 2026-04-11
---

# Phase 999.3 Plan 02: Timeout Runtime Wiring Summary

**Dynamic cleanup_loop with SIGHUP-reloadable timeout interval, client error feedback via send_timeout_error, and request_timeouts_total Prometheus counter**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-11T17:16:38Z
- **Completed:** 2026-04-11T17:20:14Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Added request_timeouts_total atomic counter to RelayMetrics and Prometheus text exposition
- Rewrote cleanup_loop to dynamically compute interval from SIGHUP-reloadable atomic (timeout/2 min 1s when enabled, 60s when disabled)
- Implemented send_timeout_error helper that builds JSON error with type, code, original_type, conditional request_id, and increments both errors_total and request_timeouts_total counters
- Wired request_timeout atomic and metrics pointer from relay_main.cpp with SIGHUP reload handler

## Task Commits

Each task was committed atomically:

1. **Task 1: Add request_timeouts_total counter to metrics** - `f05a1d2` (feat)
2. **Task 2: UdsMultiplexer timeout wiring + relay_main SIGHUP** - `2edb6fd` (feat)

## Files Created/Modified
- `relay/core/metrics_collector.h` - Added request_timeouts_total atomic counter to RelayMetrics struct
- `relay/core/metrics_collector.cpp` - Added Prometheus exposition block for request_timeouts_total
- `relay/core/uds_multiplexer.h` - Added set_request_timeout, set_metrics setters, send_timeout_error method, private members
- `relay/core/uds_multiplexer.cpp` - Rewrote cleanup_loop, implemented send_timeout_error and setter methods
- `relay/relay_main.cpp` - Added request_timeout atomic, wired to UdsMultiplexer, SIGHUP reload, startup log

## Decisions Made
- Both errors_total and request_timeouts_total counters increment on timeout -- errors_total for general error dashboards, request_timeouts_total for timeout-specific monitoring
- Forward-declare RelayMetrics in uds_multiplexer.h instead of including metrics_collector.h to keep header coupling minimal (full include only in .cpp)
- send_timeout_error gracefully handles session-gone case with early return (no crash if client disconnects before timeout fires)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Timeout infrastructure fully wired: Plan 01 provided the data model, Plan 02 wired it into runtime
- Ready for Plan 03 (unit tests for timeout behavior) if planned
- All 242 relay tests pass with 2601 assertions (no regressions)

---
## Self-Check: PASSED

All 5 files found, both commit hashes verified, all 9 content checks passed.

---
*Phase: 999.3-relay-request-timeout-with-error-feedback*
*Completed: 2026-04-11*
