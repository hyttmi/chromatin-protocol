# Phase 999.3: Relay Request Timeout with Error Feedback - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-04-11
**Phase:** 999.3-relay-request-timeout-with-error-feedback
**Areas discussed:** Timeout value & config, Error code for timeout, Timeout granularity, Cleanup loop behavior
**Mode:** --auto (all decisions auto-selected)

---

## Timeout Value & Config

| Option | Description | Selected |
|--------|-------------|----------|
| 10s default, configurable, SIGHUP-reloadable | Standard timeout with config field `request_timeout_seconds` | ✓ |
| 30s default | More conservative, closer to current 60s | |
| No default, require explicit config | Forces operator to choose | |

**User's choice:** [auto] 10s default, configurable via `request_timeout_seconds`, SIGHUP-reloadable
**Notes:** 10s is responsive enough for client UX, long enough to survive brief node load spikes

---

## Error Code for Timeout

| Option | Description | Selected |
|--------|-------------|----------|
| New code 6 `timeout` | Distinct from existing 5 codes, clearly communicates cause | ✓ |
| Reuse `internal_error` (5) | Fewer codes but ambiguous | |

**User's choice:** [auto] New code 6 `timeout`
**Notes:** Timeout is semantically different from internal_error -- client can distinguish "node didn't respond" from "node had a bug"

---

## Timeout Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Single global timeout | All request types share one value, simple | ✓ |
| Per-type timeouts | Map of type -> timeout, more flexible | |

**User's choice:** [auto] Single global timeout
**Notes:** No demonstrated need for per-type. Can always add later if needed.

---

## Cleanup Loop Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Interval = timeout/2 | Responsive, catches within ~1.5x timeout | ✓ |
| Interval = timeout | Simpler but could miss by up to 2x | |
| Fixed 10s interval | Decoupled from timeout value | |

**User's choice:** [auto] Interval = timeout/2
**Notes:** Cleanup at half the timeout ensures worst-case latency is 1.5x configured timeout

---

## Claude's Discretion

- Whether to add a dedicated `request_timeouts_total` counter
- Whether to rename `purge_stale` to reflect its new error-sending behavior
- Test strategy (timer mocking vs short timeouts)

## Deferred Ideas

None
