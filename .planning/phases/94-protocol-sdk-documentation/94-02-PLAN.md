---
phase: 94-protocol-sdk-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/python/docs/getting-started.md
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "Tutorial includes a working delegation revocation example"
    - "Tutorial includes a working KEM key rotation example"
    - "Tutorial includes a working group membership management example with removal"
  artifacts:
    - path: "sdk/python/docs/getting-started.md"
      provides: "Tutorial sections for v2.1.1 features"
      contains: "Delegation Revocation"
    - path: "sdk/python/docs/getting-started.md"
      provides: "KEM rotation tutorial"
      contains: "rotate_kem"
    - path: "sdk/python/docs/getting-started.md"
      provides: "Group membership management tutorial"
      contains: "remove_member"
  key_links:
    - from: "sdk/python/docs/getting-started.md"
      to: "new tutorial sections"
      via: "new ## sections before Next Steps"
      pattern: "Delegation Revocation|Key Rotation|Group Membership"
---

<objective>
Add tutorial sections to the SDK getting-started guide for delegation revocation, KEM key rotation, and group membership management (including member removal).

Purpose: New users need working code examples to use the v2.1.1 features. The tutorial is the primary onboarding path for SDK users.
Output: Updated sdk/python/docs/getting-started.md with three new sections matching existing style per D-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-protocol-sdk-documentation/94-CONTEXT.md
@.planning/phases/91-sdk-delegation-revocation/91-01-SUMMARY.md
@.planning/phases/92-kem-key-versioning/92-01-SUMMARY.md
@.planning/phases/92-kem-key-versioning/92-02-SUMMARY.md
@.planning/phases/93-group-membership-revocation/93-01-SUMMARY.md
@.planning/phases/93-group-membership-revocation/93-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delegation revocation, key rotation, and group management tutorial sections</name>
  <files>sdk/python/docs/getting-started.md</files>
  <read_first>
    - sdk/python/docs/getting-started.md (full file -- understand style, structure, insertion point)
    - sdk/python/chromatindb/_directory.py (lines 367-421 for revoke_delegation/list_delegates signatures; lines 505-518 for resolve_recipient; lines 524-620 for create_group/add_member/remove_member)
    - sdk/python/chromatindb/identity.py (lines 421-484 for rotate_kem signature and behavior)
    - sdk/python/chromatindb/client.py (lines 1031-1066 for write_to_group)
    - sdk/python/chromatindb/exceptions.py (for DelegationNotFoundError, NotARecipientError)
  </read_first>
  <action>
    Edit sdk/python/docs/getting-started.md to add three new sections. Per D-02, match existing style: short focused snippets (5-15 lines each), `## Section` headers, `# comment` style in code blocks, minimal text between code. Per D-03, document correct behavior only.

    Insert the following three sections BEFORE the "## Next Steps" section (currently at line 415). The existing "## Monitoring with Prometheus" section ends at line 413, so insert after it.

    **Section 1: Delegation Revocation**

    ```markdown
    ## Delegation Revocation

    Namespace owners can revoke a delegate's write access. Revocation tombstones
    the delegation blob -- the node immediately rejects subsequent writes from the
    revoked delegate.

    ```python
    from chromatindb import ChromatinClient, Identity, Directory
    from chromatindb.exceptions import DelegationNotFoundError

    admin = Identity.load("admin.key")
    delegate = Identity.load("delegate.key")

    async with ChromatinClient.connect("192.168.1.200", 4201, admin) as client:
        directory = Directory(client, admin)

        # Revoke a delegate's write access
        result = await directory.revoke_delegation(delegate)
        print(f"Revoked: tombstone={result.tombstone_hash.hex()}")

        # List remaining active delegates
        delegates = await directory.list_delegates()
        for d in delegates:
            print(f"Active delegate: {d.delegate_pk_hash.hex()}")
    ```

    After revocation, the tombstone propagates to other nodes via sync. Connected
    peers see the revocation near-instantly (via BlobNotify). Disconnected peers
    see it on their next sync round (up to the safety-net interval, default 600s).

    `revoke_delegation()` raises `DelegationNotFoundError` if no active delegation
    exists for the given identity.
    ```

    **Section 2: KEM Key Rotation**

    ```markdown
    ## KEM Key Rotation

    Rotate your ML-KEM-1024 encryption keypair so that a compromised key cannot
    decrypt future data. Old keys are retained in a key ring for decrypting
    historical data.

    ```python
    from chromatindb import ChromatinClient, Identity, Directory

    identity = Identity.load("my_identity.key")

    # Rotate KEM keypair (offline operation, no network needed)
    identity.rotate_kem("my_identity.key")
    print(f"New key version: {identity.key_version}")

    # Publish the new key to the directory
    async with ChromatinClient.connect("192.168.1.200", 4201, identity) as client:
        directory = Directory(client, identity, directory_namespace=admin_ns)
        await directory.register("alice")
    ```

    After rotation:
    - **Senders** who call `resolve_recipient("alice")` get the latest KEM public
      key and encrypt to it automatically.
    - **Decryption** checks all keys in the ring -- data encrypted under old or
      new keys decrypts transparently via `read_encrypted()`.
    - **Key files** are saved as numbered files (`my_identity.kem.0`,
      `my_identity.kem.1`, etc.) alongside the canonical `.kem` file.

    Old keys are never deleted. Discarding a historical key permanently prevents
    decryption of any data encrypted under that key.
    ```

    **Section 3: Group Membership Management**

    ```markdown
    ## Group Membership Management

    Directory admins can add and remove group members. After removal, new
    encrypted group writes exclude the removed member.

    ```python
    admin = Identity.load("admin.key")

    async with ChromatinClient.connect("192.168.1.200", 4201, admin) as client:
        directory = Directory(client, admin)

        # Create a group with initial members
        await directory.create_group("engineering", members=[alice, bob, carol])

        # Add a member later
        await directory.add_member("engineering", dave)

        # Remove a member
        await directory.remove_member("engineering", bob)

        # New group writes exclude removed member
        result = await client.write_to_group(
            b"Team update", "engineering", directory, ttl=3600
        )
    ```

    After `remove_member()`:
    - `write_to_group()` forces a directory cache refresh before resolving
      membership, ensuring the removed member is excluded from encryption.
    - The removed member gets `NotARecipientError` when trying to decrypt new
      group data.
    - Old data remains readable -- removal is forward-only. The removed member
      can still decrypt any data encrypted before their removal.
    ```

    Also update the "## Next Steps" section (line 415) to add references to the new features. Add these bullet points:

    ```
    - Revoke delegate access with `directory.revoke_delegation()` -- tombstone-based, propagates via sync
    - Rotate encryption keys with `identity.rotate_kem()` -- old data stays readable
    - Manage group membership with `directory.add_member()` and `directory.remove_member()`
    ```

    Insert these after the existing "Set up a directory for user discovery and group management" bullet point and before the PROTOCOL.md reference bullet.
  </action>
  <verify>
    <automated>grep -c "Delegation Revocation" sdk/python/docs/getting-started.md | grep -q "[1-9]" && grep -c "KEM Key Rotation" sdk/python/docs/getting-started.md | grep -q "[1-9]" && grep -c "Group Membership Management" sdk/python/docs/getting-started.md | grep -q "[1-9]" && grep -c "rotate_kem" sdk/python/docs/getting-started.md | grep -q "[1-9]" && grep -c "remove_member" sdk/python/docs/getting-started.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Delegation Revocation" sdk/python/docs/getting-started.md returns a match
    - grep "revoke_delegation" sdk/python/docs/getting-started.md returns at least 2 matches (code + text)
    - grep "list_delegates" sdk/python/docs/getting-started.md returns a match
    - grep "DelegationNotFoundError" sdk/python/docs/getting-started.md returns a match
    - grep "## KEM Key Rotation" sdk/python/docs/getting-started.md returns a match
    - grep "rotate_kem" sdk/python/docs/getting-started.md returns at least 2 matches (code + text)
    - grep "key_version" sdk/python/docs/getting-started.md returns a match
    - grep "resolve_recipient" sdk/python/docs/getting-started.md returns a match
    - grep "## Group Membership Management" sdk/python/docs/getting-started.md returns a match
    - grep "remove_member" sdk/python/docs/getting-started.md returns at least 2 matches (code + text)
    - grep "write_to_group" sdk/python/docs/getting-started.md returns at least 2 matches (existing + new)
    - grep "NotARecipientError" sdk/python/docs/getting-started.md returns a match (in group section)
    - grep "forward-only" sdk/python/docs/getting-started.md returns a match (old data readable note)
    - The three new sections appear BEFORE "## Next Steps"
    - The "## Next Steps" section includes bullet points for revoke, rotate, and group management
    - Code snippets are 5-15 lines each (matching existing style per D-02)
  </acceptance_criteria>
  <done>
    getting-started.md contains three new tutorial sections (Delegation Revocation, KEM Key Rotation, Group Membership Management) with short focused code examples matching existing style, and Next Steps updated with references to new features
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `grep -c "## Delegation Revocation" sdk/python/docs/getting-started.md` returns 1
2. `grep -c "## KEM Key Rotation" sdk/python/docs/getting-started.md` returns 1
3. `grep -c "## Group Membership Management" sdk/python/docs/getting-started.md` returns 1
4. `grep -c "revoke_delegation\|rotate_kem\|remove_member" sdk/python/docs/getting-started.md` returns >= 6
5. New sections appear between "## Monitoring with Prometheus" and "## Next Steps"
6. Code examples use correct method signatures matching actual SDK API
</verification>

<success_criteria>
SDK getting-started tutorial includes:
- Delegation revocation example with revoke_delegation() and list_delegates()
- KEM key rotation example with rotate_kem(), register(), and key ring explanation
- Group membership management example with create_group, add_member, remove_member, write_to_group
- All examples use correct API signatures from the actual SDK source code
- Style matches existing tutorial (short snippets, ## headers, minimal prose)
- Next Steps section updated with references to new features
</success_criteria>

<output>
After completion, create `.planning/phases/94-protocol-sdk-documentation/94-02-SUMMARY.md`
</output>
