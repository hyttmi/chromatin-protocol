---
phase: 105-operational-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/core/rate_limiter.h
  - relay/core/metrics_collector.h
  - relay/core/metrics_collector.cpp
  - relay/config/relay_config.h
  - relay/config/relay_config.cpp
  - relay/core/subscription_tracker.h
  - relay/CMakeLists.txt
  - relay/tests/CMakeLists.txt
  - relay/tests/test_rate_limiter.cpp
  - relay/tests/test_metrics_collector.cpp
  - relay/tests/test_relay_config.cpp
autonomous: true
requirements: [OPS-01, OPS-03, OPS-02]

must_haves:
  truths:
    - "RateLimiter allows messages under the rate and rejects messages over the rate"
    - "RateLimiter with rate=0 allows all messages (disabled mode)"
    - "RateLimiter tracks consecutive rejections for disconnect threshold"
    - "RelayMetrics struct provides atomic counters for all 7 counter types"
    - "MetricsCollector formats Prometheus text exposition with chromatindb_relay_ prefix"
    - "MetricsCollector starts/stops HTTP listener on metrics_bind address"
    - "RelayConfig parses metrics_bind and rate_limit_messages_per_sec from JSON"
    - "SubscriptionTracker exposes namespace_count() for gauge computation"
  artifacts:
    - path: "relay/core/rate_limiter.h"
      provides: "Header-only token bucket rate limiter"
      contains: "class RateLimiter"
    - path: "relay/core/metrics_collector.h"
      provides: "RelayMetrics struct and MetricsCollector class"
      contains: "struct RelayMetrics"
    - path: "relay/core/metrics_collector.cpp"
      provides: "Prometheus HTTP endpoint and text format output"
      contains: "format_prometheus"
    - path: "relay/tests/test_rate_limiter.cpp"
      provides: "Unit tests for token bucket"
      contains: "[rate_limiter]"
    - path: "relay/tests/test_metrics_collector.cpp"
      provides: "Unit tests for Prometheus output format"
      contains: "[metrics_collector]"
  key_links:
    - from: "relay/core/metrics_collector.h"
      to: "relay/core/metrics_collector.cpp"
      via: "class implementation"
      pattern: "MetricsCollector::"
    - from: "relay/config/relay_config.cpp"
      to: "relay/config/relay_config.h"
      via: "loads new fields"
      pattern: "metrics_bind|rate_limit_messages_per_sec"
---

<objective>
Create the core building blocks for Phase 105: RateLimiter (header-only token bucket), RelayMetrics struct with atomic counters, MetricsCollector with Prometheus /metrics HTTP endpoint, config field extensions, SubscriptionTracker gauge helper, and comprehensive unit tests for all new components.

Purpose: These components are the foundation that Plan 02 wires into the existing relay. Keeping creation separate from integration ensures each plan stays within context budget.
Output: 4 new files (rate_limiter.h, metrics_collector.h/cpp, test_rate_limiter.cpp, test_metrics_collector.cpp), 4 modified files (relay_config.h/cpp, subscription_tracker.h, CMakeLists.txt x2), extended test_relay_config.cpp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-operational-polish/105-CONTEXT.md
@.planning/phases/105-operational-polish/105-RESEARCH.md

@relay/core/subscription_tracker.h
@relay/config/relay_config.h
@relay/config/relay_config.cpp
@relay/CMakeLists.txt
@relay/tests/CMakeLists.txt

<interfaces>
<!-- Node MetricsCollector reference pattern (relay version mirrors this) -->

From db/peer/metrics_collector.h:
```cpp
class MetricsCollector {
public:
    void start_metrics_listener();
    void stop_metrics_listener();
    void set_metrics_bind(const std::string& bind);
    std::string format_prometheus_metrics();
    uint64_t compute_uptime_seconds() const;
private:
    asio::awaitable<void> metrics_accept_loop();
    asio::awaitable<void> metrics_handle_connection(asio::ip::tcp::socket socket);
    std::unique_ptr<asio::ip::tcp::acceptor> metrics_acceptor_;
    std::string metrics_bind_;
};
```

From db/peer/metrics_collector.cpp (HTTP handler pattern, lines 183-248):
```cpp
// Read until \r\n\r\n, check first line for "GET /metrics",
// respond 200 with Prometheus text or 404.
// Socket moved into coroutine at accept time -- safe for lifecycle.
```

From relay/core/subscription_tracker.h:
```cpp
class SubscriptionTracker {
    // subs_ maps Namespace32 -> set of session IDs
    std::unordered_map<Namespace32, std::unordered_set<uint64_t>, Namespace32Hash> subs_;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RateLimiter header-only + RelayMetrics struct + MetricsCollector class</name>
  <files>
    relay/core/rate_limiter.h
    relay/core/metrics_collector.h
    relay/core/metrics_collector.cpp
    relay/core/subscription_tracker.h
    relay/CMakeLists.txt
  </files>
  <read_first>
    relay/core/subscription_tracker.h
    relay/CMakeLists.txt
    db/peer/metrics_collector.h
    db/peer/metrics_collector.cpp
    db/peer/peer_types.h
  </read_first>
  <behavior>
    RateLimiter (relay/core/rate_limiter.h):
    - rate=0 means disabled: try_consume() always returns true
    - rate=100: first 100 calls to try_consume() return true (burst=rate per D-12), 101st returns false
    - consecutive_rejects_ increments on rejection, resets to 0 on acceptance
    - should_disconnect(10) returns true after 10 consecutive rejections
    - set_rate() resets tokens to full bucket and consecutive_rejects to 0
    - Uses steady_clock (never system_clock per anti-pattern)
    - Tokens refill proportional to elapsed time * rate, capped at burst

    RelayMetrics (relay/core/metrics_collector.h):
    - 7 std::atomic<uint64_t> fields: ws_connections_total, ws_disconnections_total, messages_received_total, messages_sent_total, auth_failures_total, rate_limited_total, errors_total
    - All initialized to 0

    MetricsCollector:
    - Constructor takes (io_context&, metrics_bind string, stopping atomic bool ref)
    - start() opens TCP acceptor on metrics_bind host:port, spawns accept_loop coroutine
    - stop() closes acceptor
    - set_metrics_bind(new_bind) calls stop() then start() with new address (per D-15)
    - format_prometheus(active_connections, active_subscriptions) returns Prometheus text with chromatindb_relay_ prefix
    - All 7 counters output with # HELP, # TYPE counter, metric line
    - 3 gauges output: ws_connections_active (param), subscriptions_active (param), uptime_seconds (computed)
    - HTTP handler: GET /metrics -> 200 with text/plain; version=0.0.4, anything else -> 404
    - uptime_seconds() returns seconds since construction via steady_clock

    SubscriptionTracker addition:
    - Add `size_t namespace_count() const { return subs_.size(); }` public method
  </behavior>
  <action>
    1. Create `relay/core/rate_limiter.h` (header-only, per D-12):
       - namespace chromatindb::relay::core
       - class RateLimiter with:
         - Private: uint32_t rate_ = 0, uint32_t burst_ = 0, double tokens_ = 0.0, uint32_t consecutive_rejects_ = 0, steady_clock::time_point last_refill_ (init to now())
         - Private refill(): elapsed = duration<double>(now - last_refill_).count(), tokens_ = min(tokens_ + elapsed * rate_, double(burst_)), last_refill_ = now
         - Public set_rate(uint32_t rate): rate_ = rate, burst_ = rate, tokens_ = rate (full bucket), consecutive_rejects_ = 0, last_refill_ = now()
         - Public try_consume() -> bool: if rate_==0 return true. refill(). if tokens_ < 1.0: ++consecutive_rejects_, return false. tokens_ -= 1.0, consecutive_rejects_ = 0, return true
         - Public should_disconnect(uint32_t threshold) const -> bool: return consecutive_rejects_ >= threshold
         - Public consecutive_rejects() const -> uint32_t (for testing)

    2. Create `relay/core/metrics_collector.h`:
       - namespace chromatindb::relay::core
       - struct RelayMetrics with 7 std::atomic<uint64_t> fields (per D-04): ws_connections_total{0}, ws_disconnections_total{0}, messages_received_total{0}, messages_sent_total{0}, auth_failures_total{0}, rate_limited_total{0}, errors_total{0}
       - class MetricsCollector:
         - Constructor: MetricsCollector(asio::io_context& ioc, const std::string& metrics_bind, const std::atomic<bool>& stopping)
         - void start() -- start HTTP listener if metrics_bind_ non-empty
         - void stop() -- stop HTTP listener
         - void set_metrics_bind(const std::string& bind) -- stop() + update bind + start()
         - RelayMetrics& metrics() -- returns metrics_ reference
         - uint64_t uptime_seconds() const -- steady_clock diff from start_time_
         - std::string format_prometheus(size_t active_connections, size_t active_subscriptions)
         - Private: accept_loop(), handle_connection(tcp::socket), ioc_, stopping_ ref, metrics_, start_time_, acceptor_ unique_ptr, metrics_bind_

    3. Create `relay/core/metrics_collector.cpp`:
       - Mirror db/peer/metrics_collector.cpp accept loop pattern (lines 130-248):
         - start(): parse host:port from metrics_bind_, resolve, open/bind/listen, co_spawn accept_loop
         - stop(): close acceptor, reset unique_ptr
         - accept_loop(): while (!stopping_ && acceptor_ open) async_accept -> co_spawn handle_connection
         - handle_connection(): read until \r\n\r\n (4KB cap, 5s timeout), check first line for "GET /metrics", respond 200 with format_prometheus() or 404
         - set_metrics_bind(): stop(), metrics_bind_ = new_bind, start()
         - uptime_seconds(): duration_cast<seconds>(steady_clock::now() - start_time_).count()
       - format_prometheus(active_connections, active_subscriptions):
         - Reserve 2048 bytes
         - For each of 7 counters: # HELP chromatindb_relay_{name} {description}\n# TYPE chromatindb_relay_{name} counter\nchromatindb_relay_{name} {value}\n\n
         - Counters (per D-04): ws_connections_total, ws_disconnections_total, messages_received_total, messages_sent_total, auth_failures_total, rate_limited_total, errors_total
         - For 3 gauges (per D-05): ws_connections_active (from param), subscriptions_active (from param), uptime_seconds (from uptime_seconds())
         - Use .load(std::memory_order_relaxed) for all atomic reads (per D-06)

    4. Add `size_t namespace_count() const { return subs_.size(); }` to SubscriptionTracker public section in relay/core/subscription_tracker.h (after client_subscription_count).

    5. Add `core/metrics_collector.cpp` to relay/CMakeLists.txt add_library sources (after core/message_filter.cpp).
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - relay/core/rate_limiter.h exists with class RateLimiter, try_consume(), set_rate(), should_disconnect()
    - relay/core/metrics_collector.h exists with struct RelayMetrics (7 atomic fields) and class MetricsCollector
    - relay/core/metrics_collector.cpp exists with accept_loop, handle_connection, format_prometheus
    - subscription_tracker.h contains namespace_count() method
    - relay/CMakeLists.txt includes core/metrics_collector.cpp
    - Project compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Config extensions for metrics_bind and rate_limit_messages_per_sec</name>
  <files>
    relay/config/relay_config.h
    relay/config/relay_config.cpp
  </files>
  <read_first>
    relay/config/relay_config.h
    relay/config/relay_config.cpp
    relay/tests/test_relay_config.cpp
  </read_first>
  <action>
    1. Add two fields to RelayConfig struct in relay/config/relay_config.h (after allowed_client_keys):
       - `std::string metrics_bind;` -- empty=disabled, "host:port"=enabled (per D-02)
       - `uint32_t rate_limit_messages_per_sec = 0;` -- 0=disabled (per D-10)

    2. Add loading in load_relay_config() in relay/config/relay_config.cpp (after allowed_client_keys loading):
       - `cfg.metrics_bind = j.value("metrics_bind", cfg.metrics_bind);`
       - `cfg.rate_limit_messages_per_sec = j.value("rate_limit_messages_per_sec", cfg.rate_limit_messages_per_sec);`

    3. Add validation in validate_relay_config() in relay/config/relay_config.cpp:
       - metrics_bind: if non-empty, must contain ':' (host:port format). Same check as db/config/config.cpp lines 311-327.
         ```cpp
         if (!cfg.metrics_bind.empty()) {
             if (cfg.metrics_bind.rfind(':') == std::string::npos) {
                 errors += "metrics_bind must be 'host:port' format, got '" + cfg.metrics_bind + "'. ";
             }
         }
         ```
       - rate_limit_messages_per_sec: no validation needed (0=disabled, any positive value is valid)
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - relay_config.h contains `std::string metrics_bind;` and `uint32_t rate_limit_messages_per_sec = 0;`
    - relay_config.cpp load_relay_config reads both fields from JSON
    - relay_config.cpp validate_relay_config checks metrics_bind format
    - Project compiles cleanly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for RateLimiter, MetricsCollector, and config extensions</name>
  <files>
    relay/tests/test_rate_limiter.cpp
    relay/tests/test_metrics_collector.cpp
    relay/tests/test_relay_config.cpp
    relay/tests/CMakeLists.txt
  </files>
  <read_first>
    relay/core/rate_limiter.h
    relay/core/metrics_collector.h
    relay/core/metrics_collector.cpp
    relay/config/relay_config.h
    relay/tests/test_relay_config.cpp
    relay/tests/CMakeLists.txt
    relay/tests/test_subscription_tracker.cpp
  </read_first>
  <behavior>
    test_rate_limiter.cpp:
    - "[rate_limiter] disabled": rate=0, try_consume() returns true 1000 times
    - "[rate_limiter] allows burst": rate=10, first 10 try_consume() return true
    - "[rate_limiter] rejects over rate": rate=10, 11th try_consume() returns false
    - "[rate_limiter] consecutive rejects": rate=1, exhaust bucket, 10 more calls -> should_disconnect(10) is true
    - "[rate_limiter] rejects reset on accept": exhaust, wait, accept -> consecutive_rejects() is 0
    - "[rate_limiter] set_rate resets bucket": set_rate(5), exhaust, set_rate(5), first call returns true

    test_metrics_collector.cpp:
    - "[metrics_collector] format_prometheus counters": set counter values, format_prometheus returns correct text with chromatindb_relay_ prefix, _total suffix, TYPE counter
    - "[metrics_collector] format_prometheus gauges": pass active_connections=5, active_subscriptions=3, verify ws_connections_active 5, subscriptions_active 3, uptime_seconds > 0
    - "[metrics_collector] atomic increments": increment ws_connections_total from multiple threads, verify final value is correct
    - "[metrics_collector] uptime_seconds": create collector, sleep briefly, verify uptime_seconds() > 0

    test_relay_config.cpp (extend existing):
    - "[relay_config] metrics_bind default empty": load config without metrics_bind, verify empty string
    - "[relay_config] metrics_bind parsed": load config with "metrics_bind": "0.0.0.0:9100", verify value
    - "[relay_config] metrics_bind validation": validate config with metrics_bind="no_colon" -> throws
    - "[relay_config] rate_limit default zero": load config without rate_limit, verify 0
    - "[relay_config] rate_limit parsed": load config with "rate_limit_messages_per_sec": 100, verify 100
  </behavior>
  <action>
    1. Create relay/tests/test_rate_limiter.cpp:
       - #include "relay/core/rate_limiter.h" and catch2
       - TEST_CASE("[rate_limiter] disabled mode"): RateLimiter rl; (default rate=0), loop 1000 try_consume(), all true
       - TEST_CASE("[rate_limiter] allows full burst"): rl.set_rate(10); loop 10, all true
       - TEST_CASE("[rate_limiter] rejects over burst"): set_rate(10), consume 10, CHECK_FALSE 11th
       - TEST_CASE("[rate_limiter] consecutive reject tracking"): set_rate(1), consume 1, then 10 more (all false), CHECK(rl.should_disconnect(10))
       - TEST_CASE("[rate_limiter] accept resets consecutive"): set_rate(100), consume all, then sleep_for(20ms) to refill some tokens, try_consume() true -> CHECK(rl.consecutive_rejects() == 0)
       - TEST_CASE("[rate_limiter] set_rate resets"): set_rate(5), consume 5, set_rate(5), CHECK(try_consume() == true)

    2. Create relay/tests/test_metrics_collector.cpp:
       - #include "relay/core/metrics_collector.h" and catch2
       - TEST_CASE("[metrics_collector] format prometheus counters"):
         asio::io_context ioc; std::atomic<bool> stopping{false};
         MetricsCollector mc(ioc, "", stopping);
         mc.metrics().ws_connections_total.store(42); mc.metrics().auth_failures_total.store(3);
         auto text = mc.format_prometheus(0, 0);
         CHECK_THAT(text, ContainsSubstring("chromatindb_relay_ws_connections_total 42"));
         CHECK_THAT(text, ContainsSubstring("# TYPE chromatindb_relay_ws_connections_total counter"));
         CHECK_THAT(text, ContainsSubstring("chromatindb_relay_auth_failures_total 3"));
       - TEST_CASE("[metrics_collector] format prometheus gauges"):
         auto text = mc.format_prometheus(5, 3);
         CHECK_THAT(text, ContainsSubstring("chromatindb_relay_ws_connections_active 5"));
         CHECK_THAT(text, ContainsSubstring("chromatindb_relay_subscriptions_active 3"));
         CHECK_THAT(text, ContainsSubstring("chromatindb_relay_uptime_seconds"));
         CHECK_THAT(text, ContainsSubstring("# TYPE chromatindb_relay_ws_connections_active gauge"));
       - TEST_CASE("[metrics_collector] uptime seconds"):
         create collector, sleep_for(10ms), CHECK(mc.uptime_seconds() >= 0) -- uptime is computed from start_time_
       - TEST_CASE("[metrics_collector] atomic increment"):
         mc.metrics().messages_received_total.fetch_add(1, relaxed); x100 -> CHECK(load() == 100)

    3. Extend relay/tests/test_relay_config.cpp with new test cases:
       - Add tests for metrics_bind and rate_limit_messages_per_sec parsing
       - Write temp JSON with/without the new fields, load, verify defaults and explicit values
       - Validate metrics_bind without colon throws

    4. Add test_rate_limiter.cpp and test_metrics_collector.cpp to relay/tests/CMakeLists.txt add_executable sources.
  </action>
  <verify>
    <automated>cmake --build build && ./build/relay/tests/chromatindb_relay_tests "[rate_limiter]" "[metrics_collector]" "[relay_config]" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test_rate_limiter.cpp exists with 6+ test cases tagged [rate_limiter]
    - test_metrics_collector.cpp exists with 4+ test cases tagged [metrics_collector]
    - test_relay_config.cpp extended with 5+ new test cases for metrics_bind and rate_limit_messages_per_sec
    - All new tests pass: `./build/relay/tests/chromatindb_relay_tests "[rate_limiter]"` exits 0
    - All new tests pass: `./build/relay/tests/chromatindb_relay_tests "[metrics_collector]"` exits 0
    - All existing + new config tests pass: `./build/relay/tests/chromatindb_relay_tests "[relay_config]"` exits 0
  </done>
  <acceptance_criteria>
    - relay/tests/test_rate_limiter.cpp contains `TEST_CASE` and `[rate_limiter]`
    - relay/tests/test_metrics_collector.cpp contains `TEST_CASE` and `[metrics_collector]`
    - relay/tests/test_relay_config.cpp contains `metrics_bind` and `rate_limit_messages_per_sec`
    - relay/tests/CMakeLists.txt contains `test_rate_limiter.cpp` and `test_metrics_collector.cpp`
    - `./build/relay/tests/chromatindb_relay_tests "[rate_limiter]"` exits 0
    - `./build/relay/tests/chromatindb_relay_tests "[metrics_collector]"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `cmake --build build` -- full build succeeds with no warnings in new files
2. `./build/relay/tests/chromatindb_relay_tests "[rate_limiter]"` -- all rate limiter tests pass
3. `./build/relay/tests/chromatindb_relay_tests "[metrics_collector]"` -- all metrics tests pass
4. `./build/relay/tests/chromatindb_relay_tests "[relay_config]"` -- all config tests pass (existing + new)
5. `./build/relay/tests/chromatindb_relay_tests` -- full test suite passes (no regressions)
</verification>

<success_criteria>
- RateLimiter class compiles and passes 6+ unit tests covering disabled mode, burst, rejection, disconnect threshold, and reset
- RelayMetrics struct has 7 atomic counter fields with chromatindb_relay_ prefix naming convention
- MetricsCollector produces valid Prometheus text exposition format with all 7 counters and 3 gauges
- MetricsCollector HTTP listener starts/stops cleanly (tested via format_prometheus, not network)
- RelayConfig has metrics_bind and rate_limit_messages_per_sec fields with correct defaults and validation
- SubscriptionTracker has namespace_count() method
- Full relay test suite (188+ existing + new tests) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/105-operational-polish/105-01-SUMMARY.md`
</output>
