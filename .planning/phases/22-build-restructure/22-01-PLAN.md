---
phase: 22-build-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/CMakeLists.txt
  - db/schemas/blob.fbs
  - db/schemas/transport.fbs
  - CMakeLists.txt
autonomous: true
requirements:
  - BUILD-01

must_haves:
  truths:
    - "db/ is a self-contained CMake component with its own project(chromatindb-core)"
    - "cmake -S db/ -B build-db configures successfully (standalone build)"
    - "cmake -S . -B build configures and builds the full project via add_subdirectory(db)"
    - "All 284 existing tests compile and pass without modification"
    - "Schema files live in db/schemas/ and codegen targets produce headers in db/wire/"
  artifacts:
    - path: "db/CMakeLists.txt"
      provides: "Self-contained CMake component with guarded FetchContent deps, codegen, and chromatindb_lib target"
      min_lines: 80
    - path: "db/schemas/blob.fbs"
      provides: "FlatBuffers blob schema (moved from schemas/)"
    - path: "db/schemas/transport.fbs"
      provides: "FlatBuffers transport schema (moved from schemas/)"
    - path: "CMakeLists.txt"
      provides: "Root build consuming db/ via add_subdirectory, owning daemon/bench/test targets"
      min_lines: 40
  key_links:
    - from: "CMakeLists.txt"
      to: "db/CMakeLists.txt"
      via: "add_subdirectory(db)"
      pattern: "add_subdirectory\\(db\\)"
    - from: "db/CMakeLists.txt"
      to: "db/schemas/"
      via: "CMAKE_CURRENT_SOURCE_DIR/schemas for codegen paths"
      pattern: "CMAKE_CURRENT_SOURCE_DIR.*schemas"
    - from: "CMakeLists.txt"
      to: "chromatindb_lib"
      via: "target_link_libraries for daemon, bench, and test targets"
      pattern: "target_link_libraries.*chromatindb_lib"
---

<objective>
Restructure the build system so db/ is a self-contained CMake component that can be built independently or consumed via add_subdirectory from the root.

Purpose: Enable db/ to function as a reusable library component. The root CMakeLists.txt becomes a thin consumer that adds the daemon binary, benchmarks, and tests.

Output: db/CMakeLists.txt (new), db/schemas/ (moved), CMakeLists.txt (rewritten)
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-build-restructure/22-CONTEXT.md
@.planning/phases/22-build-restructure/22-RESEARCH.md

<interfaces>
<!-- Current root CMakeLists.txt structure (235 lines) being split -->
<!-- Key targets and their dependencies: -->

Library target: chromatindb_lib (STATIC)
  Sources: 19 .cpp files in db/ subdirectories (crypto/, wire/, config/, logging/, identity/, storage/, engine/, net/, sync/, peer/, acl/)
  Links: oqs, sodium, flatbuffers, spdlog::spdlog, nlohmann_json::nlohmann_json, mdbx-static, asio::asio
  Include: ${CMAKE_CURRENT_SOURCE_DIR} (root, so #include "db/..." works)

Consumer targets (stay at root):
  chromatindb (daemon): db/main.cpp -> chromatindb_lib
  chromatindb_bench: bench/bench_main.cpp -> chromatindb_lib
  chromatindb_tests: 19 test .cpp files -> chromatindb_lib + Catch2::Catch2WithMain

FetchContent deps (8 total):
  liboqs (0.15.0), sodium (master), flatbuffers (v25.2.10), Catch2 (v3.7.1),
  spdlog (v1.15.1), json (v3.11.3), libmdbx (v0.13.11), asiocmake (main)

Codegen: blob.fbs -> db/wire/blob_generated.h, transport.fbs -> db/wire/transport_generated.h
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move schemas and create db/CMakeLists.txt</name>
  <files>db/schemas/blob.fbs, db/schemas/transport.fbs, db/CMakeLists.txt</files>
  <action>
1. Move schema files from root to db/:
   ```
   mkdir -p db/schemas
   git mv schemas/blob.fbs db/schemas/blob.fbs
   git mv schemas/transport.fbs db/schemas/transport.fbs
   rmdir schemas/  (if empty after moves)
   ```

2. Create `db/CMakeLists.txt` with the following structure. Use the research skeleton from 22-RESEARCH.md as the base, verified against the current root CMakeLists.txt for accuracy:

   a. `cmake_minimum_required(VERSION 3.20)` and `project(chromatindb-core LANGUAGES C CXX)`
   b. `set(CMAKE_CXX_STANDARD 20)`, `set(CMAKE_CXX_STANDARD_REQUIRED ON)`
   c. `include(FetchContent)`
   d. All 7 library dependencies (NOT Catch2 -- that's test-only, stays at root) with `if(NOT TARGET <target>)` guards:
      - liboqs: guard `if(NOT TARGET oqs)`, includes all OQS cache variables for algorithm stripping, FetchContent_Declare + MakeAvailable + GetProperties + target_include_directories for build-dir headers
      - sodium: guard `if(NOT TARGET sodium)`, SODIUM_DISABLE_TESTS cache var
      - flatbuffers: guard `if(NOT TARGET flatbuffers)`, BUILD_TESTS/FLATC/FLATHASH cache vars
      - spdlog: guard `if(NOT TARGET spdlog::spdlog)`, FMT_EXTERNAL cache var
      - json: guard `if(NOT TARGET nlohmann_json::nlohmann_json)`, URL-based FetchContent
      - libmdbx: guard `if(NOT TARGET mdbx-static)`, BUILD_CXX/TOOLS/TESTS/SHARED/INSTALL cache vars
      - asiocmake: guard `if(NOT TARGET asio::asio)`, ASIO_TAG cache var
      CRITICAL: Both FetchContent_Declare AND FetchContent_MakeAvailable go INSIDE the if(NOT TARGET) guard. Each dependency gets its own MakeAvailable call (not batched), since each guard is independent.
   e. FlatBuffers schema compilation section:
      - `set(FLATBUFFERS_SCHEMA_DIR ${CMAKE_CURRENT_SOURCE_DIR}/schemas)` (NOT CMAKE_SOURCE_DIR)
      - `set(FLATBUFFERS_GENERATED_DIR ${CMAKE_CURRENT_SOURCE_DIR}/wire)` (NOT CMAKE_SOURCE_DIR)
      - Two `if(EXISTS ...)` blocks for blob.fbs and transport.fbs with add_custom_command + add_custom_target (same pattern as current root, just paths updated)
   f. Library target:
      - `add_library(chromatindb_lib STATIC ...)` with all 19 source files using RELATIVE paths (e.g., `crypto/hash.cpp` not `db/crypto/hash.cpp`)
      - `target_include_directories(chromatindb_lib PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>)` -- parent dir so `#include "db/..."` works
      - `target_link_libraries(chromatindb_lib PUBLIC oqs sodium flatbuffers spdlog::spdlog nlohmann_json::nlohmann_json mdbx-static asio::asio)` -- same as current

   Do NOT include: ENABLE_ASAN option (YAGNI per research -- sanitizers are a consumer concern), install() rules (YAGNI), CMAKE_BUILD_TYPE (inherited from root or set by standalone user), Catch2 (test-only, stays at root), CTest (tests stay at root).
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && test -f db/CMakeLists.txt && test -f db/schemas/blob.fbs && test -f db/schemas/transport.fbs && ! test -d schemas/ && echo "PASS: files in place"</automated>
  </verify>
  <done>db/CMakeLists.txt exists with project(chromatindb-core), 7 guarded FetchContent blocks, codegen commands referencing db/schemas/, and chromatindb_lib STATIC target. Schema files are in db/schemas/. Old schemas/ directory is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite root CMakeLists.txt and verify full build</name>
  <files>CMakeLists.txt</files>
  <action>
1. Rewrite root `CMakeLists.txt` to consume db/ via add_subdirectory. Structure:

   a. Keep: cmake_minimum_required(VERSION 3.20), project(chromatindb LANGUAGES C CXX)
   b. Keep: CMAKE_CXX_STANDARD 20, CMAKE_CXX_STANDARD_REQUIRED, CMAKE_EXPORT_COMPILE_COMMANDS, CMAKE_BUILD_TYPE Debug
   c. Keep: ENABLE_ASAN option block (unchanged)
   d. Keep: include(FetchContent)
   e. Keep: ALL FetchContent declarations (liboqs with algorithm stripping, sodium, flatbuffers, Catch2, spdlog, json, libmdbx, asiocmake) -- verbatim from current file. These run FIRST so targets exist before add_subdirectory.
   f. Keep: Single `FetchContent_MakeAvailable(liboqs sodium flatbuffers Catch2 spdlog json libmdbx asiocmake)` call -- batched at root is fine since no guards needed (root always fetches).
   g. Keep: `FetchContent_GetProperties(liboqs)` + `target_include_directories(oqs PUBLIC ...)` for liboqs build-dir headers.
   h. REPLACE library definition section with: `add_subdirectory(db)` -- db/CMakeLists.txt guards will detect all targets exist and skip re-fetching.
   i. REMOVE: FlatBuffers schema compilation section (moved to db/CMakeLists.txt)
   j. REMOVE: add_library(chromatindb_lib ...), target_include_directories, target_link_libraries for the library (moved to db/CMakeLists.txt)
   k. Keep: Daemon binary section (add_executable chromatindb db/main.cpp, target_link_libraries chromatindb PRIVATE chromatindb_lib)
   l. Keep: Benchmark binary section (unchanged)
   m. Keep: Test target section (unchanged -- all test files, Catch2 link, CTest, catch_discover_tests)

2. Build and test the full project:
   ```
   cd /home/mika/dev/chromatin-protocol
   rm -rf build
   cmake -S . -B build 2>&1
   cmake --build build -j$(nproc) 2>&1
   cd build && ctest --output-on-failure 2>&1
   ```
   ALL 284 tests must pass. Zero regressions.

3. Verify standalone db/ build configures:
   ```
   cd /home/mika/dev/chromatin-protocol
   cmake -S db/ -B build-db-standalone 2>&1
   ```
   Must configure without errors (full build takes too long for verification -- configure is sufficient to prove standalone viability).
   Clean up: `rm -rf build-db-standalone`
  </action>
  <verify>
    <automated>cd /home/mika/dev/chromatin-protocol && rm -rf build && cmake -S . -B build 2>&1 | tail -5 && cmake --build build -j$(nproc) 2>&1 | tail -5 && cd build && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Root CMakeLists.txt consumes db/ via add_subdirectory(db). Full project builds with zero warnings. All 284 tests pass. Standalone `cmake -S db/ -B build-db-standalone` configures without errors.</done>
</task>

</tasks>

<verification>
1. `db/CMakeLists.txt` exists with `project(chromatindb-core)`, 7 `if(NOT TARGET)` guarded FetchContent blocks, codegen commands, and `chromatindb_lib` STATIC library target
2. `db/schemas/blob.fbs` and `db/schemas/transport.fbs` exist (moved from root `schemas/`)
3. Root `CMakeLists.txt` uses `add_subdirectory(db)` and no longer defines chromatindb_lib directly
4. Full build: `cmake -S . -B build && cmake --build build -j$(nproc)` succeeds
5. All tests: `cd build && ctest --output-on-failure` passes 284 tests
6. Standalone: `cmake -S db/ -B build-db-standalone` configures without error
7. No source file changes -- only CMake files and schema file locations changed
</verification>

<success_criteria>
- db/ is a self-contained CMake component with project(chromatindb-core) that can build independently
- Root consumes db/ via add_subdirectory -- no duplicate library definitions
- Zero test regressions (all 284 tests pass)
- Include paths unchanged (#include "db/..." works everywhere)
- Schema codegen paths updated to db/schemas/ and produce output in db/wire/
</success_criteria>

<output>
After completion, create `.planning/phases/22-build-restructure/22-01-SUMMARY.md`
</output>
