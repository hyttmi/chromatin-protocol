---
phase: 53-release-cleanup-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/test-crash-recovery.sh
  - db/TESTS.md
  - scripts/run-e2e-reliability.sh
  - CMakeLists.txt
  - .planning/phases/16-storage-foundation
  - .planning/phases/17-operational-stability
  - .planning/phases/18-abuse-prevention-topology
  - .planning/phases/19-documentation-release
  - .planning/phases/20-metrics-completeness-consistency
  - .planning/phases/21-test-260-segfault-fix
  - .planning/phases/32-test-relocation
  - .planning/phases/33-crypto-throughput-optimization
  - .planning/phases/34-sync-resumption
  - .planning/phases/35-namespace-quotas
  - .planning/phases/36-deletion-benchmarks
  - .planning/phases/37-general-cleanup
  - .planning/phases/38-thread-pool-crypto-offload
  - .planning/phases/39-negentropy-set-reconciliation
  - .planning/phases/40-sync-rate-limiting
  - .planning/phases/41-benchmark-validation
  - .planning/phases/46-sanitizers-bug-fix
  - .planning/phases/47-crypto-transport-verification
  - .planning/phases/48-access-control-topology
  - .planning/phases/49-network-resilience-reconciliation
  - .planning/phases/50-operations-disaster-recovery-resource-limits
  - .planning/phases/51-ttl-lifecycle-e2e-primitives
  - .planning/phases/52-stress-chaos-fuzzing
  - db/README.md
  - README.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "git tag v1.0.0 exists on the shipped commit and git describe returns it"
    - "Stale files (deploy/test-crash-recovery.sh, db/TESTS.md, scripts/run-e2e-reliability.sh) no longer exist in the tree"
    - "Completed milestone phases are archived into .planning/milestones/ following established patterns"
    - "CMake project version reads 1.1.0 and built binary reports v1.1.0"
    - "db/README.md has a Testing section after Build covering unit tests, sanitizers, Docker integration, and stress/chaos/fuzz"
    - "README.md has a version line anchoring to v1.0.0"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Version bump to 1.1.0"
      contains: "project(chromatindb VERSION 1.1.0"
    - path: "db/README.md"
      provides: "Testing section with sanitizer, Docker, stress/chaos/fuzz coverage"
      contains: "## Testing"
    - path: "README.md"
      provides: "Version line"
      contains: "v1.0.0"
    - path: ".planning/milestones/v0.4.0-phases"
      provides: "Archived v0.4.0 phase directories"
    - path: ".planning/milestones/v0.7.0-phases"
      provides: "Archived v0.7.0 phase directories"
    - path: ".planning/milestones/v0.8.0-phases"
      provides: "Archived v0.8.0 phase directories"
    - path: ".planning/milestones/v1.0.0-phases"
      provides: "Archived v1.0.0 phase directories"
  key_links:
    - from: "CMakeLists.txt"
      to: "db/version.h"
      via: "configure_file injection"
      pattern: "project\\(chromatindb VERSION 1\\.1\\.0"
---

<objective>
Clean up the repository to reflect shipped v1.0.0 reality: remove stale artifacts, archive completed milestone phases, bump version to 1.1.0, and update documentation to match v1.0.0 capabilities.

Purpose: The repository should accurately represent what shipped and be ready for v1.1.0 development.
Output: Clean tree with archived phases, accurate docs, and v1.1.0 version.
</objective>

<execution_context>
@/home/mika/.claude/get-shit-done/workflows/execute-plan.md
@/home/mika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-release-cleanup-documentation/53-CONTEXT.md

@CMakeLists.txt
@db/README.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale artifacts and bump version to 1.1.0</name>
  <files>deploy/test-crash-recovery.sh, db/TESTS.md, scripts/run-e2e-reliability.sh, CMakeLists.txt</files>
  <action>
1. Delete stale files via git rm:
   - `deploy/test-crash-recovery.sh` (bash crash recovery test, replaced by Docker integration suite)
   - `db/TESTS.md` (stale design doc, no longer reflects reality)
   - `scripts/run-e2e-reliability.sh` (one-time v1.0.0 validation tool, no longer needed)

2. In `CMakeLists.txt` line 2, change:
   `project(chromatindb VERSION 1.0.0 LANGUAGES C CXX)`
   to:
   `project(chromatindb VERSION 1.1.0 LANGUAGES C CXX)`

3. Verify v1.0.0 tag exists: `git tag -l v1.0.0` should return "v1.0.0" (REL-01 already satisfied per CONTEXT.md -- tag exists on shipped commit).

Note: Do NOT remove `.planning/milestones/v1.0.0-*` files -- these are standard archives per user decision.
  </action>
  <verify>
    <automated>test ! -f deploy/test-crash-recovery.sh && test ! -f db/TESTS.md && test ! -f scripts/run-e2e-reliability.sh && grep -q 'VERSION 1.1.0' CMakeLists.txt && git tag -l v1.0.0 | grep -q v1.0.0 && echo "PASS"</automated>
  </verify>
  <done>Three stale files removed from tree, CMake version reads 1.1.0, v1.0.0 tag confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Archive completed milestone phases</name>
  <files>.planning/phases/16-* through 21-*, .planning/phases/32-* through 37-*, .planning/phases/38-* through 41-*, .planning/phases/46-* through 52-*</files>
  <action>
Archive unarchived completed milestone phases into `.planning/milestones/` following the established pattern (e.g., `v0.5.0-phases/`, `v0.6.0-phases/`, `v0.9.0-phases/`).

Create archive directories and move phase directories using `git mv`:

1. **v0.4.0 (phases 16-21):**
   ```
   mkdir -p .planning/milestones/v0.4.0-phases
   git mv .planning/phases/16-storage-foundation .planning/milestones/v0.4.0-phases/
   git mv .planning/phases/17-operational-stability .planning/milestones/v0.4.0-phases/
   git mv .planning/phases/18-abuse-prevention-topology .planning/milestones/v0.4.0-phases/
   git mv .planning/phases/19-documentation-release .planning/milestones/v0.4.0-phases/
   git mv .planning/phases/20-metrics-completeness-consistency .planning/milestones/v0.4.0-phases/
   git mv .planning/phases/21-test-260-segfault-fix .planning/milestones/v0.4.0-phases/
   ```

2. **v0.7.0 (phases 32-37):**
   ```
   mkdir -p .planning/milestones/v0.7.0-phases
   git mv .planning/phases/32-test-relocation .planning/milestones/v0.7.0-phases/
   git mv .planning/phases/33-crypto-throughput-optimization .planning/milestones/v0.7.0-phases/
   git mv .planning/phases/34-sync-resumption .planning/milestones/v0.7.0-phases/
   git mv .planning/phases/35-namespace-quotas .planning/milestones/v0.7.0-phases/
   git mv .planning/phases/36-deletion-benchmarks .planning/milestones/v0.7.0-phases/
   git mv .planning/phases/37-general-cleanup .planning/milestones/v0.7.0-phases/
   ```

3. **v0.8.0 (phases 38-41):**
   ```
   mkdir -p .planning/milestones/v0.8.0-phases
   git mv .planning/phases/38-thread-pool-crypto-offload .planning/milestones/v0.8.0-phases/
   git mv .planning/phases/39-negentropy-set-reconciliation .planning/milestones/v0.8.0-phases/
   git mv .planning/phases/40-sync-rate-limiting .planning/milestones/v0.8.0-phases/
   git mv .planning/phases/41-benchmark-validation .planning/milestones/v0.8.0-phases/
   ```

4. **v1.0.0 (phases 46-52):**
   ```
   mkdir -p .planning/milestones/v1.0.0-phases
   git mv .planning/phases/46-sanitizers-bug-fix .planning/milestones/v1.0.0-phases/
   git mv .planning/phases/47-crypto-transport-verification .planning/milestones/v1.0.0-phases/
   git mv .planning/phases/48-access-control-topology .planning/milestones/v1.0.0-phases/
   git mv .planning/phases/49-network-resilience-reconciliation .planning/milestones/v1.0.0-phases/
   git mv .planning/phases/50-operations-disaster-recovery-resource-limits .planning/milestones/v1.0.0-phases/
   git mv .planning/phases/51-ttl-lifecycle-e2e-primitives .planning/milestones/v1.0.0-phases/
   git mv .planning/phases/52-stress-chaos-fuzzing .planning/milestones/v1.0.0-phases/
   ```

After archiving, `.planning/phases/` should contain only `53-release-cleanup-documentation/` (the current active phase).
  </action>
  <verify>
    <automated>test -d .planning/milestones/v0.4.0-phases/16-storage-foundation && test -d .planning/milestones/v0.7.0-phases/32-test-relocation && test -d .planning/milestones/v0.8.0-phases/38-thread-pool-crypto-offload && test -d .planning/milestones/v1.0.0-phases/46-sanitizers-bug-fix && test ! -d .planning/phases/16-storage-foundation && test ! -d .planning/phases/46-sanitizers-bug-fix && echo "PASS"</automated>
  </verify>
  <done>All 23 completed phase directories archived into their respective milestone directories (v0.4.0-phases, v0.7.0-phases, v0.8.0-phases, v1.0.0-phases). Only phase 53 remains in .planning/phases/.</done>
</task>

<task type="auto">
  <name>Task 3: Update db/README.md and README.md documentation</name>
  <files>db/README.md, README.md</files>
  <action>
**db/README.md changes (DOCS-01):**

1. Update the Build section to include sanitizer build instructions. After the standard build commands and before the Test subsection, add a "Sanitizer Builds" subsection:
   ```
   ### Sanitizer Builds

   Build with AddressSanitizer, ThreadSanitizer, or UndefinedBehaviorSanitizer:

   ```bash
   cmake .. -DSANITIZER=asan   # AddressSanitizer
   cmake .. -DSANITIZER=tsan   # ThreadSanitizer
   cmake .. -DSANITIZER=ubsan  # UndefinedBehaviorSanitizer
   cmake --build .
   ```

   The codebase is clean under all three sanitizers.
   ```

2. Add a new **Testing** section placed AFTER the Build section and BEFORE the Usage section. Content:

   ```markdown
   ## Testing

   ### Unit Tests

   469 unit tests covering all subsystems (crypto, storage, sync, ACL, delegation, pub/sub, rate limiting, quotas, config validation):

   ```bash
   cd build
   ctest --output-on-failure
   ```

   ### Docker Integration Tests

   54 integration tests across 12 categories running real multi-node topologies in Docker containers:

   ```bash
   cd deploy
   ./run-integration-tests.sh
   ```

   Categories include: basic sync, ACL enforcement, delegation, deletion propagation, pub/sub, storage limits, rate limiting, namespace quotas, trusted peers, crash recovery, concurrent sync, and large blob transfer.

   ### Stress, Chaos & Fuzz Testing

   - **Stress testing:** 5-node cluster with continuous SIGKILL churn, verifying data consistency after node recovery
   - **Chaos testing:** 1000-namespace scaling with concurrent writes and deletions across multiple nodes
   - **Protocol fuzzing:** Random byte injection and malformed message testing against the wire protocol parser
   ```

   Exact wording/formatting is at executor's discretion per CONTEXT.md, but must cover: unit test count + run command, sanitizer clean status, Docker integration (54 tests, 12 categories), and stress/chaos/fuzz summary.

3. Remove the existing `### Test` subsection under Building (lines ~47-52 of current db/README.md) since testing info is now in its own dedicated section.

**README.md changes (DOCS-02):**

Add a version line after the intro paragraph. The file should read:

```markdown
# chromatindb

A decentralized, post-quantum secure database node with signed blob storage and peer-to-peer replication.

**Current release: v1.0.0** | **Development: v1.1.0**

chromatindb is a standalone daemon that stores cryptographically signed blobs organized into namespaces. Every blob is verified before storage, encrypted in transit using post-quantum algorithms, and replicated automatically across connected peers. Nodes form a self-organizing network where data is censorship-resistant and cryptographically authenticated end-to-end.

See [db/README.md](db/README.md) for full documentation including build instructions, configuration reference, protocol overview, and deployment scenarios.

## License

MIT
```

Keep it minimal per user decision: intro paragraph + version line + pointer to db/README.md + license.
  </action>
  <verify>
    <automated>grep -q '## Testing' db/README.md && grep -q 'Sanitizer Builds' db/README.md && grep -q '469' db/README.md && grep -q '54 integration' db/README.md && grep -q 'v1.0.0' README.md && echo "PASS"</automated>
  </verify>
  <done>db/README.md has Testing section (unit tests, Docker integration, stress/chaos/fuzz) and sanitizer build instructions. README.md has version line anchoring to v1.0.0.</done>
</task>

</tasks>

<verification>
1. `git tag -l v1.0.0` returns "v1.0.0" (REL-01)
2. `test ! -f deploy/test-crash-recovery.sh && test ! -f db/TESTS.md && test ! -f scripts/run-e2e-reliability.sh` (REL-02)
3. `test -d .planning/milestones/v0.4.0-phases && test -d .planning/milestones/v0.7.0-phases && test -d .planning/milestones/v0.8.0-phases && test -d .planning/milestones/v1.0.0-phases` (REL-03)
4. `grep -q 'VERSION 1.1.0' CMakeLists.txt` (REL-04)
5. `grep -q '## Testing' db/README.md && grep -q 'Sanitizer' db/README.md` (DOCS-01)
6. `grep -q 'v1.0.0' README.md` (DOCS-02)
</verification>

<success_criteria>
- v1.0.0 tag exists on shipped commit
- Three stale files removed from tree
- 23 phase directories archived into 4 milestone directories
- CMake version reads 1.1.0
- db/README.md has Testing section with unit tests (469), sanitizer status, Docker integration (54 tests), stress/chaos/fuzz
- README.md has version line
</success_criteria>

<output>
After completion, create `.planning/phases/53-release-cleanup-documentation/53-01-SUMMARY.md`
</output>
