---
phase: 64-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/PROTOCOL.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "PROTOCOL.md documents the request_id field in the TransportMessage schema with its three semantics"
    - "PROTOCOL.md documents ExistsRequest/ExistsResponse wire format with byte-level tables"
    - "PROTOCOL.md documents NodeInfoRequest/NodeInfoResponse wire format with byte-level tables"
    - "PROTOCOL.md message type reference table lists all 40 message types"
    - "PROTOCOL.md notes that responses may arrive out of order, correlated by request_id"
  artifacts:
    - path: "db/PROTOCOL.md"
      provides: "Complete v1.3.0 wire protocol documentation"
      contains: "request_id"
  key_links:
    - from: "db/PROTOCOL.md"
      to: "db/schemas/transport.fbs"
      via: "TransportMessage schema documentation"
      pattern: "request_id.*uint32"
---

<objective>
Update db/PROTOCOL.md with all v1.3.0 wire protocol additions: request_id field in TransportMessage, ExistsRequest/ExistsResponse wire format, NodeInfoRequest/NodeInfoResponse wire format, and the expanded message type reference table.

Purpose: Client developers need accurate byte-level documentation to implement v1.3.0 protocol features (concurrent pipelining, blob existence checks, node capability discovery).
Output: Updated db/PROTOCOL.md with 4 new message type sections and request_id semantics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-documentation/64-CONTEXT.md
@db/schemas/transport.fbs
@db/peer/peer_manager.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add request_id to TransportMessage and transport overview</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    - db/PROTOCOL.md (current state — includes pre-existing type number fixes in unstaged changes)
    - db/schemas/transport.fbs (source of truth for TransportMessage schema)
  </read_first>
  <action>
Update db/PROTOCOL.md with the following changes. The file has pre-existing unstaged fixes (PQRequired type number 25->24, ReconcileInit 27->26, etc.) — preserve those fixes.

**1. Update the TransportMessage schema display** (in "Plaintext Format" subsection under "Transport Layer", around line 30-36).

Change the existing schema display from:
```
table TransportMessage {
    type: TransportMsgType;   // 1 byte enum
    payload: [ubyte];         // variable length, type-dependent
}
```

To:
```
table TransportMessage {
    type: TransportMsgType;   // 1 byte enum
    payload: [ubyte];         // variable length, type-dependent
    request_id: uint32;       // client-assigned correlation ID
}
```

Per D-01: This is a single update to the transport envelope — do NOT annotate every individual message section with request_id.

**2. Add request_id semantics paragraph** immediately after the TransportMessage schema display block. Per D-02, state the three semantics clearly:

```
The `request_id` field enables request pipelining. Clients assign a unique `request_id` to each request and the node echoes it on the corresponding response, allowing clients to send multiple requests without waiting and match responses by `request_id`. Three rules govern its use:

1. **Client-assigned** -- the client sets `request_id` on every request message. Values are arbitrary `uint32`s chosen by the client.
2. **Node-echoed** -- the node copies the `request_id` from the request into the corresponding response (or error signal such as StorageFull or QuotaExceeded).
3. **Per-connection scope** -- `request_id` values are meaningful only within a single connection. Different connections may reuse the same values independently.

Server-initiated messages (Notification) always carry `request_id = 0`.
```

**3. Add concurrent-response note** (per D-03). Append this sentence to the end of the new request_id semantics paragraph above:

```
The node may process requests concurrently and responses may arrive in a different order than requests were sent. Clients must use `request_id` to correlate responses, not assume ordering.
```

Per D-04: Do NOT add any dispatch model details (inline vs offload) to PROTOCOL.md — that goes in db/README.md.
  </action>
  <verify>
    <automated>grep -c "request_id" db/PROTOCOL.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - db/PROTOCOL.md contains `request_id: uint32;       // client-assigned correlation ID` in the TransportMessage table
    - db/PROTOCOL.md contains the string "Client-assigned"
    - db/PROTOCOL.md contains the string "Node-echoed"
    - db/PROTOCOL.md contains the string "Per-connection scope"
    - db/PROTOCOL.md contains the string "request_id = 0"
    - db/PROTOCOL.md contains the string "responses may arrive in a different order"
    - db/PROTOCOL.md does NOT contain the words "inline" or "offload" or "thread pool" or "dispatch model"
  </acceptance_criteria>
  <done>TransportMessage schema shows request_id field, three semantics documented, concurrent-response note present, no dispatch model leakage</done>
</task>

<task type="auto">
  <name>Task 2: Add ExistsRequest/ExistsResponse, NodeInfoRequest/NodeInfoResponse sections, and update message type table</name>
  <files>db/PROTOCOL.md</files>
  <read_first>
    - db/PROTOCOL.md (current state after Task 1 modifications)
    - db/peer/peer_manager.cpp lines 836-948 (ExistsRequest and NodeInfoRequest handler implementations — source of truth for wire formats)
    - db/schemas/transport.fbs (type enum values 37-40)
  </read_first>
  <action>
**1. Add ExistsRequest/ExistsResponse section** in the "Client Protocol" section, after the StatsRequest/StatsResponse subsection (after line ~496). Follow the exact same structure as ReadRequest/ReadResponse and StatsRequest/StatsResponse sections (heading, description, payload table).

Add this content:

```markdown
### ExistsRequest / ExistsResponse (types 37-38)

Check whether a specific blob exists without transferring its data.

**ExistsRequest payload:** 64 bytes

| Field | Offset | Size | Description |
|-------|--------|------|-------------|
| namespace_id | 0 | 32 | Target namespace |
| blob_hash | 32 | 32 | Content hash of the blob |

**ExistsResponse payload:** 33 bytes

| Field | Offset | Size | Encoding | Description |
|-------|--------|------|----------|-------------|
| exists | 0 | 1 | uint8 | 0x01 = exists, 0x00 = not found |
| blob_hash | 1 | 32 | raw bytes | Echo of the requested blob hash |

Tombstoned blobs return `exists = 0x00`. The echoed `blob_hash` allows clients to correlate responses when pipelining multiple existence checks.
```

**2. Add NodeInfoRequest/NodeInfoResponse section** immediately after the ExistsRequest/ExistsResponse section:

```markdown
### NodeInfoRequest / NodeInfoResponse (types 39-40)

Query node version, state, and supported message types for SDK capability discovery.

**NodeInfoRequest payload:** empty (0 bytes)

**NodeInfoResponse payload:** variable length

| Field | Offset | Size | Encoding | Description |
|-------|--------|------|----------|-------------|
| version_len | 0 | 1 | uint8 | Length of version string |
| version | 1 | version_len | UTF-8 | Node software version (e.g., "1.1.0") |
| git_hash_len | 1 + version_len | 1 | uint8 | Length of git hash string |
| git_hash | 2 + version_len | git_hash_len | UTF-8 | Build git commit hash |
| uptime | 2 + version_len + git_hash_len | 8 | big-endian uint64 | Node uptime in seconds |
| peer_count | +8 | 4 | big-endian uint32 | Number of connected peers |
| namespace_count | +4 | 4 | big-endian uint32 | Number of namespaces with stored blobs |
| total_blobs | +4 | 8 | big-endian uint64 | Total blob count across all namespaces |
| storage_used | +8 | 8 | big-endian uint64 | Storage bytes used |
| storage_max | +8 | 8 | big-endian uint64 | Configured storage limit (0 = unlimited) |
| types_count | +8 | 1 | uint8 | Number of supported message types |
| supported_types | +1 | types_count | uint8[] | List of supported TransportMsgType values |

The `supported_types` list contains only client-facing message types (types that the relay allows through). Internal protocol types (handshake, sync, PEX) are excluded. Clients use this list for feature detection -- if a type value is present, the node supports that operation.
```

**3. Update the Message Type Reference table** at the bottom of the file. Add 4 new rows after StatsResponse (36), maintaining the same `| Value | Name | Description |` format:

```
| 37 | ExistsRequest | Client blob existence check: namespace + hash (64 bytes) |
| 38 | ExistsResponse | Client blob existence result: exists flag + hash (33 bytes) |
| 39 | NodeInfoRequest | Client node info query (empty payload) |
| 40 | NodeInfoResponse | Client node info: version, state, supported types (variable) |
```
  </action>
  <verify>
    <automated>grep -c "ExistsRequest\|ExistsResponse\|NodeInfoRequest\|NodeInfoResponse" db/PROTOCOL.md | xargs test 8 -le</automated>
  </verify>
  <acceptance_criteria>
    - db/PROTOCOL.md contains "### ExistsRequest / ExistsResponse (types 37-38)"
    - db/PROTOCOL.md contains "### NodeInfoRequest / NodeInfoResponse (types 39-40)"
    - db/PROTOCOL.md contains "**ExistsRequest payload:** 64 bytes"
    - db/PROTOCOL.md contains "**ExistsResponse payload:** 33 bytes"
    - db/PROTOCOL.md contains "**NodeInfoRequest payload:** empty (0 bytes)"
    - db/PROTOCOL.md contains "**NodeInfoResponse payload:** variable length"
    - db/PROTOCOL.md contains "| 37 | ExistsRequest"
    - db/PROTOCOL.md contains "| 38 | ExistsResponse"
    - db/PROTOCOL.md contains "| 39 | NodeInfoRequest"
    - db/PROTOCOL.md contains "| 40 | NodeInfoResponse"
    - Message type reference table has exactly 41 rows (header + 40 types + None)
    - db/PROTOCOL.md contains "Tombstoned blobs return"
    - db/PROTOCOL.md contains "supported_types"
  </acceptance_criteria>
  <done>ExistsRequest/ExistsResponse and NodeInfoRequest/NodeInfoResponse documented with byte-level wire format tables matching implementation; message type reference table has 40 entries covering types 0-40</done>
</task>

</tasks>

<verification>
- `grep -c "request_id" db/PROTOCOL.md` returns >= 5
- `grep "ExistsRequest" db/PROTOCOL.md` finds section heading AND table row
- `grep "NodeInfoResponse" db/PROTOCOL.md` finds section heading AND table row
- `grep "| 40 |" db/PROTOCOL.md` returns a match (last type in table)
- `grep -c "^|" db/PROTOCOL.md` in the message type reference table section shows 42 lines (header separator + 41 rows)
- No occurrence of "inline", "offload", "thread pool", or "dispatch model" in PROTOCOL.md (per D-04)
</verification>

<success_criteria>
db/PROTOCOL.md documents request_id semantics, ExistsRequest/ExistsResponse wire format, NodeInfoRequest/NodeInfoResponse wire format, and a complete 40-entry message type reference table, all matching the actual implementation in peer_manager.cpp and transport.fbs
</success_criteria>

<output>
After completion, create `.planning/phases/64-documentation/64-01-SUMMARY.md`
</output>
