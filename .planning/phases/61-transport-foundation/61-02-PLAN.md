---
phase: 61-transport-foundation
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - db/net/connection.h
  - db/net/connection.cpp
  - db/peer/peer_manager.h
  - db/peer/peer_manager.cpp
  - relay/core/relay_session.h
  - relay/core/relay_session.cpp
  - db/tests/net/test_connection.cpp
autonomous: true
requirements:
  - CONC-02
  - CONC-05
must_haves:
  truths:
    - "Connection::send_message accepts a request_id parameter"
    - "MessageCallback signature includes request_id"
    - "Relay transparently forwards request_id in both directions"
    - "test_connection and relay compile with the new MessageCallback signature"
  artifacts:
    - path: "db/net/connection.h"
      provides: "Updated MessageCallback and send_message signatures"
    - path: "relay/core/relay_session.cpp"
      provides: "Transparent forwarding of request_id"
  key_links:
    - from: "Connection::message_loop"
      to: "MessageCallback"
      via: "invocation"
      pattern: "message_cb_(.*, request_id)"
    - from: "RelaySession::handle_client_message"
      to: "send_message"
      via: "forwarding"
      pattern: "send_message(.*, rid)"
---

<objective>
Plumb the `request_id` through the `Connection` layer, `MessageCallback`, and transparently forward it in the `Relay` layer. This sets up the signature for `on_peer_message` to be implemented in the next wave.

Purpose: Both node and relay must process messages carrying correlation IDs without inspecting them. Updating the callback signature requires coordinated updates across `Connection`, tests, `Relay`, and `PeerManager` signatures.
Output: Updated signatures across the pipeline and bidirectional transparent forwarding in the relay.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Interfaces and context needed for the tasks
From `db/net/connection.h`:
```cpp
using MessageCallback = std::function<void(
    std::shared_ptr<Connection>, wire::TransportMsgType, std::vector<uint8_t>, uint32_t)>;

// send_message signature needs default parameter
asio::awaitable<bool> send_message(wire::TransportMsgType type,
                                   std::span<const uint8_t> payload,
                                   uint32_t request_id = 0);
```

Per D-04: Relay signature updates must be bundled with codec/plumbing changes because the relay won't compile without the updated `MessageCallback` signature.

Per D-01: `request_id` is opaque to the relay — no logging, no inspection, pure pass-through.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb request_id through Connection and Tests</name>
  <files>db/net/connection.h, db/net/connection.cpp, db/tests/net/test_connection.cpp</files>
  <action>
    1. In `db/net/connection.h`:
       - Update `MessageCallback` typedef to add `uint32_t request_id` as the final parameter.
       - Update `send_message` signature to add `uint32_t request_id = 0`.
    2. In `db/net/connection.cpp`:
       - Update `send_message` implementation to accept and pass `request_id` to `TransportCodec::encode`.
       - In `message_loop()`, when invoking `message_cb_` (around the `default:` case), pass `decoded->request_id` as the 4th argument.
    3. In `db/tests/net/test_connection.cpp`:
       - Update all lambda bindings to `on_message` (or `set_message_handler`) to accept the 4th `uint32_t request_id` parameter to fix compilation.
  </action>
  <verify>
    <automated>cd build && cmake .. && make chromatindb_tests && ctest -R test_connection --output-on-failure</automated>
  </verify>
  <done>Connection sends and triggers callbacks with request_id.</done>
</task>

<task type="auto">
  <name>Task 2: Transparent Relay Forwarding (CONC-05)</name>
  <files>relay/core/relay_session.h, relay/core/relay_session.cpp</files>
  <action>
    1. In `relay/core/relay_session.h`:
       - Update `handle_client_message` and `handle_node_message` to take `uint32_t request_id` as the last parameter.
    2. In `relay/core/relay_session.cpp`:
       - In `start()`, update the lambda assignments for `client_conn_->set_message_handler` and `node_conn_->set_message_handler` to accept `request_id` and pass it to the respective `handle_*_message` function.
       - In `handle_client_message` and `handle_node_message`, capture `request_id` by value in the `co_spawn` lambdas (`[..., rid = request_id]`) and pass it down to `conn->send_message(t, p, rid)`.
       - Ensure NO logging of `request_id` is added (per D-01).
  </action>
  <verify>
    <automated>cd build && cmake .. && make relay</automated>
  </verify>
  <done>Relay compiles successfully and passes request_id bidirectionally.</done>
</task>

<task type="auto">
  <name>Task 3: PeerManager Signature Update</name>
  <files>db/peer/peer_manager.h, db/peer/peer_manager.cpp</files>
  <action>
    1. In `db/peer/peer_manager.h`, update `on_peer_message` signature to include `uint32_t request_id`.
    2. In `db/peer/peer_manager.cpp`, update the definition of `on_peer_message`.
    3. Since we will implement the actual echoing in the next plan, just add `(void)request_id;` at the top of the function body to silence any `-Wunused-parameter` compiler warnings for now.
  </action>
  <verify>
    <automated>cd build && cmake .. && make chromatindb_lib</automated>
  </verify>
  <done>PeerManager signature matches MessageCallback and compiles cleanly.</done>
</task>

</tasks>

<verification>
Check that the codebase (both `chromatindb_lib` and `relay`) compiles with the updated `MessageCallback` signatures.
Run `ctest -R "test_connection"` to ensure it passes.
</verification>

<success_criteria>
- `MessageCallback` includes `request_id`.
- `Connection::send_message` handles `request_id` with a default value.
- Relay cleanly passes the correlation ID from client to node and node to client without modification.
- Project builds correctly without arity/signature errors.
</success_criteria>

<output>
After completion, create `.planning/phases/61-transport-foundation/61-02-SUMMARY.md`
</output>