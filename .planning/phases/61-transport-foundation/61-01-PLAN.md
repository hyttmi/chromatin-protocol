---
phase: 61-transport-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schemas/transport.fbs
  - db/wire/transport_generated.h
  - db/net/protocol.h
  - db/net/protocol.cpp
  - db/tests/net/test_protocol.cpp
autonomous: true
requirements:
  - CONC-01
must_haves:
  truths:
    - "TransportMessage schema includes a request_id field"
    - "request_id successfully round-trips through codec encoding and decoding"
    - "Default request_id is 0"
  artifacts:
    - path: "db/schemas/transport.fbs"
      provides: "request_id field in TransportMessage"
    - path: "db/net/protocol.h"
      provides: "request_id parameter in encode and DecodedMessage"
  key_links:
    - from: "TransportCodec::decode"
      to: "DecodedMessage"
      via: "field assignment"
      pattern: "result.request_id = msg->request_id()"
---

<objective>
Add the `request_id` correlation ID to the transport envelope and codec. This is the first step (Wave 1) of plumbing `request_id` through the system.

Purpose: Clients need a way to correlate responses to their requests when concurrent dispatch is enabled in Phase 62.
Output: Updated FlatBuffers schema, regenerated header, and updated codec pipeline with verifying tests.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Interfaces and context needed for the tasks
From `db/schemas/transport.fbs`:
We are adding `request_id: uint32` to the end of the `TransportMessage` table. 
New fields in FlatBuffers MUST be appended to the end.

From `db/net/protocol.h`:
```cpp
struct DecodedMessage {
    chromatindb::wire::TransportMsgType type;
    std::vector<uint8_t> payload;
    // Will need: uint32_t request_id = 0;
};

class TransportCodec {
public:
    static std::vector<uint8_t> encode(chromatindb::wire::TransportMsgType type,
                                       std::span<const uint8_t> payload);
    // Will need: static std::vector<uint8_t> encode(..., uint32_t request_id = 0);
    // ...
};
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update TransportMessage Schema</name>
  <files>db/schemas/transport.fbs, db/wire/transport_generated.h</files>
  <behavior>
    - Test 1: `flatc` successfully compiles the updated schema without errors.
    - Test 2: Generated header `transport_generated.h` contains `request_id` getter.
  </behavior>
  <action>
    1. Edit `db/schemas/transport.fbs`. Add `request_id: uint32;` to the end of the `TransportMessage` table. (Crucial: MUST be the last field per FlatBuffers schema evolution rules).
    2. Run `flatc` to regenerate the C++ header: `flatc --cpp --gen-object-api -o db/wire/ db/schemas/transport.fbs`
    Note: Do not manually edit `db/wire/transport_generated.h`.
  </action>
  <verify>
    <automated>grep -q "request_id" db/wire/transport_generated.h</automated>
  </verify>
  <done>Schema updated and `transport_generated.h` regenerated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plumb request_id through Codec</name>
  <files>db/net/protocol.h, db/net/protocol.cpp, db/tests/net/test_protocol.cpp</files>
  <behavior>
    - Test 1: encode/decode of non-zero request_id round-trips correctly.
    - Test 2: encode with no explicit request_id defaults to 0 and decodes as 0.
  </behavior>
  <action>
    1. In `db/net/protocol.h`:
       - Add `uint32_t request_id = 0;` to `DecodedMessage` struct.
       - Update `TransportCodec::encode` to accept `uint32_t request_id = 0` as a third parameter.
    2. In `db/net/protocol.cpp`:
       - Update `TransportCodec::encode` signature. Update the `CreateTransportMessage` call to pass `request_id`.
       - Ensure `builder.ForceDefaults(true)` remains present to ensure explicit serialization of the default value.
       - In `TransportCodec::decode`, extract `request_id` from the FlatBuffer message and assign it to the `DecodedMessage` result (`result.request_id = msg->request_id();`).
    3. In `db/tests/net/test_protocol.cpp`:
       - Add a new `TEST_CASE("TransportCodec preserves request_id", "[protocol]")` with `SECTION`s verifying round-trip of non-zero IDs, max uint32, and the default 0 value.
  </action>
  <verify>
    <automated>cd build && cmake .. && make chromatindb_tests && ctest -R test_protocol --output-on-failure</automated>
  </verify>
  <done>Codec successfully encodes and decodes request_id, passing round-trip tests.</done>
</task>

</tasks>

<verification>
Check that the transport schema has been updated and the generated header committed. Check that `ctest -R test_protocol` runs cleanly and validates the new behavior.
</verification>

<success_criteria>
- `TransportMessage` table in `.fbs` file includes `request_id` as the final field.
- `DecodedMessage` carries `request_id`.
- Protocol tests cover `request_id` default and explicit values.
- No other existing behavior is broken.
</success_criteria>

<output>
After completion, create `.planning/phases/61-transport-foundation/61-01-SUMMARY.md`
</output>